---
quick_id: 260919-ou8
description: Ignorar "Claude outputs/" e versionar o protótipo aprovado do Estoque
mode: quick
---

# Quick 260919-ou8 — `.gitignore` de `Claude outputs/` e protótipo do Estoque

Executado inline na árvore principal: o protótipo do Estoque só existe sem commit nesta cópia,
então um executor em worktree isolada não o enxergaria.

## Tarefa 1 — Ignorar `Claude outputs/`
- files: `.gitignore`
- action: acrescentar `/Claude outputs/` com comentário (contexto de negócio; repositório público).
- verify: `git ls-files "Claude outputs"` vazio; `git check-ignore` casa os arquivos; `git status` não lista a pasta.
- done: commit `chore(gitignore)`.

## Tarefa 2 — Versionar o protótipo do Estoque
- files: `.planning/phases/06-estoque/prototipo.html`
- action: `git add` e commit. O protótipo foi aprovado pelo dono em 18/09 e continua válido.
- verify: `git ls-files` lista o arquivo.
- done: commit `docs(06)`.
