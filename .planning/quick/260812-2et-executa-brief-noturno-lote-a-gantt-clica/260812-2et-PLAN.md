---
phase: quick-260812-2et
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [A1, A2, A3, C]
files_modified:
  - components/amassa/encomendas/gantt.tsx
  - components/amassa/encomendas/trilha-segmentos.tsx
  - components/amassa/encomendas/cartao-encomenda.tsx
  - components/amassa/encomendas/lista-encomendas.tsx
  - lib/encomendas/gantt.ts
  - lib/encomendas/trilha.ts
  - lib/auth/esquema-senha.ts
  - lib/auth/acoes-senha.ts
  - components/amassa/menu-usuario.tsx
  - components/amassa/conta/formulario-troca-de-senha.tsx
  - app/(app)/conta/senha/page.tsx
  - tests/unit/gantt.test.ts
  - tests/unit/trilha.test.ts
  - tests/unit/esquema-senha.test.ts
  - tests/e2e/encomendas-indice.spec.ts
  - tests/e2e/encomendas-filtros.spec.ts
  - tests/e2e/trocar-senha.spec.ts

estimate:
  tokens: 190000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "No desktop, clicar no nome de uma encomenda no Gantt abre /encomendas/{id} — o mesmo destino que o cartão do celular já abre (A1)."
    - "No celular, a barra de etapas mostra onde hoje cai dentro do período e as datas de início e entrega nas duas pontas (A2)."
    - "O Gantt do desktop abre em hoje: a timeline começa na segunda-feira da semana corrente, com células semanais, e nada é desenhado à esquerda da origem da régua (A3)."
    - "Uma encomenda iniciada antes da abertura da timeline aparece cortada na borda esquerda, com marca visível, nunca com barra vazando para fora da linha (A3)."
    - "Um gestor troca a própria senha por uma tela alcançável pelo menu do usuário; a senha antiga para de funcionar e a nova entra (C)."
    - "Senha atual errada devolve mensagem em português e NÃO troca a senha (C)."
  artifacts:
    - lib/encomendas/trilha.ts
    - lib/auth/esquema-senha.ts
    - lib/auth/acoes-senha.ts
    - app/(app)/conta/senha/page.tsx
    - components/amassa/conta/formulario-troca-de-senha.tsx
    - tests/unit/trilha.test.ts
    - tests/unit/esquema-senha.test.ts
    - tests/e2e/trocar-senha.spec.ts
  key_links:
    - "lista-encomendas.tsx passa `hoje` para CartaoEncomenda, que passa para TrilhaSegmentos — o componente NUNCA lê o relógio."
    - "retanguloDaEtapa devolve `cortadaNaEsquerda` e gantt.tsx desenha a marca de corte a partir desse campo."
    - "menu-usuario.tsx (as DUAS variantes) leva a /conta/senha."
    - "trocarSenha lê o id da sessão via exigirUsuario(), nunca do cliente."
---

<objective>
Executar o `BRIEF-NOTURNO.md` por inteiro, na ordem obrigatória A1 → A2 → A3 → C, com um commit
atômico por item.

Purpose: fechar os três defeitos do índice de Encomendas apontados pelo dono (encomenda não
clicável no Gantt do desktop, barra do celular que "não informa exatamente nada", timeline que abre
numa quinzena passada) e entregar a tela de trocar senha que hoje não existe.

Output: 4 commits atômicos; nenhuma migração; nenhuma mudança em `db/schema.ts`; nenhuma mudança em
`TABELAS_ESPERADAS` de `scripts/testar-migracoes.mjs`.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/BRIEF-NOTURNO.md
@.claude/CLAUDE.md

Todas as decisões de produto já estão tomadas e marcadas como **DECIDIDO** no brief. Não reabra
nenhuma, não proponha alternativas, não pergunte ao dono. Se algo parecer ambíguo, escolha a opção
que respeita a decisão registrada e siga.
</context>

<orcamento_de_e2e>
Regra dura do `CLAUDE.md`, medida: `npm run test:e2e` custa ~53s de imposto fixo (15s de Postgres
efêmero + 38s de `next build`) ANTES do primeiro teste rodar. Esse imposto é igual para a suíte
inteira e para um `--grep` que pega um teste só.

- **No máximo UMA invocação de `npm run test:e2e` por tarefa**, sempre com `--grep`.
- **Nunca `npm run build` como passo separado** — o e2e já constrói.
- **Uma varredura completa sem `--grep`, UMA vez só, no fechamento** (depois da Tarefa 4).
- Se um `--grep` falhar e você precisar da suíte inteira para diagnosticar, **rode** — a regra é
  sobre o padrão, não uma proibição. Registre no SUMMARY quais comandos rodou de fato.
- `lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e `npm test` são baratos: rode à vontade.
</orcamento_de_e2e>

<tasks>

<task type="auto">
  <name>Tarefa 1 (A1): encomenda clicável no Gantt do desktop</name>
  <files>
    components/amassa/encomendas/gantt.tsx,
    tests/e2e/encomendas-indice.spec.ts
  </files>
  <read_first>
    components/amassa/encomendas/cartao-encomenda.tsx (linhas 22-27 — o `Link` que já existe no
    celular, com o anel de foco que A1 manda repetir),
    components/amassa/encomendas/gantt.tsx (linhas 152-182 — a linha e a coluna fixa)
  </read_first>
  <action>
Envolver o CONTEÚDO da coluna fixa (nome + selo de rascunho + cliente) num `Link` do `next/link`
para `/encomendas/${encomenda.id}`.

Estrutura exigida — o `div` externo continua sendo o elemento `sticky`, e o `Link` mora DENTRO
dele:

- O `div` externo mantém `sticky left-0 z-10 flex flex-shrink-0 border-r border-b border-border
  bg-card` e o `style={{ width: LARGURA_COLUNA_FIXA, height: ALTURA_LINHA }}`. Tire dele
  `flex-col justify-center gap-0.5 px-4` (essas passam para o `Link`).
- O `Link` recebe `className="focus-visible:ring-ring flex h-full w-full flex-col justify-center
  gap-0.5 px-4 focus-visible:ring-2 focus-visible:outline-none"` — mesmas classes de anel de foco
  do cartão do celular (`cartao-encomenda.tsx:25`).
- Os dois filhos atuais (o `div` com nome + selo, e o `span` do cliente) passam a ser filhos do
  `Link`, sem nenhuma outra mudança.

Restrições que valem como parte do contrato desta tarefa:
- **NÃO** envolver a linha inteira num `Link`. A linha contém a área rolável horizontal e as barras
  com `role="img"`; um link por cima quebraria a semântica e atrapalharia a rolagem por gesto.
- O `Link` não pode escapar do `sticky` — por isso ele fica dentro do `div` sticky, nunca por fora.
- Preservar `data-testid={`gantt-linha-${encomenda.id}`}` intacto no `div` da linha: o e2e depende
  dele.
- Não mexer em `ALTURA_LINHA` (64px, já acima do mínimo de 44px de toque).

Teste novo em `tests/e2e/encomendas-indice.spec.ts`, dentro do describe `Gantt desktop (ENC-03,
ENC-06, ENC-07)` (que já tem o `test.skip` de projeto não-desktop). Ausência desse teste é o que
deixou o defeito passar. Ele deve:
1. criar uma encomenda com `criarEncomenda` e `dataInicio: dataEmDias(0)`;
2. localizar a linha com `linhaDoGantt(page, nome)` e ler o `data-testid` dela para extrair o id
   (o sufixo depois de `gantt-linha-`);
3. afirmar que o `Link` da linha (`linha.getByRole("link")`) tem `href` exatamente
   `/encomendas/{id}` — leitura determinística, sem depender de navegação;
4. clicar nele e afirmar `await expect(page).toHaveURL(new RegExp(`/encomendas/${id}$`))`;
5. afirmar que a página de destino mostra o nome da encomenda (prova que abriu a encomenda certa,
   não uma qualquer).
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
    <automated>npm run test:e2e -- --grep "Gantt desktop"</automated>
  </verify>
  <done>
    O novo teste passa no projeto `desktop`; o teste já existente "rolar a área de barras não move
    a coluna de nome+cliente" continua passando (o `Link` não quebrou o `sticky`); `lint` e `tsc`
    limpos. Commit atômico: `fix(encomendas): nome no Gantt do desktop vira link para a encomenda`.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2 (A2): eixo de tempo na barra do celular</name>
  <files>
    lib/encomendas/trilha.ts,
    tests/unit/trilha.test.ts,
    components/amassa/encomendas/trilha-segmentos.tsx,
    components/amassa/encomendas/cartao-encomenda.tsx,
    components/amassa/encomendas/lista-encomendas.tsx,
    tests/e2e/encomendas-indice.spec.ts
  </files>
  <read_first>
    lib/encomendas/gantt.ts (linhas 1-11 e 37-98 — o cabeçalho que explica a disciplina "módulo
    puro, zero import, nenhuma instância de Date" e as funções `diasDesdeAEpoca`/`civilDesdeDias`
    que serão duplicadas pela mesma razão),
    lib/encomendas/formato.ts (`formatarDiaCurto`)
  </read_first>
  <behavior>
    `posicaoDeHojeNaTrilha(faixas, hoje)` — percentual (0 a 100) de onde "hoje" cai dentro do
    período desenhado, ou `null` quando está fora:
    - faixas padrão começando em `hoje` (3/6/1/1/1/1 = 13 dias) → `0`
    - `hoje` = início + 3 dias, mesmas faixas → `3 / 13 * 100`
    - `hoje` anterior ao início → `null`
    - `hoje` igual ao `fimExclusivo` do período (dia seguinte ao último) → `null`
    - `hoje` no último dia desenhado → percentual < 100, nunca 100
    - todas as faixas com `dias: 0` → `null`
    - faixas com `dias: 0` no meio não deslocam a conta (não são desenhadas)
  </behavior>
  <action>
**DECIDIDO no brief: enriquecer a barra atual. NÃO trocar pela timeline rolável** — isso reverteria
uma decisão travada da Fase 3 (`03-UI-SPEC.md`: 18px/dia não cabe no celular). O que falta na barra
é informação, não escala.

**(a) `lib/encomendas/trilha.ts` — módulo puro novo, zero imports.**
Mesma disciplina de `lib/encomendas/gantt.ts` e `lib/encomendas/cronograma.ts`: nenhum `import` no
arquivo, nenhuma instância de `Date`, "hoje" sempre por argumento. Duplique `diasDesdeAEpoca`
(Howard Hinnant) e um `diferencaEmDias` local pela mesma razão que `gantt.ts` já duplica — dois
módulos puros sem import não podem compartilhar função nenhuma. Copie o comentário-cabeçalho de
`gantt.ts` adaptado, explicando o porquê.

Exporte:
- `type FaixaDaTrilha = { readonly dias: number; readonly inicio: string; readonly fimExclusivo: string }`
  (tipo estrutural compatível com `FaixaDeEtapa`, redeclarado localmente).
- `posicaoDeHojeNaTrilha(faixas: readonly FaixaDaTrilha[], hoje: string): number | null` — filtra
  `dias > 0`; se não sobrar nenhuma, devolve `null`; soma `dias` para a duração total; calcula
  `decorridos = diferencaEmDias(hoje, primeiraFaixaDesenhada.inicio)`; devolve `null` se
  `decorridos < 0` ou `decorridos >= duracaoTotal`; senão devolve `decorridos / duracaoTotal * 100`.

Testes em `tests/unit/trilha.test.ts` cobrindo cada linha do bloco `<behavior>` acima.

**(b) `trilha-segmentos.tsx`.**
`TrilhaSegmentosProps` ganha `hoje: string` (obrigatório). **Armadilha do brief: passar por prop,
nunca ler o relógio dentro do componente** — é a mesma disciplina que `Gantt` já segue.

A função passa a devolver um fragmento com DOIS filhos: a barra (como hoje) e uma linha de datas
abaixo dela.

- A barra mantém `data-testid="trilha-segmentos"`, `role="list"`, `aria-label` e todas as classes
  atuais, e ganha `relative` no `className` (a marca de hoje é filho absoluto dela).
- Marca de hoje: chame `posicaoDeHojeNaTrilha(faixasDesenhadas, hoje)`. Quando devolver `null`,
  **não desenhe nada** — nunca grude numa ponta, o que mentiria a posição. Quando devolver um
  número, renderize dentro da barra:
  `<div aria-hidden="true" data-testid="trilha-hoje" data-posicao={String(posicao)}
   className="absolute top-0 z-10 h-full w-0.5 bg-erro" style={{ left: `${posicao}%` }} />`.
  A marca é decorativa: a informação já existe em texto no cartão ("Etapa atual: … · faltam N dias
  para …") e não deve ser duplicada em leitor de tela.
- Linha de datas: `<div data-testid="trilha-datas" className="text-micro text-muted-foreground mt-1
  flex justify-between">` com dois `<span>`. Esquerda = `formatarDiaCurto(primeiraFaixaDesenhada.inicio)`;
  direita = `formatarDiaCurto(ultimoDiaDesenhado)`, onde `ultimoDiaDesenhado` é o dia anterior ao
  `fimExclusivo` da última faixa desenhada. Cada `<span>` começa com um
  `<span className="sr-only">` de rótulo ("Início: " / "Entrega: ") para a leitura fazer sentido
  fora do contexto visual — as datas exatas não existem em nenhum outro lugar do cartão.
  `formatarDiaCurto` vem de `@/lib/encomendas/formato` (o componente já é livre para importar; a
  proibição de import vale só para os módulos puros).
- O ramo `duracaoTotal === 0` (nenhuma etapa ligada) continua devolvendo o parágrafo atual, sem
  barra e sem datas.

**(c) Fiação de `hoje`.**
`CartaoEncomendaProps` ganha `hoje: string`, repassado direto para `TrilhaSegmentos`.
`lista-encomendas.tsx` já tem `hoje` no escopo (prop do componente) — passe em
`<CartaoEncomenda encomenda={encomenda} hoje={hoje} />` (linha 174).

**(d) Testes e2e** no describe `Lista mobile (ENC-08, ENC-09)` de `encomendas-indice.spec.ts`:
1. encomenda com `dataInicio: dataEmDias(0)` → `trilha-hoje` existe e seu `data-posicao` é `"0"`;
2. mesma encomenda → `trilha-datas` contém `formatarDiaCurto` da data de início e da data de
   entrega (importe `formatarDiaCurto` de `@/lib/encomendas/formato` e recompute — nunca redigite
   a string formatada no teste);
3. encomenda com `dataInicio: dataEmDias(30)` (ainda não começou) → `trilha-hoje` tem count 0;
4. encomenda com `dataInicio: dataEmDias(-60)` (já acabou) → `trilha-hoje` tem count 0.

Não mexa nos testes já existentes desse describe: eles medem `trilha-segmentos` e os segmentos, que
continuam com os mesmos testids e a mesma geometria.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
    <automated>grep -c "^import" lib/encomendas/trilha.ts</automated>
    <automated>grep -v '^\s*//' lib/encomendas/trilha.ts | grep -c "new Date"</automated>
    <automated>npm run test:e2e -- --grep "Lista mobile"</automated>
  </verify>
  <done>
    `tests/unit/trilha.test.ts` passa com todos os casos do `<behavior>`; os dois `grep` acima
    devolvem `0` (zero import, zero `Date`); os quatro testes e2e novos passam no projeto `celular`
    e os já existentes continuam passando. Commit atômico:
    `feat(encomendas): marca de hoje e datas das pontas na barra do celular`.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3 (A3): timeline começa hoje, agrupada por semana</name>
  <files>
    lib/encomendas/gantt.ts,
    components/amassa/encomendas/gantt.tsx,
    tests/unit/gantt.test.ts,
    tests/e2e/encomendas-indice.spec.ts,
    tests/e2e/encomendas-filtros.spec.ts
  </files>
  <read_first>
    lib/encomendas/gantt.ts inteiro (é o contrato que muda),
    tests/unit/gantt.test.ts inteiro,
    tests/e2e/encomendas-filtros.spec.ts linhas 314-358
  </read_first>
  <behavior>
    Semana começa na SEGUNDA (mesma convenção que a Fase 4 usa nos relatórios de queima, FOR-12 —
    duas definições de semana no mesmo sistema seria dívida imediata). `diasDesdeAEpoca` devolve
    dias desde 1970-01-01, que foi uma QUINTA. Logo a segunda da semana que contém o dia `d` é
    `d - (((d + 3) % 7) + 7) % 7`. Confira: `d = 0` → `-3` (segunda, 29/12/1969); `d = 4`
    (segunda, 05/01/1970) → `4`.

    Âncora aritmética verificada para os testes: **2026-08-12 é uma quarta-feira**, logo a segunda
    da sua semana é **2026-08-10**. Derive os demais valores esperados a partir da regra, e confirme
    rodando `npm test` — não copie números de cabeça.

    - `calcularIntervalo([{inicio, fimExclusivo}], hoje)` → `primeiroDia` é sempre a segunda da
      semana de `hoje`, independentemente de quão antiga seja a encomenda mais antiga.
    - `totalDeDias` é sempre múltiplo de 7 (as duas pontas caem em segunda-feira).
    - `retanguloDaEtapa` de faixa iniciada antes de `primeiroDia` → `esquerda: 0`, largura reduzida,
      `cortadaNaEsquerda: true`.
    - `retanguloDaEtapa` de faixa que termina em ou antes de `primeiroDia` → `null`.
    - `celulasDeSemana` de um intervalo alinhado à semana → todas as células com `dias: 7`.
  </behavior>
  <action>
Item mais pesado do lote: **muda o contrato de um módulo puro testado.** Faça com cuidado.

**(a) `lib/encomendas/gantt.ts` — aritmética de semana.**
Substitua `quinzenaQueContem` / `quinzenaAnterior` / `quinzenaPosterior` pelo equivalente semanal.
`quinzenaAnterior` some por completo: não há mais folga na ponta inicial.

- `semanaQueContem(dataIso): { primeiroDia, ultimoDia }` — segunda a domingo, usando a fórmula do
  `<behavior>` sobre `diasDesdeAEpoca` + `civilDesdeDias`. `ultimoDia` = `primeiroDia + 6`.
- `semanaPosterior(dataIso)` — a semana imediatamente seguinte à que contém `dataIso`.
- `ultimoDiaDoMes` deixa de ser usado; remova se ficar órfão (o `lint` roda com
  `--max-warnings=0`).
- **A aritmética continua sem `Date`.** O módulo usa `diasDesdeAEpoca`/`civilDesdeDias` exatamente
  para isso. O dia da semana sai de `diasDesdeAEpoca % 7`. Não introduza nenhuma chamada de método
  de dia-da-semana de `Date` neste arquivo — e, por causa do portão de `grep` do `<verify>`, não
  escreva o nome desse método nem em comentário: o portão filtra linhas de comentário `//` mas não
  blocos, e um comentário que cita o identificador proibido invalidaria o próprio portão.

**(b) `calcularIntervalo`.**
- `primeiroDia = semanaQueContem(hoje).primeiroDia`. **DECIDIDO: a timeline começa hoje**; a parte
  passada de uma encomenda em curso fica de fora, com marca de corte na borda esquerda.
- Remova a variável `menorInicio` e o ramo que a atualiza — o menor início não influencia mais
  nada. Mantenha `maiorFimExclusivo` inicializado em `hoje` e atualizado pelo laço.
- `ultimoDiaExclusivo = somarDias(semanaPosterior(maiorFimExclusivo).ultimoDia, 1)` — uma semana de
  folga no fim (antes era uma quinzena).
- Atualize o comentário-cabeçalho da função e o bloco de comentário do topo do arquivo (que hoje
  fala em "quinzenas") para descreverem semanas e a abertura em hoje.
- `rolagemInicial` **não muda**: com hoje na borda esquerda, o `Math.max(…, 0)` resolve para 0.
  `deslocamentoEmPixels` e `ordenarParaGantt` também não mudam.

**(c) `retanguloDaEtapa` — recorte explícito.**
`RetanguloDeEtapa` ganha `cortadaNaEsquerda: boolean`. Nova ordem do corpo:
1. `faixa.dias === 0` → `null` (inalterado).
2. `esquerdaBruta = deslocamentoEmPixels(intervalo, faixa.inicio)`;
   `direita = esquerdaBruta + faixa.dias * PIXELS_POR_DIA`.
3. `direita <= 0` → `null`. É a faixa que termina inteiramente antes da abertura da timeline —
   mesmo tratamento que `dias === 0` já recebia.
4. `esquerdaBruta < 0` → `{ esquerda: 0, largura: direita, cortadaNaEsquerda: true, ... }`.
5. caso contrário → `{ esquerda: esquerdaBruta, largura: faixa.dias * PIXELS_POR_DIA,
   cortadaNaEsquerda: false, ... }`.
6. `mostrarRotulo = largura > LARGURA_MINIMA_PARA_ROTULO` — sobre a largura DESENHADA (a reduzida,
   quando cortada), porque o rótulo precisa caber no que aparece na tela.

**Nunca deixe `esquerda` negativo**: a linha não tem `overflow-hidden` e a barra vazaria para fora.

**(d) `celulasDeQuinzena` → `celulasDeSemana`, `CelulaDeQuinzena` → `CelulaDeSemana`.**
- Mesmo laço, trocando `quinzenaQueContem` por `semanaQueContem`.
- `chave` passa a ser o próprio `cursor` (a data de início da célula já é única por célula).
- Rótulo: mesmo formato do atual (`1–15 ago` vira `10–16 ago`) quando a semana não cruza o mês.
  Semana que CRUZA o mês (caso novo — quinzena nunca cruzava) usa os dois meses:
  `28 ago–3 set`, ou seja `${diaInicio} ${formatarMes(cursor)}–${diaFim} ${formatarMes(ultimoDiaDaCelula)}`.
  Mesmo separador (travessão), mesmo `formatarMes` injetado por parâmetro — o módulo continua sem
  import.

**(e) `components/amassa/encomendas/gantt.tsx`.**
- Importe `celulasDeSemana`; renomeie o helper local `mesAbreviadoDaQuinzena` →
  `mesAbreviadoDaSemana` e atualize os comentários que falam em quinzena.
- `data-testid="gantt-celula-quinzena"` → `data-testid="gantt-celula-semana"`.
- Marca de corte, no ramo do RETÂNGULO (barras). Quando `retangulo.cortadaNaEsquerda`:
  acrescente ao `style` `borderLeft: "3px solid var(--color-borda-forte)"`,
  `borderTopLeftRadius: 0` e `borderBottomLeftRadius: 0` (canto reto + borda grossa = corte), e
  ponha `data-cortada="true"` no elemento. Quando não cortada, `data-cortada="false"` e nenhum
  estilo extra.
- Quando cortada, o `aria-label` da barra ganha o sufixo `" — começou antes"`, para o corte ser
  perceptível também sem enxergar. Cuidado: os testes existentes casam o rótulo por
  `getByRole("img", { name: /^Produção/ })` (âncora no início), então o sufixo não os quebra.
- Marcos (losangos) não precisam de tratamento: valem 1 dia, então ou cabem inteiros no intervalo
  ou `retanguloDaEtapa` já devolveu `null`.

**Consequência aceita, não corrija:** uma encomenda cujo cronograma inteiro está no passado (ex.: a
atrasada de `dataEmDias(-60)` das specs) passa a desenhar uma linha do Gantt sem nenhuma barra. É a
decisão do brief operando — a linha continua com nome e cliente, e o celular continua dizendo
"Atrasada".

**(f) Testes que quebram — é o contrato mudando, não regressão. ATUALIZE, nunca contorne nem pule.**

`tests/unit/gantt.test.ts`:
- `describe("calcularIntervalo")`, os 5 testes: retitule e reescreva as asserções de `primeiroDia`
  (agora a segunda da semana de `hoje`, sem folga) e de `ultimoDiaExclusivo` (fim da semana
  POSTERIOR à do maior `fimExclusivo`, +1). O teste "lista vazia" e o "uma encomenda só" perdem a
  frase "quinzena de folga de cada lado". O teste "com duas encomendas" passa a afirmar que o menor
  início NÃO afeta mais `primeiroDia`. Acrescente ao teste de largura a asserção
  `expect(intervalo.totalDeDias % 7).toBe(0)`.
- `describe("celulasDeQuinzena")` → `describe("celulasDeSemana")`, os 4 testes: mesmas invariantes
  de cobertura sem vão; "quinzenas inteiras" vira "semanas inteiras de 7 dias"; o teste de rótulo
  passa a esperar o formato semanal; "quinzenas parciais" vira "semanas parciais" (mantenha o
  intervalo montado à mão `2026-08-05` → `2026-08-20`, derive as células novas pela regra).
  **Acrescente um teste novo** para o rótulo de semana que cruza o mês.
- `describe("retanguloDaEtapa")`: acrescente `cortadaNaEsquerda: false` aos `toMatchObject`
  existentes. **Acrescente os dois casos de borda novos e obrigatórios:** (1) faixa iniciada antes
  de `primeiroDia` devolve `esquerda: 0`, largura reduzida e `cortadaNaEsquerda: true`, nunca
  `esquerda` negativa; (2) faixa que termina em ou antes de `primeiroDia` devolve `null`.
- `describe("constantes")`, `describe("deslocamentoEmPixels")`, `describe("rolagemInicial")` e
  `describe("ordenarParaGantt")` **não mudam** — usam intervalos montados à mão.

`tests/e2e/encomendas-indice.spec.ts`:
- `getByTestId("gantt-celula-quinzena")` → `"gantt-celula-semana"` nos dois pontos (linhas ~314 e
  ~453) e nos títulos/comentários que falam em quinzena.
- No teste do `scrollLeft` inicial, acrescente `expect(esperado).toBe(0)` — é a prova observável de
  que a timeline abre em hoje. O mecanismo (recomputar com `rolagemInicial` a partir de
  `data-primeiro-dia`) continua válido; só o número mudou.
- No teste "com uma encomenda só, a timeline ainda desenha a folga", retitule para refletir a folga
  de UMA SEMANA no fim e nenhuma no começo. `quantidadeDeCelulas > 1` continua valendo.
- **Teste novo obrigatório** (caso de borda do brief), no describe `Gantt desktop`: encomenda com
  `dataInicio: dataEmDias(-10)`; leia `caixaRegua = await page.getByTestId("gantt-regua").boundingBox()`;
  para TODOS os elementos `[data-testid^="gantt-barra-"]` e `[data-testid^="gantt-marco-"]` dentro
  da linha daquela encomenda, afirme `x >= caixaRegua.x - 1` (nada é desenhado à esquerda da origem
  da régua); e para qualquer elemento com `data-cortada="true"`, afirme
  `Math.round(x) === Math.round(caixaRegua.x)`. Escrito assim o teste independe do dia da semana em
  que ele roda — que é exatamente o que uma asserção sobre "a etapa X está cortada" não seria.

`tests/e2e/encomendas-filtros.spec.ts` (o brief não nomeou este arquivo, mas A3 o quebra):
- Teste "filtrar reduz o intervalo do Gantt (D-14)" (linha ~314): a encomenda "longínqua" hoje nasce
  em `dataEmDias(-200)` para alargar o intervalo. Com A3 o passado deixou de alargar qualquer coisa,
  então ela não alarga mais nada e o `toBeLessThan` passa a depender de dado concorrente de outras
  specs. **Troque para `dataEmDias(200)`** (longe no FUTURO, o único lado que ainda estica) e
  atualize o comentário das linhas ~326-328 e o nome da variável se ele mencionar o passado. A
  recomputação com `calcularIntervalo` (linhas 354-357) continua correta como está — ela usa a
  função de produção.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
    <automated>grep -v '^\s*//' lib/encomendas/gantt.ts | grep -c "getDay"</automated>
    <automated>grep -v '^\s*//' lib/encomendas/gantt.ts | grep -c "new Date"</automated>
    <automated>npm run test:e2e -- --grep "Gantt desktop|filtrar reduz o intervalo"</automated>
    <automated>npm run verificar</automated>
  </verify>
  <done>
    `tests/unit/gantt.test.ts` verde com o contrato novo, incluindo os dois casos de borda de
    recorte e o rótulo de semana que cruza o mês; os dois `grep` devolvem `0`; o e2er grepado passa;
    `npm run verificar` (fim do Lote A) passa limpo. Commit atômico:
    `feat(encomendas): timeline do Gantt abre em hoje, agrupada por semana`.
  </done>
</task>

<task type="auto">
  <name>Tarefa 4 (C): tela de trocar senha</name>
  <files>
    lib/auth/esquema-senha.ts,
    lib/auth/acoes-senha.ts,
    app/(app)/conta/senha/page.tsx,
    components/amassa/conta/formulario-troca-de-senha.tsx,
    components/amassa/menu-usuario.tsx,
    tests/unit/esquema-senha.test.ts,
    tests/e2e/trocar-senha.spec.ts
  </files>
  <read_first>
    app/(auth)/login/page.tsx (a analogia que o brief manda seguir: forma do formulário, tratamento
    de erro, frases),
    lib/auth/senha.ts (`gerarHash`, `conferirHash`),
    lib/auth/exigir-usuario.ts,
    lib/encomendas/acoes.ts linhas 1-50 (forma de Server Action com Zod e `ResultadoDeAcao`),
    tests/e2e/sessao.spec.ts linhas 129-150 (conta dedicada criada na hora via
    `npm run criar-usuario`)
  </read_first>
  <behavior>
    Política de senha — comprimento, não regrinha de símbolo:
    - senha nova com 11 caracteres → recusa, com mensagem em português
    - senha nova com 12 caracteres → aceita
    - `panela-barro-forno-quente` (sem maiúscula, sem número, sem símbolo além do hífen) → aceita
    - confirmação diferente da senha nova → recusa, com mensagem em português
    - senha atual vazia → recusa
  </behavior>
  <action>
**DECIDIDO: troca voluntária, NÃO obrigatória.** A tela existe e quem quiser troca. **Não criar
coluna `senha_provisoria`, não criar migração, não forçar troca no primeiro acesso.** Trocar senha é
só um `UPDATE` em `senha_hash`, coluna que já existe (`db/schema.ts:47`). Não toque em
`db/schema.ts` nem em `TABELAS_ESPERADAS` de `scripts/testar-migracoes.mjs`.

**(a) `lib/auth/esquema-senha.ts`.**
`export const TAMANHO_MINIMO_SENHA = 12;` e `esquemaTrocaDeSenha` (Zod):
- `senhaAtual: z.string().min(1, "Digite sua senha atual.")`
- `senhaNova: z.string().min(TAMANHO_MINIMO_SENHA, "A senha nova precisa ter pelo menos 12 caracteres.")`
- `confirmacao: z.string()`
- `.refine(dados => dados.senhaNova === dados.confirmacao, { message: "A confirmação não bate com a senha nova.", path: ["confirmacao"] })`

Comprimento, não regrinha de símbolo: **nenhuma exigência de maiúscula, número ou símbolo.** Uma
frase de quatro palavras é mais fácil de lembrar e mais difícil de quebrar que `Amassa@2026`.
**A política nova vale só para a troca** — não mexa em `lib/auth/entrada-credenciais.ts`, cujo
`senha: z.string().min(1)` existe para não vazar informação na tela de entrada.

**(b) `lib/auth/acoes-senha.ts` — Server Action.**
Arquivo novo, não `lib/auth/acoes.ts`: aquele arquivo documenta explicitamente que NÃO toca o banco,
e esta ação toca. `"use server"` no topo.

```
export async function trocarSenha(entradaBruta: unknown): Promise<{ ok: true } | { ok: false; erro: string }>
```
- **`const usuarioAtual = await exigirUsuario();` como PRIMEIRA instrução do corpo** — regra do
  `CLAUDE.md`, verificada por `npm run verificar-acoes` por árvore sintática.
- `esquemaTrocaDeSenha.safeParse(entradaBruta)`; em falha, devolva a primeira mensagem de `issues`.
- Leia `senhaHash` do banco por `usuarioAtual.id` (`exigirUsuario` devolve o usuário SEM o hash, de
  propósito): `db.select({ senhaHash: usuarios.senhaHash }).from(usuarios).where(eq(usuarios.id,
  usuarioAtual.id)).limit(1)`.
- `conferirHash(senhaHash, dados.senhaAtual)` falso → `{ ok: false, erro: "A senha atual não
  confere. Confira e tente de novo." }` — erro em linguagem humana, dizendo o que fazer.
- Certo → `db.update(usuarios).set({ senhaHash: await gerarHash(dados.senhaNova) })
  .where(eq(usuarios.id, usuarioAtual.id))` e `{ ok: true }`.
- **Reaproveite `gerarHash`/`conferirHash` de `lib/auth/senha.ts`. Não escreva segunda função de
  hash.**
- **Troca só a própria senha.** O id vem da sessão via `exigirUsuario()`, **nunca do cliente** — o
  esquema Zod não tem campo de id, e não deve ganhar um.

**(c) Tela `/conta/senha`.**
Rota, não diálogo: o brief permite "Tela/diálogo", e uma rota funciona igual nos dois viewports sem
duplicar `Dialog` + `Sheet` nem esbarrar nas armadilhas de composição do Radix já documentadas em
`menu-usuario.tsx`. As telas de erro e carregamento do grupo (`app/(app)/error.tsx`,
`app/(app)/loading.tsx`) já cobrem a rota nova — não crie arquivos por rota.

- `app/(app)/conta/senha/page.tsx`: Server Component com `await exigirUsuario();` na primeira linha,
  `CabecalhoPagina` (mesmo componente das outras telas) e o formulário cliente.
- `components/amassa/conta/formulario-troca-de-senha.tsx`: Client Component. Três campos
  `type="password"` — senha atual (`autoComplete="current-password"`), senha nova e confirmação
  (`autoComplete="new-password"`). Copie as classes de input de `app/(auth)/login/page.tsx:63-69`
  verbatim: elas já garantem `min-h-[44px]` de toque e `text-corpo` — **nunca abaixo de 16px, senão
  o iOS dá zoom sozinho ao focar**. Cada campo com `<label>` associado, em português.
- Dica em pt-BR abaixo do campo de senha nova, sugerindo a frase de palavras — por exemplo
  "Pelo menos 12 caracteres. Uma frase de quatro palavras funciona bem: panela-barro-forno-quente".
- Estado de erro: `<p role="alert" aria-live="assertive" className="text-apoio text-destructive">`,
  igual ao login. Estado de sucesso: `<p role="status">` com frase em português, e limpe os três
  campos. Botão desabilitado enquanto a ação está no ar (`useState` simples de pendência basta —
  três campos não pedem `react-hook-form`).
- Validação no cliente é conveniência, não segurança: a decisão é sempre a do servidor. Exiba o
  `erro` devolvido por `trocarSenha`.

**(d) `components/amassa/menu-usuario.tsx` — as DUAS variantes.**
Acrescente "Trocar senha" (ícone `KeyRound` do `lucide-react`) apontando para `/conta/senha`,
copiando exatamente o padrão do item "Orçamentos" que já existe em cada variante: `<Link>` dentro do
`<div className="flex flex-col gap-1 px-4 pb-4">` no celular, `<DropdownMenuItem asChild><Link>` no
desktop. Item de navegação puro — nada de `onClick`, nada de `asChild` sobre `<button>`, nenhuma das
armadilhas do Radix documentadas nesse arquivo.

Nota: D-15 dizia "exatamente três coisas no menu do usuário". O brief manda a tela ser "alcançável
pelo menu do usuário", o que substitui aquela decisão. Registre isso no SUMMARY.

**(e) Testes.**
`tests/unit/esquema-senha.test.ts` — cada linha do bloco `<behavior>`, afirmando também que as
mensagens saem em português.

`tests/e2e/trocar-senha.spec.ts` — describe `"trocar senha"`, `test.describe.configure({ mode:
"serial" })` (mesma prudência dos outros arquivos que fazem login argon2id de verdade). Trocar senha
MUTA estado compartilhado, então **cada teste cria sua própria conta na hora**, nunca reaproveita a
conta global de `preparar-usuario.ts`. Use o padrão de `tests/e2e/sessao.spec.ts:135-146`
(`execSync("npm run criar-usuario -- --nome ... --email ...")` com `DATABASE_URL:
process.env.DATABASE_URL_TESTE`, lendo a linha `SENHA: ` da saída). O e-mail precisa incluir
`testInfo.project.name` **e `testInfo.retry`** — sem o `retry` a segunda tentativa em CI colide com
a conta criada na primeira (causa raiz real corrigida no plano 04-07 em `autenticacao.spec.ts`).

Três testes:
1. **senha atual errada não troca**: entra, vai a `/conta/senha`, envia com senha atual errada e
   senha nova válida → mensagem de erro visível; limpa os cookies, entra de novo com a senha
   ORIGINAL → chega em `/` (a senha não foi trocada).
2. **senha nova curta é recusada pelo servidor**: envia com senha nova de 11 caracteres → mensagem
   de erro visível, sem troca.
3. **troca, sai, entra com a nova**: envia com senha atual certa e nova válida (uma frase de quatro
   palavras) → mensagem de sucesso; `await page.context().clearCookies()` (é o "sair" desta spec —
   o botão de sair já é provado por `casca.spec.ts`/`sessao.spec.ts`, e repeti-lo aqui só
   acrescentaria interação de menu específica de viewport); entrar com a senha ANTIGA → erro de
   credenciais; entrar com a NOVA → chega em `/`.

Sobre o item "Teste da Server Action" do brief: ele é satisfeito por esses e2e, não por Vitest.
`exigirUsuario()` importa `@/lib/auth/auth` dinamicamente e só resolve dentro do bundler do Next.js,
e a ação toca o banco — não existe camada de mock para isso neste projeto, e inventar uma aqui seria
maior que a feature. Os e2e provam a mesma coisa com banco, hash e sessão reais, que é evidência
mais forte. **Registre essa troca no SUMMARY.**
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test</automated>
    <automated>npm run test:e2e -- --grep "trocar senha"</automated>
    <automated>npm run verificar</automated>
  </verify>
  <done>
    `npm run verificar-acoes` continua passando (a ação nova tem `exigirUsuario()` na primeira
    linha); os testes unitários da política passam; os três e2e passam nos dois projetos;
    `npm run verificar` (fim do Lote C) passa limpo; `git status` não mostra nenhuma migração nova
    nem mudança em `db/schema.ts`. Commit atômico:
    `feat(auth): tela de trocar a própria senha, com política de 12 caracteres`.

    Depois do commit, execute o fechamento descrito em `<verification>`.
  </done>
</task>

</tasks>

<verification>
Fechamento, depois dos quatro commits:

1. `npm run verificar` — tem que passar limpo.
2. **Uma** varredura completa de `npm run test:e2e` **sem `--grep`**, uma vez só. Esta é a única
   invocação de suíte inteira autorizada neste plano.
3. Se algum teste falhar e a causa for ambiente Windows já conhecido, consulte `WINDOWS.md` antes de
   tratar como regressão.
4. `git log --oneline -4` mostra exatamente quatro commits, um por item do brief.
5. `git status` limpo, sem migração nova e sem mudança em `db/schema.ts`.
6. Registre no SUMMARY **quais comandos de e2e rodou de fato** e quantas vezes.
</verification>

<o_que_nao_fazer>
Do brief, literal:
- **Não** mexer nas datas de queima/entrega da encomenda (escolher data de entrega, âncora
  invertida). É outro lote, precisa de migração e tem pergunta de produto em aberto.
- **Não** criar migração nenhuma.
- **Não** iniciar a Fase 5 (Agenda de Aulas).
- **Não** trocar a barra do celular pela timeline rolável (A2).
- **Não** reabrir nenhuma decisão marcada como DECIDIDO, nem propor alternativas ao dono.
- **Não** contornar, pular ou marcar como `skip` nenhum teste que A3 quebrar — atualizar é o
  trabalho.
</o_que_nao_fazer>

<success_criteria>
- Quatro commits atômicos, na ordem A1 → A2 → A3 → C.
- Desktop: nome no Gantt abre `/encomendas/{id}`, provado por teste e2e novo no projeto `desktop`.
- Celular: barra com marca de hoje (ausente quando fora do intervalo) e datas nas duas pontas.
- Gantt abre em hoje, células semanais começando na segunda, nenhuma barra com `esquerda` negativa,
  barra cortada marcada visualmente e no `aria-label`.
- `lib/encomendas/gantt.ts` e `lib/encomendas/trilha.ts` sem `new Date` e sem método de dia da
  semana de `Date`; `trilha.ts` sem nenhum `import`.
- Tela de trocar senha alcançável pelas duas variantes do menu do usuário; senha antiga para de
  funcionar depois da troca; senha atual errada não troca.
- `npm run verificar` limpo ao fim do Lote A e ao fim do Lote C.
- Uma varredura completa de e2e, uma vez só, verde no fechamento.
</success_criteria>

<output>
Ao terminar, crie `.planning/quick/260812-2et-executa-brief-noturno-lote-a-gantt-clica/260812-2et-SUMMARY.md`
registrando: os quatro commits, os comandos de e2e efetivamente executados (e quantas vezes), a
substituição do "teste da Server Action" em Vitest por prova e2e, e a substituição de D-15
("exatamente três coisas no menu do usuário") pela instrução do brief.
</output>
