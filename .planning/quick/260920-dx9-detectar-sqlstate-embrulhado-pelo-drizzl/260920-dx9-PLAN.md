---
quick_id: 260920-dx9
description: Detectar SQLSTATE embrulhado pelo Drizzle em Abertura, Cotações e Queimas
mode: quick
phase: quick-260920-dx9
plan: 1
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - lib/erro/postgres.ts
  - tests/unit/erro-postgres.test.ts
  - lib/abertura/acoes.ts
  - lib/cotacoes/acoes.ts
  - lib/queimas/acoes.ts
  - components/amassa/queimas/registrar-queima.tsx
  - tests/e2e/apoio/apagar-referencia.ts
  - tests/e2e/erro-chave-estrangeira.spec.ts

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low   # zero amostras de calibração para este repositório

must_haves:
  truths:
    - "Registrar uma queima num forno apagado há um segundo mostra na tela «Esse forno não existe mais. Recarregue a página.» — nunca a frase genérica de falha."
    - "Salvar uma tarefa ligada a um item apagado há um segundo mostra «O item ligado a esta tarefa não existe mais. Recarregue a página e tente de novo.»"
    - "Salvar uma cotação numa categoria apagada há um segundo mostra «Essa categoria não existe mais. Recarregue a página e tente de novo.»"
    - "Um SQLSTATE que chega embrulhado em `.cause.code` é lido igual a um que chega solto em `.code` — os dois caminhos provados por teste unitário."
    - "Nada é perdido em silêncio: nos formulários de Abertura e Cotações o texto digitado continua na tela junto com a mensagem de erro."
  artifacts:
    - lib/erro/postgres.ts
    - tests/unit/erro-postgres.test.ts
    - tests/e2e/apoio/apagar-referencia.ts
    - tests/e2e/erro-chave-estrangeira.spec.ts
  key_links:
    - "lib/abertura/acoes.ts, lib/cotacoes/acoes.ts e lib/queimas/acoes.ts importam de @/lib/erro/postgres e não declaram mais nenhum detector próprio."
    - "components/amassa/queimas/registrar-queima.tsx repassa `resposta.erro` ao toast — sem isso a mensagem humana de Queimas nunca chega à tela, mesmo com o detector certo."
    - "lib/erro/postgres.ts não importa React, nem o cliente do banco, nem nenhum módulo de domínio."
---

<objective>
`drizzle-orm/node-postgres` embrulha todo erro de query num `DrizzleQueryError`, cujo `.code` é
`undefined`; o SQLSTATE real do Postgres mora em `erro.cause.code`. Os detectores de uma linha só
que Abertura, Cotações e Queimas copiaram uns dos outros olham o embrulho, nunca a causa — então
eles NUNCA casam, e as três telas mostram a frase genérica de falha no lugar da mensagem humana.
Os três módulos estão no ar.

Este plano extrai o detector já corrigido de `lib/cadastros/acoes.ts` para
`lib/erro/postgres.ts`, aplica nos três módulos, destrava a exibição da mensagem em Queimas, e
prova por um e2e que apaga a linha referenciada DE VERDADE — direto no banco, com o formulário já
na tela — que a frase humana volta a aparecer nos três.

Purpose: o dono está de pé no ateliê com a mão suja. "Não deu para salvar" não diz o que fazer;
"Esse forno não existe mais. Recarregue a página." diz.
Output: um módulo compartilhado com teste unitário, três módulos corrigidos, um componente
destravado, e um spec e2e que reproduz a corrida real nos três.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/STATE.md

Implementação de referência, JÁ CORRIGIDA — copiar a semântica daqui, não reinventar:
@lib/cadastros/acoes.ts   (linhas 56-90: `codigoDoErroPostgres` e os dois usos dele)

Os três módulos a corrigir:
@lib/abertura/acoes.ts    (detector nas linhas 40-45; usos nos `catch` das linhas ~136 e ~296)
@lib/cotacoes/acoes.ts    (detector nas linhas 31-36; uso no `catch` da linha ~108)
@lib/queimas/acoes.ts     (detector nas linhas 27-32; uso no `catch` da linha ~99)

Vizinhança e moldes:
@lib/erro/textos.ts                              (o módulo puro sem domínio onde o novo arquivo vai morar)
@components/amassa/queimas/registrar-queima.tsx  (linha 51: descarta `resposta.erro`)
@tests/e2e/apoio/semear-queimas.ts               (molde do acesso direto ao banco com o cliente `pg`)
@tests/e2e/queimas-registro.spec.ts              (molde de spec: `fazerLogin`, `nomeUnico`, `cartaoDoForno`)
@playwright.config.ts                            (projetos e a cadeia `vazio-* → desktop/celular`)
</context>

<orcamento_de_e2e>
Regra do CLAUDE.md §Conventions, que ESTE PLANO RESPEITA POR CONSTRUÇÃO:

- `npm run test:e2e` cobra ~53s de imposto fixo antes do primeiro teste (15s de Postgres efêmero
  + 38s de `next build`), valha para a suíte inteira ou para um `--grep` que pega um teste só.
- **No máximo UMA invocação de `npm run test:e2e` por tarefa**, sempre com `--grep`.
- **NUNCA `npm run build` como passo separado** — a execução do e2e já constrói.
- `npm run lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e `npm test` são rápidos: à vontade.

**Consequência de projeto, deliberada:** as Tarefas 1 e 2 NÃO invocam o e2e. A prova dos TRÊS
módulos cabe numa ÚNICA invocação, na Tarefa 3, porque os três testes moram no mesmo arquivo sob
um `describe` cujo título contém "chave estrangeira".

**Por que não uma fatia vertical por módulo:** um traçador por módulo custaria três invocações
(~2min50s só de imposto). O orçamento do CLAUDE.md vence a decomposição vertical padrão aqui. O
que substitui o traçador é o teste unitário da Tarefa 1, que prova o formato real do erro
embrulhado sem servidor nenhum, antes de qualquer módulo ser tocado.

Total autorizado por este plano: **1 invocação de `npm run test:e2e`**. Registre no SUMMARY quais
comandos rodou de fato.
</orcamento_de_e2e>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: `lib/erro/postgres.ts` — o detector compartilhado, com teste unitário primeiro</name>
  <files>lib/erro/postgres.ts, tests/unit/erro-postgres.test.ts</files>

  <read_first>
    `lib/cadastros/acoes.ts` linhas 56-90 — `codigoDoErroPostgres` já está correto lá; a semântica
    exata (inclusive o fall-through) tem de ser preservada, não "melhorada".
    `lib/erro/textos.ts` — o cabeçalho deste arquivo explica a disciplina do diretório: módulo
    puro, sem nenhum import.
  </read_first>

  <behavior>
    Escreva `tests/unit/erro-postgres.test.ts` ANTES da implementação. Casos obrigatórios de
    `codigoDoErroPostgres(erro: unknown): string | undefined`:

    - Erro cru do `pg`, com a propriedade `code` de tipo string no próprio objeto → devolve esse
      código.
    - Erro EMBRULHADO no formato real de produção: objeto cuja propriedade `code` vale `undefined`
      E cuja `cause` é um objeto com `code: "23503"` → devolve `"23503"`. **Este é o caso que hoje
      falha em produção; se algum outro passar e este não, a correção não aconteceu.**
    - Erro embrulhado cujo objeto NÃO tem a propriedade `code` de jeito nenhum, só `cause.code` →
      devolve o código da causa.
    - Não-objeto: a string `"23503"`, `null`, `undefined`, o número `23503` → todos devolvem
      `undefined`.
    - Objeto sem nenhuma dessas propriedades (`{}` e um `Error` comum) → `undefined`.
    - Propriedade `code` de tipo não-string no objeto raiz (o número `23503`) sem `cause` →
      `undefined` (nunca o número convertido em texto).
    - `cause` presente mas não é objeto (uma string, `null`) → `undefined`.
    - `cause` é objeto com `code` de tipo não-string (número) → `undefined`.

    E de `ehViolacaoDeChaveEstrangeira(erro: unknown): boolean`:
    - `true` para o código `"23503"` tanto solto na raiz quanto embrulhado em `cause`.
    - `false` para `"23505"` (unicidade), `"P0001"` (gatilho), `"23514"` (check) e para
      `undefined`/`null`/`{}`.
  </behavior>

  <action>
    Crie `lib/erro/postgres.ts`. Ele é o TERCEIRO módulo sem domínio do projeto, ao lado de
    `lib/erro/textos.ts` e `lib/acessibilidade/rotulos.ts`: **zero import** — nem React, nem
    `@/db`, nem `drizzle-orm`, nem nenhum módulo de domínio. Nenhuma dependência nova entre
    domínios nasce daqui (decisão travada com o dono).

    Exporte duas funções:

    `codigoDoErroPostgres(erro: unknown): string | undefined` — porte a implementação de
    `lib/cadastros/acoes.ts` linhas 63-75 sem mudar a semântica. Preserve o **fall-through**: se o
    objeto raiz tem a propriedade `code` mas ela não é uma string, a função NÃO retorna ali —
    segue para a causa. Esse detalhe é o que faz o formato real do `DrizzleQueryError` funcionar,
    porque lá o `code` da raiz existe valendo `undefined`. Inverter isso para um retorno antecipado
    reintroduz o defeito exato que este plano corrige.

    `ehViolacaoDeChaveEstrangeira(erro: unknown): boolean` — compara o resultado de
    `codigoDoErroPostgres` com o SQLSTATE `23503` (`foreign_key_violation`).

    Comentário de cabeçalho em português, explicando POR QUE as duas propriedades são consultadas
    (o driver embrulha hoje; se um dia parar de embrulhar, a leitura da raiz continua valendo) e
    citando que a origem do achado foi o plano 04.4-02. Não copie o texto do detector antigo para
    dentro de nenhum comentário — descreva o defeito com suas palavras.

    NÃO exporte nada além dessas duas funções. NÃO acrescente detectores de `23505`/`P0001`
    especulativamente: `lib/cadastros/acoes.ts` está fora do escopo travado deste plano e continua
    com a cópia privada dele.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/erro-postgres.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -c '^import ' lib/erro/postgres.ts   # precisa ser 0 — âncora de coluna zero, nunca casa comentário</automated>
  </verify>

  <done>
    `tests/unit/erro-postgres.test.ts` cobre os oito cenários do bloco `<behavior>` e passa
    inteiro. O caso do erro embrulhado (raiz com `code` valendo `undefined`, causa com
    `code: "23503"`) devolve `"23503"`. `lib/erro/postgres.ts` não tem nenhuma linha de import.
    `lint` e `tsc --noEmit` limpos. Nenhuma invocação de `npm run test:e2e` nesta tarefa.
    Commit: `feat(erro): detector compartilhado de SQLSTATE que enxerga o erro embrulhado`.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: aplicar nos três módulos e destravar a exibição da mensagem em Queimas</name>
  <files>lib/abertura/acoes.ts, lib/cotacoes/acoes.ts, lib/queimas/acoes.ts, components/amassa/queimas/registrar-queima.tsx</files>

  <read_first>
    Nos três `acoes.ts`, localize a declaração local de `ehViolacaoDeChaveEstrangeira` e TODOS os
    `catch` que a chamam. Em `lib/abertura/acoes.ts` são DOIS usos (criação e atualização de
    tarefa), não um.
  </read_first>

  <action>
    Nos três módulos — `lib/abertura/acoes.ts`, `lib/cotacoes/acoes.ts`, `lib/queimas/acoes.ts`:

    1. Acrescente `import { ehViolacaoDeChaveEstrangeira } from "@/lib/erro/postgres";` no grupo de
       imports com alias `@/`, junto de `@/lib/auth/exigir-usuario`, respeitando a ordenação que o
       eslint do projeto já impõe nesses arquivos.
    2. Apague a declaração local homônima e o comentário de bloco que a acompanha. No lugar do
       comentário apagado, se quiser deixar rastro, escreva uma linha curta apontando o módulo
       novo por caminho — sem reproduzir o texto do detector antigo.
    3. Não toque em mais nada. Os `catch` continuam chamando `ehViolacaoDeChaveEstrangeira(erro)`
       com a mesma assinatura, e **as mensagens humanas existentes ficam LETRA POR LETRA iguais** —
       o que muda é só a detecção. Nada de mover essas frases para `textos.ts`, nada de reescrever
       redação, nada de renomear.
    4. `exigirUsuario()` continua sendo a PRIMEIRA instrução do corpo de toda Server Action. Você
       não vai mexer nisso; o portão `npm run verificar-acoes` existe para provar que também não
       quebrou por acidente ao reorganizar imports.

    Em `components/amassa/queimas/registrar-queima.tsx`, no ramo `if (!resposta.ok)` da função
    `registrar` (hoje na linha 51): troque o argumento de `toast.error` pelo texto que o servidor
    devolveu, `resposta.erro`. É seguro e não muda o caso genérico: a própria `registrarQueima` já
    usa a frase genérica de falha como fallback do seu último `catch`, então um erro desconhecido
    continua produzindo exatamente o texto de hoje. Sem esta troca, a mensagem humana de Queimas
    nunca chega à tela, por melhor que seja o detector.

    Depois da troca, o texto genérico importado deixa de ser usado NESTE arquivo — remova esse
    especificador do import, senão o `eslint --max-warnings=0` reprova. Cuidado: o outro texto
    importado do mesmo módulo (o do "Desfazer", usado na função `desfazer`) CONTINUA em uso, não
    remova a linha de import inteira. Atualize o comentário adjacente para dizer que a tela mostra
    a frase que o servidor devolveu.

    `components/amassa/abertura/formulario-tarefa.tsx` e
    `components/amassa/cotacoes/formulario-cotacao.tsx` JÁ exibem `resposta.erro` via `setErro`
    (linhas 170 e 176). **Não mude esses dois arquivos.**
  </action>

  <verify>
    <automated>grep -c 'from "@/lib/erro/postgres"' lib/abertura/acoes.ts lib/cotacoes/acoes.ts lib/queimas/acoes.ts   # cada um precisa reportar 1</automated>
    <automated>grep -c '^function ehViolacaoDeChaveEstrangeira' lib/abertura/acoes.ts lib/cotacoes/acoes.ts lib/queimas/acoes.ts   # cada um precisa reportar 0 (âncora de coluna zero: nunca casa comentário)</automated>
    <automated>grep -c 'toast.error(resposta.erro)' components/amassa/queimas/registrar-queima.tsx   # precisa reportar 1</automated>
    <automated>git diff --stat components/amassa/abertura/formulario-tarefa.tsx components/amassa/cotacoes/formulario-cotacao.tsx   # precisa sair vazio</automated>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run verificar-acoes</automated>
    <automated>npm test</automated>
  </verify>

  <done>
    Os três módulos importam o detector compartilhado e nenhum declara mais o seu próprio. Os dois
    `catch` de Abertura, o de Cotações e o de Queimas seguem intactos, com as mesmas frases. O
    componente de Queimas repassa `resposta.erro` ao toast e os dois formulários de Abertura e
    Cotações ficaram sem diff. `lint`, `tsc --noEmit`, `verificar-acoes` e `npm test` limpos.
    Nenhuma invocação de `npm run test:e2e` nesta tarefa.
    Commit: `fix(erro): Abertura, Cotações e Queimas passam a enxergar o SQLSTATE embrulhado`.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: a prova e2e — apagar a linha referenciada com o formulário já na tela, nos três módulos, numa invocação só</name>
  <files>tests/e2e/apoio/apagar-referencia.ts, tests/e2e/erro-chave-estrangeira.spec.ts</files>

  <precondition>
    O Docker precisa estar rodando — `npm run test:e2e` sobe um Postgres efêmero por
    `scripts/testar-e2e.mjs` e publica `DATABASE_URL_TESTE`. Se o daemon não estiver de pé, pare e
    avise; não tente contornar apontando para outro banco.
  </precondition>

  <read_first>
    `tests/e2e/apoio/semear-queimas.ts` — o molde exato do acesso direto ao banco: `comCliente`,
    `DATABASE_URL_TESTE`, consulta parametrizada, e a disciplina de FALHAR ALTO quando o número de
    linhas afetadas não é o esperado.
    `tests/e2e/queimas-registro.spec.ts` — `fazerLogin`, `nomeUnico`, `cartaoDoForno`, os
    `data-testid` do cartão e do seletor de tipo.
    `tests/e2e/abertura-tarefas.spec.ts` linhas 250-266 — o fluxo pronto de criar um item de
    abertura pela interface e depois cadastrar uma tarefa ligada a ele.
    `tests/e2e/cotacoes-ciclo.spec.ts` linhas 70-105 — os auxiliares `criarCategoria` e
    `criarCotacao`.
  </read_first>

  <action>
    **Auxiliar — `tests/e2e/apoio/apagar-referencia.ts`.** Mesmo molde de `semear-queimas.ts`: uma
    função `comCliente` privada que abre um `Client` do `pg` com
    `process.env.DATABASE_URL_TESTE` e sempre fecha no `finally`. **Nunca** `DATABASE_URL`, nunca
    um fallback para outra variável — apagar linha é destrutivo e o único banco em que isso pode
    acontecer é o efêmero de teste.

    Exporte TRÊS funções estreitas, uma por tabela — nada de um executor de SQL genérico num
    repositório público:
    `apagarFornoPeloNome(nome: string): Promise<void>`,
    `apagarItemDeAberturaPeloNome(nome: string): Promise<void>`,
    `apagarCategoriaDeCotacaoPeloNome(nome: string): Promise<void>`.
    Cada uma roda um `delete` nas tabelas `fornos`, `abertura_itens` e `cotacao_categorias`
    respectivamente, filtrando pela coluna `nome` por **parâmetro posicional** (`$1`), jamais por
    concatenação de texto. Cada uma confere `rowCount === 1` e lança um erro nomeando a função, a
    tabela e o nome procurado quando não for — um apagamento silenciosamente vazio deixaria o
    teste passando pelo motivo errado (a mensagem humana não apareceria e nós não saberíamos por
    quê). Comentário de cabeçalho explicando que o propósito destas funções é criar a corrida real
    "a linha sumiu entre a montagem do formulário e o envio", que nenhum caminho da aplicação
    consegue produzir sob demanda.

    Vale confirmar, ao escrever: as três exclusões são possíveis porque nenhuma restrição as
    barra. `queimas.forno_id` e `cotacoes.categoria_id` são `on delete cascade`;
    `abertura_tarefas.item_id` é `on delete set null`. E o `insert` posterior com o identificador
    já apagado é o que levanta o SQLSTATE `23503`, que é exatamente o que queremos provar.

    **Spec — `tests/e2e/erro-chave-estrangeira.spec.ts`.** Um `test.describe` cujo título contém
    a expressão **"chave estrangeira"** — é por ela que a invocação única do `--grep` pega os três
    casos. Copie `fazerLogin` e `nomeUnico` do molde de `queimas-registro.spec.ts` (nomes com
    prefixo `[e2e] ` e sufixo de projeto + instante, para os dois projetos do Playwright não
    disputarem dado). Sem nenhuma etiqueta `@vazio-*`: os três casos criam os próprios dados e
    **nenhum deles pode afirmar condição global do banco** (convenção do CLAUDE.md — a asserção é
    sempre sobre a linha que o próprio teste criou). Atenção ao orçamento de caracteres do nome da
    categoria de cotação: a restrição `cotacao_categorias_nome_comprimento` limita a 60, e o
    envoltório de `nomeUnico` já consome ~35.

    Caso 1 — Queimas. Faça login, cadastre um forno pela interface (`/queimas?novo`, "Nome" e
    "Limite"), espere o cartão do forno ficar visível. **Então** chame `apagarFornoPeloNome`.
    Depois clique em "Queimar" e no tipo `biscoito` (`data-testid="tipo-queima-biscoito"`).
    Afirme que o texto `Esse forno não existe mais. Recarregue a página.` fica visível. Afirme
    também que a frase genérica de falha ao registrar NÃO aparece.

    Caso 2 — Abertura. Faça login, crie um item em `/abertura?item=novo` preenchendo "O que é" e
    "Valor total". Abra `/abertura?aba=tarefas&tarefa=nova`, preencha "O que fazer", preencha "Até
    quando" com o dia de hoje em Brasília (`hojeEmBrasilia` de `@/lib/abertura/formato`, como a
    spec de tarefas já faz) e escolha o item no combobox "Ligada a algum item?". Deixe "Quem" no
    padrão — um responsável escolhido dispara uma conferência de gestor ativo ANTES do `insert`, e
    essa não é a corrida que queremos medir. **Então** chame `apagarItemDeAberturaPeloNome` e só
    depois clique em "Adicionar tarefa". Afirme que o texto `O item ligado a esta tarefa não
    existe mais. Recarregue a página e tente de novo.` fica visível.

    Caso 3 — Cotações. Faça login, vá a `/abertura?aba=cotacoes`, crie uma categoria e abra o
    formulário "Nova cotação" dentro dela, preenchendo "Empresa". **Então** chame
    `apagarCategoriaDeCotacaoPeloNome` e só depois clique em "Salvar". Afirme que o texto `Essa
    categoria não existe mais. Recarregue a página e tente de novo.` fica visível.

    Nos Casos 2 e 3, afirme ainda que o formulário CONTINUA aberto com o texto digitado intacto no
    campo — é a prova de que não houve perda silenciosa (CLAUDE.md). Nos três, prefira
    `getByText` sobre a frase visível a depender de `role="alert"`, para o teste valer igual para
    o toast de Queimas e para os dois `<p>` de erro.

    Use `timeout: 10000` nas asserções que dependem de resposta do servidor — o servidor Next é
    único e compartilhado por todos os workers, mesma justificativa já documentada em
    `queimas-registro.spec.ts`.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -c 'DATABASE_URL_TESTE' tests/e2e/apoio/apagar-referencia.ts   # precisa reportar 1</automated>
    <automated>grep -c 'chave estrangeira' tests/e2e/erro-chave-estrangeira.spec.ts   # o título do describe que o --grep usa</automated>
    <automated>npm run test:e2e -- --grep "chave estrangeira"</automated>
    <automated>npm run verificar</automated>
  </verify>

  <done>
    A ÚNICA invocação de `npm run test:e2e` autorizada por este plano roda `--grep "chave
    estrangeira"` e os três casos passam nos dois projetos (`desktop` e `celular`) — seis
    execuções, uma construção. As três mensagens humanas aparecem na tela; nos Casos 2 e 3 o
    formulário continua aberto com o texto digitado. `npm run verificar` passa inteiro
    (`lint`, `tsc --noEmit`, `verificar-acoes`, `npm test`, `test:migracoes` — este último sem
    mudança de `TABELAS_ESPERADAS`, porque este plano não toca em `db/schema.ts`).
    O SUMMARY registra quais comandos rodaram de fato.
    Commit: `test(erro): e2e prova a mensagem humana de chave estrangeira nos três módulos`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → Server Action | O texto cru do erro do banco cruza para dentro da aplicação e pode vazar para a tela. |
| Server Action → navegador | O que `ResultadoDeAcao.erro` carrega é exibido literalmente ao usuário. |
| Suíte de teste → banco | `tests/e2e/apoio/apagar-referencia.ts` abre conexão direta e APAGA linhas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-dx9-01 | Information Disclosure | `lib/erro/postgres.ts` | medium | mitigate | O detector devolve SOMENTE o SQLSTATE (cinco caracteres), nunca `message`, `detail`, `table` ou `constraint` do erro do Postgres. O texto cru continua indo só para `console.error` no servidor, como já é hoje nos três `catch`. |
| T-dx9-02 | Tampering | `tests/e2e/apoio/apagar-referencia.ts` | high | mitigate | O auxiliar lê exclusivamente `DATABASE_URL_TESTE`, sem nenhum fallback para `DATABASE_URL`. Apaga por `nome` (sempre prefixado `[e2e] ` pelos testes) e exige `rowCount === 1`, então uma conexão apontando para o lugar errado falha alto em vez de apagar em silêncio. |
| T-dx9-03 | Tampering | `tests/e2e/apoio/apagar-referencia.ts` | medium | mitigate | Consulta parametrizada (`$1`), jamais concatenação — o nome do teste vem de `nomeUnico` mas o auxiliar não confia nisso. Três funções estreitas por tabela, nenhum executor de SQL genérico versionado num repositório público. |
| T-dx9-04 | Elevation of Privilege | `lib/{abertura,cotacoes,queimas}/acoes.ts` | high | mitigate | A reorganização de imports não pode deslocar `exigirUsuario()` da primeira instrução de nenhuma Server Action. `npm run verificar-acoes` (árvore sintática, nunca regex) roda nas Tarefas 2 e 3 como portão. |
| T-dx9-05 | Information Disclosure | `components/amassa/queimas/registrar-queima.tsx` | low | mitigate | Passar `resposta.erro` direto ao toast só é seguro porque TODO caminho de `registrarQueima` devolve texto controlado: mensagem de Zod, a frase humana de chave estrangeira, ou a frase genérica do `catch` final — o erro cru do banco nunca entra em `erro`. Conferido nos três `return { ok: false }` da ação antes de commitar a Tarefa 2. |
| T-dx9-SC | Tampering | npm/pip/cargo installs | high | accept | Nenhum pacote novo: `pg`, `@playwright/test` e `vitest` já estão no `package-lock.json`. Nada a instalar, portão de legitimidade não se aplica. |
</threat_model>

<fora_de_escopo>
Travado com o dono — NÃO faça, mas o SUMMARY precisa registrar como pendência explícita:

1. **`lib/financeiro/acoes.ts:51-53` tem EXATAMENTE o mesmo defeito** e não é tocado aqui. A Fase
   04.4 está parada no checkpoint do dono; mexer nesses arquivos agora atravessaria o trabalho
   dele. Fica para depois que a 04.4 fechar.
2. **`lib/cadastros/acoes.ts` mantém a cópia privada** de `codigoDoErroPostgres` (e os detectores
   de `23505`/`P0001` que ela alimenta). Mesma razão. A migração dele para `@/lib/erro/postgres` é
   trabalho seguinte.
3. **Nenhum `git push`.** O dono tem 46 commits locais aguardando autorização dele. Commite
   localmente e pare.
</fora_de_escopo>

<verification>
- `npm run verificar` passa inteiro ao fim da Tarefa 3.
- Exatamente UMA invocação de `npm run test:e2e` em todo o plano, na Tarefa 3, com
  `--grep "chave estrangeira"`. Nenhum `npm run build` avulso.
- `tests/unit/erro-postgres.test.ts` cobre o formato real do erro embrulhado, não só o cru.
- Os três módulos importam `@/lib/erro/postgres`; nenhum declara detector próprio.
- As mensagens humanas em produção não mudaram de redação — só passaram a aparecer.
- `git status` sem arquivo não rastreado sobrando; nada empurrado para o remoto.
</verification>

<success_criteria>
1. Apagar o forno com o cartão na tela e então registrar a queima mostra "Esse forno não existe
   mais. Recarregue a página." — provado por e2e nos dois viewports.
2. Apagar o item com o formulário de tarefa na tela e então salvar mostra "O item ligado a esta
   tarefa não existe mais. Recarregue a página e tente de novo." — provado por e2e.
3. Apagar a categoria com o formulário de cotação na tela e então salvar mostra "Essa categoria
   não existe mais. Recarregue a página e tente de novo." — provado por e2e.
4. O teste unitário falha se alguém reintroduzir a leitura que olha só a raiz do erro.
5. `npm run verificar` verde; três commits atômicos; nenhum push.
</success_criteria>

<output>
Escreva `.planning/quick/260920-dx9-detectar-sqlstate-embrulhado-pelo-drizzl/260920-dx9-SUMMARY.md`
ao terminar. Registre obrigatoriamente: (a) quais comandos de teste rodou de fato, com a contagem
de invocações de `npm run test:e2e`; (b) as duas pendências de `<fora_de_escopo>` (Financeiro e
Cadastros) como trabalho seguinte; (c) qualquer decisão tomada sem o dono, na seção
"Decidido sem o Theo".
</output>
