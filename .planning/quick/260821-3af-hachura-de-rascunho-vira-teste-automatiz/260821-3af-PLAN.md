---
phase: quick-260821-3af
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [VERIF-03-C, D-10]
files_modified:
  - tests/e2e/encomendas-indice.spec.ts
  - .planning/phases/03-gestor-de-encomendas/03-VERIFICATION.md

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "Uma encomenda em `rascunho` desenha, no Gantt do desktop, barras E losangos com `background-image` de `repeating-linear-gradient` e borda tracejada de 1px — medido no CSS computado do navegador, nunca por texto na tela (D-10)."
    - "A hachura preserva a COR CHEIA da etapa e nunca reduz opacidade: a `opacity` computada da barra em rascunho é `1`, e o gradiente contém a mesma cor que a barra de uma encomenda não-rascunho usa como `background-color`."
    - "O selo RASCUNHO aparece exatamente UMA vez na linha do Gantt daquela encomenda e exatamente UMA vez no cartão do celular — e ZERO vezes na encomenda de controle."
    - "No cartão do celular, os segmentos da trilha da encomenda em rascunho têm `background-image` de `repeating-linear-gradient`; os da encomenda de controle têm `background-image: none` e cor de fundo cheia."
    - "O teste afirma SÓ sobre as encomendas que ele mesmo criou, localizadas pelo `id` que ele mesmo descobriu — nenhuma afirmação sobre condição global do banco, nenhuma dependência de `--grep` para isolar."
    - "Nenhuma linha de código de produção mudou: `gantt.tsx`, `trilha-segmentos.tsx`, `cartao-encomenda.tsx`, `textos.ts` e `db/schema.ts` ficam intactos."
  artifacts:
    - tests/e2e/encomendas-indice.spec.ts
    - .planning/phases/03-gestor-de-encomendas/03-VERIFICATION.md
  key_links:
    - "O texto do selo vem de `SELO_RASCUNHO` (`lib/encomendas/textos.ts`) importado no teste — a string literal 'RASCUNHO' nunca é redigitada, mesma disciplina que o plano anterior usou com `formatarDiaCurto`/`textoDaContagemDeItens`."
    - "O rascunho é semeado por `marcarComoRascunho` (`tests/e2e/apoio/marcar-rascunho.ts`), que escreve em `DATABASE_URL_TESTE` — o Postgres efêmero que `scripts/testar-e2e.mjs` sobe — e nunca no banco real. Nenhuma conexão nova é aberta por este plano."
    - "A encomenda de CONTROLE (não-rascunho) é o que prova que o teste discrimina: se a hachura fosse aplicada a tudo, o controle falha; se não fosse aplicada a nada, o rascunho falha. É o substituto do passo RED, que não existe aqui porque o código de produção já está pronto."
    - "Os dois testes entram nos describes que já existem (`Gantt desktop` e `Lista mobile`), herdando o `test.skip` por projeto que aqueles blocos já declaram no `beforeEach` — nenhum describe novo, nenhuma etiqueta `@vazio-*` nova, nenhuma mudança em `playwright.config.ts`."
---

<objective>
Transformar a verificação manual C da Fase 3 — a hachura diagonal e o selo RASCUNHO no Gantt e no
cartão — em teste automatizado, para que ela pare de ser um item humano que ninguém nunca vai fazer.

Purpose: hoje NENHUM teste mede a hachura. Confirmado por varredura: `repeating-linear-gradient`
aparece em `components/amassa/encomendas/gantt.tsx:58` e
`components/amassa/encomendas/trilha-segmentos.tsx:85`, e em ZERO arquivos de `tests/`. O tratamento
visual do rascunho (D-10) foi aprovado por leitura de código e nunca exercitado com dado real. Essa
é exatamente a classe de lacuna que deixou o defeito do rodapé do formulário passar despercebido
desde a Fase 3 — código certo, nenhuma asserção de estilo ou geometria por cima dele.

O item era inexecutável à mão pelo motivo certo: a interface não tem NENHUM caminho para criar uma
encomenda em rascunho (só `criarEncomenda` grava, sempre em `em_producao`), então conferir à mão
exigiria um `UPDATE` em dado de produção. `tests/e2e/apoio/marcar-rascunho.ts` já resolve isso no
banco de TESTE, e já é usado assim pelo teste do sufixo " (rascunho)" da folha impressa.

Output: 1 commit de teste. Nenhuma migração, nenhuma mudança em `db/schema.ts`, nenhuma mudança em
`TABELAS_ESPERADAS` de `scripts/testar-migracoes.mjs`, nenhuma dependência nova, nenhuma linha de
código de produção.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/03-gestor-de-encomendas/03-VERIFICATION.md

O diagnóstico está fechado e a decisão de produto NÃO está em jogo. A pergunta "rascunho deveria ser
alcançável pela interface, ou deveria sumir do produto?" está deliberadamente parqueada no backlog
(`STATE.md` §Deferred Items) e **não é deste plano**. Não a reabra, não proponha alternativas ao
dono, não peça confirmação. Este plano cobre o CSS que já existe. Execute.
</context>

<limites_duros>
Três coisas que este plano **não** pode fazer, em nenhuma circunstância:

1. **Nenhuma mudança de código de produção.** `components/amassa/encomendas/gantt.tsx`,
   `trilha-segmentos.tsx`, `cartao-encomenda.tsx`, `lib/encomendas/textos.ts` e qualquer arquivo de
   `app/` ficam byte a byte iguais. Se você concluir que o código de produção precisa mudar para o
   teste ser possível — **pare e escreva por quê**, em vez de mudar. Essa conclusão é informação
   valiosa; a mudança silenciosa é um defeito.
2. **Nenhuma dependência nova.** Zero `npm install`. Nenhum pacote entra neste plano, então não há
   nada para a auditoria de legitimidade de pacotes examinar. Se você achar que precisa de um,
   **pare** — não instale.
3. **Nenhum caminho de escrita novo no banco.** Use `marcarComoRascunho` como está. Não abra um
   `Client` novo, não escreva SQL novo, não toque em `tests/e2e/apoio/marcar-rascunho.ts`.
</limites_duros>

<orcamento_de_e2e>
Regra dura do `CLAUDE.md`, medida: `npm run test:e2e` custa ~53s de imposto fixo (15s de Postgres
efêmero + 38s de `next build`) ANTES do primeiro teste rodar. O imposto é o mesmo para a suíte
inteira e para um `--grep` que pega um teste só.

- **UMA invocação de `npm run test:e2e` nesta tarefa**, com `--grep "hachura"`. A palavra "hachura"
  não aparece hoje em nenhum lugar de `tests/` (verificado) — o recorte pega exatamente os dois
  testes novos e mais nada.
- **Nunca `npm run build` como passo separado** — a execução do e2e já constrói.
- **NÃO rode a varredura completa sem `--grep`.** A varredura de fim de fase já rodou no quick
  anterior (`260820-uot`); repeti-la aqui não compra nada e custa minutos.
- Se o `--grep` falhar e você precisar de mais uma rodada para diagnosticar, **rode** — a regra é
  sobre o padrão, não uma proibição. Registre no SUMMARY quantas vezes rodou e por quê.
- `npm run lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e `npm test` são baratos: rode à
  vontade, e rode ANTES do e2e — um erro de tipo pego pelo `tsc` custa segundos; pego pelo
  Playwright custa 53.
</orcamento_de_e2e>

<tasks>

<task type="auto">
  <name>Tarefa única: a hachura de rascunho vira asserção de CSS computado, no Gantt e no cartão</name>
  <files>
    tests/e2e/encomendas-indice.spec.ts
  </files>
  <precondition>
    O Docker está rodando e consegue subir o Postgres efêmero que `scripts/testar-e2e.mjs` cria — é
    de lá que sai `DATABASE_URL_TESTE`, a única variável que `marcarComoRascunho` lê. Sem Docker de
    pé, `npm run test:e2e` falha na infraestrutura, não no teste; nesse caso pare e diga, não
    conclua que a asserção está errada.
  </precondition>
  <reversibility rating="reversible">
    Só acrescenta testes num arquivo de spec. Reverter é apagar o bloco; nenhum consumidor depende
    dele, nenhum dado persiste fora do banco efêmero.
  </reversibility>
  <read_first>
    tests/e2e/encomendas-indice.spec.ts (linhas 1-180: o cabeçalho de imports e TODOS os auxiliares
      — `fazerLogin`, `criarEncomenda`, `nomeUnico`, `dataEmDias`, `linhaDoGantt`,
      `cartaoDoCelular`; depois as linhas 231-245, que são o `beforeEach` com `test.skip` do
      describe "Gantt desktop", e as linhas 580-590, o `beforeEach` espelhado do describe
      "Lista mobile"),
    tests/e2e/encomendas-indice.spec.ts (linhas 625-650 — o teste "o segmento da etapa atual tem
      borda de 2px": é o padrão da casa para ler CSS computado com
      `locator.evaluate((el) => getComputedStyle(el).prop)`, e é o formato a imitar),
    tests/e2e/encomendas-impressao.spec.ts (linhas 55-100 — a versão de `criarEncomenda` daquele
      arquivo, que devolve o `id` lendo o `data-testid` do cartão; e a linha ~205, o único uso
      atual de `marcarComoRascunho`),
    components/amassa/encomendas/gantt.tsx (`estiloDeEtapa` 52-63, o selo 178-182, o losango 215-232
      e a barra 245-266 — em especial o `borderLeft` de 3px que o ramo `cortadaNaEsquerda`
      sobrescreve),
    components/amassa/encomendas/trilha-segmentos.tsx (72-95 — o `style` do segmento),
    tests/e2e/apoio/marcar-rascunho.ts (as 19 linhas inteiras)
  </read_first>
  <behavior>
    Dois testes novos, um por viewport, cada um provando a mesma regra (D-10) na superfície daquele
    viewport. Cada teste cria DUAS encomendas: uma que vira rascunho e uma de CONTROLE que fica em
    `em_producao`. O controle não é enfeite — é o que faz o teste discriminar, e substitui o passo
    RED que não existe aqui porque o código de produção já está escrito.

    **Teste 1 — Gantt do desktop.** Título contendo a palavra "hachura". Entra no FIM do describe
    "Gantt desktop (ENC-03, ENC-06, ENC-07)", que já pula os projetos != `desktop` no `beforeEach`.

    Da encomenda em rascunho, no CSS computado:
    - a barra de `producao` tem `background-image` contendo `repeating-linear-gradient`
    - a barra de `producao` tem `border-top-style` igual a `dashed` e `border-top-width` igual a `1px`
    - a barra de `producao` tem `opacity` igual a `1` — a regra literal de D-10 é hachura preservando
      a cor cheia, NUNCA opacidade reduzida (que é a linguagem de "desabilitado" do sistema)
    - o losango de `queima1` (marco) repete as três asserções acima — a hachura vale para as duas
      formas, não só para as barras
    - o selo aparece exatamente 1 vez na linha
    Da encomenda de controle:
    - a barra de `producao` tem `background-image` igual a `none`
    - a barra de `producao` tem `background-color` diferente de `rgba(0, 0, 0, 0)`
    - a barra de `producao` tem `border-top-style` diferente de `dashed`
    - o selo aparece 0 vezes na linha
    E a asserção que amarra as duas: o `background-image` da barra em rascunho CONTÉM a string exata
    do `background-color` computado da barra de controle — é assim que "preservando a cor cheia da
    etapa" vira medição em vez de promessa.

    **Teste 2 — cartão do celular.** Título contendo "hachura". Entra no FIM do describe
    "Lista mobile (ENC-08, ENC-09)", que já pula os projetos != `celular`.

    Da encomenda em rascunho: os segmentos `producao` e `secagem` da trilha têm `background-image`
    contendo `repeating-linear-gradient` e `opacity` igual a `1`; o selo aparece exatamente 1 vez no
    cartão. Da de controle: `background-image` igual a `none`, `background-color` diferente de
    transparente, selo 0 vezes. Mais a mesma asserção de cor cheia, comparando o gradiente do
    rascunho com o `background-color` do controle.
  </behavior>
  <action>
Só `tests/e2e/encomendas-indice.spec.ts` muda. Nenhum outro arquivo.

**1. Imports.** Acrescente `SELO_RASCUNHO` à lista que já vem de `@/lib/encomendas/textos` (depois
de `ROTULO_NOVA_ENCOMENDA`, antes de `textoDaContagemDeItens`). Abaixo desse bloco, separado por uma
linha em branco, acrescente `marcarComoRascunho` vindo de `./apoio/marcar-rascunho` — pacotes,
depois aliases `@/`, depois relativos, a ordem que o repositório já usa. Não existe regra de
`import/order` no `eslint.config.mjs`, então a ordem é convenção, não obrigação de lint.

O selo NUNCA é redigitado como string literal no teste. Ele vem de `SELO_RASCUNHO`. É a mesma
disciplina que este arquivo já aplica a `ROTULO_ETAPA` e `textoDaContagemDeItens`.

**2. Um auxiliar novo, `idDaEncomenda`.** Coloque-o logo depois de `cartaoDoCelular`, no nível de
topo do arquivo. Assinatura: recebe `page` e o `nome`, devolve `Promise<string>`. Corpo: localiza
`[data-testid^="cartao-encomenda-"]` filtrado por `hasText` do nome, espera `toHaveCount(1)`, lê o
atributo `data-testid` e devolve o valor sem o prefixo `cartao-encomenda-`; se o atributo vier
nulo, lance um `Error` dizendo qual nome não foi encontrado.

Duas coisas para registrar em comentário acima dele, porque as duas são não-óbvias:
  - o cartão está SEMPRE no HTML nos dois viewports (é escondido por CSS no desktop, D-02), então
    esta leitura funciona igual nos projetos `desktop` e `celular`. É exatamente por isso que
    `encomendas-impressao.spec.ts` descobre o `id` do mesmo jeito.
  - o `criarEncomenda` DESTE arquivo devolve `void`, ao contrário do irmão em
    `encomendas-impressao.spec.ts`. **Não mexa nele.** Ele é chamado por perto de vinte testes deste
    arquivo, e o orçamento de e2e deste plano é UMA rodada — uma alteração no auxiliar compartilhado
    põe testes que não são deste plano em risco por uma economia de três linhas.

**3. Teste do Gantt.** Acrescente no FIM do describe "Gantt desktop (ENC-03, ENC-06, ENC-07)", como
último teste do bloco, DENTRO das chaves dele — o `beforeEach` daquele describe já pula quem não é
o projeto `desktop`, e é assim que o requisito "roda nos dois projetos, com `test.skip` por projeto
onde a asserção não se aplica" é atendido sem inventar um describe novo.

Passos, nesta ordem:
  - `fazerLogin(page)`
  - crie a encomenda que vai virar rascunho com `criarEncomenda(page, { nome, dataInicio: dataEmDias(0) })`,
    onde `nome` vem de `nomeUnico("Gantt hachura")`; logo depois, `idDaEncomenda(page, nome)`
  - crie a de controle do mesmo jeito, com `nomeUnico("Gantt sem hachura")`, e pegue o `id` dela
  - `await marcarComoRascunho(idRascunho)` — só a primeira
  - `await page.goto("/encomendas")`, para o servidor renderizar de novo lendo o status já gravado

Nomes: `nomeUnico` já garante unicidade por chamada e prefixo `[e2e]`. Escolha rótulos que **não
contenham a palavra rascunho em nenhuma caixa** — o nome da encomenda é texto na mesma linha do
selo, e mantê-lo limpo elimina qualquer chance de a contagem do selo casar com o nome por acidente.

Localize tudo por `data-testid` construído com o `id` que o próprio teste descobriu:
`gantt-linha-{id}`, `gantt-barra-{id}-producao`, `gantt-marco-{id}-queima1`. Nunca por posição,
nunca por contagem global, nunca por `linhaDoGantt` neste teste — o `id` é mais preciso e é imune a
dado concorrente de outras specs no mesmo banco.

Espere `toBeVisible()` na barra antes de ler estilo. É a mesma prudência que o comentário de
`larguraVisivelDaAreaRolavel` (linha ~171) já documenta: `evaluate` só espera o elemento estar
ANEXADO, não estilizado, e ler CSS computado cedo demais devolve valor de antes da folha de estilo.

Leia o estilo em UMA passada por elemento, devolvendo um objeto de `evaluate` com as quatro chaves
de que você precisa (imagem de fundo, estilo e largura da borda de topo, opacidade) — não quatro
`evaluate` separados.

**Leia a borda pelo TOPO, nunca pela esquerda.** O ramo `cortadaNaEsquerda` de `gantt.tsx`
sobrescreve `borderLeft` com 3px sólidos quando a etapa começa antes do primeiro dia da timeline.
A encomenda deste teste começa hoje e não é cortada — trave essa premissa com
`toHaveAttribute("data-cortada", "false")` na barra e siga lendo o topo. Assim a asserção não
depende de um ramo que não é o assunto do teste.

Compare o número de selos com `linha.getByText(SELO_RASCUNHO, { exact: true })` e
`toHaveCount(1)`; na linha do controle, `toHaveCount(0)`.

Para o controle, `background-image` computado deve ser exatamente `none`, e `border-top-style` deve
ser DIFERENTE de `dashed` — não teste por igualdade a `none`: o preflight do Tailwind v4 declara
`border: 0 solid` em todo elemento, então o valor real é `solid` com largura `0px`, e afirmar `none`
falharia por um motivo que não tem nada a ver com rascunho.

**A asserção de cor cheia** (a mais importante das duas, porque é ela que distingue "tem algum
gradiente" de "tem a hachura de D-10"): guarde o `background-color` computado da barra de controle e
afirme que o `background-image` da barra em rascunho o CONTÉM. As duas leituras saem do mesmo
`getComputedStyle`, no mesmo documento, logo passam pela mesma serialização de cor.

Esta é a ÚNICA asserção deste plano autorizada a ser afrouxada, e só se a rodada de e2e mostrar que
o Chromium serializa a parada de cor dentro de `background-image` num espaço de cor diferente do de
`background-color`. Se isso acontecer, o afrouxamento permitido é um só: extraia de ambas as strings
o trio numérico de canais com uma expressão regular e compare os trios — não troque por
`toContain("gradient")`, que não prova nada, e **não mude `gantt.tsx` para fazer a asserção passar**.
Registre no SUMMARY se precisou afrouxar.

**4. Teste do cartão.** Acrescente no FIM do describe "Lista mobile (ENC-08, ENC-09)", como último
teste do bloco. Mesma sequência: login, duas encomendas (`nomeUnico("Cartao hachura")` e
`nomeUnico("Cartao sem hachura")`), `marcarComoRascunho` só na primeira, `page.goto("/encomendas")`.

Localize por `cartao-encomenda-{id}` e, dentro dele, por `trilha-segmento-producao` e
`trilha-segmento-secagem`. Afirme imagem de fundo com `repeating-linear-gradient` e `opacity` igual
a `1` nos dois segmentos do rascunho; `none` e cor de fundo não-transparente no controle; a mesma
comparação de cor cheia entre gradiente e cor do controle; e a contagem do selo, 1 no cartão do
rascunho e 0 no do controle.

**Uma diferença deliberada entre os dois testes, e ela é obrigatória:** o segmento da trilha **não
tem borda tracejada**. `trilha-segmentos.tsx` define borda num caso só — 2px sólidos na etapa atual
— e nunca tracejada, em nenhum status. **Não afirme borda tracejada no cartão.** Se você achar que a
trilha deveria ter uma para ficar coerente com o Gantt, isso é uma pergunta de design para o dono:
**pare e escreva no SUMMARY**, não altere o componente. Deixe um comentário curto no teste dizendo
que a ausência é do componente e é intencional neste plano, para o próximo leitor não achar que foi
esquecimento.

**5. Isolamento (regra dura do `CLAUDE.md`).** Nenhum dos dois testes pode afirmar condição global
do banco. Proibido: "existe exatamente uma encomenda em rascunho", "o índice tem N linhas", qualquer
`toHaveCount` sobre um seletor que pegue encomendas de outras specs. Toda contagem é feita DENTRO da
linha ou do cartão de um `id` que este teste criou. Nenhuma etiqueta `@vazio-global` ou
`@vazio-historico`: estes testes criam dado e pertencem aos projetos `desktop`/`celular`, no fim da
cadeia de `dependencies`. `playwright.config.ts` não muda.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -vE '^[[:space:]]*//' tests/e2e/encomendas-indice.spec.ts | grep -c "repeating-linear-gradient"</automated>
    <automated>git show --stat --name-only --format= HEAD</automated>
    <automated>npm run test:e2e -- --grep "hachura"</automated>
    <automated>npm run verificar</automated>
  </verify>
  <leitura_dos_gates>
    - O `grep -c` roda sobre o arquivo SEM as linhas de comentário (senão a própria prosa que
      explica a asserção contaria como se fosse asserção) e tem que devolver **2 ou mais** — pelo
      menos uma ocorrência por teste.
    - `git show --stat --name-only --format= HEAD` tem que listar **exatamente uma linha**:
      `tests/e2e/encomendas-indice.spec.ts`. Qualquer segundo caminho ali é escopo vazado.
    - `npm run test:e2e -- --grep "hachura"` é a **única** invocação de e2e autorizada, e tem que
      terminar com 2 passados e 2 pulados.
  </leitura_dos_gates>
  <done>
    `npm run test:e2e -- --grep "hachura"` roda verde numa única invocação, com dois testes
    executados e dois pulados por projeto. `npm run verificar` passa limpo. `git diff --name-only`
    lista `tests/e2e/encomendas-indice.spec.ts` e mais nada — `gantt.tsx`, `trilha-segmentos.tsx`,
    `cartao-encomenda.tsx`, `textos.ts`, `db/schema.ts`, `scripts/testar-migracoes.mjs` e
    `playwright.config.ts` intactos. A string literal `"RASCUNHO"` não aparece no arquivo de teste
    (o selo vem de `SELO_RASCUNHO`). Um commit, com mensagem no formato
    `test(encomendas): ...`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo de teste → Postgres | `marcarComoRascunho` abre conexão e roda `UPDATE` cru, fora do Drizzle e fora de `exigirUsuario()` |
| repositório → internet | o repositório é PÚBLICO; tudo que entra num arquivo de teste fica visível para sempre |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3AF-01 | Tampering | `tests/e2e/apoio/marcar-rascunho.ts` (UPDATE cru) | medium | mitigate | O plano proíbe abrir `Client` novo, escrever SQL novo ou editar o auxiliar. Ele lê SÓ `DATABASE_URL_TESTE` — nunca `DATABASE_URL`, nunca com fallback — e essa variável é definida por `scripts/testar-e2e.mjs:73` apontando para o Postgres efêmero em contêiner. Verificável no diff: `git diff --name-only` tem que listar um arquivo só. |
| T-3AF-02 | Information Disclosure | nomes de encomenda no arquivo de teste | high | mitigate | Todo nome sai de `nomeUnico`, que prefixa `[e2e]` e concatena projeto + timestamp + aleatório. Os rótulos exigidos pelo plano ("Gantt hachura", "Cartao hachura" e os pares "sem hachura") são inventados e reconhecíveis como tais. Zero nome de cliente ou aluna real, zero credencial: as do teste vêm de `E2E_EMAIL_TESTE`/`E2E_SENHA_TESTE`, publicadas pelo `globalSetup`. |
| T-3AF-03 | Information Disclosure | `DATABASE_URL_TESTE` em mensagem de erro | low | accept | O auxiliar não imprime a string de conexão; um erro do `pg` pode citar host/porta do contêiner local, que é efêmero e sem valor. Aceito conscientemente — nenhum segredo real transita aqui. |
| T-3AF-SC | Tampering | npm/pip/cargo installs | high | mitigate | Este plano instala ZERO pacotes — não há tarefa de instalação, logo nada para a auditoria de legitimidade examinar. `package.json` e `package-lock.json` ficam intactos, e isso é verificável pelo mesmo `git diff --name-only` do critério de conclusão. Se o executor concluir que precisa de um pacote, o `<limites_duros>` manda **parar**, não instalar. |
</threat_model>

<verification>
Fechamento, depois do commit:

1. `npm run verificar` — tem que passar limpo (`lint`, `tsc --noEmit`, `verificar-acoes`, unitários
   e `test:migracoes`). O `test:migracoes` é barato e não deveria ter nada a dizer aqui, já que
   `db/schema.ts` não foi tocado — se ele reclamar, algo saiu do escopo.
2. **Uma** invocação de `npm run test:e2e -- --grep "hachura"`, e só. **Não** rode a varredura
   completa: a de fim de fase já rodou no quick `260820-uot`.
3. Confira na saída do Playwright que os DOIS testes rodaram — um no projeto `desktop`, um no
   `celular` — e que os dois espelhos foram pulados. Dois "passed" e nenhum "skipped" é sinal de que
   um dos testes caiu no describe errado.
4. Se algum teste falhar por causa de ambiente Windows já conhecido, consulte `.planning/WINDOWS.md`
   antes de tratar como regressão.
5. `git log --oneline -1` mostra exatamente um commit de teste. `git status` limpo.
6. Registre no SUMMARY **quantas vezes rodou o e2e de fato** e se a asserção de cor cheia precisou
   do afrouxamento previsto.
</verification>

<success_criteria>
- Um commit atômico, só de teste, tocando apenas `tests/e2e/encomendas-indice.spec.ts`.
- Dois testes novos com "hachura" no título: um no describe do Gantt (projeto `desktop`), um no da
  lista mobile (projeto `celular`), cada um herdando o `test.skip` que o describe já declara.
- Cada teste cria a sua própria dupla rascunho + controle e afirma SÓ sobre os `id` que descobriu —
  zero afirmação sobre condição global do banco, zero dependência de `--grep` para isolar.
- A hachura é provada por CSS COMPUTADO (`background-image`, `border-top-style`,
  `border-top-width`, `opacity`), nunca por texto na tela.
- A cor cheia da etapa é provada por comparação com o `background-color` da barra de controle — a
  regra de D-10 ("nunca opacidade reduzida") vira medição, não promessa.
- O selo é contado 1 vez por linha/cartão em rascunho e 0 vezes no controle, com o texto vindo de
  `SELO_RASCUNHO` — a string literal nunca redigitada.
- Zero linhas de código de produção alteradas. Zero dependências novas. Zero migrações.
- `npm run verificar` limpo e UMA rodada de e2e verde.
</success_criteria>

<output>
Ao terminar, crie
`.planning/quick/260821-3af-hachura-de-rascunho-vira-teste-automatiz/260821-3af-SUMMARY.md`
registrando: o commit, quantas vezes o e2e rodou de fato e com qual `--grep`, se a asserção de cor
cheia precisou do afrouxamento previsto (e por quê), e — se for o caso — a observação de que a
trilha do cartão não tem borda tracejada enquanto o Gantt tem, como pergunta de coerência visual
para o dono decidir depois.

Depois disso, atualize
`.planning/phases/03-gestor-de-encomendas/03-VERIFICATION.md`: no item de `human_verification` que
começa com "Semear duas encomendas 'rascunho' por SQL direto", acrescente uma chave
`fechado_por_teste` com o caminho do arquivo de spec, os títulos dos dois testes e o hash do commit.

**Não apague o item e não reescreva os outros três.** Apagar reescreveria a história do relatório; o
item continua ali, agora com a anotação de que a razão dele ("nenhum e2e mede o CSS de hachura no
Gantt/cartão") deixou de existir. Este é o caso raro em que fechar por teste é legítimo em vez de
esperar o dono: o próprio `why_human` do item diz que ele era humano por FALTA de teste, não por
precisar de julgamento humano. Não marque nenhum dos outros itens.
</output>
</content>
</invoke>
