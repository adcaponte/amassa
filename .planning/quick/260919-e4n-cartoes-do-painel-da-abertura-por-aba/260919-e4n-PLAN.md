---
phase: quick-260919-e4n
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ABE-12]
files_modified:
  - lib/abertura/abas.ts
  - tests/unit/abertura-abas.test.ts
  - components/amassa/abertura/painel-resumo.tsx
  - app/(app)/abertura/page.tsx
  - components/amassa/abertura/abas-abertura.tsx
  - components/amassa/abertura/botao-adicionar-abertura.tsx
  - app/(app)/abertura/loading.tsx
  - tests/e2e/abertura-painel.spec.ts

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Na aba Itens (`/abertura` sem `?aba=` E `/abertura?aba=itens`) aparece SÓ o cartão \"Precisa de atenção\" — \"Comprometido\" e \"Sai neste mês\" não existem no DOM."
    - "Na aba Por mês (`/abertura?aba=meses`) aparecem SÓ \"Comprometido\" e \"Sai neste mês\", nessa ordem — \"Precisa de atenção\" não existe no DOM."
    - "Nas abas Tarefas (`?aba=tarefas`) e Cotações (`?aba=cotacoes`) não há cartão nenhum E não há o contêiner do painel (`abertura-painel-resumo` com contagem 0) — nenhuma faixa vazia com padding; a barra de abas fica logo abaixo da linha da inauguração, com o próprio `pt-6` de sempre."
    - "Cada cartão visível mantém o tamanho de hoje: no desktop ocupa uma das três colunas (largura menor que metade do painel, dois cartões lado a lado na mesma linha em Por mês); no celular ocupa a coluna inteira, empilhado na ordem Comprometido → Sai neste mês."
    - "Os números não mudam: \"à vista + a prazo = Comprometido\", \"atrasadas + vencidas = Precisa de atenção\" e o valor de atenção em vermelho quando passa de zero continuam valendo, lidos agora cada um na aba em que o cartão aparece."
    - "A regra \"quais cartões em qual aba\" vive num módulo puro testado (`lib/abertura/abas.ts`, sem nenhum import), junto com o tipo `AbaAbertura` e a normalização `abaDaUrl` — que `page.tsx` e `abas-abertura.tsx` passam a compartilhar em vez de cada um ter a sua."
    - "`page.tsx` não ganha nenhuma referência nova a Client Component (continua com 17 imports de `@/components/`) e `PainelResumo` continua Server Component."
  artifacts:
    - path: lib/abertura/abas.ts
      provides: "AbaAbertura, abaDaUrl, CartaoDoPainel, ORDEM_DOS_CARTOES, cartoesDaAba — puro, sem import"
    - path: tests/unit/abertura-abas.test.ts
      provides: "contrato de cartoesDaAba e abaDaUrl"
    - path: components/amassa/abertura/painel-resumo.tsx
      provides: "PainelResumo({ resumo, cartoes }) — renderiza só os cartões pedidos, null com lista vazia"
    - path: app/(app)/abertura/page.tsx
      provides: "lê ?aba= por abaDaUrl, decide os cartões por cartoesDaAba, só calcula resumo quando há cartão"
    - path: app/(app)/abertura/loading.tsx
      provides: "esqueleto do painel na forma da aba padrão (um cartão)"
    - path: tests/e2e/abertura-painel.spec.ts
      provides: "presença/ausência por aba em celular e desktop + consistência dos números lida por aba"
  key_links:
    - from: "app/(app)/abertura/page.tsx"
      to: "lib/abertura/abas.ts"
      via: "abaDaUrl(aba) → cartoesDaAba(abaAtual) → prop `cartoes` de PainelResumo"
    - from: "components/amassa/abertura/abas-abertura.tsx"
      to: "lib/abertura/abas.ts"
      via: "a barra de abas usa a MESMA abaDaUrl/AbaAbertura do servidor — aba selecionada e cartões nunca divergem sobre o que é a aba padrão"
    - from: "tests/e2e/abertura-painel.spec.ts"
      to: "components/amassa/abertura/painel-resumo.tsx"
      via: "data-testid abertura-painel-resumo / abertura-bloco-comprometido / abertura-bloco-mes / abertura-bloco-atencao (inalterados)"
---

<objective>
Fazer os três cartões do painel da Abertura do Espaço dependerem da aba, por pedido do dono
(19/09): "Comprometido" e "Sai neste mês" SÓ na aba Por mês; "Precisa de atenção" SÓ na aba Itens
(a padrão); nenhum cartão em Tarefas e em Cotações ("não quero ver os cartões enquanto comparo
cotações"). Refina ABE-12 (o painel continua mostrando os mesmos números, só não em toda aba).

Purpose: hoje `PainelResumo` é renderizado acima das abas em `app/(app)/abertura/page.tsx` em
TODAS as quatro abas, e ocupa a tela do celular justamente onde a pessoa está comparando cotações
ou tocando tarefas.

Output: módulo puro `lib/abertura/abas.ts` (regra + tipo/normalização de aba consolidados) com
teste unitário; `PainelResumo` filtrando por lista de cartões; `page.tsx` ligando uma coisa à
outra; esqueleto de carregamento coerente; e2e de presença/ausência por aba em celular e desktop.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@app/(app)/abertura/page.tsx
@components/amassa/abertura/painel-resumo.tsx
@components/amassa/abertura/abas-abertura.tsx
@tests/e2e/abertura-painel.spec.ts

<regras_do_projeto>
Valem para TODAS as tarefas deste plano (CLAUDE.md + pedido do orquestrador):

- `npm run verificar` precisa passar antes de dar o plano por concluído (lint, `tsc --noEmit`,
  `verificar-acoes`, testes unitários, `test:migracoes`). Nada aqui mexe em `db/schema.ts`, então
  `TABELAS_ESPERADAS` de `scripts/testar-migracoes.mjs` NÃO muda.
- NO MÁXIMO UMA invocação de `npm run test:e2e` no plano inteiro — na Tarefa 3, com
  `--grep abertura` (sem espaço, sem `|`: `scripts/testar-e2e.mjs` monta uma string de shell e um
  `|` sem aspas viraria pipe no cmd.exe). Nunca `npm run build` separado — o e2e já constrói.
  Se o grep falhar e for preciso a suíte inteira para diagnosticar, rode e registre no SUMMARY.
  Registre no SUMMARY os comandos que rodou de fato.
- Docker Desktop precisa estar de pé para o e2e E para o `test:migracoes` do `verificar`. Se
  `docker info` falhar, inicie o Docker Desktop (PowerShell:
  `Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"`) e espere `docker info`
  responder antes de seguir.
- Nunca `git push`. Nunca `git add -A` nem `git add .` — `Claude outputs/` e
  `.planning/phases/06-estoque/` estão fora do git e NÃO fazem parte desta tarefa. Adicione
  arquivo por arquivo. Prefira `git commit` direto; se sobrar `.git/index.lock` órfão, confira o
  horário do arquivo antes de removê-lo.
- Texto de interface em português do Brasil. Identificadores: CLAUDE.md pede inglês, mas os
  módulos de domínio em `lib/` são uniformemente nomeados em português (`resumoDoPainel`,
  `ORDEM_DOS_GRUPOS`, `abaDaUrl`, `AbaAbertura`) e este plano REAPROVEITA nomes existentes; os
  poucos nomes novos abaixo são especificados exatamente, seguindo o módulo onde vivem — não
  invente outros e não renomeie nada que já existe.
- `PainelResumo` continua Server Component. `page.tsx` NÃO ganha referência nova a Client
  Component (ver `.planning/debug/resolved/e2e-toque-nao-navega-ci.md`: a chance de a navegação
  travar em produção cresce com o número de Client Components no JSX da página).
- `await exigirUsuario()` continua sendo a primeira instrução de `PaginaAbertura`.
</regras_do_projeto>

<interfaces>
Contrato que a Tarefa 1 cria e as Tarefas 2 e 3 consomem (`lib/abertura/abas.ts`, nenhum import):

- tipo `AbaAbertura` = "itens" | "tarefas" | "meses" | "cotacoes" (movido, idêntico ao de hoje)
- `abaDaUrl(valor: string | null | undefined): AbaAbertura` — "tarefas"/"meses"/"cotacoes"
  passam como estão; QUALQUER outro valor (null, undefined, "", "itens", "MESES", lixo) vira
  "itens". Mesma semântica da função local de `abas-abertura.tsx` e das comparações
  `aba === "..."` de `page.tsx` de hoje.
- tipo `CartaoDoPainel` = "comprometido" | "mes" | "atencao" (os sufixos dos `data-testid`
  `abertura-bloco-*` que já existem)
- `ORDEM_DOS_CARTOES: readonly CartaoDoPainel[]` = comprometido, mes, atencao (a ordem decidida
  em D-15, a de hoje)
- `cartoesDaAba(aba: AbaAbertura): readonly CartaoDoPainel[]` — itens → [atencao];
  meses → [comprometido, mes]; tarefas → []; cotacoes → []. Sempre na ordem de ORDEM_DOS_CARTOES.

Já existente, não muda: `resumoDoPainel(itens, tarefas, hoje): ResumoDoPainel`
(`lib/abertura/parcelas.ts`); `useAbaAtual(): string | null` (`contexto-navegacao.tsx`).
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: regra pura "cartões por aba" ligada de ponta a ponta (lib → page → PainelResumo)</name>
  <files>lib/abertura/abas.ts, tests/unit/abertura-abas.test.ts, components/amassa/abertura/painel-resumo.tsx, app/(app)/abertura/page.tsx</files>
  <behavior>
    - cartoesDaAba("itens") devolve exatamente ["atencao"]
    - cartoesDaAba("meses") devolve exatamente ["comprometido", "mes"]
    - cartoesDaAba("tarefas") e cartoesDaAba("cotacoes") devolvem listas vazias
    - para toda aba, a lista devolvida está na ordem de ORDEM_DOS_CARTOES e não repete cartão
    - abaDaUrl(undefined), abaDaUrl(null), abaDaUrl(""), abaDaUrl("itens"), abaDaUrl("desconhecida") e abaDaUrl("MESES") devolvem "itens"
    - abaDaUrl("tarefas"), abaDaUrl("meses"), abaDaUrl("cotacoes") devolvem o próprio valor
    - composição da URL padrão: cartoesDaAba(abaDaUrl(undefined)) devolve ["atencao"] (`/abertura` sem `?aba=` mostra só "Precisa de atenção")
  </behavior>
  <action>
RED primeiro: crie `tests/unit/abertura-abas.test.ts` (vitest, `describe`/`expect`/`it`, import
relativo `../../lib/abertura/abas` como em `tests/unit/abertura-prazos.test.ts`) com os casos do
`<behavior>`, esperados ESCRITOS À MÃO (é a especificação do dono, não derivar do próprio
módulo). Rode `npx vitest run tests/unit/abertura-abas.test.ts` e confirme que falha (módulo
inexistente). Commit `test(abertura): regra de cartões do painel por aba (vermelho)`.

GREEN: crie `lib/abertura/abas.ts` exatamente com o contrato de `<interfaces>`. O módulo não
importa nada (nem React, nem banco, nem `lib/abertura/*`). Mova para cá o tipo `AbaAbertura` e a
lógica da normalização de `components/amassa/abertura/abas-abertura.tsx` (a função `abaDaUrl`,
com a assinatura ampliada para aceitar também `undefined`, que é o que `searchParams` do servidor
entrega). Implemente `cartoesDaAba` a partir de uma constante `CARTOES_POR_ABA` tipada como
`Record<AbaAbertura, readonly CartaoDoPainel[]>` — assim uma quinta aba no futuro quebra o `tsc`
em vez de cair num padrão silencioso. Comentário de cabeçalho em português explicando o pedido do
dono (19/09) e o motivo de cada aba (Cotações: não ver cartões enquanto compara).

Em `components/amassa/abertura/painel-resumo.tsx`: acrescente a prop
`cartoes: readonly CartaoDoPainel[]` em `PainelResumoProps` (import de tipo de
`@/lib/abertura/abas`). Com a lista vazia, o componente devolve `null` — nenhum `div` do grid,
nenhum `pt-6` sobrando. Com a lista cheia, mantenha o MESMO contêiner de hoje (mesmas classes
`grid grid-cols-1 gap-3 px-6 pt-6 sm:grid-cols-3 md:px-8`, mesmo `data-testid`) e renderize cada
`BlocoDoPainel` só se o cartão estiver na lista, na ordem fixa comprometido → mes → atencao,
com os MESMOS `testId`s, rótulos e conteúdo de hoje. Não troque `grid-cols-3` por um número de
colunas igual ao de cartões: é isso que mantém cada cartão com o tamanho de hoje (um terço no
desktop) em vez de esticar. Continua Server Component, sem diretiva de cliente. Atualize o
comentário do componente (os três blocos continuam existindo; quais aparecem vem de
`cartoesDaAba`).

Em `app/(app)/abertura/page.tsx`: importe `abaDaUrl` e `cartoesDaAba` de `@/lib/abertura/abas`
(import de `lib/`, não de `components/` — a contagem de imports de `@/components/` fica em 17).
Logo depois de ler `searchParams`, derive `abaAtual = abaDaUrl(aba)` e reescreva `abaTarefas`,
`abaMeses` e `abaCotacoes` como comparações com `abaAtual` (mesma semântica de hoje). Derive
`cartoesDoPainel = cartoesDaAba(abaAtual)`. Decisão do planner (o pedido deixou a critério):
`resumo` passa a ser calculado SÓ quando `cartoesDoPainel` não está vazia, e fica `null` caso
contrário — é seguro porque `resumoDoPainel` é pura sobre `itens`/`tarefas`/`hoje`, que continuam
carregados do mesmo jeito para as outras leituras (nenhum número muda). No JSX, renderize
`PainelResumo` com `resumo` e `cartoes={cartoesDoPainel}` só quando `resumo` não é `null`. Não
mexa no `div className="pt-6"` que envolve `AbasAbertura` — sem painel, ele é o espaçamento
correto logo abaixo da linha da inauguração. Atualize os comentários que ficaram imprecisos
(o de cabeçalho da função fala em "qual das três listas"; o de `resumo` fala nos três blocos
sempre calculados) — referencie o pedido do dono de 19/09.

Rode o teste de novo (verde), `npx tsc --noEmit` e `npm run lint`. Commit
`feat(abertura): cartões do painel dependem da aba` com os quatro arquivos (adicionados um a um).
  </action>
  <verify>
    <automated>cd /c/Users/Andre/amassa && npx vitest run tests/unit/abertura-abas.test.ts && npx tsc --noEmit && npm run lint && test "$(grep -cE '^import' lib/abertura/abas.ts)" = "0" && test "$(grep -c '"use client"' components/amassa/abertura/painel-resumo.tsx)" = "0" && test "$(grep -c '^import .*@/components/' 'app/(app)/abertura/page.tsx')" = "17" && grep -q 'cartoesDaAba(' 'app/(app)/abertura/page.tsx' && grep -q 'abaDaUrl(' 'app/(app)/abertura/page.tsx'</automated>
  </verify>
  <done>
    Teste unitário verde com todos os casos do behavior; `lib/abertura/abas.ts` sem nenhum import;
    `PainelResumo` sem diretiva de cliente e devolvendo `null` com lista vazia; `page.tsx` com os
    mesmos 17 imports de componente, `await exigirUsuario()` ainda na primeira linha, e o painel
    montado só quando a aba tem cartão. Dois commits (vermelho, verde).
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: uma só definição de aba no cliente, e esqueleto de carregamento coerente com a regra</name>
  <files>components/amassa/abertura/abas-abertura.tsx, components/amassa/abertura/botao-adicionar-abertura.tsx, app/(app)/abertura/loading.tsx</files>
  <action>
Em `components/amassa/abertura/abas-abertura.tsx`: apague a declaração local do tipo de aba e a
função local de normalização, e importe `abaDaUrl` e o tipo `AbaAbertura` de
`@/lib/abertura/abas` — a barra de abas e o servidor passam a concordar por construção sobre qual
é a aba padrão. NÃO toque em mais nada: `ABAS`, `AbasAbertura`, `AbasAberturaConteudoBase`,
`propsIguais` e o `memo` com comparador próprio ficam como estão (são o resultado medido de
`.planning/debug/abertura-navegacao-trava.md`). Mudança só de origem de import, sem efeito no
render.

Em `components/amassa/abertura/botao-adicionar-abertura.tsx`: esse arquivo declara uma CÓPIA
exportada do mesmo tipo `AbaAbertura` que ninguém importa (confirme com
`grep -rn "AbaAbertura" --include=*.ts --include=*.tsx app components lib tests`). Remova a
declaração local duplicada. Não mude mais nada no arquivo (o componente usa `aba: string | null`
e continua assim).

Em `app/(app)/abertura/loading.tsx`: hoje o esqueleto desenha TRÊS cartões em toda aba, o que
passaria a contradizer a regra (três blocos cinza piscando em Tarefas/Cotações e empurrando o
conteúdo quando somem). `loading.tsx` não recebe `searchParams`, então não tem como saber a aba
— e acrescentar um Client Component para ler a URL aqui está fora de questão pelo mesmo motivo
de `page.tsx`. Decisão do planner: o esqueleto do painel passa a ter a forma da aba PADRÃO (Itens,
a que o menu abre): UM único cartão-esqueleto (o mesmo `div` com `rounded-lg border border-border
p-4` e os três `Skeleton` de hoje, na mesma altura), escrito uma vez só, sem iterar sobre lista,
dentro do MESMO grid `grid grid-cols-1 gap-3 px-6 pt-6 sm:grid-cols-3 md:px-8` — assim ocupa um
terço no desktop, como o cartão real. Resultado: Itens sem salto; Por mês sem salto no desktop
(uma linha de cartões em ambos) e um cartão a mais no celular; Tarefas/Cotações sobem uma linha
quando o conteúdo chega. Reescreva o comentário do bloco explicando essa troca e citando o pedido
do dono de 19/09. Barra de abas e `EsqueletoCotacoes` do esqueleto ficam como estão.

Rode `npx tsc --noEmit`, `npm run lint` e `npm test`. Commit
`refactor(abertura): tipo de aba num lugar só e esqueleto do painel na forma da aba padrão`
com os três arquivos (adicionados um a um).
  </action>
  <verify>
    <automated>cd /c/Users/Andre/amassa && npx tsc --noEmit && npm run lint && npm test && test "$(grep -c 'function abaDaUrl' components/amassa/abertura/abas-abertura.tsx)" = "0" && test "$(cat components/amassa/abertura/abas-abertura.tsx components/amassa/abertura/botao-adicionar-abertura.tsx | grep -c 'export type AbaAbertura')" = "0" && grep -q '@/lib/abertura/abas' components/amassa/abertura/abas-abertura.tsx && test "$(grep -c '\.map(' 'app/(app)/abertura/loading.tsx')" = "0" && test "$(grep -c 'rounded-lg border border-border p-4' 'app/(app)/abertura/loading.tsx')" = "1"</automated>
  </verify>
  <done>
    `AbaAbertura` e a normalização existem só em `lib/abertura/abas.ts`; `abas-abertura.tsx` e
    `botao-adicionar-abertura.tsx` sem cópia própria, com o `memo`/comparador intactos;
    `loading.tsx` com um único cartão-esqueleto no grid de três colunas; tsc, lint e testes
    unitários verdes. Um commit.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: e2e de presença/ausência por aba (celular e desktop) e teste de consistência lido por aba</name>
  <files>tests/e2e/abertura-painel.spec.ts</files>
  <action>
Tudo dentro do PRIMEIRO `test.describe("abertura painel — o painel de três blocos e a visão Por mês", ...)`
(não o serial da inauguração), para casar com `--grep abertura`. Nada de `@vazio-*`: nenhum teste
novo afirma condição global do banco — os cartões aparecem com zero ou com número, então
presença/ausência independe dos dados de outros workers.

(a) Ajuste o teste "os três blocos do painel mostram números consistentes entre si, e o bloco de
atenção fica vermelho quando a soma passa de zero": depois de criar os itens e a tarefa como hoje,
vá para `/abertura?aba=meses` e leia ali Comprometido (consistência à vista + a prazo = total, e
piso 4400 + 1500) e Sai neste mês (piso 4400 + 1500), afirmando também que
`abertura-bloco-atencao` tem contagem 0 nessa aba. Depois vá para `/abertura` e leia ali Precisa
de atenção (consistência atrasadas + vencidas = total, piso 2, classe `text-erro`), afirmando que
`abertura-bloco-comprometido` e `abertura-bloco-mes` têm contagem 0. As asserções de número são as
mesmas de hoje — só mudam de página. Renomeie o título para dizer onde cada bloco é lido, por
exemplo: "os blocos do painel mostram números consistentes — Comprometido e Sai neste mês na aba
Por mês, Precisa de atenção na aba Itens — e o bloco de atenção fica vermelho quando a soma passa
de zero". Ajuste o comentário acima do teste citando o pedido do dono de 19/09.

(b) Novo teste, ex.: "cada aba mostra só os cartões dela — Itens: atenção; Por mês: comprometido
e sai neste mês; Tarefas e Cotações: nenhum, sem faixa vazia". Uma tabela escrita À MÃO no próprio
spec (é a expectativa do dono — NÃO importe `cartoesDaAba`, senão o e2e concorda com a regra
mesmo quando a regra está errada) com cinco casos: `/abertura` (aba esperada itens, cartões
[atencao]), `/abertura?aba=itens` (itens, [atencao]), `/abertura?aba=tarefas` (tarefas, []),
`/abertura?aba=meses` (meses, [comprometido, mes]), `/abertura?aba=cotacoes` (cotacoes, []). Um
login só, e para cada caso: `page.goto(url)`; PRIMEIRO espere
`abertura-aba-{abaEsperada}` com `aria-selected="true"` (sem isso, `toHaveCount(0)` passaria com a
página ainda carregando — a barra de abas e o painel chegam no mesmo render do servidor); depois,
para cada um dos três testids `abertura-bloco-comprometido`/`-mes`/`-atencao`, `toBeVisible()` se
está na lista do caso e `toHaveCount(0)` se não está; e com lista vazia,
`abertura-painel-resumo` também `toHaveCount(0)`. Use a mensagem customizada do `expect` com a
URL do caso, para a falha dizer qual aba quebrou.

Tamanho ("sem esticar"), no mesmo teste, por projeto (`test.info().project.name === "celular"`):
em `/abertura`, compare `boundingBox()` de `abertura-bloco-atencao` com o de
`abertura-painel-resumo` (afirme que nenhum é `null`) — desktop: largura do cartão MENOR que
metade da do painel (ocupa uma das três colunas); celular: MAIOR que 0,75 da do painel (coluna
inteira). Em `/abertura?aba=meses`: desktop — os dois cartões com `y` a menos de 2px um do outro
(mesma linha) e cada um com largura menor que metade do painel; celular — `y` de
`abertura-bloco-mes` maior que o de `abertura-bloco-comprometido` (empilhados, na ordem).

Antes do e2e: `npm run lint` e `npx tsc --noEmit` (rápidos — pegam erro de digitação no spec sem
pagar o build). Confirme `docker info` (inicie o Docker Desktop se falhar). Então a ÚNICA
invocação do plano: `npm run test:e2e -- --grep abertura` (cobre todos os specs `abertura-*`,
porque a Tarefa 2 mexeu na barra de abas que todos eles usam; timeout de Bash 600000). Se algo
falhar, corrija e — se precisar rodar de novo — registre no SUMMARY quantas invocações foram e
por quê. Por fim `npm run verificar` completo (regra do CLAUDE.md para dar o plano por
concluído). Commit `test(e2e): cartões do painel da abertura por aba` só com o spec.
  </action>
  <verify>
    <automated>cd /c/Users/Andre/amassa && npm run lint && npx tsc --noEmit && npm run test:e2e -- --grep abertura && npm run verificar</automated>
  </verify>
  <done>
    `npm run test:e2e -- --grep abertura` verde nos projetos celular e desktop (e na cadeia
    vazio-*), incluindo o novo teste de presença/ausência nas cinco URLs e o teste de consistência
    lido por aba; `npm run verificar` verde; SUMMARY lista os comandos rodados de fato. Um commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `PaginaAbertura` (Server Component) | `?aba=` é texto livre controlado por quem monta a URL; decide o que o servidor renderiza |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e4n-01 | Tampering | `abaDaUrl` em `lib/abertura/abas.ts` / `page.tsx` | low | mitigate | `?aba=` nunca é usado cru: `abaDaUrl` reduz qualquer valor a uma das quatro abas da união fechada (desconhecido → "itens"), coberto pelo teste unitário; o valor não é refletido no HTML |
| T-e4n-02 | Information Disclosure | painel em `page.tsx` | low | mitigate | Nenhum dado novo exposto — o painel só deixa de aparecer em duas abas; `await exigirUsuario()` continua a primeira instrução de `PaginaAbertura` (conferido na Tarefa 1) |
| T-e4n-03 | Denial of Service | `?aba=` com valor enorme | low | accept | Mesma leitura de sempre; o valor é só comparado com quatro literais, custo constante; o Next já limita o tamanho da URL |
</threat_model>

<verification>
- `npx vitest run tests/unit/abertura-abas.test.ts` verde (regra pura).
- `npm run test:e2e -- --grep abertura` verde em celular e desktop — uma invocação só.
- `npm run verificar` verde no fim (lint, tsc, verificar-acoes, unitários, test:migracoes).
- `git status` no fim: só os 8 arquivos deste plano commitados; `Claude outputs/` e
  `.planning/phases/06-estoque/` continuam fora do git; nada foi enviado (`git push` não rodou).
</verification>

<success_criteria>
- Itens (`/abertura`, `?aba=itens`): só "Precisa de atenção". Por mês: só "Comprometido" e "Sai
  neste mês". Tarefas e Cotações: nenhum cartão e nenhum contêiner de painel.
- Cartões com o tamanho de hoje (um terço no desktop, coluna inteira no celular), números
  idênticos aos de antes.
- Regra num módulo puro testado; `AbaAbertura`/`abaDaUrl` definidos num lugar só.
- `page.tsx` sem Client Component novo; `PainelResumo` Server Component.
</success_criteria>

<output>
Create `.planning/quick/260919-e4n-cartoes-do-painel-da-abertura-por-aba/260919-e4n-SUMMARY.md` when done
</output>
