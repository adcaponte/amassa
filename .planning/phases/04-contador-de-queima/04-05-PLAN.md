---
phase: 04-contador-de-queima
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - components/amassa/queimas/banner-atencao.tsx
  - components/amassa/queimas/filtro-fornos.tsx
  - lib/queimas/filtros.ts
  - lib/queimas/consultas.ts
  - lib/queimas/textos.ts
  - components/amassa/queimas/lista-fornos.tsx
  - app/(app)/queimas/page.tsx
  - app/(app)/page.tsx
  - components/amassa/cartao-painel.tsx
  - tests/unit/filtros-fornos.test.ts
  - tests/e2e/queimas-banner.spec.ts
autonomous: true
requirements: [FOR-06, FOR-11]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward: observable truths (D-05) ---
    - "O banner no topo de /queimas lista os fornos que precisam de atenção, com o contador de cada um (FOR-06)"
    - "O banner inclui o nível atenção, não só o crítico (FOR-06)"
    - "Nenhum forno precisando de atenção → o banner não renderiza; a ausência é o sinal de que está tudo em dia"
    - "O índice mostra os fornos ativos por padrão, com um seletor discreto oferecendo 'Desativados' e 'Todos' (D-05, FOR-11)"
    - "O filtro do índice é o único lugar da interface que responde 'quais fornos o ateliê tem, incluindo os desativados' — é a consequência aceita de D-02 e não pode ser cortado"
    - "Um forno em atenção ou crítico aparece no painel inicial, com link para /queimas"
    - "O cartão 'Fornos em atenção' do painel inicial não renderiza quando nenhum forno está em atenção ou crítico"

    # --- Edge probe: FOR-06 (adjacency, empty, encoding, ordering) ---
    - "Um forno com contador exatamente igual ao limiar de atenção aparece no banner; um com um a menos não aparece — a fronteira é `contador >= atencao`"
    - "N=0 → o banner não renderiza nenhum elemento: nem faixa vazia, nem texto '0 fornos'"
    - "O nome do forno é interpolado como texto e quebra por palavra; o limite de 80 caracteres do schema é contado em pontos de código, não em bytes"
    - "O banner lista os críticos antes dos em atenção e, dentro de cada nível, por contador decrescente — ordem estável entre recargas"

    # --- Edge probe: FOR-11 (empty, ordering) ---
    - "Filtro 'Desativados' sem nenhum resultado mostra 'Nada por aqui com esse filtro.', copy distinta do vazio de 'Nenhum forno cadastrado ainda.'"
    - "A ordem dos cartões não muda ao alternar o filtro — o filtro só remove itens da mesma lista ordenada"

    # --- UI-SPEC lift: E5 (banner agregado) ---
    - "E5 empty: não renderiza quando N=0; a ausência do banner é o sinal de 'está tudo em dia'"
    - "E5 loading: faz parte do mesmo esqueleto da página e não pisca aparecendo depois dos cartões"
    - "E5 error: se a carga falha o banner não renderiza e o `EstadoErro` da página cobre a tela inteira — nunca um banner ao lado de um erro"
    - "E5 populated: template literal '**{N} fornos precisam de atenção:** Forno 01 (95/100) · Forno 02 (103/100)'"
    - "E5 partial: forno em atenção sem manutenção anterior aparece normalmente — o banner mostra contador/limite, que existe desde a primeira queima"
    - "E5 overflow: mostra os 3 primeiros fornos e trunca com '· e mais {N}', mantendo a altura previsível e sem empurrar os cartões para baixo da dobra no celular"
    - "E5 zero-one-many: 0 → não renderiza; 1 → '1 forno precisa de atenção:' no singular; 2–3 → lista completa; ≥4 → 3 mais 'e mais {N}'"
    - statement: "E5 long-text: o banner permanece com altura previsível no pior caso realista — 3 fornos com nomes de 80 caracteres mais 'e mais 2', em viewport de celular estreito"
      verification: backstop

    # --- UI-SPEC lift: E11 (alerta de forno no painel inicial) ---
    - "E11 empty: não renderiza quando nenhum forno está em atenção ou crítico — a ausência é o sinal de normalidade, igual ao banner"
    - "E11 loading: esqueleto do `CartaoPainel`, na mesma grade do resto do painel inicial"
    - "E11 error: se a consulta falhar, o cartão não renderiza pela metade — o painel exibe o `EstadoErro` do bloco, para um forno crítico invisível por erro nunca parecer 'tudo em dia'"
    - "E11 populated: mostra os fornos em atenção/crítico com o contador de cada um e link para /queimas"
    - "E11 partial: forno em atenção sem manutenção anterior aparece normalmente"
    - statement: "E11 overflow: a mesma regra do banner (3 fornos mais 'e mais {N}') mantém altura previsível dentro do `CartaoPainel`, verificada no contêiner menor do painel inicial"
      verification: backstop
    - "E11 zero-one-many: 0 → não renderiza; 1 → copy no singular; muitos → plural mais truncagem; o mesmo par de strings do banner, nunca uma segunda redação"
    - "E11 long-text: nomes de até 80 caracteres quebram dentro do cartão do painel; a truncagem em 3 já limita quantos aparecem de uma vez"

  artifacts:
    - path: "components/amassa/queimas/banner-atencao.tsx"
      provides: "banner agregado com singular/plural, truncagem em 3 e ausência quando N=0"
      min_lines: 30
    - path: "components/amassa/queimas/filtro-fornos.tsx"
      provides: "seletor Ativos / Desativados / Todos"
    - path: "lib/queimas/filtros.ts"
      provides: "filtro puro por estado ativo e a ordenação do banner"
      exports: ["filtrarPorAtivo", "ordenarParaBanner"]
    - path: "components/amassa/cartao-painel.tsx"
      provides: "CartaoPainel passa a aceitar conteúdo populado além da frase de vazio"
    - path: "app/(app)/page.tsx"
      provides: "cartão Fornos em atenção com dado real"

  key_links:
    - from: "app/(app)/queimas/page.tsx"
      to: "components/amassa/queimas/banner-atencao.tsx"
      via: "o banner é montado entre CabecalhoPagina e a lista, e não renderiza com lista vazia"
      pattern: "<BannerAtencao"
    - from: "app/(app)/page.tsx"
      to: "lib/queimas/consultas.ts"
      via: "fornosQuePrecisamDeAtencao() alimenta o cartão do painel inicial"
      pattern: "fornosQuePrecisamDeAtencao\\("
    - from: "components/amassa/queimas/filtro-fornos.tsx"
      to: "lib/queimas/filtros.ts"
      via: "filtrarPorAtivo aplicado no cliente sobre a mesma lista carregada"
      pattern: "filtrarPorAtivo\\("
---

<objective>
O aviso passivo do módulo: o banner agregado no topo de `/queimas`, o filtro que torna os fornos
desativados alcançáveis, e o cartão "Fornos em atenção" do painel inicial finalmente com dado real.

Purpose: FOR-06 e a metade de FOR-11 que faltava. O módulo é um controle de vida útil — sem um aviso
antes de estourar, ele é só um histórico. O projeto recusou notificação ativa (custo recorrente,
canal que não existe), então o aviso é passivo e precisa aparecer nos dois lugares onde o gestor
já olha: o topo da tela de fornos e o painel inicial. O filtro do índice é a consequência aceita de
D-02 registrada em `04-CONTEXT.md`: sem tela de cadastro, ele é o **único** caminho que responde
"quais fornos o ateliê tem, incluindo os desativados".

Output: `banner-atencao.tsx` e `filtro-fornos.tsx` (nenhum dos dois tem análogo exato no
repositório), `lib/queimas/filtros.ts` puro, `fornosQuePrecisamDeAtencao` e o painel inicial ligado.

## Nota de reconciliação do lift do UI-SPEC

`04-UI-SPEC.md` declara no cabeçalho de `## UI Considerations` **65 aplicáveis — 61 covered, 4
backstop**, mas marca `🧪 backstop` em apenas três linhas das tabelas: E3 loading, E6
zero-one-many e E5 long-text. O quarto backstop citado no contrato de consumo ("banner long-text
worst case") repete o de E5. Para não fabricar nem descartar em silêncio, o quarto foi atribuído a
**E11 overflow**, a única outra linha cuja resolução herda literalmente a afirmação não verificada
de altura do banner ("Mesma regra de E5") num contêiner diferente — o `CartaoPainel` do painel
inicial. Assim os 65 estão representados e a contagem 61/4 fecha. Distribuição por plano:
`04-01` 16 linhas · `04-02` 8 · `04-03` 12 · `04-04` 5 · `04-05` 16 · `04-06` 8 = 65.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/04-contador-de-queima/04-CONTEXT.md
@.planning/phases/04-contador-de-queima/04-PATTERNS.md
@.planning/phases/04-contador-de-queima/04-UI-SPEC.md
@.planning/phases/04-contador-de-queima/04-04-SUMMARY.md
@amassa-plataforma/04-DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: O banner agregado — singular, plural, truncagem em 3 e ausência em zero</name>
  <read_first>
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §8 — o template literal do banner agregado e a nota de
      que ele inclui o nível **atenção**, não só o crítico
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"Copywriting Contract" (linha "Banner
      agregado"), §"Visual Hierarchy" (o banner é o foco primário de `/queimas` quando existe) e
      §"UI Considerations" E5 (as oito categorias)
    - `.planning/phases/04-contador-de-queima/04-PATTERNS.md` §"No Analog Found" — o banner é
      componente novo; `CartaoPainel` é o parente estrutural mais próximo, mas é cartão estático
    - `lib/queimas/contador.ts` — `NivelDeForno`, `limiarDeAtencao`, `medirForno`
    - `lib/queimas/consultas.ts` — `listarFornosDoIndice` e `FornoMedido`
    - `app/globals.css` linhas 48-51 — os pares `-fundo`/`-texto` de atenção e crítico
    - `app/(app)/queimas/page.tsx` — onde o banner é montado
    - `lib/encomendas/filtros.ts` linhas 1-13 e 81-89 — a disciplina de módulo puro sem import e o
      formato de uma função de filtro pura
  </read_first>
  <files>lib/queimas/filtros.ts, tests/unit/filtros-fornos.test.ts, lib/queimas/textos.ts, components/amassa/queimas/banner-atencao.tsx, app/(app)/queimas/page.tsx</files>
  <action>
    **Módulo puro.** Crie `lib/queimas/filtros.ts` **sem nenhum import**, no molde de
    `lib/encomendas/filtros.ts`, com: `type FiltroDeForno = "ativos" | "desativados" | "todos"`;
    `filtrarPorAtivo(fornos, filtro)`, que devolve a mesma lista na mesma ordem, apenas removendo
    itens; e `ordenarParaBanner(fornos)`, que devolve os fornos que precisam de atenção — os de
    nível `critico` primeiro, os de `atencao` depois, e dentro de cada nível por contador
    decrescente, com o nome como terceiro critério para a ordem ser estável entre recargas. Fornos
    desativados **não** entram no banner (um forno fora de uso não precisa de manutenção urgente);
    registre isso num comentário do módulo.

    **Textos.** Acrescente a `lib/queimas/textos.ts` a função `fraseDoBanner(fornosEmAtencao)`, que
    monta o texto literal do design system: prefixo em negrito `{N} fornos precisam de atenção:` no
    plural e `1 forno precisa de atenção:` no singular, seguido da lista dos **3 primeiros** no
    formato `{nome} ({contador}/{limite})`, separados por ` · `, e do sufixo ` · e mais {N}` quando
    houver mais de três. Nunca uma segunda redação: o mesmo par de strings serve o banner e o cartão
    do painel inicial (Tarefa 3).

    **Componente.** Crie `components/amassa/queimas/banner-atencao.tsx` como Server Component puro
    de apresentação. Recebe a lista já ordenada e **devolve `null` quando ela está vazia** — nada de
    faixa vazia, nada de "0 fornos". O contêiner usa `--color-forno-atencao-fundo`/`-texto` quando o
    pior nível da lista é atenção, e `--color-forno-critico-fundo`/`-texto` quando há algum crítico,
    com o ícone de alerta `aria-hidden="true"` ao lado do texto visível no caso crítico. Copy em
    `text-apoio`. Os nomes quebram por palavra (`[overflow-wrap:anywhere]`); a truncagem em 3 é o que
    mantém a altura previsível no celular — os nomes completos estão nos cartões logo abaixo.

    **Montagem.** Em `app/(app)/queimas/page.tsx`, monte `<BannerAtencao>` entre o `CabecalhoPagina`
    e o bloco de lista/vazio, alimentado por `ordenarParaBanner(...)` sobre os fornos já carregados —
    **sem uma segunda consulta**, o que garante que banner e cartões nunca discordem. Como o banner
    faz parte do mesmo Server Component, ele já entra no `loading.tsx` da rota e não pisca aparecendo
    depois dos cartões; e se a carga falha, `error.tsx` cobre a tela inteira e o banner não aparece.

    **Testes.** `tests/unit/filtros-fornos.test.ts` cobrindo: `ordenarParaBanner` com contador
    exatamente no limiar (entra) e um abaixo (não entra); crítico antes de atenção; empate de
    contador desempatado por nome; forno desativado fora do banner; lista vazia devolvendo lista
    vazia. E `fraseDoBanner` com N=1 (singular), N=2, N=3 (lista completa) e N=5 (3 mais "e mais 2").
  </action>
  <verify>
    <automated>npm test -- filtros-fornos</automated>
    <automated>npm run verificar</automated>
  </verify>
  <acceptance_criteria>
    - `lib/queimas/filtros.ts` não tem nenhuma linha de `import`
    - `ordenarParaBanner` inclui um forno com contador igual ao limiar e exclui um com o limiar menos
      um — a fronteira é `>=`, provado no teste unitário
    - `ordenarParaBanner` exclui fornos desativados
    - `fraseDoBanner` produz o singular com N=1 e o plural com N≥2, e acrescenta "e mais 2" com N=5
    - `banner-atencao.tsx` devolve `null` com lista vazia — nenhum elemento é renderizado
    - O banner é alimentado pela mesma lista que os cartões, sem uma segunda chamada ao banco
    - O ícone de alerta do caso crítico tem `aria-hidden="true"` e há texto visível ao lado
    - `npm test -- filtros-fornos` passa
    - `npm run verificar` passa inteiro
  </acceptance_criteria>
  <done>
    O banner no topo lista os fornos que precisam de atenção com o contador de cada um, em singular
    ou plural, truncando em três, e desaparece quando não há nenhum.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: O filtro Ativos / Desativados / Todos no índice</name>
  <read_first>
    - `components/amassa/encomendas/filtro-encomendas.tsx` — o mecanismo de filtro no cliente que
      D-05 manda reaproveitar em vez de inventar um segundo
    - `lib/encomendas/filtros.ts` — o filtro puro aplicado no cliente
    - `.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md` D-07 e D-11 — histórico como filtro do
      próprio índice, e filtro no cliente
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"Copywriting Contract" (linha "Empty
      state (índice filtrado)") e §"UI Considerations" E1 empty
    - `components/amassa/estado-vazio.tsx` — `aoClicar` (ação de cliente, acrescentado no 03-07) é o
      que o botão "Limpar filtro" precisa, já que zerar o filtro não muda de URL
    - `lib/queimas/filtros.ts` — `FiltroDeForno` e `filtrarPorAtivo` da Tarefa 1
    - `components/amassa/queimas/lista-fornos.tsx` e `app/(app)/queimas/page.tsx`
  </read_first>
  <files>components/amassa/queimas/filtro-fornos.tsx, components/amassa/queimas/lista-fornos.tsx, lib/queimas/textos.ts, app/(app)/queimas/page.tsx</files>
  <action>
    Crie `components/amassa/queimas/filtro-fornos.tsx` como Client Component: um seletor discreto de
    três posições — "Ativos" (padrão), "Desativados", "Todos" — no molde de
    `components/amassa/encomendas/filtro-encomendas.tsx`, que é o mecanismo que D-05 manda
    reaproveitar. Cada posição com alvo de no mínimo 44px e estado ativo perceptível sem depender só
    de cor. Sem busca e sem ordenação: este caso é estritamente mais simples que o de Encomendas.

    Mova o estado do filtro para `components/amassa/queimas/lista-fornos.tsx`, que passa a ser
    Client Component recebendo a lista completa do servidor e aplicando `filtrarPorAtivo` — filtro no
    cliente, sobre a mesma lista já carregada (D-11 da Fase 3), sem nova ida ao servidor e sem mudar
    de URL. O banner continua sendo calculado sobre a lista **completa**, não sobre a filtrada: um
    forno em atenção não some do aviso porque alguém trocou o filtro.

    Lista filtrada vazia → `EstadoVazio` com título "Nada por aqui com esse filtro." e corpo "Troque
    para 'Ativos' ou cadastre um forno novo.", usando `aoClicar` para um botão que volta o filtro
    para "Ativos". Guarde as duas frases em `lib/queimas/textos.ts` como
    `FRASE_FILTRO_VAZIO_TITULO`/`FRASE_FILTRO_VAZIO_CORPO`. **Essas duas frases são distintas das do
    vazio de "Nenhum forno cadastrado ainda."** e os dois estados nunca se confundem: um diz que não
    há forno nenhum, o outro que o filtro não achou nada.

    Alternar o filtro não reordena nada — `filtrarPorAtivo` só remove itens da mesma lista já
    ordenada por nome.
  </action>
  <verify>
    <automated>npm run verificar</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - O seletor tem exatamente três posições e "Ativos" é a inicial
    - Cada posição do seletor tem alvo de no mínimo 44px e o estado ativo é distinguível sem
      depender apenas de cor
    - `filtrarPorAtivo` é aplicado no cliente sobre a lista já carregada; nenhuma consulta nova é
      disparada ao trocar o filtro
    - O banner é calculado sobre a lista completa, não sobre a filtrada
    - Filtro sem resultado renderiza "Nada por aqui com esse filtro.", texto diferente de "Nenhum
      forno cadastrado ainda."
    - O botão do vazio filtrado usa a prop de ação de cliente de `EstadoVazio`, não `hrefBotao`
    - A ordem dos cartões visíveis é a mesma em "Ativos" e em "Todos", apenas com itens a menos
    - `npm run verificar` passa inteiro
  </acceptance_criteria>
  <done>
    `/queimas` mostra os ativos por padrão e oferece "Desativados" e "Todos" — o único caminho da
    interface que responde quais fornos o ateliê tem, incluindo os desativados.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: O cartão "Fornos em atenção" do painel inicial com dado real</name>
  <read_first>
    - `app/(app)/page.tsx` (arquivo inteiro, 29 linhas) — os quatro cartões nomeados e vazios, e o
      comentário que anuncia que cada um ganha dado real na fase do seu módulo
    - `components/amassa/cartao-painel.tsx` (arquivo inteiro) — as props atuais e o comentário das
      linhas 6-10, que descreve o formato do esqueleto para quando houver consulta real; esta é a
      fase em que esse gatilho se aplica para Fornos
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"UI Considerations" E11 (as oito
      categorias)
    - `lib/queimas/consultas.ts` — `listarFornosDoIndice`, o `left join lateral` e `FornoMedido`
    - `lib/queimas/filtros.ts` e `lib/queimas/textos.ts` — `ordenarParaBanner` e `fraseDoBanner` da
      Tarefa 1
    - `components/amassa/estado-erro.tsx` — o bloco de erro que o painel usa quando a consulta falha
    - `tests/e2e/casca.spec.ts` — as convenções de teste do painel inicial e da casca
    - `playwright.config.ts` — a cadeia de projetos e as etiquetas
  </read_first>
  <files>lib/queimas/consultas.ts, app/(app)/page.tsx, components/amassa/cartao-painel.tsx, tests/e2e/queimas-banner.spec.ts</files>
  <action>
    **Consulta.** Em `lib/queimas/consultas.ts`, exporte `fornosQuePrecisamDeAtencao()`, que carrega
    os fornos ativos com contador, total e limite pelo mesmo `left join lateral` do índice e devolve
    já filtrado e ordenado por `ordenarParaBanner`. É uma consulta de propósito próprio, separada de
    `listarFornosDoIndice` — o painel inicial não paga o custo dos dados de rodapé que só o cartão do
    índice usa (a mesma disciplina de "não confundir duas consultas com propósitos diferentes" do
    plano 03-08).

    **Cartão.** Estenda `components/amassa/cartao-painel.tsx` de forma **aditiva**: mantenha `titulo`
    e `vazio` exatamente como estão (os outros três cartões do painel continuam funcionando sem tocar
    numa linha) e acrescente uma prop opcional de conteúdo populado. Quando ela vem, o cartão
    renderiza o conteúdo; quando não vem, o comportamento de hoje não muda em nada.

    **Painel.** Em `app/(app)/page.tsx`, alimente o cartão "Fornos em atenção" com
    `fornosQuePrecisamDeAtencao()`. Zero fornos em atenção → **o cartão não renderiza**: a ausência é
    o sinal de normalidade, igual ao banner, e é o que impede um forno crítico invisível por erro de
    parecer "tudo em dia". Um ou mais → a mesma copy do banner (`fraseDoBanner`, singular/plural,
    truncagem em 3 mais "e mais {N}"), com o contador de cada forno e um link para `/queimas` com
    alvo de no mínimo 44px. Se a consulta falhar, o bloco mostra o `EstadoErro` em vez de um cartão
    pela metade. A grade do painel continua `md:grid-cols-2` e a altura do cartão permanece
    previsível — a truncagem em 3 é o que garante isso no contêiner menor do painel.

    **Teste ponta a ponta.** Crie `tests/e2e/queimas-banner.spec.ts`, sem etiqueta de vazio (cria
    dado), cobrindo: (1) com nenhum forno em atenção, `/queimas` **não** contém o texto "precisam de
    atenção" e o painel inicial não mostra o cartão de fornos; (2) levar um forno de limite 10 ao
    limiar e afirmar o banner no singular com o contador; (3) levar um segundo forno ao crítico e
    afirmar o plural, a ordem com o crítico primeiro, e o mesmo texto aparecendo no painel inicial;
    (4) o filtro "Desativados" mostrando o vazio filtrado com a copy própria. Uma única invocação de
    `npm run test:e2e` nesta tarefa, com `--grep`. Nenhum caso deste arquivo pode afirmar uma
    condição global do banco sem estar na cadeia de projetos — use fornos criados pelo próprio teste
    e afirme sobre eles pelo nome, nunca "nenhum forno em atenção existe no sistema".
  </action>
  <verify>
    <automated>npm run verificar</automated>
    <automated>npm run test:e2e -- --grep "banner"</automated>
  </verify>
  <acceptance_criteria>
    - `fornosQuePrecisamDeAtencao` existe em `lib/queimas/consultas.ts` e é uma consulta separada de
      `listarFornosDoIndice`
    - `components/amassa/cartao-painel.tsx` continua aceitando `titulo` e `vazio`, e os outros três
      cartões de `app/(app)/page.tsx` não mudaram de assinatura
    - Com zero fornos em atenção, o cartão "Fornos em atenção" não aparece no DOM do painel inicial
    - O painel usa a mesma função de copy do banner — não há uma segunda redação da frase em lugar
      nenhum do repositório
    - O link para `/queimas` dentro do cartão tem alvo de no mínimo 44px
    - Nenhum caso de `tests/e2e/queimas-banner.spec.ts` afirma uma condição global do banco fora da
      cadeia de projetos; as afirmações são sobre fornos que o próprio teste criou
    - O teste passa nos projetos `desktop` e `celular`
    - `npm run test:e2e` foi invocado no máximo uma vez nesta tarefa, com `--grep`
    - `npm run verificar` passa inteiro
  </acceptance_criteria>
  <done>
    Um forno em atenção ou crítico aparece no banner do topo de `/queimas` e no painel inicial, com
    o contador de cada um, e some dos dois quando nenhum precisa de atenção.
  </done>
</task>

</tasks>

## Artifacts this phase produces

**Produzidos por este plano**

- `lib/queimas/filtros.ts`: `FiltroDeForno`, `filtrarPorAtivo`, `ordenarParaBanner`
- `lib/queimas/textos.ts` += `fraseDoBanner`, `FRASE_FILTRO_VAZIO_TITULO`,
  `FRASE_FILTRO_VAZIO_CORPO`, `ROTULO_FILTRO_ATIVOS`, `ROTULO_FILTRO_DESATIVADOS`,
  `ROTULO_FILTRO_TODOS`
- `lib/queimas/consultas.ts` += `fornosQuePrecisamDeAtencao`
- `components/amassa/queimas/banner-atencao.tsx`, `components/amassa/queimas/filtro-fornos.tsx`
- `components/amassa/queimas/lista-fornos.tsx` passa a ser componente de cliente com o filtro
- `components/amassa/cartao-painel.tsx` ganha, de forma aditiva, uma prop de conteúdo populado
- `app/(app)/page.tsx`: cartão "Fornos em atenção" com dado real
- Testes: `tests/unit/filtros-fornos.test.ts`, `tests/e2e/queimas-banner.spec.ts`

**Produzidos pelos planos irmãos da Fase 4** (contexto, não escopo deste plano)

`04-01`: tipo `tipo_queima`; tabelas `fornos`, `queimas`, `manutencoes`; migrações `0007_queimas.sql`
e `0008_gatilhos-queimas.sql`; gatilhos `tocar_atualizado_em_*`; `TABELAS_ESPERADAS` += três
entradas; `lib/queimas/contador.ts`; `esquemaId`, `esquemaForno`, `esquemaQueima`; `criarForno`,
`registrarQueima`, `excluirQueima`; rota `/queimas`; `registrar-queima.tsx`, `formulario-forno.tsx`. ·
`04-02`: `medidor.tsx`, `cartao-forno.tsx` final, `lib/queimas/formato.ts`, `textoDoNivel`,
`fraseDoRodape`, `app/(app)/queimas/loading.tsx`, `app/(app)/queimas/error.tsx`. ·
`04-03`: rota `/queimas/[id]` (+ `loading`/`error`), `buscarForno`, `historico-queimas.tsx`,
`historico-manutencoes.tsx`, `confirmar-excluir-queima.tsx`. ·
`04-04`: `registrarManutencao`, `atualizarForno`, `desativarForno`, `reativarForno`,
`esquemaManutencao`, `esquemaAtualizacaoDeForno`, `registrar-manutencao.tsx`, `acoes-forno.tsx`. ·
`04-06`: `recharts`, `--color-chart-1..5`, `lib/queimas/relatorios.ts`, rota `/queimas/relatorios`,
`relatorios-recharts.tsx`, `estatisticas-queimas.tsx`, `seletor-queimas.tsx`. ·
`04-07`: migração em produção, varredura completa e verificação humana.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessão → `app/(app)/page.tsx` | O painel inicial passa a ler dado de produto pela primeira vez para Fornos. |
| servidor → filtro no cliente | A lista completa (inclusive desativados) trafega para o navegador. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | `app/(app)/page.tsx` e `app/(app)/queimas/page.tsx` | high | mitigate | `exigirUsuario()` na primeira linha das duas páginas; `middleware.ts` protege tudo fora de `/login` e `/api/health` |
| T-04-18 | Information Disclosure | lista completa de fornos (inclusive desativados) enviada ao cliente para o filtro | low | accept | Os 3 a 5 gestores têm acesso total por desenho (REQUIREMENTS.md §Out of Scope); nome e contador de um forno desativado não são dado sensível, e é justamente o que o filtro "Todos" existe para mostrar |
| T-04-19 | Denial of Service | `fornosQuePrecisamDeAtencao` executada em toda carga do painel inicial | low | mitigate | Consulta agregada por forno (2 a 6 linhas), não por queima, com índice em `queimas (forno_id, ocorrida_em desc)` |
| T-04-20 | Tampering | filtro aplicado no cliente | low | accept | O filtro é conveniência de apresentação sobre dados que o gestor já tem direito de ver; nenhuma decisão de autorização depende dele |
| T-04-21 | Information Disclosure | forno crítico invisível por falha de consulta parecer "tudo em dia" | medium | mitigate | Falha na consulta renderiza `EstadoErro` no bloco; o cartão nunca aparece pela metade nem silencia um crítico (E11 error) |
</threat_model>

<verification>
1. `npm run verificar` passa inteiro
2. `npm test -- filtros-fornos` passa
3. `npm run test:e2e -- --grep "banner"` passa nos dois projetos — uma única invocação, na Tarefa 3
4. Nenhum caso novo afirma condição global do banco fora da cadeia de projetos do Playwright
5. Nenhum `npm run build` como passo separado
</verification>

<success_criteria>
- O banner no topo lista os fornos que precisam de atenção, com o contador de cada um, incluindo o
  nível atenção e não só o crítico
- O banner não renderiza quando nenhum forno precisa de atenção
- O índice mostra os ativos por padrão e oferece "Desativados" e "Todos"
- Um forno em atenção ou crítico aparece no painel inicial
- `npm run verificar` passa
</success_criteria>

<output>
Create `.planning/phases/04-contador-de-queima/04-05-SUMMARY.md` when done.
Registre no SUMMARY quais comandos de teste ponta a ponta foram executados de fato e quantas vezes.
</output>
