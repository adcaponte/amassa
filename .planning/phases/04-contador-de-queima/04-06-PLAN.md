---
phase: 04-contador-de-queima
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - package.json
  - package-lock.json
  - app/globals.css
  - lib/queimas/relatorios.ts
  - lib/queimas/consultas.ts
  - lib/queimas/textos.ts
  - tests/unit/relatorios-queimas.test.ts
  - app/(app)/queimas/relatorios/page.tsx
  - app/(app)/queimas/relatorios/loading.tsx
  - app/(app)/queimas/relatorios/error.tsx
  - components/amassa/queimas/relatorios-recharts.tsx
  - components/amassa/queimas/estatisticas-queimas.tsx
  - components/amassa/queimas/seletor-queimas.tsx
  - app/(app)/queimas/page.tsx
  - app/(app)/queimas/[id]/page.tsx
  - tests/e2e/queimas-relatorios.spec.ts
autonomous: false
requirements: [FOR-12]

user_setup: []

estimate:
  tokens: 112000
  raw_tokens: 112000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward: observable truths (D-01, D-07, D-08) ---
    - "`/queimas/relatorios` é uma rota de verdade com endereço próprio; o seletor do topo parece aba mas navega, e cada tela busca só o que precisa (D-01)"
    - "Os gráficos batem com a contagem manual do histórico (FOR-12)"
    - "O alternador troca entre 8 semanas e 6 meses, e a semana começa na segunda (FOR-12)"
    - "As quatro estatísticas do topo mostram total, últimos 30 dias e a contagem dos dois primeiros tipos"
    - "No celular as quatro estatísticas vêm empilhadas primeiro e cada gráfico rola horizontalmente dentro do próprio contêiner — a página nunca rola lateralmente (D-07)"
    - "Os dois tamanhos de tela mostram o mesmo recorte de dados: 8 semanas e 6 meses, nada reduzido no celular (D-07)"
    - "Sem nenhuma queima registrada, os gráficos dão lugar a um estado vazio com o caminho de volta para os fornos, e o item 'Relatórios' continua visível no seletor (D-08)"
    - "`recharts` só entra em `package.json` depois de aprovado no portão de legitimidade de pacote"

    # --- Edge probe: FOR-12 (boundary, adjacency, empty, ordering, precision, idempotency, concurrency) ---
    - "A semana começa na segunda no fuso America/Sao_Paulo: uma queima às 23:30 de um domingo, horário de Brasília, cai na semana que termina naquele domingo; uma às 00:10 da segunda abre a semana seguinte"
    - "O balde é semiaberto: uma queima no instante exato do fim de um balde pertence ao balde seguinte, nunca aos dois"
    - "Semanas e meses sem nenhuma queima aparecem com valor zero — os 8 baldes (ou 6) são sempre plotados por inteiro, o eixo fica estável e o buraco de produção fica visível"
    - "Os baldes vão do mais antigo ao mais recente, da esquerda para a direita, e a série empilhada mantém sempre a ordem biscoito, esmalte, ouro"
    - "'6 meses' são meses civis — o atual mais os cinco anteriores — calculados sem `Date`, e a soma das barras bate exatamente com a contagem manual do histórico"
    - "As funções de agregação são puras: mesma entrada, mesma saída, e nenhuma delas lê o relógio por dentro — 'hoje' chega sempre por argumento"
    - statement: "Alternar Semana/Mês não refaz a consulta nem muda os números: as duas agregações vêm do mesmo conjunto de queimas carregado uma vez"
      verification: backstop

    # --- UI-SPEC lift: E9 (relatórios) ---
    - "E9 empty: D-08 — `EstadoVazio` no lugar dos gráficos, com 'Nenhuma queima registrada ainda.' e o botão 'Ver fornos'; a aba 'Relatórios' continua visível e alcançável mesmo vazia"
    - "E9 loading: esqueleto na forma do conteúdo — 4 blocos de estatística mais retângulos na altura dos gráficos"
    - "E9 error: `EstadoErro` com a copy padrão do projeto; as estatísticas não renderizam meio-preenchidas"
    - "E9 populated: 4 estatísticas no topo, depois barras empilhadas por tipo e barras horizontais por forno, com o alternador Semana/Mês"
    - "E9 partial: recortes sem queima aparecem como zero e não somem — as 8 semanas ou 6 meses são sempre plotadas por inteiro"
    - "E9 overflow: cada gráfico rola horizontalmente dentro do próprio contêiner; a página em si nunca rola horizontalmente"
    - "E9 zero-one-many: um único forno produz barras horizontais com uma barra só, não um gráfico degenerado; as 4 estatísticas renderizam sempre, com zero quando aplicável"
    - "E9 long-text: nome de forno no eixo das barras horizontais trunca com reticências e mostra o nome completo no tooltip, mantendo largura de eixo previsível e espaço para a barra no celular"

  artifacts:
    - path: "lib/queimas/relatorios.ts"
      provides: "agregação pura de 8 semanas e 6 meses, semana começando na segunda, sem Date e sem imports"
      exports: ["agregarPorSemana", "agregarPorMes", "estatisticasDeQueimas"]
    - path: "tests/unit/relatorios-queimas.test.ts"
      provides: "provas de fronteira de semana, balde semiaberto e soma batendo com a contagem manual"
    - path: "app/(app)/queimas/relatorios/page.tsx"
      provides: "rota própria dos relatórios com estatísticas, gráficos e o vazio de D-08"
    - path: "components/amassa/queimas/relatorios-recharts.tsx"
      provides: "barras empilhadas por tipo e barras horizontais por forno, cada uma com rolagem própria"
    - path: "components/amassa/queimas/seletor-queimas.tsx"
      provides: "seletor de topo Fornos · Relatórios, que parece aba mas navega"
    - path: "app/globals.css"
      provides: "tokens --color-chart-1..5 mapeados nos tokens de tipo de queima"
      contains: "--color-chart-1"

  key_links:
    - from: "components/amassa/queimas/relatorios-recharts.tsx"
      to: "lib/queimas/relatorios.ts"
      via: "os baldes já agregados no servidor chegam como prop; o componente não agrega nada"
      pattern: "(agregarPorSemana|agregarPorMes)"
    - from: "app/(app)/queimas/relatorios/page.tsx"
      to: "lib/queimas/consultas.ts"
      via: "carregarQueimasParaRelatorio() traz uma vez as queimas da janela mais longa"
      pattern: "carregarQueimasParaRelatorio\\("
    - from: "components/amassa/queimas/seletor-queimas.tsx"
      to: "app/(app)/queimas/relatorios/page.tsx"
      via: "link de navegação real para a rota dos relatórios (D-01)"
      pattern: "/queimas/relatorios"
---

<objective>
Os relatórios: quatro estatísticas, barras empilhadas por tipo alternando entre 8 semanas e 6 meses,
barras horizontais por forno — e o seletor de topo que fecha a estrutura de telas de D-01.

Purpose: FOR-12. O critério de aceite é duro e específico: **os gráficos batem com a contagem manual
do histórico**. Isso só é verificável se a agregação for um módulo puro testado, e é aí que mora a
armadilha desta fase — a agregação semanal e mensal trabalha com **dias civis de Brasília**, e
`getDay()` no fuso do processo dá o resultado errado. O `formato.ts` do plano 04-02 já resolveu esse
problema com `Intl` e `timeZone: "America/Sao_Paulo"`; `relatorios.ts` reaproveita a abordagem sem
nunca chamar `new Date(...).getDay()`.

`recharts` é a **única dependência npm nova da fase** e a primeira desde a Fase 3.

Output: `lib/queimas/relatorios.ts` testado antes do código, a rota `/queimas/relatorios` com os
três estados, os gráficos, as quatro estatísticas, o seletor de topo e os tokens de gráfico.

## Portão de legitimidade de pacote — política aplicada

A pesquisa está desligada na configuração deste projeto, então **não existe RESEARCH.md nem uma
tabela `## Package Legitimacy Audit`** para esta fase. A política de fallback se aplica na íntegra:
`recharts` é tratado como **`[ASSUMED]`**, e por isso a Tarefa 1 é um ponto de parada humano
**bloqueante**, colocado **antes** de qualquer instalação. Ele não é auto-aprovável, independente da
configuração de avanço automático. É o mesmo tratamento que `next-auth`, `@node-rs/argon2`,
`lucide-react` e `axe-core` receberam nas fases 02a e 02b.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/04-contador-de-queima/04-CONTEXT.md
@.planning/phases/04-contador-de-queima/04-PATTERNS.md
@.planning/phases/04-contador-de-queima/04-UI-SPEC.md
@.planning/phases/04-contador-de-queima/04-05-SUMMARY.md
@amassa-plataforma/02-MODELO-DE-DADOS.md
@amassa-plataforma/04-DESIGN-SYSTEM.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Tarefa 1: Portão de legitimidade — `recharts`</name>
  <what-built>
    Nada ainda. Este ponto de parada vem **antes** da instalação, de propósito. `recharts` é a única
    dependência npm nova da Fase 4 e não passou por nenhuma auditoria de legitimidade, porque esta
    fase não tem RESEARCH.md — a pesquisa está desligada na configuração do projeto. Pela política
    de fallback, ele é tratado como `[ASSUMED]` e a instalação fica bloqueada até aprovação humana.
  </what-built>
  <how-to-verify>
    1. Abra `https://www.npmjs.com/package/recharts` e confira: o repositório apontado é
       `github.com/recharts/recharts`, a contagem de downloads semanais é da ordem de milhões, e há
       publicação recente (não é um pacote abandonado).
    2. Confira que o nome está escrito exatamente `recharts` — sem hífen, sem sufixo, sem escopo. O
       modo de falha clássico deste portão é um pacote com nome parecido publicado por outra pessoa.
    3. Anote a versão exata que será fixada (sem `^`, sem `~`, no formato que
       `package.json` já usa para `next-auth`, `sonner` e `lucide-react`) e confira no npm que essa
       versão específica existe e é a linha estável mais recente.
    4. Rode, no repositório, e leia a saída antes de aprovar:
       `npm view recharts versions --json` e `npm view recharts@<versao> dist.integrity repository.url`
    5. Confira se a instalação traz dependências transitivas novas. Depois de `npm install`, o diff
       de `package-lock.json` deve ser lido: `recharts` traz `d3-*` e `victory-vendor`. Se aparecer
       qualquer pacote fora dessa família, pare e traga para conversa.
  </how-to-verify>
  <resume-signal>Responda com a versão aprovada (ex.: `aprovado 3.2.1`) ou descreva o que impede</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A agregação pura — 8 semanas, 6 meses e as quatro estatísticas</name>
  <precondition>o portão de legitimidade da Tarefa 1 foi respondido com uma versão aprovada, e ela é a versão fixada em `package.json`</precondition>
  <read_first>
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` §3, "Regras de agregação — o módulo puro precisa
      reproduzir exatamente" — as quatro regras literais
    - `lib/encomendas/cronograma.ts` linhas 1-11 e 105-136 — a disciplina do módulo puro e
      `diasDesdeAEpoca`/`civilDesdeDias`, a base correta para calcular dia da semana a partir de uma
      contagem de dias, sem tocar em `Date`
    - `lib/encomendas/filtros.ts` linhas 1-13 e 208-230 — `calcularJanelaDoHistorico` e o auxiliar de
      dias no mês: a forma de aritmética de data civil só com string `YYYY-MM-DD`
    - `lib/queimas/formato.ts` — `hojeEmBrasilia` e `diaCivilEmBrasilia` do plano 04-02; é por aqui
      que um `timestamptz` vira dia civil de Brasília antes de entrar num balde
    - `.planning/phases/04-contador-de-queima/04-CONTEXT.md` §Specific Ideas — a descrição literal da
      armadilha de fuso deste módulo
    - `tests/unit/cronograma.test.ts` e `tests/unit/filtros-encomendas.test.ts` — convenções de teste
    - `lib/queimas/consultas.ts` — o padrão de consulta sem diretiva de Server Action
    - `package.json` — o formato de versão fixa (sem intervalo) usado pelas dependências de produção
  </read_first>
  <files>package.json, package-lock.json, lib/queimas/relatorios.ts, tests/unit/relatorios-queimas.test.ts, lib/queimas/consultas.ts, lib/queimas/textos.ts</files>
  <behavior>
    `tests/unit/relatorios-queimas.test.ts` é escrito ANTES de `lib/queimas/relatorios.ts` e roda
    vermelho antes de rodar verde. Casos, todos com "hoje" passado por argumento:
    - `inicioDaSemana("2026-08-10")` (uma segunda) devolve `"2026-08-10"`; `inicioDaSemana("2026-08-16")`
      (o domingo seguinte) devolve `"2026-08-10"` — a semana começa na segunda e o domingo fecha a
      semana anterior
    - Uma queima com `ocorrida_em` = `2026-08-10T02:30:00Z`, que é 23:30 de domingo 9 de agosto em
      Brasília, cai no balde da semana iniciada em `2026-08-03`, não na iniciada em `2026-08-10`
    - Uma queima com `ocorrida_em` exatamente no instante de início de um balde entra **naquele**
      balde; uma no instante de fim entra no **seguinte** — o intervalo é `inicio <= x < fim`
    - `agregarPorSemana` devolve sempre 8 baldes, os oito a partir do início da semana de
      `hoje − 49 dias`, com zero nos que não têm queima
    - `agregarPorMes` devolve sempre 6 baldes de meses civis: o mês de `hoje` mais os cinco
      anteriores, atravessando virada de ano corretamente (dezembro → janeiro)
    - A soma de todos os baldes de `agregarPorSemana` sobre um conjunto inteiramente dentro da
      janela é igual ao número de queimas do conjunto — a prova de "os gráficos batem com a
      contagem manual"
    - Cada balde traz a contagem separada por tipo, sempre na ordem biscoito, esmalte, ouro, e a
      soma dos três é o total do balde
    - `estatisticasDeQueimas` devolve total geral, total dos últimos 30 dias civis e a contagem de
      biscoito e esmalte (os dois primeiros tipos)
    - Entrada vazia devolve os 8 (ou 6) baldes zerados e estatísticas em zero, nunca lista vazia
    - Chamar qualquer das funções duas vezes com a mesma entrada devolve o mesmo resultado
  </behavior>
  <action>
    **Instalação.** Instale `recharts` na versão exata aprovada na Tarefa 1, fixada sem intervalo em
    `package.json` (mesmo formato de `next-auth`, `sonner`, `lucide-react`). Leia o diff de
    `package-lock.json` e registre no SUMMARY quais dependências transitivas entraram.

    **Módulo puro.** Escreva primeiro `tests/unit/relatorios-queimas.test.ts` com os casos do bloco
    acima e confirme que ele falha. Só então crie `lib/queimas/relatorios.ts`, **sem nenhum import**
    (nem React, nem cliente de banco, nem `date-fns`, nem `lib/queimas/formato.ts` — o dia civil
    chega já convertido pelo chamador), com:
    `inicioDaSemana(diaCivil: string): string` — o dia civil da segunda-feira daquela semana,
    calculado a partir de uma contagem de dias desde a época, calibrada contra uma segunda-feira
    conhecida, **nunca** por `new Date(...).getDay()`, que é sensível ao fuso do processo;
    `janelaDeOitoSemanas(hoje: string)` e `janelaDeSeisMeses(hoje: string)`, devolvendo os limites de
    cada balde como pares de dias civis;
    `agregarPorSemana(queimas, hoje)` e `agregarPorMes(queimas, hoje)`, recebendo cada queima já
    reduzida a `{ diaCivil: string; tipo: "biscoito" | "esmalte" | "ouro" }` e devolvendo sempre a
    quantidade fixa de baldes, do mais antigo ao mais recente, cada um com as três contagens na
    ordem fixa;
    `agregarPorForno(queimas)` para as barras horizontais;
    `estatisticasDeQueimas(queimas, hoje)` para as quatro estatísticas.
    Uma queima entra num balde quando `inicio <= diaCivil < fim`. "Hoje" nunca é lido de dentro.

    **Consulta.** Em `lib/queimas/consultas.ts`, acrescente `carregarQueimasParaRelatorio()`, que
    carrega **uma vez** as queimas da janela mais longa das duas (6 meses civis cobrem mais dias que
    8 semanas) com `ocorridaEm`, `tipo`, `fornoId` e o nome do forno, ordenadas por `ocorridaEm`. As
    duas agregações e as quatro estatísticas saem do mesmo conjunto — alternar Semana/Mês na tela não
    dispara consulta nova nem pode mudar os números.

    **Textos.** Acrescente `ROTULO_SEMANA`, `ROTULO_MES`, `ROTULO_RELATORIOS`, `ROTULO_FORNOS`,
    `ROTULO_ESTATISTICA_TOTAL`, `ROTULO_ESTATISTICA_30_DIAS`, `FRASE_RELATORIOS_VAZIO_TITULO`
    ("Nenhuma queima registrada ainda."), `FRASE_RELATORIOS_VAZIO_CORPO` ("Registre a primeira queima
    para ver os relatórios aqui.") e `ROTULO_VER_FORNOS` ("Ver fornos").
  </action>
  <verify>
    <automated>npm test -- relatorios-queimas</automated>
    <automated>npm run verificar</automated>
    <automated>node -e "const p=require('./package.json'); const v=p.dependencies.recharts; if(!v){console.error('recharts ausente');process.exit(1)} if(/[\^~*]|x/.test(v)){console.error('versao nao fixa: '+v);process.exit(1)} console.log('recharts fixado em '+v)"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/queimas/relatorios.ts` não tem nenhuma linha de `import`
    - `lib/queimas/relatorios.ts` não constrói nem consulta nenhum objeto de data do runtime — a
      aritmética é toda sobre strings `YYYY-MM-DD` e contagens de dias
    - O teste prova que `2026-08-10T02:30:00Z` (23:30 de domingo em Brasília) cai na semana anterior,
      não na que começa na segunda seguinte
    - O teste prova o balde semiaberto: instante de início dentro, instante de fim fora
    - `agregarPorSemana` devolve exatamente 8 baldes e `agregarPorMes` exatamente 6, inclusive com
      entrada vazia
    - `agregarPorMes` atravessa virada de ano corretamente
    - A soma dos baldes é igual à contagem de queimas do conjunto de entrada — provado no teste
    - `recharts` está em `dependencies` com versão exata, sem intervalo (comando automatizado acima)
    - `npm test -- relatorios-queimas` e `npm run verificar` passam
  </acceptance_criteria>
  <done>
    A agregação de 8 semanas e 6 meses existe como módulo puro testado, com a semana começando na
    segunda em Brasília, e a soma dos baldes bate com a contagem manual.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: A tela de relatórios, os gráficos e o seletor de topo</name>
  <read_first>
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"Design System" (a nota de que os
      tokens `--color-chart-1..5` ainda não existem e o planejador deve acrescentá-los, reusando os
      três tokens de tipo de queima), §"Color" (a nota de que `--color-ouro` tem o mesmo hex de
      `--color-forno-atencao` e a regra que os desambigua), §"Copywriting Contract" (seletor de topo,
      alternador, vazio dos relatórios) e §"UI Considerations" E9
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §6 — a regra dura da rolagem horizontal no celular, que
      D-07 acomoda contendo a exceção dentro do gráfico
    - `components/amassa/encomendas/gantt.tsx` — o único lugar do repositório que já contém rolagem
      horizontal dentro do elemento; o padrão de CSS de overflow contido vem daqui
    - `app/(app)/encomendas/imprimir/page.tsx` — o padrão de rota própria com Server Component
      buscando só o que a sua tela precisa
    - `components/amassa/estado-vazio.tsx` — `hrefBotao`, que é o que D-08 precisa para "Ver fornos"
    - `app/globals.css` linhas 44-51 — os tokens de tipo e de nível já declarados
    - `lib/queimas/relatorios.ts` e `lib/queimas/consultas.ts` — o que a Tarefa 2 criou
    - `app/(app)/queimas/page.tsx` e `app/(app)/queimas/[id]/page.tsx` — onde o seletor de topo entra
    - `playwright.config.ts` — a cadeia de projetos e as etiquetas
  </read_first>
  <files>app/globals.css, app/(app)/queimas/relatorios/page.tsx, app/(app)/queimas/relatorios/loading.tsx, app/(app)/queimas/relatorios/error.tsx, components/amassa/queimas/relatorios-recharts.tsx, components/amassa/queimas/estatisticas-queimas.tsx, components/amassa/queimas/seletor-queimas.tsx, app/(app)/queimas/page.tsx, app/(app)/queimas/[id]/page.tsx, tests/e2e/queimas-relatorios.spec.ts</files>
  <action>
    **Tokens.** Em `app/globals.css`, acrescente `--color-chart-1`, `-2` e `-3` apontando para
    `--color-biscoito`, `--color-esmalte` e `--color-ouro` respectivamente, mais `-4` e `-5` livres
    para séries futuras. Os três tokens de tipo permanecem intocados — são marcados **NÃO ALTERAR**.

    **Seletor de topo.** Crie `components/amassa/queimas/seletor-queimas.tsx`: dois itens, "Fornos"
    e "Relatórios", que **parecem aba mas navegam** — são `Link` de verdade para `/queimas` e
    `/queimas/relatorios` (D-01: cada tela tem endereço próprio e o botão voltar do celular
    funciona). O item ativo é derivado da rota atual, marcado com `aria-current="page"` e com o
    acento do projeto, e distinguível também sem cor. Alvos de no mínimo 44px. Monte o seletor em
    `app/(app)/queimas/page.tsx`, em `app/(app)/queimas/[id]/page.tsx` e na nova rota de relatórios,
    logo abaixo do `CabecalhoPagina`. O item "Relatórios" **permanece visível e alcançável mesmo sem
    nenhuma queima registrada** (D-08 — controle que aparece e some é mais difícil de aprender que
    um sempre presente).

    **Rota.** Crie `app/(app)/queimas/relatorios/page.tsx`: `exigirUsuario()` na primeira instrução,
    `hoje = hojeEmBrasilia(new Date())` calculado no servidor, `carregarQueimasParaRelatorio()`, e as
    agregações rodadas no servidor. Zero queimas → `EstadoVazio` com "Nenhuma queima registrada
    ainda." / "Registre a primeira queima para ver os relatórios aqui." e `hrefBotao="/queimas"` com
    o rótulo "Ver fornos" (D-08). Uma ou mais → as quatro estatísticas primeiro, depois os gráficos —
    nessa ordem, porque no celular é a ordem que D-07 exige e no desktop ela não atrapalha.
    `loading.tsx` com esqueleto na forma do conteúdo (4 blocos de estatística mais retângulos na
    altura dos gráficos) e `error.tsx` com `EstadoErro` e a copy padrão.

    **Estatísticas.** `components/amassa/queimas/estatisticas-queimas.tsx`: quatro blocos — total,
    últimos 30 dias, contagem de biscoito e contagem de esmalte — rótulo em `text-apoio` e número em
    `text-mono`. Empilhados e legíveis no celular, em linha no desktop. Renderizam **sempre**, com
    zero quando aplicável; nunca meio-preenchidos.

    **Gráficos.** `components/amassa/queimas/relatorios-recharts.tsx`, Client Component, recebendo as
    duas agregações já prontas como props — **o componente não agrega nada**. Um alternador
    "Semana" / "Mês" troca qual das duas é exibida, sem disparar consulta nova. Barras empilhadas por
    tipo usando os tokens de gráfico na ordem fixa biscoito, esmalte, ouro, com legenda; e barras
    horizontais por forno. Baldes sem queima aparecem como zero — os 8 (ou 6) são sempre plotados,
    para o eixo ficar estável e o buraco de produção ficar visível. Nome de forno no eixo das barras
    horizontais trunca com reticências e mostra o nome completo no tooltip. **Nenhum selo de nível de
    forno entra em gráfico algum** — é essa separação que mantém `--color-ouro` e
    `--color-forno-atencao` inequívocos, apesar do mesmo hex (§3, nota de conflito 2).

    **Rolagem.** Cada gráfico fica dentro de um contêiner com rolagem horizontal **própria**, no
    molde do que `components/amassa/encomendas/gantt.tsx` já faz: a exceção à regra dura de
    `04-DESIGN-SYSTEM.md` §6 fica contida no elemento, e a página em si nunca rola lateralmente em
    nenhuma largura. Os dois tamanhos de tela mostram o mesmo recorte de dados — 8 semanas e 6 meses,
    nada reduzido no celular (D-07).

    **Teste ponta a ponta.** Crie `tests/e2e/queimas-relatorios.spec.ts`, sem etiqueta de vazio,
    cobrindo: (1) o seletor de topo navegando entre `/queimas` e `/queimas/relatorios`, com o botão
    voltar funcionando; (2) as quatro estatísticas presentes; (3) registrar um número conhecido de
    queimas de tipos conhecidos e afirmar que a soma exibida bate com a contagem — a prova de FOR-12;
    (4) o alternador trocando entre Semana e Mês sem mudar o total; (5) no viewport de celular,
    afirmar que o documento não rola horizontalmente enquanto um contêiner de gráfico rola. Uma
    única invocação de `npm run test:e2e` nesta tarefa, com `--grep`.
  </action>
  <verify>
    <automated>npm run verificar</automated>
    <automated>npm run test:e2e -- --grep "relatórios"</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` declara `--color-chart-1` a `--color-chart-5`, com os três primeiros
      apontando para os tokens de tipo de queima, e os três tokens de tipo continuam com os mesmos
      valores hexadecimais de antes
    - O seletor de topo usa `Link` (navegação real, D-01), marca o item ativo com `aria-current` e
      mantém "Relatórios" visível mesmo sem queima registrada
    - `app/(app)/queimas/relatorios/page.tsx` tem `exigirUsuario()` na primeira instrução e passa por
      `npm run verificar-acoes`
    - Zero queimas renderiza `EstadoVazio` com o botão "Ver fornos" apontando para `/queimas`, e
      nenhum eixo de gráfico é desenhado
    - `relatorios-recharts.tsx` recebe os baldes prontos como props e não chama nenhuma função de
      agregação
    - As oito (ou seis) colunas aparecem mesmo quando algumas têm valor zero
    - O teste ponta a ponta afirma que a soma exibida bate com o número de queimas que o próprio
      teste registrou
    - O teste afirma, no viewport de celular, que a largura de rolagem do documento não excede a
      largura visível, enquanto um contêiner de gráfico tem rolagem própria
    - Nenhum componente de gráfico usa os tokens de nível de forno
    - `npm run test:e2e` foi invocado no máximo uma vez nesta tarefa, com `--grep`
    - `npm run verificar` passa inteiro
  </acceptance_criteria>
  <done>
    `/queimas/relatorios` mostra as quatro estatísticas e os gráficos batendo com a contagem manual,
    alternando entre 8 semanas e 6 meses, com a semana começando na segunda, sem a página rolar
    lateralmente no celular — e um estado vazio no lugar dos gráficos quando não há queima nenhuma.
  </done>
</task>

</tasks>

## Artifacts this phase produces

**Produzidos por este plano**

- `recharts` em `package.json`/`package-lock.json` (versão fixa, aprovada no portão de legitimidade)
- `app/globals.css` += `--color-chart-1` … `--color-chart-5`
- `lib/queimas/relatorios.ts`: `inicioDaSemana`, `janelaDeOitoSemanas`, `janelaDeSeisMeses`,
  `agregarPorSemana`, `agregarPorMes`, `agregarPorForno`, `estatisticasDeQueimas`
- `lib/queimas/consultas.ts` += `carregarQueimasParaRelatorio`
- `lib/queimas/textos.ts` += `ROTULO_SEMANA`, `ROTULO_MES`, `ROTULO_RELATORIOS`, `ROTULO_FORNOS`,
  `ROTULO_ESTATISTICA_TOTAL`, `ROTULO_ESTATISTICA_30_DIAS`, `FRASE_RELATORIOS_VAZIO_TITULO`,
  `FRASE_RELATORIOS_VAZIO_CORPO`, `ROTULO_VER_FORNOS`
- Rota `/queimas/relatorios`: `page.tsx`, `loading.tsx`, `error.tsx`
- `components/amassa/queimas/relatorios-recharts.tsx`,
  `components/amassa/queimas/estatisticas-queimas.tsx`,
  `components/amassa/queimas/seletor-queimas.tsx`
- Testes: `tests/unit/relatorios-queimas.test.ts`, `tests/e2e/queimas-relatorios.spec.ts`

**Produzidos pelos planos irmãos da Fase 4** (contexto, não escopo deste plano)

`04-01`: tipo `tipo_queima`; tabelas `fornos`, `queimas`, `manutencoes`; migrações `0007_queimas.sql`
e `0008_gatilhos-queimas.sql`; gatilhos `tocar_atualizado_em_*`; `TABELAS_ESPERADAS` += três
entradas; `lib/queimas/contador.ts`; `esquemaId`, `esquemaForno`, `esquemaQueima`; `criarForno`,
`registrarQueima`, `excluirQueima`; rota `/queimas`; `registrar-queima.tsx`, `formulario-forno.tsx`. ·
`04-02`: `medidor.tsx`, `lista-fornos.tsx`, `cartao-forno.tsx`, `lib/queimas/formato.ts`,
`textoDoNivel`, `fraseDoRodape`, `app/(app)/queimas/loading.tsx`, `app/(app)/queimas/error.tsx`. ·
`04-03`: rota `/queimas/[id]` (+ `loading`/`error`), `buscarForno`, `historico-queimas.tsx`,
`historico-manutencoes.tsx`, `confirmar-excluir-queima.tsx`. ·
`04-04`: `registrarManutencao`, `atualizarForno`, `desativarForno`, `reativarForno`,
`esquemaManutencao`, `esquemaAtualizacaoDeForno`, `registrar-manutencao.tsx`, `acoes-forno.tsx`. ·
`04-05`: `banner-atencao.tsx`, `filtro-fornos.tsx`, `lib/queimas/filtros.ts`,
`fornosQuePrecisamDeAtencao`, cartão "Fornos em atenção" em `app/(app)/page.tsx`. ·
`04-07`: migração em produção, varredura completa e verificação humana.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → repositório | Uma dependência nova entra na cadeia de suprimentos de uma aplicação em produção. |
| sessão → `/queimas/relatorios` | Rota nova, protegida pelo mesmo `exigirUsuario()`. |
| dados agregados → componente de cliente | Os baldes atravessam para o navegador; nenhuma regra de agregação vai junto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | instalação de `recharts` (npm) | high | mitigate | Ponto de parada humano **bloqueante** na Tarefa 1, antes de qualquer `npm install`, tratando o pacote como `[ASSUMED]` por não haver `## Package Legitimacy Audit` nesta fase; versão fixada sem intervalo; diff de `package-lock.json` lido e registrado no SUMMARY. Não auto-aprovável em nenhuma configuração de avanço automático |
| T-04-01 | Spoofing | `app/(app)/queimas/relatorios/page.tsx` | high | mitigate | `exigirUsuario()` na primeira instrução; provado por `npm run verificar-acoes`; `middleware.ts` já protege a rota nova por nascimento |
| T-04-22 | Information Disclosure | volume de produção do ateliê exposto num gráfico | low | accept | O sistema é interno, para 3 a 5 gestores com acesso total; o volume de queimas é exatamente o que o relatório existe para mostrar. Mesma avaliação que levou `/api/health/backup` a **não** expor bytes — lá o consumidor era a internet, aqui é o gestor logado |
| T-04-23 | Denial of Service | `carregarQueimasParaRelatorio` sem janela | medium | mitigate | A consulta é limitada à janela mais longa (6 meses civis) e usa o índice `queimas_data_idx`; não carrega o histórico inteiro do ateliê |
| T-04-24 | Tampering | agregação feita no cliente | low | mitigate | As agregações rodam no servidor, no módulo puro testado; o componente de gráfico só desenha o que recebe, então um cliente adulterado não muda o número que outro gestor vê |
| T-04-06 | Information Disclosure | erro de carga da rota | low | mitigate | `error.tsx` com copy fixa em português; erro real só em `console.error` no servidor |
</threat_model>

<verification>
1. O portão de legitimidade da Tarefa 1 foi respondido por uma pessoa, com uma versão aprovada
2. `npm run verificar` passa inteiro
3. `npm test -- relatorios-queimas` passa, incluindo a prova de fuso de domingo 23:30
4. `npm run test:e2e -- --grep "relatórios"` passa nos dois projetos — uma única invocação, na
   Tarefa 3
5. `recharts` está fixado em versão exata e o diff de `package-lock.json` está registrado no SUMMARY
6. Nenhum `npm run build` como passo separado
</verification>

<success_criteria>
- Os gráficos batem com a contagem manual do histórico
- O alternador troca entre 8 semanas e 6 meses e a semana começa na segunda
- As quatro estatísticas aparecem no topo e são lidas antes dos gráficos no celular
- A página nunca rola horizontalmente; a rolagem lateral fica contida dentro de cada gráfico
- Sem queima registrada, os gráficos dão lugar a um estado vazio com "Ver fornos", e o item
  "Relatórios" continua visível no seletor
- `npm run verificar` passa
</success_criteria>

<output>
Create `.planning/phases/04-contador-de-queima/04-06-SUMMARY.md` when done.
Registre no SUMMARY a versão de `recharts` aprovada, as dependências transitivas que entraram, e
quais comandos de teste ponta a ponta foram executados de fato e quantas vezes.
</output>
