---
phase: 04-contador-de-queima
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/amassa/queimas/medidor.tsx
  - components/amassa/queimas/cartao-forno.tsx
  - components/amassa/queimas/lista-fornos.tsx
  - lib/queimas/formato.ts
  - lib/queimas/textos.ts
  - lib/queimas/consultas.ts
  - app/(app)/queimas/page.tsx
  - app/(app)/queimas/loading.tsx
  - app/(app)/queimas/error.tsx
  - tests/unit/formato-queima.test.ts
  - tests/unit/textos-queima.test.ts
  - tests/e2e/queimas-cartao.spec.ts
autonomous: true
requirements: [FOR-04, FOR-05, FOR-08]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward: observable truths ---
    - "Um forno com contador 90 de limite 100 mostra o cartão em atenção e o selo textual 'Manutenção próxima' (FOR-04)"
    - "Um forno com contador 100 de limite 100 mostra o cartão em crítico, o selo 'Manutenção vencida' e um ícone de alerta ao lado do texto (FOR-04)"
    - "Um forno em nível ok não mostra selo nenhum (FOR-04)"
    - "O medidor tem entalhes a cada 10 queimas, uma marca vertical no limiar de atenção e os rótulos '0', 'atenção N' e 'limite N' sob a barra — nunca uma barra lisa (FOR-05)"
    - "O rodapé do cartão lê 'Última manutenção em {data} · {responsável}' ou 'Sem manutenção registrada', seguido de '· {total} no total' (FOR-08)"
    - "O cartão mostra as duas contagens ao mesmo tempo: o contador desde a última manutenção e o total que o forno já fez na vida (FOR-08)"
    - "A tela de fornos tem estado de carregamento com esqueleto na forma do conteúdo e estado de erro em linguagem humana (D-01)"
    - "O botão 'Queimar' continua sendo o único botão do cartão depois de todo o conteúdo novo (D-03)"

    # --- Edge probe: FOR-04 (boundary, precision, idempotency, concurrency) ---
    - "Com limite 100: contador 89 → nível ok e nenhum selo; 90 → atenção com 'Manutenção próxima'; 99 → atenção; 100 → crítico com 'Manutenção vencida'; 101 → crítico. Provado em tests/unit/contador.test.ts e afirmado na tela pelo teste ponta a ponta"
    - "O limiar é `Math.max(1, limite - 10)`: com limite 10 o limiar de atenção é 1, não 0 — o piso é preservado mesmo parecendo redundante"
    - "`medirForno` chamada duas vezes com a mesma entrada devolve o mesmo nível — a classificação não depende do relógio nem de estado externo"
    - statement: "O nível mostrado no cartão é sempre derivado do contador lido na mesma consulta, nunca de dois números lidos em momentos diferentes"
      verification: backstop

    # --- Edge probe: FOR-05 (boundary, precision) ---
    - "Os entalhes caem a cada 10 queimas de 0 até `limite`; com limite 100 são 11 marcas contando as extremidades, e a marca vertical do limiar fica exatamente na fração `atencao/limite` da largura da barra"
    - "Contador acima do limite (ex.: 103 de 100) preenche a barra em 100% sem estourar o contêiner do cartão, e o rótulo continua lendo '103 / 100' em números tabulares"

    # --- Edge probe: FOR-08 (boundary, precision) ---
    - "Forno sem nenhuma manutenção: contador igual ao total, e o rodapé lê 'Sem manutenção registrada · {total} no total'"
    - "Uma queima com `ocorrida_em` exatamente igual ao `ocorrida_em` da última manutenção NÃO entra no contador (o corte é estritamente maior) e continua contando no total"

    # --- UI-SPEC lift: E1 (loading, error, partial, overflow), E2 (todas) ---
    - "E1 loading: esqueleto na forma do conteúdo — grade de esqueletos com a altura do cartão real (medidor + selo + rodapé), nunca um 'carregando…' solto"
    - "E1 error: `EstadoErro` com 'Algo não funcionou.' e 'Não deu para carregar os fornos. Verifique a internet e tente de novo.'"
    - "E1 partial: forno com queimas mas sem manutenção registrada conta sobre todas as queimas e o rodapé lê 'Sem manutenção registrada · {total} no total'"
    - "E1 overflow: os cartões empilham verticalmente e a página nunca rola horizontalmente"
    - "E2 loading: o esqueleto do cartão tem a mesma altura do cartão real, para o layout não pular quando os dados chegam"
    - "E2 error: falha ao registrar queima a partir do cartão reverte o contador ao valor anterior e o toast vira erro"
    - "E2 overflow: o medidor ocupa a largura do cartão e escala com o `limite`; o rodapé quebra em duas linhas no celular em vez de estourar o cartão"
    - "E2 long-text: nome (≤80 caracteres) e responsável no rodapé quebram por palavra; nenhum dos dois trunca — o cartão cresce"

  prohibitions:
    - statement: "MUST NOT usar linguagem de culpa, vergonha ou cobrança pessoal na copy de forno em atenção ou crítico — o selo diz o fato e o que fazer ('Manutenção próxima' / 'Manutenção vencida'), nunca quem deixou o forno passar do limite"
      category: values

  artifacts:
    - path: "components/amassa/queimas/medidor.tsx"
      provides: "barra com entalhes a cada 10 queimas, marca no limiar de atenção e rótulos 0 / atenção N / limite N"
      min_lines: 40
    - path: "lib/queimas/formato.ts"
      provides: "hojeEmBrasilia e a formatação de instantes em data curta de Brasília, sem date-fns"
      exports: ["hojeEmBrasilia", "formatarInstanteCurto"]
    - path: "app/(app)/queimas/loading.tsx"
      provides: "esqueleto na forma da grade de cartões"
    - path: "app/(app)/queimas/error.tsx"
      provides: "EstadoErro da tela de fornos"
    - path: "components/amassa/queimas/lista-fornos.tsx"
      provides: "grade no desktop e cartões empilhados no celular"

  key_links:
    - from: "components/amassa/queimas/cartao-forno.tsx"
      to: "components/amassa/queimas/medidor.tsx"
      via: "o cartão passa contador, limite e limiar de atenção para o medidor"
      pattern: "<Medidor"
    - from: "components/amassa/queimas/cartao-forno.tsx"
      to: "lib/queimas/textos.ts"
      via: "textoDoNivel() traduz NivelDeForno no selo textual"
      pattern: "textoDoNivel\\("
    - from: "components/amassa/queimas/cartao-forno.tsx"
      to: "lib/queimas/formato.ts"
      via: "formatarInstanteCurto() monta a data do rodapé"
      pattern: "formatarInstanteCurto\\("
---

<objective>
O cartão do forno deixa de ser um número solto e vira o instrumento que o módulo existe para
oferecer: medidor com entalhes, os três níveis com selo textual, e o rodapé que responde as duas
perguntas diferentes — "quantas desde a última troca de resistência" e "quantas na vida".

Purpose: FOR-04, FOR-05 e FOR-08 são o que torna o contador legível de relance, no ateliê, em pé.
O medidor com entalhes é explicitamente marcado "não simplifique para uma barra lisa" em
`amassa-plataforma/04-DESIGN-SYSTEM.md` §8 — os entalhes são o que deixa o ritmo de desgaste
visível. O plano também fecha os estados de carregamento e erro da tela, que o `CLAUDE.md` exige em
toda tela e a Tarefa 2 do plano 04-01 deixou por fazer.

Output: `medidor.tsx` (primitivo visual novo, sem análogo no repositório), o cartão completo,
`lib/queimas/formato.ts`, `textoDoNivel`, a lista responsiva e os dois arquivos de estado da rota.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/04-contador-de-queima/04-CONTEXT.md
@.planning/phases/04-contador-de-queima/04-PATTERNS.md
@.planning/phases/04-contador-de-queima/04-UI-SPEC.md
@.planning/phases/04-contador-de-queima/04-01-SUMMARY.md
@amassa-plataforma/04-DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: O medidor com entalhes, a marca do limiar e os rótulos</name>
  <read_first>
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §8, bloco **Fornos** — a descrição literal do medidor,
      dos entalhes a cada 10 queimas, da marca vertical no limiar e dos rótulos `0 / atenção N / limite N`
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §2 e §3 — os três tokens de nível de forno
      (**NÃO ALTERAR**) e a regra de que cor é informação
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` §3, tabela dos três níveis — a cor da barra por nível
    - `app/globals.css` linhas 44-51 — `--color-forno-ok`, `--color-forno-atencao`,
      `--color-forno-critico` e seus pares `-fundo`/`-texto`, já declarados desde a Fase 2b
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"Typography" (`text-micro`, `text-mono`)
      e §"Spacing Scale" (a nota de que o espaçamento dos entalhes deriva de `limite/10`, não da
      escala de 8 pontos)
    - `lib/queimas/contador.ts` — `NivelDeForno`, `MedidaDoForno`, `limiarDeAtencao`
    - `components/amassa/encomendas/trilha-segmentos.tsx` — o componente de barra segmentada mais
      próximo que existe no repositório, para convenções de classe e acessibilidade (não é análogo
      funcional: o medidor é primitivo novo, `04-PATTERNS.md` §"No Analog Found")
  </read_first>
  <files>components/amassa/queimas/medidor.tsx, lib/queimas/textos.ts, tests/unit/textos-queima.test.ts</files>
  <action>
    Crie `components/amassa/queimas/medidor.tsx` como Server Component puro de apresentação,
    recebendo `{ contador, limite, atencao, nivel }` (o `MedidaDoForno` de `lib/queimas/contador.ts`)
    e nada mais — nenhuma regra de negócio dentro do componente.

    Estrutura, na ordem: (1) a linha do contador `{contador} / {limite}` em `text-mono` (13px,
    figuras tabulares, para os dígitos não dançarem quando o número muda) alinhada com o selo
    textual à direita; (2) a barra; (3) a faixa de rótulos.

    A barra é um trilho de altura fixa com o preenchimento em `Math.min(contador / limite, 1) * 100%`
    — contador acima do limite (103 de 100) preenche 100% e não estoura o contêiner. A cor do
    preenchimento vem do nível: `--color-forno-ok`, `--color-forno-atencao` ou
    `--color-forno-critico`, **nunca** `--color-acento` (o terracota é reservado para botões, um por
    tela). Os entalhes são marcas verticais claras sobre o trilho, uma a cada 10 queimas, calculadas
    de 0 até `limite` — com limite 100 são 11 posições contando as duas extremidades; com limite 10,
    duas. A marca do limiar é uma marca vertical mais forte, visualmente distinta dos entalhes, na
    fração `atencao / limite` da largura. Sob a barra, três rótulos em `text-micro`: `0` à esquerda,
    `atenção {atencao}` posicionado na mesma fração da marca do limiar, e `limite {limite}` à
    direita.

    Acessibilidade: a barra recebe `role="progressbar"` com `aria-valuemin={0}`,
    `aria-valuemax={limite}`, `aria-valuenow={contador}` e um `aria-label` em português nomeando o
    forno e o estado (ex.: `Contador do forno: {contador} de {limite}`). Os entalhes e a marca do
    limiar são decorativos e levam `aria-hidden="true"`.

    Em `lib/queimas/textos.ts`, acrescente `textoDoNivel(nivel: NivelDeForno): string | null` — `null`
    para `"ok"`, `"Manutenção próxima"` para `"atencao"`, `"Manutenção vencida"` para `"critico"` —
    com `switch` exaustivo e guarda `const _exaustivo: never` no `default`, no molde de
    `textoDaSituacao` de `lib/encomendas/textos.ts`, para um quarto nível futuro não passar em
    silêncio. Acrescente também `ROTULO_MEDIDOR_ATENCAO` ("atenção") e `ROTULO_MEDIDOR_LIMITE`
    ("limite"). A copy do selo declara o fato e o que fazer; nunca atribui culpa a quem deixou o
    forno chegar lá.

    Escreva `tests/unit/textos-queima.test.ts` cobrindo os três níveis de `textoDoNivel` e
    `rotuloDoTipo` para os três tipos, no molde de `tests/unit/textos-encomenda.test.ts`.
  </action>
  <verify>
    <automated>npm test -- textos-queima</automated>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `components/amassa/queimas/medidor.tsx` renderiza os três rótulos e a barra tem
      `role="progressbar"` com `aria-valuenow`/`aria-valuemax`
    - O número de entalhes renderizados é `Math.floor(limite / 10) + 1` — 11 para limite 100, 2 para
      limite 10
    - A marca do limiar é posicionada em `atencao / limite` e não em um valor fixo
    - Com contador 103 e limite 100, o preenchimento é 100% e o rótulo do contador lê `103 / 100`
    - `medidor.tsx` não importa nada de `@/db` nem de `@/lib/queimas/acoes` — é apresentação pura
    - `medidor.tsx` não usa a variável de cor de acento em nenhuma classe; usa apenas os três tokens
      de nível de forno
    - `textoDoNivel("ok")` devolve `null`; os outros dois devolvem exatamente
      "Manutenção próxima" e "Manutenção vencida"
    - `textoDoNivel` tem um `default` com atribuição a `never` — um quarto valor no union quebra a
      compilação em vez de cair em silêncio
    - `npm test -- textos-queima` passa
  </acceptance_criteria>
  <done>
    O medidor existe como componente, com entalhes a cada 10 queimas, marca no limiar de atenção e
    os três rótulos sob a barra, e `textoDoNivel` traduz os três níveis no selo textual.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: O cartão completo — selo, medidor e o rodapé com as duas contagens</name>
  <read_first>
    - `components/amassa/encomendas/cartao-encomenda.tsx` (arquivo inteiro, 63 linhas) — o padrão de
      selo, `[overflow-wrap:anywhere]` no nome, estilo condicional por estado
    - `lib/encomendas/formato.ts` linhas 1-48 — `hojeEmBrasilia` verbatim e a formatação de dia civil
      por `Date.UTC` + `timeZone: "UTC"`, nunca o fuso do runtime; e o cabeçalho que explica por que
      `date-fns` foi dispensado (PD-04)
    - `lib/queimas/consultas.ts` — `FornoMedido` e `listarFornosDoIndice` do plano 04-01
    - `lib/queimas/contador.ts` — `medirForno`
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §8 — a linha literal do rodapé do cartão
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §6 — a adaptação de Fornos para celular (cartões
      empilhados, botão "Queimar" em largura total)
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"Copywriting Contract" (linha "Cartão
      rodapé" e "Selo textual") e §"UI Considerations" E2
    - `components/amassa/queimas/cartao-forno.tsx` — o cartão mínimo do plano 04-01
    - `components/amassa/encomendas/lista-encomendas.tsx` — a divisão grade-desktop / empilhado-celular
  </read_first>
  <files>lib/queimas/formato.ts, tests/unit/formato-queima.test.ts, lib/queimas/textos.ts, lib/queimas/consultas.ts, components/amassa/queimas/cartao-forno.tsx, components/amassa/queimas/lista-fornos.tsx, app/(app)/queimas/page.tsx</files>
  <action>
    **Formato.** Crie `lib/queimas/formato.ts` sem nenhum import, copiando `hojeEmBrasilia(agora: Date)`
    verbatim de `lib/encomendas/formato.ts` (`Intl.DateTimeFormat("en-CA", { timeZone:
    "America/Sao_Paulo", ... })`) — é a função que `04-CONTEXT.md` §Specifics aponta como a solução
    já paga para a armadilha de fuso. Acrescente `diaCivilEmBrasilia(instanteIso: string): string`,
    que converte um `timestamptz` no dia civil `YYYY-MM-DD` de Brasília pelo mesmo `Intl`, e
    `formatarInstanteCurto(instanteIso: string): string`, que devolve a data curta em português no
    formato "12 ago 2026" reaproveitando a abordagem de mês abreviado sem ponto de
    `lib/encomendas/formato.ts`. **Sem `date-fns`** — a dependência foi dispensada de propósito no
    projeto inteiro. Escreva `tests/unit/formato-queima.test.ts` cobrindo: um instante em
    `2026-08-10T02:30:00Z` (23:30 de 9 de agosto em Brasília) cai no dia civil `2026-08-09`, e a
    formatação curta de um instante conhecido.

    **Rodapé.** Em `lib/queimas/textos.ts`, acrescente `fraseDoRodape({ ultimaManutencaoEm,
    ultimaManutencaoResponsavel, total })`, que devolve exatamente
    `Última manutenção em {data} · {responsável}` quando existe manutenção com responsável,
    `Última manutenção em {data}` quando o responsável é nulo, e `Sem manutenção registrada` quando
    não existe manutenção — sempre seguido de ` · {total} no total`. As frases são literais de
    `04-DESIGN-SYSTEM.md` §8; não reescreva. Cubra as três formas em `tests/unit/textos-queima.test.ts`.

    **Consulta.** Confirme que `listarFornosDoIndice` devolve, além de `contador` e `total`,
    `ultimaManutencaoEm` (o `ocorrida_em` da última linha de `manutencoes`) e
    `ultimaManutencaoResponsavel` (o `responsavel` daquela mesma linha), trazendo os dois pelo mesmo
    `left join lateral` — não uma segunda consulta.

    **Cartão.** Reescreva `components/amassa/queimas/cartao-forno.tsx`: cabeçalho com o nome
    (`text-titulo`, `[overflow-wrap:anywhere]`, quebra por palavra, nunca truncado) e o selo textual
    à direita quando `textoDoNivel` não é nulo, com `text-apoio` e os pares de token
    `--color-forno-atencao-fundo`/`-texto` ou `--color-forno-critico-fundo`/`-texto`; o selo crítico
    leva um `AlertTriangle` do `lucide-react` com `aria-hidden="true"` ao lado do texto visível
    (§3 do design system: este vermelho significa urgência de verdade). Abaixo, `<Medidor />`.
    Abaixo, o rodapé em `text-apoio text-muted-foreground` com `fraseDoRodape(...)`, quebrando em
    duas linhas no celular em vez de estourar o cartão. Por último, o `RegistrarQueima` — que
    continua sendo **o único botão do cartão** (D-03), em largura total no celular. O cabeçalho do
    cartão (nome) é um `Link` para `/queimas/{id}`, com pelo menos 44px de alvo; o botão "Queimar"
    fica fora desse link, porque é controle e não navegação.

    **Lista.** Crie `components/amassa/queimas/lista-fornos.tsx` no molde de
    `lista-encomendas.tsx`: grade `md:grid-cols-2` no desktop, `grid-cols-1` com `gap-4` no celular.
    Um único forno ocupa a largura toda em vez de deixar meia-grade órfã no desktop. Ajuste
    `app/(app)/queimas/page.tsx` para renderizar `<ListaFornos>` no lugar do mapeamento direto de
    cartões e para calcular `hoje` no servidor antes de qualquer consulta, como em
    `app/(app)/encomendas/page.tsx`.
  </action>
  <verify>
    <automated>npm test -- formato-queima textos-queima</automated>
    <automated>npm run verificar</automated>
  </verify>
  <acceptance_criteria>
    - `lib/queimas/formato.ts` não tem nenhuma linha de `import`
    - `tests/unit/formato-queima.test.ts` prova que `2026-08-10T02:30:00Z` mapeia para o dia civil
      `2026-08-09` em Brasília — a armadilha de fuso que o `getDay()` do processo esconderia
    - Nenhum arquivo em `lib/queimas/` referencia `date-fns`
    - `fraseDoRodape` cobre as três formas (com responsável, sem responsável, sem manutenção) e
      sempre termina em `· {total} no total`
    - O cartão de um forno crítico renderiza o texto "Manutenção vencida" e um ícone marcado
      `aria-hidden="true"` ao lado dele
    - O cartão de um forno em nível ok não renderiza nenhum selo
    - O cartão continua tendo exatamente um `<button>` de ação — o "Queimar" (D-03); o nome é um
      `Link`, não um botão
    - `npm run verificar` passa inteiro
  </acceptance_criteria>
  <done>
    O cartão mostra medidor, selo por nível, contador desde a última manutenção e total na vida, com
    o rodapé literal do design system, e continua com um único botão.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: Estados de carregamento e erro da tela de fornos, provados na tela</name>
  <read_first>
    - `app/(app)/encomendas/loading.tsx` — o esqueleto na forma do conteúdo
    - `app/(app)/encomendas/error.tsx` — o `EstadoErro` de rota, com `"use client"` e `reset`
    - `components/amassa/estado-erro.tsx` — as props e o `hrefBotao`
    - `.planning/phases/04-contador-de-queima/04-UI-SPEC.md` §"Copywriting Contract" (linhas de
      estado de erro) e §"UI Considerations" E1 (loading, error, partial, overflow)
    - `tests/e2e/encomendas-indice.spec.ts` — as convenções de teste ponta a ponta do índice
    - `playwright.config.ts` — a cadeia de projetos e as etiquetas de vazio
    - `components/amassa/queimas/cartao-forno.tsx`, `components/amassa/queimas/lista-fornos.tsx`,
      `components/amassa/queimas/medidor.tsx` — os componentes das Tarefas 1 e 2
  </read_first>
  <files>app/(app)/queimas/loading.tsx, app/(app)/queimas/error.tsx, tests/e2e/queimas-cartao.spec.ts</files>
  <action>
    Crie `app/(app)/queimas/loading.tsx` no molde de `app/(app)/encomendas/loading.tsx`: o
    `CabecalhoPagina` real (o título não pisca) mais uma grade de `Skeleton` do shadcn na **mesma
    altura do cartão real** — bloco do cabeçalho do cartão, bloco do medidor, duas linhas do rodapé,
    bloco do botão. O layout não pode pular quando os dados chegam; um "carregando…" solto é defeito
    (convenção do projeto, `04-DESIGN-SYSTEM.md` §7).

    Crie `app/(app)/queimas/error.tsx` com `"use client"`, no molde de
    `app/(app)/encomendas/error.tsx`, usando `EstadoErro` com o título literal `"Algo não funcionou."`
    (reaproveitado do projeto inteiro) e o corpo `"Não deu para carregar os fornos. Verifique a
    internet e tente de novo."`, e o botão de tentar de novo ligado ao `reset` do Next.js.

    Crie `tests/e2e/queimas-cartao.spec.ts`, **sem etiqueta de vazio** (cria dado, logo roda em
    `desktop`/`celular` depois da cadeia), com os casos de fronteira de FOR-04 na tela: cadastre um
    forno com limite 10 e registre queimas até 8 (nível ok, sem selo), 9 (atenção, selo "Manutenção
    próxima" — o limiar é `Math.max(1, 10 - 10)` = 1, então use um limite 100 num segundo forno para
    exercitar 89/90/99/100 com menos toques, ou insira as queimas necessárias pela interface na
    menor contagem que ainda prove as fronteiras). Afirme na tela: nenhum selo em ok, o texto
    "Manutenção próxima" ao cruzar o limiar, e "Manutenção vencida" ao alcançar o limite. Afirme
    também que o rodapé lê "Sem manutenção registrada" e termina com o total, e que o medidor
    renderiza os rótulos "0", "atenção" e "limite". Uma única invocação de `npm run test:e2e` nesta
    tarefa, com `--grep` no que ela mexeu.

    Se o custo de toques para chegar a 90 pela interface for proibitivo, prepare o estado pela mesma
    Server Action chamada em laço a partir do teste (nunca por SQL direto no teste) e registre a
    escolha no SUMMARY — o que não pode acontecer é o teste afirmar a fronteira sem nunca a
    atravessar.
  </action>
  <verify>
    <automated>npm run verificar</automated>
    <automated>npm run test:e2e -- --grep "cartão do forno"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(app)/queimas/loading.tsx` importa `Skeleton` de `@/components/ui/skeleton` e renderiza
      pelo menos quatro blocos por cartão-esqueleto (cabeçalho, medidor, rodapé, botão)
    - `app/(app)/queimas/error.tsx` começa com a diretiva de componente de cliente e usa `EstadoErro`
      com os dois literais de copy do UI-SPEC
    - O teste ponta a ponta afirma, na tela renderizada: ausência de selo em nível ok, presença de
      "Manutenção próxima" no limiar e "Manutenção vencida" no limite
    - O teste afirma que o rodapé de um forno sem manutenção contém "Sem manutenção registrada" e a
      contagem total
    - O teste passa nos projetos `desktop` e `celular`
    - `npm run test:e2e` foi invocado no máximo uma vez nesta tarefa, com `--grep`
    - `npm run verificar` passa inteiro
  </acceptance_criteria>
  <done>
    A tela de fornos tem esqueleto na forma do conteúdo e erro em linguagem humana, e as três
    fronteiras de nível de FOR-04 são afirmadas na tela, não só no teste unitário.
  </done>
</task>

</tasks>

## Artifacts this phase produces

**Produzidos por este plano**

- `components/amassa/queimas/medidor.tsx` (primitivo visual novo, sem análogo no repositório)
- `components/amassa/queimas/lista-fornos.tsx`
- `components/amassa/queimas/cartao-forno.tsx` (forma final: selo, medidor, rodapé, botão único)
- `lib/queimas/formato.ts`: `hojeEmBrasilia`, `diaCivilEmBrasilia`, `formatarInstanteCurto`
- `lib/queimas/textos.ts` += `textoDoNivel`, `fraseDoRodape`, `ROTULO_MEDIDOR_ATENCAO`,
  `ROTULO_MEDIDOR_LIMITE`, `FRASE_ERRO_TITULO`, `FRASE_ERRO_CORPO`
- `lib/queimas/consultas.ts`: `listarFornosDoIndice` passa a trazer `ultimaManutencaoEm` e
  `ultimaManutencaoResponsavel`
- Rotas: `app/(app)/queimas/loading.tsx`, `app/(app)/queimas/error.tsx`
- Testes: `tests/unit/formato-queima.test.ts`, `tests/unit/textos-queima.test.ts`,
  `tests/e2e/queimas-cartao.spec.ts`

**Produzidos pelos planos irmãos da Fase 4** (contexto, não escopo deste plano)

`04-01`: tipo `tipo_queima`; tabelas `fornos`, `queimas`, `manutencoes`; migrações `0007_queimas.sql`
e `0008_gatilhos-queimas.sql`; gatilhos `tocar_atualizado_em_*`; `TABELAS_ESPERADAS` += três
entradas; `lib/queimas/contador.ts` (`NivelDeForno`, `MedidaDoForno`, `limiarDeAtencao`,
`medirForno`); `lib/queimas/esquemas.ts` (`esquemaId`, `esquemaForno`, `esquemaQueima`);
`lib/queimas/acoes.ts` (`criarForno`, `registrarQueima`, `excluirQueima`); rota `/queimas`;
`formulario-forno.tsx`, `registrar-queima.tsx`. ·
`04-03`: rota `/queimas/[id]` (+ `loading`/`error`), `buscarForno`, `historico-queimas.tsx`,
`historico-manutencoes.tsx`, `confirmar-excluir-queima.tsx`. ·
`04-04`: `registrarManutencao`, `atualizarForno`, `desativarForno`, `reativarForno`,
`esquemaManutencao`, `esquemaAtualizacaoDeForno`, `registrar-manutencao.tsx`, `acoes-forno.tsx`. ·
`04-05`: `banner-atencao.tsx`, `filtro-fornos.tsx`, `lib/queimas/filtros.ts`,
`fornosQuePrecisamDeAtencao`, cartão "Fornos em atenção" em `app/(app)/page.tsx`. ·
`04-06`: `recharts`, `--color-chart-1..5`, `lib/queimas/relatorios.ts`, rota `/queimas/relatorios`,
`relatorios-recharts.tsx`, `estatisticas-queimas.tsx`, `seletor-queimas.tsx`. ·
`04-07`: migração em produção, varredura completa e verificação humana.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do servidor → componente de apresentação | O cartão e o medidor recebem números já calculados; nenhuma regra de negócio atravessa para o cliente. |
| navegador → Server Action | Inalterado neste plano: nenhuma ação nova é criada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | cálculo do nível no cliente | medium | mitigate | `medirForno` roda no servidor, dentro do Server Component; `medidor.tsx` só desenha o que recebe e não recalcula nível — um cliente adulterado não consegue exibir um forno crítico como ok para outro gestor, porque o número vem da consulta |
| T-04-09 | Information Disclosure | rodapé do cartão expõe o nome do responsável pela manutenção | low | accept | O sistema é interno, para 3 a 5 gestores, todos com acesso total (REQUIREMENTS.md §Out of Scope, "Portal para as alunas"). O responsável é justamente o dado que o rodapé existe para mostrar. |
| T-04-10 | Denial of Service | consulta do índice sem limite | low | accept | O ateliê tem entre 2 e 6 fornos; a consulta agrega por forno, não por queima. Reavaliar se o índice um dia listar centenas de linhas. |
| T-04-01 | Spoofing | páginas de `app/(app)/queimas/**` | high | mitigate | `exigirUsuario()` na primeira linha de `page.tsx`; `middleware.ts` já protege tudo fora de `/login` e `/api/health`; `loading.tsx`/`error.tsx` não fazem consulta e não precisam da guarda |
</threat_model>

<verification>
1. `npm run verificar` passa inteiro
2. `npm test -- formato-queima textos-queima` passa
3. `npm run test:e2e -- --grep "cartão do forno"` passa nos dois projetos — uma única invocação,
   na Tarefa 3
4. Nenhum `npm run build` como passo separado
</verification>

<success_criteria>
- Chegando a 90 de 100 o cartão fica em atenção e mostra "Manutenção próxima"; em 100 fica em
  crítico e mostra "Manutenção vencida"
- O medidor tem entalhes a cada 10 queimas, marca no limiar de atenção e rótulos `0 / atenção N / limite N`
- O cartão mostra quantas queimas o forno já fez na vida, além do contador desde a última manutenção
- A tela de fornos tem estado de carregamento com esqueleto e estado de erro em linguagem humana
- `npm run verificar` passa
</success_criteria>

<output>
Create `.planning/phases/04-contador-de-queima/04-02-SUMMARY.md` when done.
Registre no SUMMARY quais comandos de teste ponta a ponta foram executados de fato e quantas vezes,
e como o estado de fronteira (89/90/99/100) foi preparado no teste.
</output>
