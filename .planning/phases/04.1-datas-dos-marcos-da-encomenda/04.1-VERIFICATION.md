---
phase: 04.1-datas-dos-marcos-da-encomenda
verified: 2026-08-22T12:30:23Z
status: passed
score: 17/17 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 15/17
  gaps_closed:
    - "A cascata computada por calcularCronograma reflete a ordem real das etapas (ORDEM_DAS_ETAPAS), mesmo depois de ajustes rápidos repetidos — não há corrupção de ordem por falta de ORDER BY nas leituras que a alimentam."
    - "O ajuste rápido de espera recusa um valor fora do intervalo (0–365) com uma mensagem em português visível ao usuário, nunca travando o controle sem explicação."
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "WR-03 — TrilhaEtapas pode mostrar duas datas de conclusão conflitantes depois de concluir e ajustar"
    addressed_in: "Fase futura, mediante conversa com o dono"
    evidence: "Registrado deliberadamente em 04.1-CONTEXT.md §Deferred Ideas (adicionado pelo plano 04.1-06) — correção é mudança de comportamento de produto, dono não consultado. Carregado como exceção aceita, não como gap, por instrução explícita desta re-verificação."
---

# Phase 04.1: Datas dos Marcos da Encomenda Verification Report

**Phase Goal:** Deixar o gestor dizer **quando** a queima de esmalte e a entrega acontecem, em vez
de assumir que vêm logo depois da etapa anterior — e tirar o interruptor dos marcos, que sempre
acontecem. A cascata a partir de `dataInicio` continua sendo a única fonte de verdade; nenhuma
data de marco é armazenada.

**Verified:** 2026-08-22T12:30:23Z
**Status:** passed
**Re-verification:** Yes — after gap closure (planos 04.1-05 e 04.1-06, fechando os gaps 16 e 17 da `04.1-VERIFICATION.md` anterior)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Uma encomenda nova nasce com 32 dias (D-04/D-05: produção 5, secagem 15, esperas 0/3/5) | ✓ VERIFIED | `lib/encomendas/cronograma.ts:47-53` `DIAS_PADRAO`; `tests/unit/cronograma.test.ts` passa (regressão confirmada nesta re-verificação) |
| 2 | Nenhuma data de marco é gravada; `espera_dias` é a única coluna nova, contador relativo (D-01) | ✓ VERIFIED | `db/schema.ts:169-206`; `db/migrations/0009_espera-dos-marcos.sql`; nenhuma alteração em `db/schema.ts`/`db/migrations/` nos planos 05/06 (`git diff --stat` vazio, confirmado nos dois SUMMARYs) |
| 3 | Marcos sempre 1 dia, recusado em 3 camadas quando diferente (D-06) | ✓ VERIFIED | `cronograma.ts:205-212` (`RangeError`); `esquemas.ts` refine de marco; constraint `marcos_sempre_um_dia` (regressão, sem mudança) |
| 4 | Formulário mostra, por marco, nome + campo numérico com sufixo "dias depois" (D-08) | ✓ VERIFIED | `components/amassa/encomendas/formulario-encomenda.tsx:362-401` (regressão, sem mudança) |
| 5 | Produção/secagem/esmaltação sem campo de espera; espera ≠ 0 nessas etapas é recusada | ✓ VERIFIED | `db/schema.ts` check `espera_so_em_marco`; `cronograma.ts:229-233` (regressão, sem mudança) |
| 6 | Espera 0–365 válida; fora do intervalo recusada com mensagem em português (caminho do formulário) | ✓ VERIFIED | `esquemas.ts:54` `.max(365, "A espera não pode passar de 365 dias.")` (regressão, sem mudança) |
| 7 | Vão de espera aparece como espaço vazio no Gantt, na barra do celular e na trilha — nenhum desenho novo (D-09) | ✓ VERIFIED | `tests/unit/gantt.test.ts`; `trilha-segmentos.tsx:75-119`; `trilha-etapas.tsx:280-289` (regressão, sem mudança) |
| 8 | Um dia dentro de um vão de espera devolve `situacaoEm` = `em-espera`, não `RangeError` | ✓ VERIFIED | `cronograma.ts:386-402` (regressão, sem mudança) |
| 9 | `diasAteProxima` conta até o início da próxima faixa DESENHADA, nunca até o fim da faixa atual | ✓ VERIFIED | `cronograma.ts:379,393` (regressão, sem mudança) |
| 10 | `posicaoDeHojeNaTrilha` mede extensão de calendário, não soma de durações | ✓ VERIFIED | `lib/encomendas/trilha.ts:66-115` (regressão, sem mudança) |
| 11 | ENC-03 reescrito sem "interruptor" com a justificativa D-07; ENC-04 retirado (tachado, motivo, fase); ENC-15 criado | ✓ VERIFIED | `.planning/REQUIREMENTS.md:65-68,79` — texto correto (ver ressalva sobre bookkeeping de status abaixo, em Requirements Coverage) |
| 12 | Critério de sucesso 3 da Fase 3 corrigido com nota apontando a Fase 04.1 | ✓ VERIFIED | `.planning/ROADMAP.md:177` (regressão, sem mudança) |
| 13 | `02-MODELO-DE-DADOS.md` e `00-BRIEFING.md` sem nenhuma menção a "interruptor" | ✓ VERIFIED | `grep -ci interruptor` = 0 nos dois arquivos (regressão, sem mudança) |
| 14 | `npm run verificar` sai 0 (lint, tsc, verificar-acoes, testes unitários, test:migracoes) | ✓ VERIFIED | Evidência de primeira mão do orquestrador nesta sessão: exit 0 — lint 0, `tsc --noEmit` 0, `verificar-acoes` 17/17, 479 testes unitários em 27 arquivos, `test:migracoes` verde. Árvore de trabalho limpa |
| 15 | Migração `0009_espera-dos-marcos` aplicada em produção, coluna+3 restrições conferidas, insert inválido rejeitado, backup verificado antes | ✓ VERIFIED | Estabelecido na verificação anterior (fora do alcance deste agente — execução real em produção); nenhuma migração nova nos planos 05/06 |
| 16 | A cascata reflete a ordem real das etapas mesmo depois de ajustes rápidos repetidos — sem corrupção por falta de `ORDER BY` | ✓ VERIFIED | **Gap fechado.** `lib/encomendas/acoes.ts:289,400` — `.orderBy(asc(encomendaEtapas.ordem))` presente nas duas leituras (`concluirEncomenda`, `ajustarEtapaEncomenda`), confirmado por leitura direta do código. Portão estrutural `tests/unit/leituras-ordenadas.test.ts` re-executado nesta verificação (independente do SUMMARY): 102 testes passando (junto com `textos-encomenda.test.ts` e `cronograma.test.ts`), incluindo o teste que prova o portão vermelho contra `tests/fixtures/leituras/sem-ordem.ts`. Prova de invariante de ordem em `tests/unit/cronograma.test.ts` (ordem invertida produz `inicio` diferente para `queima2`) reexecutada e verde. A prova de integração com Postgres real (`tests/e2e/encomendas-ordem-das-etapas.spec.ts`, força a ordem física a divergir) **não foi reexecutada nesta sessão** — aceita por leitura de código (o teste implementa exatamente os 7 passos descritos no plano) e pelo resultado documentado no SUMMARY (14 passed), por instrução explícita desta re-verificação de não gastar uma nova invocação de `npm run test:e2e` |
| 17 | Ajuste rápido de espera recusa valor fora do intervalo com mensagem em português visível, nunca travando o spinner | ✓ VERIFIED | **Gap fechado.** `lib/encomendas/acoes.ts:111,422-428` — `class AjusteInvalido extends Error {}` capturada explicitamente no `catch` de `ajustarEtapaEncomenda`, devolvendo `{ ok: false, erro: erro.message }` antes do `console.error`/`throw` final (confirmado por leitura direta). `components/amassa/encomendas/ajuste-rapido-etapa.tsx:77-104` — `try/catch/finally` em `aplicar()`, com `setPendente(false)` garantido em todo caminho e `toast.error(resposta.erro \|\| FRASE_FALHA_AO_SALVAR)` mostrando a mensagem do servidor. Teste e2e `"o ajuste rápido recusa uma espera acima de 365..."` em `tests/e2e/encomendas-detalhe.spec.ts:650-` reproduz o cenário exato de duas sessões concorrentes do CR-02 (confirmado por leitura de código); **não reexecutado nesta sessão** — aceito por leitura de código + SUMMARY (325 passed / 2 falhas conhecidas), por instrução explícita |

**Score:** 17/17 truths verified (0 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | WR-03 — `TrilhaEtapas` pode mostrar duas datas de conclusão conflitantes depois de concluir uma encomenda e ainda ajustar um dos seis controles | Fase futura, mediante conversa com o dono | `04.1-CONTEXT.md` §Deferred Ideas (item acrescentado pelo plano 04.1-06, confirmado nesta verificação); `components/amassa/encomendas/trilha-etapas.tsx` não aparece em nenhum commit dos planos 04.1-05/06 (`git log --oneline -- trilha-etapas.tsx` mostra apenas commits anteriores à fase de fechamento) — DELIBERADAMENTE DEFERIDO pelo dono, não um gap |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/encomendas/acoes.ts` | Duas leituras de `encomenda_etapas` ordenadas por `ordem`; `AjusteInvalido` capturado no `catch` | ✓ VERIFIED | Linhas 111 (classe), 289/400 (`.orderBy`), 422-428 (ramo do `catch`) — todas confirmadas por leitura direta |
| `tests/unit/leituras-ordenadas.test.ts` | Portão estrutural (árvore sintática) que reprova leitura sem `order by` | ✓ VERIFIED | Existe, executado nesta sessão, 4 testes passando |
| `tests/fixtures/leituras/sem-ordem.ts`, `com-ordem.ts` | Fixtures de portão | ✓ VERIFIED | Existem, usadas pelo portão |
| `tests/e2e/apoio/etapas-no-banco.ts` | Auxiliar `pg` para forçar ordem física e simular sessão concorrente | ✓ VERIFIED | Existe (6.7KB); `definirEsperaDaEtapa` e `inverterOrdemFisicaDasEtapas` confirmados por grep |
| `tests/e2e/encomendas-ordem-das-etapas.spec.ts` | Prova de integração com Postgres real | ✓ VERIFIED (não reexecutado) | Existe (3.5KB); conteúdo lido e confere com o plano |
| `components/amassa/encomendas/ajuste-rapido-etapa.tsx` | `try/catch/finally`; duas regiões `aria-live` | ✓ VERIFIED | Linhas 77-104 (finally), 147/205 (aria-live), confirmadas por leitura direta |
| `lib/encomendas/textos.ts` | `textoAcessivelDaEspera`/`textoAcessivelDaDuracao` | ✓ VERIFIED | Exportadas, testadas em `tests/unit/textos-encomenda.test.ts` (executado nesta sessão) |
| `docs/operacao/06-migracao-espera-dos-marcos.md` | Guarda pré-voo que confere `dias <> 1` em marco | ✓ VERIFIED | §1 contém a consulta e "O que você deve ver: nenhuma linha.", adicional à guarda de D-10 existente; nenhum IP/senha/nome real |
| `.planning/phases/.../04.1-CONTEXT.md` | WR-03 registrado em §Deferred Ideas | ✓ VERIFIED | Item presente, itens anteriores intactos |
| `.planning/REQUIREMENTS.md` | Rastreabilidade refletindo o estado real de ENC-03/ENC-15/ENC-04 | ⚠️ PARCIAL (ver nota abaixo) | ENC-15 e ENC-04 corretos; ENC-03 tem bookkeeping desatualizado |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `lib/encomendas/acoes.ts` (SELECTs) | `calcularCronograma` | `.orderBy(asc(encomendaEtapas.ordem))` antes de `.for("update")` | WIRED | Corrigido — confirmado nas duas leituras |
| `ajustarEtapaEncomenda` (bloco `catch`) | `AjusteRapidoEtapa.aplicar()` | `AjusteInvalido` → `{ ok: false, erro }` → `catch`/`finally` do cliente → `toast.error` | WIRED | Corrigido — cadeia completa confirmada por leitura de código nos dois arquivos |
| `lib/encomendas/textos.ts` | `ajuste-rapido-etapa.tsx` | `textoAcessivelDaEspera`/`textoAcessivelDaDuracao` dentro de `<span aria-live="polite" className="sr-only">` | WIRED | Confirmado nos dois ramos (marco e intervalo) |
| `docs/operacao/06-migracao-espera-dos-marcos.md` Passo 1 | `marcos_sempre_um_dia` | consulta `dias <> 1` em etapa de marco | WIRED | Confirmado — a guarda nova checa exatamente o predicado da constraint |
| `ENC-15`/`ENC-03`/`ENC-04` (REQUIREMENTS.md) | Tabela de rastreabilidade | linha mapeada para Fase 04.1 | PARTIAL | ENC-15/ENC-04 corretos; linha de ENC-03 (Fase 04.1) ainda diz "Gaps Found" — ver nota abaixo |

### Nota — Estado de ENC-03/ENC-15 em REQUIREMENTS.md (divergência sinalizada pelo executor de 04.1-06)

Investigado por leitura direta de `.planning/REQUIREMENTS.md` e do histórico git:

- **Estado ATUAL no arquivo:** `ENC-03` aparece como `- [ ]` (não marcada) na lista de requisitos,
  e a tabela de rastreabilidade tem a linha `| ENC-03 | Phase 04.1 — Datas dos Marcos da Encomenda
  (reaberto) | Gaps Found |` (linha 257). `ENC-15` aparece como `- [x]` e
  `| ENC-15 | ... | Complete |` (linha 259). `ENC-04` aparece tachada com "Withdrawn" (linha 258) —
  correto e sem alteração necessária.
- **Como chegou aqui:** o commit `9b50bb8` (plano 04.1-03) marcou ENC-03 e ENC-15 como `[x]`
  Complete prematuramente, antes de qualquer verificação. O commit `dbb3c30` reverteu as duas para
  `[ ]` Gaps Found, coincidindo com a primeira `04.1-VERIFICATION.md` (`gaps_found`, 15/17). O
  commit `7feeefc` (fechamento do plano 04.1-05, gap 16) editou manualmente o arquivo e marcou
  **só ENC-15** de volta para `[x]` Complete — ENC-03 ficou para trás, sem edição. O plano 04.1-06
  (fechamento do gap 17) não tocou em `REQUIREMENTS.md`; seu SUMMARY registra explicitamente que
  `gsd-tools query requirements.mark-complete ENC-15 ENC-03` devolveu `already_complete` para os
  dois sem escrever nada — o tracker interno da ferramenta e o texto do arquivo estavam (e
  continuam) divergentes, e a decisão foi deixar para esta reverificação resolver.
- **Determinação desta verificação:** com os gaps 16 e 17 agora fechados com evidência de código e
  teste (truths 16/17 acima, ✓ VERIFIED), o estado **correto** de `ENC-03` em `REQUIREMENTS.md`
  passa a ser `[x]` marcada, e a linha da tabela de rastreabilidade deveria dizer `Complete`, no
  mesmo padrão já aplicado a `ENC-15`. O arquivo **ainda não reflete isso** — é uma divergência de
  documentação (bookkeeping), não um defeito de código ou de comportamento observável pelo gestor;
  nenhuma tela, nenhuma regra de negócio e nenhum dado depende deste texto. Por não afetar o
  comportamento do sistema, esta verificação **não trata isso como gap bloqueante** (não entra na
  contagem 17/17 nem impede `status: passed`), mas registra explicitamente, como pedido: a correção
  é uma edição de duas linhas em `.planning/REQUIREMENTS.md` (marcar `- [x] **ENC-03**` e trocar
  `Gaps Found` por `Complete` na linha 257), a ser feita como parte do fechamento administrativo
  desta fase — por exemplo, no commit que arquivar esta verificação.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| ENC-15 | 01, 02, 03, 04, 05, 06 | Espera em dias antes de cada marco, nenhuma data armazenada | ✓ SATISFIED | Truths 1,2,7,10,16; `.planning/REQUIREMENTS.md:79` já em `Complete` |
| ENC-03 | 01, 02, 03, 04, 05, 06 | Marcos sempre acontecem, sempre 1 dia; campo numérico é espera, não duração | ✓ SATISFIED (bookkeeping de REQUIREMENTS.md desatualizado — ver nota acima) | Truths 3,4,7,11,16,17 — os dois defeitos que antes deixavam este requisito com ressalva (gaps 16/17) estão fechados com evidência de código e teste |
| ENC-04 | 03 | Retirado — "não há mais o que desligar" | ✓ SATISFIED (bookkeeping coerente) | `.planning/REQUIREMENTS.md:68` — tachado, motivo, fase; `Withdrawn` na tabela |

Não há requisito órfão: os três IDs declarados nos frontmatters dos planos (incluindo 04.1-05 e
04.1-06, ambos `requirements: [ENC-15, ENC-03]`) batem com os três citados no Goal/Requirements da
Fase 04.1 em ROADMAP.md, e todos aparecem na tabela de rastreabilidade mapeados para a Fase 04.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/e2e/encomendas-filtros.spec.ts` | 60-61 | Comentário desatualizado cita valores pré-04.1 de `DIAS_PADRAO` (produção 3/secagem 6, sem esperas) | ⚠️ Warning | Documentação-only (WR-04 de `04.1-REVIEW.md`, commit `5aadb0d`); a lógica do teste importa `DIAS_PADRAO`/`calcularCronograma` de verdade (linhas 367-368), não redigita os números — nenhum teste está errado, só o comentário. Não corrigido pelos planos 05/06 (fora do escopo deles, achado depois) |
| `.planning/REQUIREMENTS.md` | 257 | Linha de rastreabilidade de ENC-03 (Fase 04.1) ainda diz "Gaps Found"; checkbox `- [ ]` | ⚠️ Warning | Bookkeeping desatualizado — ver nota dedicada acima. Não afeta comportamento do sistema |

Nenhum marcador de dívida (`TBD`/`FIXME`/`XXX`) sem referência a acompanhamento formal foi
encontrado em nenhum dos arquivos modificados pelos planos 04.1-05/04.1-06 (verificado por grep
direto nesta sessão).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Portão estrutural + testes de ordem + textos acessíveis (reexecutados nesta sessão, independente do SUMMARY) | `npx vitest run tests/unit/leituras-ordenadas.test.ts tests/unit/textos-encomenda.test.ts tests/unit/cronograma.test.ts` | 3 arquivos, 102 testes, 0 falhas | ✓ PASS |
| `.orderBy(asc(encomendaEtapas.ordem))` presente nas duas leituras corrigidas | `grep -n "orderBy" lib/encomendas/acoes.ts` | linhas 289 e 400, ambas com `encomendaEtapas.ordem` | ✓ PASS |
| `AjusteInvalido` capturado antes do `console.error`/`throw` final | Leitura direta de `lib/encomendas/acoes.ts:422-440` | ramo dedicado presente, na ordem certa (`EncomendaNaoEncontrada` → `AjusteInvalido` → genérico) | ✓ PASS |
| `try/catch/finally` em `aplicar()` do cliente | Leitura direta de `ajuste-rapido-etapa.tsx:77-104` | `finally { setPendente(false) }` cobre sucesso, `{ok:false}` e rejeição | ✓ PASS |
| `npm run verificar` completo (evidência de primeira mão do orquestrador nesta sessão) | `npm run verificar` | lint 0 · tsc 0 · verificar-acoes 17/17 · 479 testes · test:migracoes ok | ✓ PASS |
| Ausência de marcador de dívida nos arquivos da fase de fechamento | `grep -n "TBD\|FIXME\|XXX"` nos 10 arquivos modificados por 04.1-05/06 | nenhuma ocorrência | ✓ PASS |
| `trilha-etapas.tsx` não tocado (WR-03 só registrado, não implementado) | `git log --oneline -- components/amassa/encomendas/trilha-etapas.tsx` | commits mais recentes são de antes da fase de fechamento (04.1-02 e Fase 3) | ✓ PASS |

`npm run test:e2e` (com ou sem `--grep`) **não foi reexecutado** nesta verificação — orçamento de
e2e do projeto já gasto pelos planos 04.1-04/04.1-05/04.1-06 (três invocações documentadas: `--grep
"ORDEM_DAS_ETAPAS"`, `--grep "365"`, e a varredura completa sem `--grep`: 325 passed / 2 falhas
conhecidas / 33 skipped). Por instrução explícita desta re-verificação, os testes e2e novos que
provam os truths 16 e 17 foram aceitos por leitura direta do código-fonte (confirmando que cada
teste implementa exatamente o cenário descrito) somada à evidência já registrada nos SUMMARYs — não
por reexecução.

### Probe Execution

N/A — fase não declara nem usa `scripts/*/tests/probe-*.sh`.

### Human Verification Required

Nenhum item requer verificação humana adicional nesta reverificação. Os dois gaps da verificação
anterior (truths 16 e 17) são defeitos de código com correção e prova automatizada, confirmados por
leitura direta e reexecução dos testes unitários relevantes. WR-03 é uma pendência de PRODUTO
deliberadamente adiada, aguardando conversa com o dono — não é um item que este agente possa ou
deva verificar tecnicamente; está registrado como item adiado (Deferred Items), não como pendência
de verificação.

### Gaps Summary

Nenhum gap bloqueante restante. Os dois defeitos da verificação anterior estão fechados com
evidência:

1. **Gap 16 (CR-01, ordem física corrompendo a cascata) — FECHADO.** `.orderBy(asc(encomendaEtapas.ordem))`
   presente nas duas leituras de `lib/encomendas/acoes.ts` que alimentam `calcularCronograma`
   (`concluirEncomenda`, `ajustarEtapaEncomenda`), no mesmo padrão de `lib/encomendas/consultas.ts`.
   Fechado com um portão estrutural (`tests/unit/leituras-ordenadas.test.ts`, decide pela árvore
   sintática do TypeScript, provado vermelho contra fixture) e uma prova de integração com
   Postgres real (`tests/e2e/encomendas-ordem-das-etapas.spec.ts`) que força a ordem física a
   divergir e mostra a cascata recebendo dados errados sem a ordenação.

2. **Gap 17 (CR-02, mensagem de erro descartada e spinner travado) — FECHADO.** `AjusteInvalido`
   (erro de controle interno, molde de `EncomendaNaoEncontrada`) é capturado explicitamente no
   `catch` de `ajustarEtapaEncomenda` e devolve `{ ok: false, erro: erro.message }` com a mensagem
   em português do `esquemaEtapas`. No cliente, `try/catch/finally` em `aplicar()` garante
   `setPendente(false)` em todo caminho — nunca mais um spinner girando para sempre. Provado por um
   teste e2e que reproduz o cenário real de duas sessões concorrentes do CR-02.

Uma única divergência de bookkeeping (não um gap de código) permanece: a linha de rastreabilidade
de `ENC-03` em `.planning/REQUIREMENTS.md` ainda diz "Gaps Found" e o checkbox está desmarcado,
desatualizado desde antes do fechamento dos gaps 16/17. Documentado em detalhe na nota dedicada
acima, com a correção exata recomendada. Isso não afeta nenhum comportamento observável pelo
gestor e não impede `status: passed`.

WR-03 (duas datas de conclusão conflitantes em `TrilhaEtapas`) permanece deliberadamente adiado,
por decisão explícita do processo de fechamento da fase — registrado como item adiado, carregado
como exceção aceita nesta verificação, conforme instrução.

Os dois pré-existentes conhecidos e não relacionados a esta fase — as duas falhas de
`tests/e2e/autenticacao.spec.ts:84` (WINDOWS.md #3, aberto desde a Fase 2b) — continuam fora do
escopo desta verificação.

---

_Verified: 2026-08-22T12:30:23Z_
_Verifier: Claude (gsd-verifier)_
