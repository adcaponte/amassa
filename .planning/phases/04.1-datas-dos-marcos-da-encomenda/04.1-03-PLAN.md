---
phase: 04.1-datas-dos-marcos-da-encomenda
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - amassa-plataforma/02-MODELO-DE-DADOS.md
  - amassa-plataforma/00-BRIEFING.md
autonomous: true
requirements: [ENC-03, ENC-04, ENC-15]
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "ENC-03 em `.planning/REQUIREMENTS.md` não fala mais em interruptor, e carrega a frase que explica por que um campo numérico num marco não é regressão: o número novo significa a espera ANTES do marco, não a duração dele (D-07)."
    - "ENC-04 não fica marcado como entregue descrevendo uma capacidade que o código não tem mais: a linha permanece no arquivo, explicitamente retirada, com o motivo e a fase que a retirou (D-06)."
    - "Existe um requisito novo com o próximo ENC livre — ENC-15 — descrevendo a espera em dias antes de cada marco, presente tanto na lista de Encomendas quanto na tabela de rastreabilidade, mapeado para a Fase 04.1."
    - "ENC-03 e ENC-04 aparecem na tabela de rastreabilidade com a Fase 04.1 registrada, não apenas com a Fase 3 — quem lê a tabela vê que foram reabertos."
    - "O critério de sucesso 3 da Fase 3 em `.planning/ROADMAP.md` não afirma mais que desligar a Entrega encurta a encomenda, e traz uma nota dizendo que foi corrigido na Fase 04.1 e por quê — a correção é anotada, nunca lavada."
    - "`amassa-plataforma/02-MODELO-DE-DADOS.md` mostra o DDL de `encomenda_etapas` com a coluna `espera_dias` e as restrições novas, e os padrões `producao 5 · secagem 15` com as esperas 0 · 3 · 5."
    - "O parágrafo \"Por que não guardar as datas\" continua de pé em `02-MODELO-DE-DADOS.md`, agora dizendo explicitamente que a espera é um contador relativo de dias e por isso não cria uma segunda versão da verdade (D-01)."
    - "`amassa-plataforma/00-BRIEFING.md` não afirma mais, em nenhum lugar, que um marco é um interruptor ou que pode valer 0 dia."
  prohibitions:
    - statement: "Nenhum requisito continua marcado como entregue descrevendo comportamento que o código não tem mais — uma reescrita silenciosa que apaga o histórico do que passou na Fase 3 é tão ruim quanto deixar a mentira lá."
      status: resolved
      verification: judgment
    - statement: "Nenhum dado real de cliente e nenhum segredo entra em qualquer um destes arquivos — o repositório é público."
      status: resolved
      verification: test
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - amassa-plataforma/02-MODELO-DE-DADOS.md
    - amassa-plataforma/00-BRIEFING.md
  key_links:
    - "ENC-15 é o ID que os planos 01 e 02 já citam no campo `requirements` do próprio cabeçalho — é aqui que ele passa a existir de verdade."
    - "A tabela de rastreabilidade é o único lugar que liga requisito a fase; sem a linha da Fase 04.1 os requisitos reabertos ficam invisíveis para quem auditar o milestone."
---

<objective>
Fazer os documentos dizerem a verdade nova. Três deles afirmam hoje, por escrito e marcados como
entregues, uma regra que o ateliê nunca teve: a de que um marco é um interruptor que pode ser
desligado.

- **ENC-03** está marcado `Complete` dizendo que os marcos "são um interruptor, nunca um campo
  numérico". D-06 e D-07 o reabrem.
- **ENC-04** está marcado `Complete` dizendo que desligar a Entrega faz o losango sumir. Não há
  mais o que desligar.
- O **critério de sucesso 3 da Fase 3** repete a mesma frase — e **passou** nas duas rodadas de
  verificação. Isso não pode ser lavado em silêncio.
- **`02-MODELO-DE-DADOS.md`** e **`00-BRIEFING.md`** carregam o DDL antigo, os padrões antigos e a
  frase do interruptor.

Purpose: reabrir um requisito marcado `Complete` foi decisão do dono, registrada em
`03-VERIFICATION.md`; aquele documento deixou `REQUIREMENTS.md` e `ROADMAP.md` intocados **de
propósito**, esperando esta fase. Um requisito verde que descreve capacidade que o código não tem
é pior do que um requisito vermelho: ele mente para a auditoria do milestone.

Output: ENC-03 reescrito com a justificativa de D-07, ENC-04 retirado com motivo, ENC-15 criado
para a capacidade nova, a tabela de rastreabilidade em dia, o critério da Fase 3 corrigido **com
nota**, e o modelo de dados e o briefing atualizados.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.1-datas-dos-marcos-da-encomenda/04.1-CONTEXT.md
@.planning/phases/04.1-datas-dos-marcos-da-encomenda/04.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artefato | Onde |
|---|---|
| ID de requisito **ENC-15** (novo — não existe hoje; o último em uso é ENC-14) | `.planning/REQUIREMENTS.md`, lista de Encomendas e tabela de rastreabilidade |
| Linha `ENC-15 \| Phase 04.1 — Datas dos Marcos da Encomenda` | tabela de rastreabilidade |
| Linhas de rastreabilidade da Fase 04.1 para ENC-03 e ENC-04 | tabela de rastreabilidade |

<!-- planner-discipline-allow: interruptor -->
<!-- planner-discipline-allow: marcos_sempre_um_dia -->
| Coluna `espera_dias` e as restrições `marcos_sempre_um_dia`, `espera_so_em_marco` e `encomenda_etapas_espera_no_intervalo` no DDL de referência | `amassa-plataforma/02-MODELO-DE-DADOS.md` |

<tasks>

<task type="auto">
  <name>Tarefa 1: ENC-03 reescrito, ENC-04 retirado, ENC-15 criado</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §Encomendas (ENC-01 a ENC-14, por volta da linha 63) e a tabela de rastreabilidade (por volta da linha 240) — leia as duas antes de editar, para copiar a forma exata de cada uma.
    - `.planning/phases/04.1-datas-dos-marcos-da-encomenda/04.1-CONTEXT.md` §`<decisions>` — D-01, D-02, D-05, D-06, D-07.
    - `.planning/phases/03-gestor-de-encomendas/03-VERIFICATION.md` §"Achado de produto — ENC-03 está errado sobre o ateliê" — as palavras literais do dono, que são a origem desta reescrita.
    - `.planning/phases/04.1-datas-dos-marcos-da-encomenda/04.1-01-SUMMARY.md` — o que de fato foi construído, para o texto do requisito descrever o código e não a intenção.
  </read_first>
  <action>
Edite `.planning/REQUIREMENTS.md` com substituições localizadas — nunca reescreva o arquivo
inteiro, que tem requisitos de todos os módulos.

**ENC-03 — reescrever.** O texto de hoje é *"Os três marcos (queima 1, queima 2, entrega)
aparecem como losango e são um interruptor, nunca um campo numérico"*. Substitua por um texto que
diga: os três marcos aparecem como losango, **sempre acontecem** e **sempre duram 1 dia**; o campo
numérico ao lado de cada um é a **espera antes** do marco, nunca a duração dele. Logo abaixo,
acrescente a frase de justificativa que D-07 manda registrar explicitamente: a proibição original
existia para o gestor não digitar *duração* de algo que dura um dia, e o número novo significa
outra coisa — sem essa frase, quem ler daqui a seis meses vai achar que foi regressão. Cite a
origem: a caminhada do dono em produção em 2026-08-20, registrada em `03-VERIFICATION.md`, e as
palavras dele — *"as queimas e entregas sempre acontecem"*.

**ENC-04 — retirar, não apagar.** O texto de hoje é *"Desligar a etapa 'Entrega' faz o losango
sumir e encurta a encomenda"*. Não existe mais o que desligar, então não há reescrita possível:
a capacidade deixou de existir. Mantenha a linha no arquivo, com o texto antigo preservado em
tachado, e acrescente na mesma linha a marca de retirada, a fase que retirou (04.1) e o motivo em
uma frase. Ele **não pode** continuar lendo-se como entregue — um requisito verde descrevendo
capacidade inexistente é exatamente o que esta tarefa existe para consertar. E não pode
simplesmente sumir: ele passou nas duas rodadas de verificação da Fase 3, e apagar isso é
reescrever a história.

**ENC-15 — criar.** O último ID em uso na seção de Encomendas é ENC-14; o próximo livre é
**ENC-15**. Acrescente-o ao fim da lista de Encomendas, descrevendo a capacidade que os planos 01
e 02 entregaram: o gestor diz quantos dias a peça fica parada antes de cada um dos três marcos,
digitando o número num campo com o sufixo "dias depois"; a espera desloca o marco e todas as
etapas seguintes na cascata; e **nenhuma data é armazenada** — a espera é um contador relativo de
dias e `data_inicio` continua sendo a única âncora gravada (D-01). Mencione os padrões dados pelo
dono: espera 0 na queima de biscoito, 3 na queima de esmalte e 5 na entrega (D-05).

**Tabela de rastreabilidade.** Acrescente a linha de ENC-15 mapeada para `Phase 04.1 — Datas dos
Marcos da Encomenda`. Acrescente também linhas registrando que ENC-03 e ENC-04 foram **reabertos**
pela Fase 04.1 — hoje os dois aparecem só como `Phase 3 | Complete`, e quem auditar o milestone
precisa ver que voltaram a mexer. Use exatamente o mesmo formato de coluna das linhas vizinhas.
O estado que você registra para ENC-03 e ENC-15 é o que os planos 01 e 02 de fato entregaram; se
algum deles ainda não tiver rodado quando você chegar aqui, registre isso no SUMMARY em vez de
afirmar entrega que não aconteceu.

Português do Brasil em todo o texto, como o resto do arquivo. Nenhum dado real de cliente ou de
aluna, e nenhum segredo: o repositório é público.
  </action>
  <acceptance_criteria>
    - `grep -c 'ENC-15' .planning/REQUIREMENTS.md` retorna pelo menos 2 — uma ocorrência na lista de Encomendas e outra na tabela de rastreabilidade.
    - `grep -n 'ENC-03' .planning/REQUIREMENTS.md` mostra o texto novo, e a linha de ENC-03 na lista **não** contém a palavra "interruptor".
    - A linha de ENC-03 (ou a imediatamente abaixo dela) contém a justificativa de D-07 — que o número é a espera antes do marco e não a duração dele.
    - A linha de ENC-04 contém a marca de retirada e cita a Fase 04.1.
    - `grep -c 'Phase 04.1' .planning/REQUIREMENTS.md` retorna pelo menos 3 — ENC-15, ENC-03 reaberto e ENC-04 retirado.
    - `grep -c 'ENC-14' .planning/REQUIREMENTS.md` continua com o mesmo valor de antes da edição — nenhum requisito vizinho foi perdido.
    - `git diff --stat .planning/REQUIREMENTS.md` mostra apenas linhas acrescentadas e alteradas na região de Encomendas e na tabela, nunca o arquivo inteiro reescrito.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ENC-15' .planning/REQUIREMENTS.md && grep -c 'Phase 04.1' .planning/REQUIREMENTS.md && git diff --stat .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>ENC-03 descreve a regra nova com a justificativa que impede a leitura de regressão, ENC-04 está explicitamente retirado com motivo, ENC-15 existe na lista e na rastreabilidade, e nenhum requisito vizinho se perdeu.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O critério da Fase 3, o modelo de dados e o briefing</name>
  <files>.planning/ROADMAP.md, amassa-plataforma/02-MODELO-DE-DADOS.md, amassa-plataforma/00-BRIEFING.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 3: Gestor de Encomendas" — a lista de critérios de sucesso, especificamente o de número 3.
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` linhas ~180-209 — o DDL de `encomenda_etapas`, a lista de padrões, o item sobre marcos valerem 0 ou 1, e o parágrafo **"Por que não guardar as datas"**.
    - `amassa-plataforma/00-BRIEFING.md` linhas ~124-135 e ~168-176 — a tabela de etapas e o item que diz que duração 0 é permitida inclusive nos marcos e que na interface um marco é um interruptor.
    - `db/schema.ts` já alterado pelo plano 01 — a fonte de verdade do DDL que o documento espelha.
  </read_first>
  <action>
Três arquivos, substituições localizadas em cada um. **Nunca reescreva `.planning/ROADMAP.md`
inteiro** — ele descreve sete fases e uma reescrita de arquivo apaga tudo o que estiver fora da
janela editada.

**`.planning/ROADMAP.md` — critério de sucesso 3 da Fase 3.** O texto de hoje afirma que os marcos
são um interruptor e que desligar a etapa "Entrega" faz o losango sumir e encurta a encomenda.
Substitua pela regra nova — losango, sempre acontece, sempre 1 dia, e o número ao lado é a espera
antes do marco — e **anexe uma nota** dizendo que o critério foi corrigido na Fase 04.1, que o
critério original passou nas duas rodadas de verificação da Fase 3 e que o que estava errado era a
especificação, não a implementação, apontando para `03-VERIFICATION.md` §"Achado de produto". A
nota não é enfeite: sem ela, um leitor futuro comparando o ROADMAP com o relatório de verificação
da Fase 3 encontra uma contradição sem explicação. Não mexa em nenhum outro critério nem em
nenhuma outra fase.

**`amassa-plataforma/02-MODELO-DE-DADOS.md`.** Espelhe o `db/schema.ts` já alterado:
- No DDL de `encomenda_etapas`, acrescente a coluna `espera_dias integer not null default 0`,
  substitua a restrição de marco pela que exige `dias = 1` para `queima1`, `queima2` e `entrega`,
  e acrescente as duas restrições novas — a de intervalo da espera e a que só permite espera nas
  etapas de marco. Os nomes das restrições precisam bater exatamente com os de `db/schema.ts`;
  confira lá, não escreva de memória.
- Na regra dos padrões, troque `producao 3 · secagem 6 · queima1 1 · esmaltacao 1 · queima2 1 ·
  entrega 1` pelos valores novos: produção 5 e secagem 15, com as demais durações em 1, e as
  esperas 0 na queima de biscoito, 3 na queima de esmalte e 5 na entrega (D-04, D-05).
- O item que hoje diz que marcos valem 0 ou 1 dia e que na interface são um interruptor precisa
  ser substituído: marco sempre acontece, sempre dura 1 dia, e o campo numérico ao lado é a espera
  antes dele (D-06, D-07). O exemplo de "zero é caso real — peça que só vai a biscoito" perdeu o
  objeto e sai junto.
- **O parágrafo "Por que não guardar as datas" continua exatamente de pé** — ele diz que datas
  armazenadas se desincronizam de `dias` na primeira edição e passam a existir duas versões da
  verdade. Acrescente logo abaixo dele uma frase dizendo que `espera_dias` **não** é uma data: é
  um contador relativo de dias que entra na mesma cascata, e por isso a regra continua valendo
  inteira. É a razão de existir de D-01, e é o que impede um leitor futuro de achar que a fase
  abriu uma exceção na regra mais delicada do modelo.

**`amassa-plataforma/00-BRIEFING.md`.** Corrija os dois pontos que afirmam o interruptor: a
ressalva sobre marcos zerados junto da tabela de etapas, e o item que diz que duração 0 é permitida
inclusive nos marcos e que na interface um marco é um interruptor. A tabela em si continua certa —
os três marcos seguem sendo marcos de 24h — e não muda. Acrescente onde couber que a espera antes
do marco é o que o gestor passa a controlar.

Português do Brasil em tudo. Nenhum dado real e nenhum segredo — o repositório é público.
  </action>
  <acceptance_criteria>
    - `grep -c 'espera_dias' amassa-plataforma/02-MODELO-DE-DADOS.md` retorna pelo menos 1.
    - `grep -c 'marcos_sempre_um_dia' amassa-plataforma/02-MODELO-DE-DADOS.md` retorna 1, e o nome bate com `grep -c 'marcos_sempre_um_dia' db/schema.ts`.
    - `grep -c 'secagem 15' amassa-plataforma/02-MODELO-DE-DADOS.md` retorna pelo menos 1.
    - `grep -c 'Por que não guardar as datas' amassa-plataforma/02-MODELO-DE-DADOS.md` continua retornando 1 — o parágrafo não foi removido.
    - `grep -ci 'interruptor' amassa-plataforma/02-MODELO-DE-DADOS.md` retorna 0 e `grep -ci 'interruptor' amassa-plataforma/00-BRIEFING.md` retorna 0.
    - O critério de sucesso 3 da Fase 3 em `.planning/ROADMAP.md` cita a Fase 04.1 e não afirma mais que desligar a Entrega encurta a encomenda.
    - `grep -c '^### Phase' .planning/ROADMAP.md` retorna o mesmo valor de antes da edição — nenhuma fase se perdeu.
    - `git diff --stat .planning/ROADMAP.md` mostra uma janela pequena de alteração, nunca o arquivo inteiro.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'espera_dias' amassa-plataforma/02-MODELO-DE-DADOS.md && grep -c 'Por que não guardar as datas' amassa-plataforma/02-MODELO-DE-DADOS.md && grep -c '^### Phase' .planning/ROADMAP.md && git diff --stat .planning/ROADMAP.md</automated>
  </verify>
  <done>O critério da Fase 3 está corrigido com nota que preserva o histórico, o modelo de dados espelha o schema real e reafirma por que a espera não viola a regra das datas, e o briefing não fala mais em interruptor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → público | O repositório é público; tudo escrito nestes arquivos fica visível para qualquer pessoa |
| documento → auditoria de milestone | A tabela de rastreabilidade é a fonte que uma auditoria futura lê para saber o que foi entregue |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-11 | Repudiation | `.planning/REQUIREMENTS.md` e `.planning/ROADMAP.md` | medium | mitigate | ENC-04 e o critério 3 da Fase 3 são corrigidos **com nota e com o texto antigo preservado**, nunca substituídos em silêncio — o histórico do que passou na verificação da Fase 3 continua legível |
| T-04.1-12 | Information Disclosure | os quatro arquivos editados | medium | mitigate | Nenhum dado real de cliente ou de aluna e nenhum segredo entra em nenhum deles; os exemplos usam os valores de referência que os documentos já usam |
| T-04.1-13 | Tampering | `.planning/ROADMAP.md` | medium | mitigate | Só substituição localizada, nunca reescrita do arquivo inteiro; a contagem de seções `### Phase` antes e depois é o portão que prova que nenhuma fase se perdeu |
| T-04.1-14 | Spoofing | DDL de referência divergindo do schema real | low | mitigate | Os nomes das restrições no documento são conferidos contra `db/schema.ts` por comparação de contagem, não escritos de memória |
| T-04.1-SC | Tampering | instalação de pacotes | low | accept | Este plano não instala nenhum pacote de npm — só edita documentos |
</threat_model>

<verification>
- ENC-15 existe na lista de Encomendas e na tabela de rastreabilidade, mapeado para a Fase 04.1.
- ENC-03 não fala mais em interruptor e carrega a justificativa de D-07.
- ENC-04 está retirado com motivo e com o texto antigo preservado.
- O critério de sucesso 3 da Fase 3 está corrigido e anotado.
- `02-MODELO-DE-DADOS.md` espelha o `db/schema.ts` real e reafirma a regra das datas.
- A contagem de seções `### Phase` em `ROADMAP.md` não mudou.
</verification>

<success_criteria>
- Nenhum requisito marcado como entregue descreve comportamento que o código não tem.
- Quem ler ENC-03 daqui a seis meses entende por que existe um campo numérico num marco.
- Quem comparar o ROADMAP com a verificação da Fase 3 encontra a explicação, não uma contradição.
- O documento de modelo de dados pode ser usado para reconstruir o schema.
</success_criteria>

<output>
Create `.planning/phases/04.1-datas-dos-marcos-da-encomenda/04.1-03-SUMMARY.md` when done
</output>
