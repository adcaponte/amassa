---
phase: 04.4-financeiro-parte-1
plan: 06
type: execute
wave: 3
depends_on: ["04.4-03"]
files_modified:
  - lib/financeiro/calendario.ts
  - lib/financeiro/parcelas.ts
  - lib/financeiro/taxa.ts
  - lib/financeiro/esquemas.ts
  - lib/financeiro/acoes.ts
  - lib/financeiro/textos.ts
  - components/amassa/financeiro/bloco-pagamento.tsx
  - components/amassa/financeiro/linha-parcela.tsx
  - components/amassa/financeiro/painel-venda.tsx
  - tests/unit/financeiro-calendario.test.ts
  - tests/unit/financeiro-parcelas.test.ts
  - tests/unit/financeiro-taxa.test.ts
  - tests/e2e/financeiro-pagamento.spec.ts
autonomous: true
requirements: [FNC-03, FNC-05, FNC-17]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 170000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "'Como recebe' oferece À vista · Sinal de 50% + saldo · 2x · 3x · 4x · 6x · 10x · 12x (os do protótipo; na despesa o segundo se chama 'Entrada de 50% + saldo') — FNC-03."
    - "À vista = uma parcela paga na data do documento. Sinal = duas parcelas: a primeira na data e paga, com o valor da metade arredondado para cima no centavo (R$ 150,01 → R$ 75,01 + R$ 75,00), a segunda em aberto 30 dias depois. Nx = N parcelas mensais, a primeira na data do documento e já paga, os centavos que sobram da divisão na PRIMEIRA (R$ 100,00 em 3x → 33,34 + 33,33 + 33,33)."
    - "Parcela mensal que cai num dia que não existe no mês cai no último dia daquele mês (31/01 + 1 mês = 28/02 ou 29/02), sempre contado a partir da data do documento, nunca da parcela anterior."
    - "Depois de gerada, cada parcela tem data, valor e 'já recebi'/'já paguei' editáveis (caixa de marcação nativa com zona de toque de 44×44px); mudar linhas, data ou plano gera as parcelas de novo, como no protótipo."
    - "Se a soma das parcelas não fecha com o total, aparece 'As parcelas somam {R$ X}. {Faltam/Sobram} {R$ Y} para fechar com o total.' em fundo de erro, 'Lançar venda' fica desabilitado, e o servidor recusa com a mesma frase mesmo quando o botão é habilitado à força no navegador (critério 2 do ROADMAP)."
    - "Cada parcela precisa ter valor maior que zero; um total pequeno demais para o número de parcelas (1 centavo em 2x) é recusado com frase; no máximo 12 parcelas."
    - "Uma parcela marcada como paga não pode vencer depois de hoje ('Uma parcela que vence depois de hoje não pode estar paga — desmarque e registre no Caixa quando o dinheiro entrar.'), nem ter data anterior ao saldo inicial quando ele existir."
    - "A forma de pagamento é de cada parcela (D-07): o seletor 'Forma' vale para todas as parcelas do plano, e no à vista o botão '+ outra forma' divide o recebimento em duas parcelas pagas na data do documento, cada uma com o próprio valor e a própria forma; 'tirar' volta a uma forma só; a soma tem de fechar com o total, com a mesma mensagem de falta/sobra (D-08). Vale para venda e, no plano 07, para despesa — é o mesmo componente."
    - "No cartão, o preço não muda: a taxa da configuração é congelada na parcela no momento em que ela é paga (`taxa_pontos_base`), entra no caixa o valor menos a taxa arredondada no centavo, e mudar a taxa em Cadastros depois não reescreve o que já foi lançado (FNC-05, critério 3 do ROADMAP)."
    - "No pagamento misto, a taxa incide só na parcela do cartão (D-08): R$ 100 no cartão + R$ 50 em dinheiro a 3,5% → taxa R$ 3,50, entram R$ 146,50."
    - "O aviso do cartão aparece só na venda e só quando há parcela no cartão: 'Cartão: a maquininha fica com {taxa}% ({R$ X}). Entram {R$ Y} no caixa e a taxa vira custo do mês. A taxa muda em Cadastros → Taxas.' — despesa paga no cartão não tem taxa."
    - "O exemplo 8 do protótipo (Pacote trimestral de R$ 900 em 3x) lança a primeira parcela no saldo e deixa duas em aberto: o aviso diz '· 2 parcela(s) em aberto no Caixa' e o extrato mostra '+ R$ 300,00' com '1 de 3'."
    - statement: "Com 12 parcelas no celular, a grade de parcelas (k/N, data, valor, já recebi) cabe a 320px sem rolagem horizontal."
      verification: backstop
  prohibitions:
    - statement: "A forma de pagamento nunca é gravada no documento — só na parcela (D-07)."
      status: resolved
      verification: test
    - statement: "A taxa do cartão nunca muda o preço da venda nem vira linha do documento — ela só reduz o que entra no caixa e aparece como custo Geral no Mês."
      status: resolved
      verification: test
    - statement: "O servidor nunca confia na soma calculada no cliente: recalcula total e soma das parcelas a partir dos textos recebidos."
      status: resolved
      verification: test
  artifacts:
    - lib/financeiro/calendario.ts
    - lib/financeiro/parcelas.ts
    - components/amassa/financeiro/bloco-pagamento.tsx
    - tests/e2e/financeiro-pagamento.spec.ts
  key_links:
    - "`gerarPlano` e `conferirParcelas` são a regra única do plano de parcelas: o bloco de pagamento usa as duas para mostrar, e `lancarVenda` (e `lancarDespesa`, no plano 07) usa `conferirParcelas` para recusar. A restrição adiada do banco (plano 01) é a última camada."
    - "`taxaEmCentavos` é chamada em dois lugares só: no aviso do cartão (estimativa com a taxa de hoje) e no servidor ao congelar a taxa na parcela paga. Tudo o que vem depois (extrato, Mês) lê `taxa_pontos_base` da parcela, nunca a configuração."
    - "`BlocoPagamento` recebe `tipo` ('venda' | 'despesa') e decide só rótulos e o aviso do cartão — é o mesmo componente nas duas telas (D-08)."

coverage:
  - id: D1
    description: "Planos de parcelas, fim de mês, soma, recusas, misto e taxa provados sem servidor"
    requirement: "FNC-03, FNC-05"
    verification:
      - kind: unit
        ref: "tests/unit/financeiro-calendario.test.ts, financeiro-parcelas.test.ts, financeiro-taxa.test.ts"
        status: unknown
    human_judgment: false
  - id: D2
    description: "3x, sinal, misto, cartão e a recusa do servidor com o botão habilitado à força"
    requirement: "FNC-03, FNC-05"
    verification:
      - kind: e2e
        ref: "tests/e2e/financeiro-pagamento.spec.ts — exemplos 4 e 8 do protótipo, o exemplo do dono de R$ 100 Pix + R$ 50 dinheiro, e o contorno do cliente"
        status: unknown
    human_judgment: false
---

<objective>
O pagamento da Venda completo: **à vista, sinal de 50% + saldo, ou 2x a 12x com parcelas
editáveis; pagamento misto com "+ outra forma" (D-07/D-08); e o cartão com a taxa congelada na
parcela** — com a soma conferida na tela, no servidor e no banco.

Purpose: é aqui que o dinheiro que entra hoje se separa do que entra depois. O critério 2 do
ROADMAP exige que parcelas que não fecham sejam barradas também no servidor, e o critério 3 exige
que mudar a taxa não reescreva o passado. O bloco nasce como componente único porque a Despesa
(plano 07) usa o mesmo.

Output: `calendario.ts` e `parcelas.ts` testados, os testes da taxa, o `BlocoPagamento` na Venda e
o e2e com os exemplos 4 e 8 do protótipo e o exemplo de pagamento misto do dono.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-03-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| `somarDias`, `somarMeses`, `ultimoDiaDoMes`, `primeiroDiaDoMes`, `mesSeguinte`, `mesAnterior` | `lib/financeiro/calendario.ts` |
| `PLANOS_DE_PAGAMENTO`, `gerarPlano`, `dividirEmDuasFormas`, `conferirParcelas`, tipos `PlanoDePagamento`, `ParcelaDoPlano` | `lib/financeiro/parcelas.ts` |
| `avisoDoCartao` | `lib/financeiro/taxa.ts` |
| `BlocoPagamento`, `LinhaParcela` | `components/amassa/financeiro/` |
| `data-testid`: `pagamento-plano`, `pagamento-forma`, `parcela-linha`, `parcela-paga`, `pagamento-falta`, `pagamento-aviso-cartao`, `pagamento-outra-forma` | componentes |

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O calendário, o plano de parcelas e a taxa — regras puras, testadas primeiro</name>
  <files>lib/financeiro/calendario.ts, lib/financeiro/parcelas.ts, lib/financeiro/taxa.ts, tests/unit/financeiro-calendario.test.ts, tests/unit/financeiro-parcelas.test.ts, tests/unit/financeiro-taxa.test.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 166-169 (`maisMeses`, `maisDias`), 239-243 (`gerarParcelas`), 246-247 (`taxaDe`, `liquido`), 278-284 (`pagamentoHTML`, `podeLancar`).
    - `.planning/phases/04.4-financeiro-parte-1/BRIEFING.md` §5 "Documento e parcelas" e "Cartão".
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-07 e D-08.
    - `lib/abertura/parcelas.ts` linhas 60-125 — a aritmética de calendário inteira, sem `Date` (MOLDE — não importar; o Financeiro fica depois que a Abertura sair).
    - `lib/financeiro/taxa.ts` como o plano 01 o deixou.
  </read_first>
  <behavior>
    `financeiro-calendario.test.ts`: `somarMeses('2027-01-31', 1)` → '2027-02-28'; em 2028 → '2028-02-29'; `('2027-01-31', 2)` → '2027-03-31' (conta da data original, não da anterior); `somarDias('2026-12-18', 30)` → '2027-01-17'; virada de ano nos dois sentidos; `mesSeguinte('2026-12')` → '2027-01'; `mesAnterior('2027-01')` → '2026-12'; `ultimoDiaDoMes(2027, 2)` → 28.
    `financeiro-parcelas.test.ts`:
    - À vista R$ 150,00 em 2026-12-18 → [15000 paga em 2026-12-18].
    - Sinal R$ 150,01 → [7501 paga na data, 7500 em aberto +30 dias]; sinal R$ 150,00 → [7500, 7500].
    - 3x R$ 100,00 → [3334 paga, 3333, 3333] com vencimentos mensais; 3x R$ 900,00 → [30000, 30000, 30000]; 12x R$ 1,00 → primeira 12 e as outras onze de 8 (base 8, soma 100).
    - 2x de 1 centavo → recusa "valor pequeno demais para dividir em 2 vezes".
    - `dividirEmDuasFormas(15000, 10000)` → [10000, 5000], ambas pagas na data; segundo valor negativo ou zero → recusa.
    - `conferirParcelas`: soma igual → ok; soma menor → falta com a frase exata; maior → sobra; parcela zero → recusa; 13 parcelas → recusa; paga com vencimento depois de hoje → recusa com a frase; paga antes do saldo inicial → recusa; sem saldo inicial → sem essa regra.
    `financeiro-taxa.test.ts`: 15000 a 350 → 525; 9900 a 350 → 347 (meio centavo arredonda para cima); taxa 0 → 0; `liquidoDaParcela` de despesa no cartão → valor inteiro; `avisoDoCartao` com parcelas [cartão 10000, dinheiro 5000] a 350 → taxa 350, entram 14650, e nulo quando nenhuma parcela é cartão ou o documento é despesa.
  </behavior>
  <action>
Testes primeiro. **`calendario.ts`**: aritmética de data civil em inteiros (ano, mês, dia) sobre texto `YYYY-MM-DD`, sem `Date`, reescrita a partir do molde de `lib/abertura/parcelas.ts` (não importar). **`parcelas.ts`**: `PLANOS_DE_PAGAMENTO` (`avista`, `sinal`, `2`, `3`, `4`, `6`, `10`, `12`), `gerarPlano({ plano, totalCentavos, data, forma })`, `dividirEmDuasFormas(totalCentavos, primeiroValorCentavos, data, formas)`, `conferirParcelas({ totalCentavos, parcelas, hoje, dataSaldoInicial })` com resultado discriminado e as frases do §Copywriting. **`taxa.ts`**: `avisoDoCartao({ tipo, parcelas, taxaPontosBase })`. Nenhum dos três importa React, Next, o cliente do banco, `lib/abertura` ou `lib/cotacoes`, nem constrói `Date`. Nenhuma invocação de e2e.
  </action>
  <verify>
    <automated>npm test -- tests/unit/financeiro-calendario.test.ts tests/unit/financeiro-parcelas.test.ts tests/unit/financeiro-taxa.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os três testes passam, cada caso de `<behavior>` nomeado.
    - `grep -En "from \"(react|next|drizzle-orm|pg)|@/db|@/lib/abertura|@/lib/cotacoes" lib/financeiro/calendario.ts lib/financeiro/parcelas.ts lib/financeiro/taxa.ts` não encontra nada.
    - `grep -n "new Date\|Date.now" lib/financeiro/calendario.ts lib/financeiro/parcelas.ts lib/financeiro/taxa.ts` não encontra nada.
  </acceptance_criteria>
  <done>O plano de parcelas, o fim de mês e a taxa do cartão têm uma regra só, provada em milissegundos.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O bloco de pagamento na Venda — parcelado, sinal, "+ outra forma" e cartão, recusado no servidor quando não fecha</name>
  <files>lib/financeiro/esquemas.ts, lib/financeiro/acoes.ts, lib/financeiro/textos.ts, components/amassa/financeiro/bloco-pagamento.tsx, components/amassa/financeiro/linha-parcela.tsx, components/amassa/financeiro/painel-venda.tsx, tests/e2e/financeiro-pagamento.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Pagamento misto — '+ outra forma' (D-07/D-08) — UI nova", §Copywriting (falta/sobra, aviso do cartão), §UI Considerations (caixa de marcação de 44×44).
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 278-284 e 398.
    - `lib/financeiro/acoes.ts`, `esquemas.ts`, `painel-venda.tsx` como os planos 01 e 03 os deixaram.
    - `tests/e2e/apoio/semear-financeiro.ts` — `semearItem`, `garantirTaxaDeTeste`, `TAXA_DE_TESTE`.
    - `tests/e2e/financeiro-venda.spec.ts` — como os itens são semeados e achados pela busca.
  </read_first>
  <action>
**Servidor.** `lancarVenda` passa a usar `conferirParcelas` (com `hoje` de `hojeEmBrasilia` e `data_saldo_inicial` da configuração) no lugar da conferência simples do plano 01, e congela `taxa_pontos_base` só nas parcelas de venda pagas no cartão. Nada de forma no documento (D-07). O texto do aviso de lançamento já conta as parcelas em aberto.

**`BlocoPagamento`** (cliente; props `tipo`, total, data, taxa, estado controlado pelo painel): "Como recebe"/"Como paga" (select com os planos; rótulo do segundo por tipo), "Forma" (Dinheiro · Pix · Cartão), e — fora do à vista e com total maior que zero — uma `LinhaParcela` por parcela: "k/N", data (`type="date"`), valor (`inputmode="decimal"`), e a caixa nativa "já recebi"/"já paguei" com zona de toque de 44×44. No à vista, abaixo da forma, o botão secundário "+ outra forma" (nunca terracota) troca para duas linhas no mesmo grid, cada uma com valor e forma, e o link "tirar" (estilo do "tirar" de linha) volta a uma forma. O aviso do cartão (`avisoDoCartao`) aparece só em venda com parcela no cartão. A mensagem de falta/sobra (`conferirParcelas`) aparece em fundo de erro. `PainelVenda` usa o bloco, habilita "Lançar venda" só quando `conferirParcelas` dá certo, e o plano e as parcelas entram no rascunho. A grade das parcelas cabe a 320px (as quatro colunas encolhem; nada de rolagem da página).

**E2E** (`tests/e2e/financeiro-pagamento.spec.ts`, "financeiro pagamento" no título; itens semeados com sufixo único): **exemplo 8** — Pacote trimestral R$ 900 em 3x → três linhas de parcela, a primeira marcada; lança; o aviso termina em "· 2 parcela(s) em aberto no Caixa"; o extrato mostra "+ R$ 300,00" com "1 de 3". **Sinal** de R$ 150,01 → R$ 75,01 + R$ 75,00. **Exemplo 4 no cartão** — `garantirTaxaDeTeste()`; Bloco de papel + Pincel nº 8 + Tinta amarela = R$ 99,00, forma Cartão → o aviso diz 3,5% (R$ 3,47) e "Entram R$ 95,53"; lança; o extrato mostra "+ R$ 95,53" e "taxa R$ 3,47". **Misto (o exemplo do dono)** — valor livre R$ 150,00, "+ outra forma", Pix R$ 100,00 e Dinheiro R$ 50,00 → lança; o extrato mostra as duas linhas do mesmo número, uma "Pix" e outra "Dinheiro"; misto cartão R$ 100 + dinheiro R$ 50 → aviso com R$ 3,50. **Falta/sobra** — em 3x, muda a segunda parcela para R$ 250,00 → a frase "…Sobram R$ 50,00 para fechar com o total." e o botão desabilitado; **contorno do cliente** — com `page.evaluate` tira o `disabled` do botão e clica: a frase do SERVIDOR aparece, a URL não ganha `aviso=` e nada foi lançado. **Paga no futuro** — marca "já recebi" na terceira parcela → frase da parcela que vence depois de hoje, botão desabilitado. A 320px, com 12x, a página não rola na horizontal e a caixa "já recebi" tem 44px de zona de toque.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro pagamento"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro pagamento" passa nos dois projetos, incluindo o caso do contorno do cliente em que a frase vem do servidor.
    - `grep -n "conferirParcelas" lib/financeiro/acoes.ts components/amassa/financeiro/bloco-pagamento.tsx` encontra a mesma função nos dois lados.
    - `grep -n "forma" db/schema.ts` mostra a coluna de forma só em `parcelas` (e `forma_prevista`), nunca em `documentos`.
    - `grep -n "taxaEmCentavos" lib/financeiro/acoes.ts` mostra o congelamento só para parcela de venda paga no cartão.
  </acceptance_criteria>
  <done>A venda parcelada, a de sinal, a dividida entre Pix e dinheiro e a do cartão lançam o dinheiro certo no dia certo — e a que não fecha não passa nem forçando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `lancarVenda` (parcelas) | Datas, valores, formas e marcações de pago vêm do cliente, e o botão pode ser habilitado à força |
| configuração → parcela | A taxa vigente é congelada no instante do pagamento |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-38 | Tampering | soma das parcelas | high | mitigate | `conferirParcelas` no servidor com o total recalculado; restrição adiada no banco; e2e prova o contorno do botão |
| T-04.4-39 | Tampering | taxa do cartão | high | mitigate | Taxa lida da configuração no servidor e congelada na parcela; o cliente nunca manda taxa |
| T-04.4-40 | Tampering | pagamento no futuro / antes do saldo inicial | medium | mitigate | `conferirParcelas` recusa parcela paga depois de hoje ou antes de `data_saldo_inicial` |
| T-04.4-41 | Denial of Service | número de parcelas | low | mitigate | 1-12 parcelas no Zod |
| T-04.4-42 | Elevation of Privilege | `lancarVenda` | high | mitigate | `exigirUsuario()` primeira instrução; `verificar-acoes` |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Um recorte "financeiro pagamento" neste plano (Tarefa 2).
- Os exemplos 4 e 8 do protótipo têm caso próprio no e2e.
</verification>

<success_criteria>
- À vista, sinal + saldo, 2x a 12x com parcelas editáveis; falta/sobra na tela e recusa no servidor (FNC-03, critério 2 do ROADMAP).
- Forma por parcela e "+ outra forma" (D-07/D-08).
- Taxa do cartão congelada na parcela (FNC-05, critério 3 do ROADMAP, na parte do lançamento).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-06-SUMMARY.md` when done
</output>
