---
phase: 04.4-financeiro-parte-1
plan: 09
type: execute
wave: 6
depends_on: ["04.4-08"]
files_modified:
  - lib/financeiro/extrato.ts
  - lib/financeiro/mes.ts
  - lib/financeiro/abas.ts
  - tests/unit/financeiro-extrato.test.ts
  - tests/unit/financeiro-mes.test.ts
  - components/amassa/financeiro/abas-financeiro.tsx
  - lib/financeiro/consultas.ts
  - lib/financeiro/textos.ts
  - app/(app)/financeiro/page.tsx
  - components/amassa/financeiro/navegacao-mes.tsx
  - components/amassa/financeiro/extrato-caixa.tsx
  - components/amassa/financeiro/painel-mes.tsx
  - components/amassa/financeiro/tabela-areas.tsx
  - components/amassa/financeiro/regua-mes.tsx
  - tests/e2e/apoio/mes-reservado.ts
  - tests/e2e/financeiro-extrato.spec.ts
  - tests/e2e/financeiro-mes.spec.ts
autonomous: true
requirements: [FNC-05, FNC-09, FNC-11, FNC-17]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 180000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "O extrato do Caixa ('O que já entrou e saiu') navega por mês com ◀ nome do mês ▶, o mesmo componente da tela Mês, e tem filtro por forma Todas · Dinheiro · Pix · Cartão (`aria-pressed`, 44px), ambos na URL (`?aba=caixa&mes=AAAA-MM&forma=dinheiro`) — D-11; não existe busca por texto nesta fase."
    - "Com o filtro aplicado, o 'saldo depois' de cada linha continua o saldo acumulado GLOBAL — o filtro esconde linhas, não recalcula saldo (D-12)."
    - "Com uma forma escolhida, aparece 'Total em {forma} neste mês: {+/− R$ X}' = entradas menos saídas visíveis e não canceladas, o caso da gaveta ('quanto entrou em dinheiro?'); com 'Todas', o total não aparece (resolução do Claude's Discretion de D-11)."
    - "Um mês sem movimento mostra 'Nada neste mês ainda.'; um mês com movimento mas nada na forma escolhida mostra 'Nada neste mês, nesta forma.'; a régua de 'últimos 40 movimentos' do protótipo não existe — todos os movimentos do mês aparecem."
    - "A ordem do saldo é (data de pagamento, número do documento, número da parcela); um lançamento retroativo recalcula o 'saldo depois' de todos os movimentos posteriores, inclusive de meses seguintes (FNC-09); dentro de uma mesma tela, a diferença entre o saldo depois de dois movimentos consecutivos é exatamente o valor com sinal do mais novo."
    - "A aba Mês existe (`?aba=mes&mes=AAAA-MM`, padrão o mês de hoje; mês inválido cai no de hoje) e navega com ◀ ▶ de 44px com `aria-label` 'mês anterior'/'mês seguinte'."
    - "'Quanto cada área deixou' mostra SEMPRE as quatro áreas na ordem Cafeteria · Espaço · Peças · Loja, com Vendeu, Custou e Deixou (R$ 0,00 quando não houve nada), a linha 'Juntas' no rodapé, e 'Deixou' negativo na cor de erro — vendeu e custou contam pela DATA DO DOCUMENTO (FNC-11)."
    - "O Geral aparece num bloco só, sem dividir entre áreas: cada categoria do grupo geral com o valor do mês, mais a linha calculada 'Taxa do cartão' = soma das taxas das parcelas de venda pagas no mês (pela data de pagamento); vazio → 'Nenhum custo geral lançado neste mês.' — nenhum rateio, nenhum nível de custo."
    - "A linha de diferença entra no Geral pela categoria 'Juros, multas e descontos': em despesa, juros somam ao custo e desconto subtrai; em venda, receber a menos soma ao custo e receber a mais subtrai (D-02)."
    - "O veredito é 'Sobrou R$ X no mês.' (verde) quando o que as áreas deixaram menos o Geral é zero ou mais, e 'Faltaram R$ X para o mês se pagar.' (vermelho) quando é negativo — é o foco visual da tela."
    - "'Dinheiro que se mexeu' mostra 'Entrou no caixa' (líquido das parcelas de venda pagas no mês) e 'Saiu do caixa' (parcelas de despesa pagas no mês), pela DATA DE PAGAMENTO, com a dica do protótipo — são dois quadros separados de propósito."
    - "'Fora do resultado' lista as categorias do grupo Fora com sinal: '− R$ X' para o que saiu (equipamento, financiamento, retirada) e '+ R$ X' para o que entrou (aporte), sem entrar no veredito; vazio → 'Nada neste mês.' (suposição sinalizada no plano 03)."
    - "Documento cancelado não entra em nenhum número do Mês nem do saldo; documento datado no último dia do mês conta naquele mês e no primeiro dia do mês seguinte, no seguinte."
    - "Uma venda de três itens de áreas diferentes paga no Pix aparece no Mês com cada linha na sua área (critério 1 do ROADMAP, a parte que a máquina prova; o tempo de 20 segundos é conferido pelo dono no plano 11)."
    - "A barra do Financeiro tem as cinco pílulas Venda · Despesa · Caixa · Mês · Cadastros e, a 320px, não rola na horizontal: a pílula quebra em duas linhas, nunca trunca, e mede 44px ou mais."
    - "A tabela 'Quanto cada área deixou' em tela estreita rola DENTRO do próprio contêiner, nunca a página; os cabeçalhos são Apoio 14px/500 sem caixa alta."
  prohibitions:
    - statement: "Nenhum custo geral é dividido entre as áreas, e nenhum 'nível de custo' existe — o Geral é um bloco só."
      status: resolved
      verification: test
    - statement: "As palavras 'competência', 'regime de caixa', 'rateio' e 'nível de custo' não aparecem em nenhuma cópia da interface."
      status: resolved
      verification: test
    - statement: "Nenhum teste e2e do extrato ou do Mês afirma um total global do banco: cada teste escreve num mês reservado só para ele, por projeto, e só compara números daquele mês ou relações dentro da mesma tela."
      status: resolved
      verification: test
  artifacts:
    - lib/financeiro/mes.ts
    - lib/financeiro/extrato.ts
    - components/amassa/financeiro/painel-mes.tsx
    - components/amassa/financeiro/extrato-caixa.tsx
    - tests/e2e/apoio/mes-reservado.ts
    - tests/e2e/financeiro-mes.spec.ts
  key_links:
    - "`resumoDoMes` recebe documentos do mês (pela data do documento) E parcelas pagas no mês (pela data de pagamento) separadamente — são as duas réguas do briefing §5, e misturar as duas numa lista só é o erro que este desenho impede."
    - "O sinal de cada linha no Mês sai de uma regra só (valor com sinal do tipo do documento, aplicado conforme o grupo da categoria), o que cobre venda, despesa, diferença para mais e para menos, e aporte, sem caso especial por categoria."
    - "`filtrarExtrato` recebe as linhas JÁ com o saldo global de `montarExtrato`; ele só escolhe quais mostrar — é assim que D-12 fica verdadeiro por construção."
    - "`mes-reservado.ts` é o isolamento que CLAUDE.md exige para teste que olha número agregado: cada teste tem um mês só dele por projeto, conferido por uma guarda de unicidade no próprio arquivo."

coverage:
  - id: D1
    description: "Extrato por mês e forma, saldo global com filtro, total filtrado, recálculo retroativo"
    requirement: "FNC-09"
    verification:
      - kind: unit
        ref: "tests/unit/financeiro-extrato.test.ts"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/financeiro-extrato.spec.ts"
        status: unknown
    human_judgment: false
  - id: D2
    description: "Mês: quatro áreas, Geral sem dividir entre áreas, com taxa e diferença, veredito, dinheiro que se mexeu, fora com sinal, cancelado fora"
    requirement: "FNC-11, FNC-05"
    verification:
      - kind: unit
        ref: "tests/unit/financeiro-mes.test.ts"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/financeiro-mes.spec.ts"
        status: unknown
    human_judgment: false
---

<objective>
O que o dono pergunta no fim do dia e no fim do mês: **o extrato por mês com filtro por forma
(D-11/D-12)** — "quanto entrou em dinheiro?" — e **a tela Mês**: quanto cada área vendeu, custou e
deixou, o Geral num bloco só, sem dividir entre áreas, o veredito "sobrou/faltou", o dinheiro que se mexeu e o
que ficou fora do resultado (FNC-11).

Purpose: o objetivo da fase termina aqui — "saber no fim do mês quanto cada área deixou e se o mês
se pagou". As duas regras (data do documento para o resultado, data de pagamento para o dinheiro)
ficam em módulos puros, e o e2e escreve em meses reservados para não disputar número com o resto
da suíte.

Output: `mes.ts` e o extrato completo, testados; a navegação por mês; o filtro do extrato; a tela
Mês; a quinta pílula; e o auxiliar de mês reservado.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-08-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| `filtrarExtrato`, tipo `FiltroDoExtrato` | `lib/financeiro/extrato.ts` |
| `resumoDoMes`, `larguraDaRegua`, tipos `ResumoDoMes`, `AreaDoMes` | `lib/financeiro/mes.ts` |
| Aba `mes`, `mesDaUrl`, `formaDaUrl` | `lib/financeiro/abas.ts`, `components/amassa/financeiro/abas-financeiro.tsx` |
| `listarDocumentosDoMes`, `listarParcelasPagasNoMes` | `lib/financeiro/consultas.ts` |
| `NavegacaoMes`, `PainelMes`, `TabelaAreas`, `ReguaMes` | `components/amassa/financeiro/` |
| `MESES_RESERVADOS`, `mesReservado`, `diaDoMes` | `tests/e2e/apoio/mes-reservado.ts` |
| `data-testid`: `mes-nav`, `mes-area-<area>`, `mes-juntas`, `mes-veredito`, `mes-geral-linha`, `mes-entrou`, `mes-saiu`, `mes-fora-linha`, `extrato-filtro-<forma>`, `extrato-total-filtrado` | componentes |

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: As duas réguas do mês e o extrato filtrado — regras puras, testadas primeiro</name>
  <files>lib/financeiro/extrato.ts, lib/financeiro/mes.ts, lib/financeiro/abas.ts, tests/unit/financeiro-extrato.test.ts, tests/unit/financeiro-mes.test.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 327-332 (extrato) e 334-351 (`contasDoMes`, `telaMes`).
    - `.planning/phases/04.4-financeiro-parte-1/BRIEFING.md` §5 "Mês", "Extrato", "Cartão", "Categorias".
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-02, D-11, D-12.
    - `lib/financeiro/extrato.ts`, `taxa.ts`, `calendario.ts` e `tests/unit/financeiro-extrato.test.ts` como os planos anteriores os deixaram.
  </read_first>
  <behavior>
    `financeiro-extrato.test.ts` (acréscimos) — `filtrarExtrato(linhas, { mes, forma })`:
    - Devolve só as linhas pagas no mês, da mais recente para a mais antiga, com o `saldoDepoisCentavos` intacto.
    - Com forma `dinheiro`, esconde Pix e Cartão e NÃO muda o saldo depois de nenhuma linha (D-12).
    - Total filtrado = entradas líquidas − saídas das linhas visíveis não canceladas; cancelada visível não entra; forma `todas` → total nulo.
    - Mês sem linhas → lista vazia e motivo "sem-movimento"; mês com linhas mas nenhuma na forma → motivo "sem-movimento-na-forma".
    - Movimento retroativo num mês anterior muda o saldo depois das linhas do mês seguinte.
    `financeiro-mes.test.ts` — `resumoDoMes({ mes, documentos, parcelasPagas })`:
    - Venda de três linhas (Bebidas e comidas, Peças prontas, Materiais e papelaria) → Vendeu em Cafeteria, Peças e Loja; Espaço com R$ 0,00; as quatro áreas sempre presentes na ordem fixa.
    - Despesa de custo da Loja → Custou da Loja; Deixou = Vendeu − Custou, podendo ser negativo.
    - Despesa de Aluguel → Geral "Aluguel"; duas despesas de Aluguel no mês → somadas numa linha.
    - Parcela de venda paga no cartão no mês com taxa → linha "Taxa do cartão" no Geral; paga no mês seguinte → não entra neste mês mesmo com o documento neste mês.
    - Linha de diferença: despesa + R$ 12 → Geral +12; despesa − R$ 10 → Geral −10; venda recebida com − R$ 5 → Geral +5; venda com + R$ 5 → Geral −5.
    - Veredito: deixou 1000 − geral 400 → "sobrou" 600; deixou 100 − geral 400 → "faltou" 300; exatamente zero → "sobrou" 0.
    - Dinheiro que se mexeu: entrou = líquido das parcelas de venda pagas no mês; saiu = parcelas de despesa pagas no mês; parcela de documento de outro mês paga neste mês entra aqui e não em Vendeu.
    - Fora: "Equipamento e obra" de despesa → −v; "Aporte dos sócios" de venda → +v; nenhum dos dois mexe no veredito.
    - Documento cancelado não entra em nada; documento do dia 31 conta no mês, do dia 1 do mês seguinte não.
    - `larguraDaRegua`: percentuais relativos a max(deixou total, geral, 1); área com deixou negativo → 0%.
    `abas.ts`: `mesDaUrl('2021-03', hoje)` → '2021-03'; `'2021-13'`, `'x'`, ausente → mês de hoje; `formaDaUrl` reconhece as três formas e cai em `todas`.
  </behavior>
  <action>
Testes primeiro. **`mes.ts`** (puro): `resumoDoMes` aplica UMA regra de sinal — valor com sinal do tipo (venda +, despesa −) — conforme o grupo da categoria da linha: receita soma em Vendeu da área; custo soma o oposto em Custou da área; geral soma o oposto no Geral por nome de categoria; fora soma no Fora por nome, com o próprio sinal. Taxa do cartão só pelas parcelas pagas no mês. Geral e Fora ordenados por nome, com "Taxa do cartão" por último. Documentos e parcelas pagas chegam em listas separadas (data do documento × data de pagamento). **`extrato.ts`**: `filtrarExtrato`. **`abas.ts`**: aba `mes`, `mesDaUrl`, `formaDaUrl`. Nenhum import de React, Next, cliente do banco, `lib/abertura` ou `lib/cotacoes`; sem `Date`. Nenhuma invocação de e2e.
  </action>
  <verify>
    <automated>npm test -- tests/unit/financeiro-extrato.test.ts tests/unit/financeiro-mes.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os dois testes passam, cada caso de `<behavior>` nomeado, incluindo "filtro não muda o saldo depois" (D-12) e os quatro sinais da linha de diferença (D-02).
    - `grep -En "from \"(react|next|drizzle-orm|pg)|@/db|@/lib/abertura|@/lib/cotacoes" lib/financeiro/mes.ts lib/financeiro/extrato.ts lib/financeiro/abas.ts` não encontra nada.
    - `grep -rin "rateio\|nível de custo\|competência\|regime de caixa" lib/financeiro/mes.ts` só encontra comentário explicando que não existe — nenhuma função com esses nomes.
  </acceptance_criteria>
  <done>"Quanto cada área deixou" e "quanto entrou em dinheiro" têm regra única, provada sem servidor.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O extrato por mês e por forma — conferir a gaveta</name>
  <files>lib/financeiro/consultas.ts, lib/financeiro/textos.ts, app/(app)/financeiro/page.tsx, components/amassa/financeiro/navegacao-mes.tsx, components/amassa/financeiro/extrato-caixa.tsx, tests/e2e/apoio/mes-reservado.ts, tests/e2e/financeiro-extrato.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Extrato — navegação por mês e filtro por forma (D-11/D-12) — UI nova" e o vazio do extrato no §Copywriting.
    - `components/amassa/financeiro/extrato-caixa.tsx` e `app/(app)/financeiro/page.tsx` como o plano 08 os deixou.
    - `playwright.config.ts` — os nomes dos projetos (`desktop`, `celular`, `vazio-*`).
    - `.claude/CLAUDE.md` §Conventions — teste não afirma condição global do banco sem isolamento.
    - `tests/e2e/financeiro-venda.spec.ts` — como lançar uma venda com data retroativa pela tela.
  </read_first>
  <action>
**`tests/e2e/apoio/mes-reservado.ts`.** `MESES_RESERVADOS`: um registro por teste que olha número agregado, com um mês para `desktop` e outro para `celular`, todos entre 2021 e 2025 (antes de qualquer dado real, depois do piso de 2020 do Zod); uma guarda no carregamento do módulo lança erro se dois registros repetirem mês. `mesReservado(chave, nomeDoProjeto)` e `diaDoMes(mes, dia)`. Comentário explicando por que existe (CLAUDE.md §Conventions) e que planos futuros acrescentam registros aqui, nunca reaproveitam um mês.

**Tela.** `page.tsx` lê `mes` e `forma` com `mesDaUrl`/`formaDaUrl` e passa ao extrato as linhas de `montarExtrato` (globais) filtradas por `filtrarExtrato`. `NavegacaoMes`: ◀ nome do mês ▶ como links (`?aba=…&mes=…` preservando a forma), 44px, `aria-label` "mês anterior"/"mês seguinte", nome do mês em Título. `ExtratoCaixa`: acima da lista, `NavegacaoMes` e as pílulas Todas · Dinheiro · Pix · Cartão (links, `aria-pressed`, 44px, acento só na marcada); "Total em {forma} neste mês: {+/− R$ X}" quando há forma; os dois vazios da cópia. Os tiles continuam mostrando o saldo global atual, qualquer que seja o mês do extrato.

**E2E** (`tests/e2e/financeiro-extrato.spec.ts`, "financeiro extrato" no título), cada caso no seu mês reservado: lança pela tela três vendas de valor livre no mês (dia 10 Pix R$ 100, dia 12 Dinheiro R$ 50, dia 15 Pix R$ 30) e uma despesa (dia 14 Dinheiro R$ 20); abre `?aba=caixa&mes=<mês>`: as quatro linhas em ordem do mais recente, e a diferença entre o saldo depois de linhas consecutivas é o valor com sinal da mais nova; lança uma venda retroativa no dia 5 (R$ 7) e confere que o saldo depois de TODAS as quatro linhas aumentou exatamente R$ 7,00; filtro Dinheiro → só as duas de dinheiro, com os MESMOS saldos depois de antes do filtro, e "Total em Dinheiro neste mês: + R$ 30,00"; filtro Cartão → "Nada neste mês, nesta forma."; ◀ para o mês anterior (vazio) → "Nada neste mês ainda."; ▶ volta. A 320px o extrato não rola na horizontal.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro extrato"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro extrato" passa nos dois projetos, cada um no próprio mês reservado.
    - O caso do filtro confere que os saldos depois são idênticos com e sem filtro (D-12).
    - `grep -n "slice(0, *40)\|últimos 40" components/amassa/financeiro/extrato-caixa.tsx lib/financeiro/extrato.ts` não encontra nada.
    - `grep -rn "busca\|search" components/amassa/financeiro/extrato-caixa.tsx` não encontra campo de busca (D-11).
  </acceptance_criteria>
  <done>No fim do dia, o gestor escolhe o mês e "Dinheiro" e sabe quanto a gaveta tem de ter mudado — sem o saldo mentir.</done>
</task>

<task type="auto">
  <name>Tarefa 3: A tela Mês — quanto cada área deixou, o Geral num bloco só e o veredito</name>
  <files>lib/financeiro/consultas.ts, lib/financeiro/textos.ts, app/(app)/financeiro/page.tsx, components/amassa/financeiro/abas-financeiro.tsx, components/amassa/financeiro/painel-mes.tsx, components/amassa/financeiro/tabela-areas.tsx, components/amassa/financeiro/regua-mes.tsx, tests/e2e/apoio/mes-reservado.ts, tests/e2e/financeiro-mes.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 113-133 (CSS de `.mes-nav`, `table`, `.regua`, `.barra`, `.veredito`, `.lista-simples`) e 340-351 (`telaMes`).
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §Typography (as duas linhas de Apoio), §Foco Visual (Mês), §Copywriting (vazios do Mês), §Responsivo, §UI Considerations (partial do Mês e overflow da tabela).
    - `lib/financeiro/mes.ts` (Tarefa 1), `navegacao-mes.tsx` (Tarefa 2), `abas-financeiro.tsx`.
    - `tests/e2e/apoio/semear-financeiro.ts`, `mes-reservado.ts` e `tests/e2e/financeiro-pagamento.spec.ts` (venda no cartão).
  </read_first>
  <action>
**Servidor.** `listarDocumentosDoMes(mes)`: documentos não cancelados com data no mês, com as linhas e grupo, área e nome da categoria de cada uma; `listarParcelasPagasNoMes(mes)`: parcelas pagas no mês de documentos não cancelados, com tipo, valor e taxa — duas consultas.

**Tela.** A pílula "Mês" entra entre Caixa e Cadastros. `PainelMes`: `NavegacaoMes` no topo; duas colunas a partir de 980px. Esquerda, bloco "Quanto cada área deixou": `TabelaAreas` — `table` semântica dentro de um contêiner com rolagem horizontal PRÓPRIA; cabeçalhos "Área", "Vendeu", "Custou", "Deixou" em Apoio 14px/500 sem caixa alta e sem espaçamento; uma linha por área com o ponto de cor; Deixou negativo em `--color-erro`; `tfoot` "Juntas"; números com `tabular-nums` e sem quebra; e a dica do protótipo sobre o "Custou". Direita, bloco "As áreas pagam a casa?": `ReguaMes` com "O que as áreas deixaram" + barra segmentada pelas cores de área e "Geral — custos da casa, sem dividir" + barra de `--color-area-geral` (larguras de `larguraDaRegua`, atributo `title` com a área); o veredito (fundo `--color-sucesso`/`--color-erro` claros, texto forte, a única cor cheia da tela); a lista do Geral ou "Nenhum custo geral lançado neste mês.". Segunda fileira: "Dinheiro que se mexeu" (Entrou no caixa em verde, Saiu do caixa em vermelho, e a dica do protótipo) e "Fora do resultado" (cada categoria com "+"/"−" e a cor do extrato, ou "Nada neste mês.", e a dica do protótipo).

**E2E** (`tests/e2e/financeiro-mes.spec.ts`, "financeiro mes" no título), cada caso num mês reservado novo (acrescente os registros em `mes-reservado.ts`): **áreas (critério 1 do ROADMAP)** — no celular e no desktop, uma venda de três itens semeados de Cafeteria, Peças e Loja, paga no Pix, datada no mês → o Mês mostra cada valor na sua área, Espaço com R$ 0,00, e "Juntas" com a soma; **custo e veredito** — uma compra de material da Loja e uma outra despesa de Aluguel no mês → Custou da Loja, Deixou certo, o Aluguel no Geral e o veredito com o valor calculado; **taxa** — `garantirTaxaDeTeste()` e uma venda no cartão no mês → "Taxa do cartão" no Geral com a taxa da parcela; **fora** — uma outra despesa de "Equipamento e obra" e um valor livre de "Aporte dos sócios" → Fora com "− R$" e "+ R$", veredito inalterado; **cancelado** — cancelar uma das vendas do mês tira o valor dela do Mês; **fronteira** — uma venda no último dia do mês reservado aparece nele e não no seguinte (▶); **mês vazio** — um mês reservado sem nada mostra as quatro áreas zeradas, "Nenhum custo geral lançado neste mês.", "Nada neste mês." e "Sobrou R$ 0,00 no mês.". A 320px: a barra de cinco pílulas não rola na horizontal e cada pílula mede 44px ou mais; a página do Mês não rola na horizontal (a tabela pode rolar dentro do próprio contêiner).

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro mes"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro mes" passa nos dois projetos, cada caso no próprio mês reservado.
    - `grep -rinP "[\"'\x60>][^\"'\x60<]*(competência|regime de caixa|rateio|nível de custo)" components/amassa/financeiro lib/financeiro/textos.ts | grep -vE ":[0-9]+:[[:space:]]*(//|[*]|[{]/[*])"` não encontra nada — a busca olha só texto entre aspas, crases ou de JSX (a cópia visível), ignorando linhas de comentário e nomes de identificador.
    - O caso das áreas afirma Vendeu por área para uma venda de três áreas paga no Pix (critério 1 do ROADMAP, parte automatizável).
    - `grep -n "overflow-x-auto" components/amassa/financeiro/tabela-areas.tsx` encontra a rolagem própria da tabela.
  </acceptance_criteria>
  <done>No fim do mês, o dono abre uma tela e sabe quanto cada área deixou, quanto a casa custou, e se o mês se pagou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?mes=`/`?forma=` → consultas | Parâmetros de leitura vindos do cliente |
| navegador não autenticado → Mês e extrato | O resultado financeiro do ateliê |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-56 | Information Disclosure | aba Mês e extrato | high | mitigate | `exigirUsuario()` primeira instrução em `layout.tsx` e `page.tsx` (já em vigor) |
| T-04.4-57 | Tampering / Injection | `?mes=`, `?forma=` | medium | mitigate | `mesDaUrl`/`formaDaUrl` validam por expressão fechada e caem no padrão; consultas por parâmetro do Drizzle, nunca texto concatenado |
| T-04.4-58 | Denial of Service | mês muito distante | low | accept | Um mês é um recorte pequeno de parcelas e documentos; sem dado, as listas vêm vazias |
| T-04.4-59 | Tampering | números do Mês | medium | mitigate | Regra única em `resumoDoMes`, provada em teste unitário, alimentada só por documentos não cancelados |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Dois recortes neste plano: "financeiro extrato" (Tarefa 2) e "financeiro mes" (Tarefa 3).
- Nenhum teste deste plano afirma total global do banco.
</verification>

<success_criteria>
- O extrato navega por mês, filtra por forma e mantém o saldo global (D-11/D-12, FNC-09).
- O Mês mostra as áreas, o Geral num bloco só com a taxa do cartão, o veredito, o dinheiro que se mexeu e o fora do resultado (FNC-11, critérios 1 e 3 do ROADMAP na parte do Mês).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-09-SUMMARY.md` when done
</output>
