---
phase: 04.4-financeiro-parte-1
verified: 2026-09-26T00:00:00Z
status: passed
score: 9/9 must-haves verificados (critérios do ROADMAP) + 17/17 FNC
behavior_unverified: 0
overrides_applied: 0
open_items_non_blocking:
  - id: OPEN-01
    ref: "WINDOWS #32"
    file: "tests/e2e/financeiro-extrato.spec.ts:212"
    summary: "Flaky só sob a varredura completa sem --grep (contenção de servidor Next único, mesma classe de #12/#21/#22/#29/#30/#31) — isolado, passa 32/32. Status open no WINDOWS.md."
    severity: qualidade (não bloqueia nenhum critério — não é regressão determinística)
  - id: OPEN-02
    ref: "WINDOWS #3"
    file: "tests/e2e/autenticacao.spec.ts"
    summary: "Bloqueio de login intermitente sob carga da suíte completa. Hipótese do custo do argon2id MEDIDA e REFUTADA (25-150ms/tentativa) em .planning/debug/auth-bloqueio-timeout-e2e.md. Não é do escopo desta fase (Fase 2a/02b), continua open."
    severity: pré-existente, fora do escopo da Fase 04.4
  - id: OPEN-03
    ref: "WINDOWS #34"
    file: "lib/auth/tentativas-memoria.ts"
    summary: "O contador de tentativas de login não parece compartilhado entre a rota REST do Auth.js e a Server Action de login nesta build (Next.js 16.3.5 + Turbopack, output: standalone) — achado novo, não confirmado como singleton real. Fora do escopo desta fase."
    severity: pré-existente, fora do escopo da Fase 04.4
  - id: OPEN-04
    ref: "REQUIREMENTS.md:403-419"
    file: ".planning/REQUIREMENTS.md"
    summary: "A tabela auxiliar de rastreabilidade marca FNC-01/04/12/16 como \"Pending\" apesar de a lista primária (com checkbox, linhas 179-195) já marcá-los [x] e implementados/confirmados no código. Mesmo padrão de tabela desatualizada já achado como DOC-01 em 04.3-VERIFICATION.md — não indica lacuna de implementação."
    severity: documentação (não bloqueia)
deferred_to_financeiro_2:
  - "Somar o desconto do mês inteiro (pedido do dono na conferência de 20/09, item 11 da Seção C)"
  - "Saldo do Caixa considerando o mês anterior e aportes de capital de giro (pedido do dono, item 13 da Seção C)"
---

# Phase 04.4: Financeiro — parte 1 — Verification Report

**Phase Goal:** O financeiro do dia a dia, usado por duas pessoas no celular, de pé: lançar uma
venda com várias linhas num recebimento só, lançar despesa, ver o caixa e o que está a pagar e a
receber, e saber no fim do mês quanto cada área deixou e se o mês se pagou — substituindo planilha
e caderno antes da inauguração de dezembro.
**Verified:** 2026-09-26
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

O objetivo da fase está alcançado no código, provado por 893 testes unitários (`npx vitest run`,
todos passando), pelo `npm run test:migracoes` contra Postgres real (inclui `conferirFinanceiro` e
`conferirImportacaoDaVirada`, ambos passando nesta verificação), por `npm run lint` e
`npx tsc --noEmit` limpos, por `npm run verificar-acoes` (48 ações conferidas, 0 violações de
`exigirUsuario()`), e — na parte que só um humano pode confirmar — pelos 23 itens de
`04.4-VERIFICACAO-HUMANA.md`, todos com Resultado escrito ou aprovação verbal registrada do dono,
incluindo a venda de três itens em 10 segundos (item 2, abaixo dos 20s exigidos pelo critério 1).
As migrações 0014/0015/0016 foram aplicadas em produção em 2026-09-20 e o código está no ar
(commit d9d2f1d, 2026-09-26).

### Observable Truths (os nove critérios de sucesso do ROADMAP)

| # | Truth (critério do ROADMAP) | Status | Evidência |
|---|---|---|---|
| 1 | No celular, venda com 3 itens de áreas diferentes, no Pix, <20s, Mês mostra cada linha na área | ✓ VERIFIED (human) | `04.4-VERIFICACAO-HUMANA.md` itens 1-3: "feito em 10 segundos" (item 2); Mês mostrou as 2 áreas corretamente (itens 1, 3). Código: `components/amassa/financeiro/painel-venda.tsx` (atalhos/busca/lista completa), `lib/financeiro/mes.ts::resumoDoMes` agrupa por `area` |
| 2 | Venda em 3x lança 3 parcelas; a 1ª entra no saldo, as outras em "A receber"; soma que não fecha impede o lançamento, também no servidor | ✓ VERIFIED | `lib/financeiro/parcelas.ts::gerarPlano`/`conferirParcelas` (única função, cliente E servidor — comentário linha 96-100 do `acoes.ts`); chamada em `lancarVenda` (`acoes.ts:102`) ANTES de gravar; restrição adiada `conferir_soma_do_documento()` no banco (migração 0015, `deferrable initially deferred`), provada em `scripts/testar-migracoes.mjs::conferirFinanceiro` (grupo 2, "Soma divergente" recusada só no commit) — rodada nesta verificação, passou. Humano: item 4 confirmado |
| 3 | Venda no cartão entra pelo valor menos taxa; mudar a taxa em Cadastros não muda o lançado; taxa vira custo Geral | ✓ VERIFIED | `lib/financeiro/taxa.ts::liquidoDaParcela`/`taxaEmCentavos`; taxa congelada em `registrarPagamento`/`lancarVenda` (`acoes.ts:236,701`, só quando `forma === "cartao"` E paga), nunca recalculada; `lib/financeiro/mes.ts` soma "Taxa do cartão" no bloco Geral (`NOME_TAXA_DO_CARTAO`). Humano: item 5 "Bem sucedido" |
| 4 | "Paguei"/"Recebi" tira da lista e move o saldo; dá para desfazer | ✓ VERIFIED | `lib/financeiro/pagamento.ts::planejarPagamento`/`planejarDesfazer`, propriedade de ida-e-volta testada em 200 documentos gerados deterministicamente (`tests/unit/financeiro-pagamento.test.ts:270-350`, passou nesta verificação); toast de 7s no componente. Humano: itens 6, 18, 20 confirmados |
| 5 | Cancelar deixa riscado no extrato, fora do saldo/Mês, com quem/quando — nada apagado | ✓ VERIFIED | `documentos.canceladoEm`/`canceladoPor` (check `documentos_cancelado_em_e_por_juntos`); `lib/financeiro/extrato.ts`/`mes.ts` filtram `cancelado` explicitamente (nunca confiam em quem chama). Humano: item 7 "ficou registrado e corrigiu o saldo e a tabela" |
| 6 | "Gerar as contas do mês" rodado 2x cria as contas fixas 1x só | ✓ VERIFIED | `documentos_conta_fixa_mes_uk` (unique em `contaFixaId`+`mesReferencia`); `lib/cadastros/contas-fixas.ts`. Humano: itens 8, 19 confirmados ("aviso apareceu e não duplicou") |
| 7 | Saldo do Caixa bate com saldo inicial + soma manual do extrato | ✓ VERIFIED (human) | `lib/financeiro/extrato.ts` (saldo acumulado por `pagoEm`+número, D-11/D-12). Humano: item 9 "a conta bate" |
| 8 | Categoria com lançamento não se apaga, só desativa, continua nos relatórios | ✓ VERIFIED | Nenhum `DELETE` de categoria em `lib/cadastros/acoes.ts` (só `ativa=false`); `revoke delete on ... categorias ... from amassa_app` (migração 0015); trigger `travar_grupo_e_area_da_categoria` impede mudar grupo/área após lançamento. Humano: item 10 "corretamente" |
| 9 | Parcelas em aberto da Abertura viram contas a pagar por script único, sem alterar a Abertura | ✓ VERIFIED | `scripts/importar-parcelas-abertura.ts` (só `select` em `aberturaItens`, nunca escreve lá); provado em `scripts/testar-migracoes.mjs::conferirImportacaoDaVirada` — `retratoDaAbertura()` idêntico antes/depois (linha 1601-1603), idempotência via `chaveDeImportacao`+`onConflictDoNothing` (roda 2x, mesmos 3 documentos), guarda contra parcela paga antes da virada (grupo 4). Rodado nesta verificação: "Todas as afirmações passaram." Confirmado NÃO rodado em produção (Roteiro 11, fim de novembro) |

**Score:** 9/9 truths verificados (0 comportamento não exercitado, 0 override)

### FNC-01 a FNC-17

| Requisito | Status | Evidência |
|---|---|---|
| FNC-01 | ✓ SATISFIED | `painel-venda.tsx` (atalhos/busca/lista completa/valor livre); `/cadastros` rota própria com link dentro do Financeiro (D-06) |
| FNC-02 | ✓ SATISFIED | `lib/financeiro/desconto.ts` (rateio proporcional, sobra na maior linha — D-09/D-10); `lib/financeiro/textos.ts:160` `"− R$ X de desconto"` distinta de `"tabela R$ X"`. Humano: item 22 confirmado 26/09 |
| FNC-03 | ✓ SATISFIED (dupla camada) | Cliente+servidor: `conferirParcelas` idêntica nos dois (ver truth 2). Banco: `conferir_soma_do_documento()` adiada (migração 0015), provada em `conferirFinanceiro` grupo 2 (rodada nesta verificação, passou). Rótulos "vence"/"valor" em `linha-parcela.tsx`. Humano: item 4 |
| FNC-04 | ✓ SATISFIED | `documentos.data` aceita qualquer `date` passada; sem checagem de "hoje" no schema; `lancarVenda` aceita `data` do formulário |
| FNC-05 | ✓ SATISFIED | Ver truth 3 acima + caso à-vista-não-paga-depois-paga-no-cartão (D-05, plano 04.4-12): `lancarVenda` grava `taxaPontosBase: null` quando a parcela nasce não paga (`acoes.ts:236`, `pagaNoCartao = parcela.pago && forma === "cartao"`); `registrarPagamento` congela a taxa só no momento do "Recebi" (`acoes.ts:701`, via `planejarPagamento`) |
| FNC-06 | ✓ SATISFIED | Dois caminhos em `painel-despesa.tsx` (compra/outra); terceiro caminho ("pagar conta que já existe") confirmado REMOVIDO do código (`painel-despesa.tsx:99`, comentário datado 26/09/2026, nenhum link/botão remanescente) |
| FNC-07 | ✓ SATISFIED | `lib/financeiro/extrato.ts` (4 pílulas, "Todas" incluída — `textos.ts:228` `"Total de todas as formas neste mês"`); saldo global mantido com filtro (D-11/D-12). Humano: itens 13, 23 |
| FNC-08 | ✓ SATISFIED | Ver truth 4 |
| FNC-09 | ✓ SATISFIED | Mesmo módulo de extrato, "Todas" confirmada por `textos.ts:228`. Humano: item 23 |
| FNC-10 | ✓ SATISFIED | `revoke delete on documentos, parcelas, categorias, itens_catalogo, contas_fixas, configuracao_financeira from amassa_app` (migração 0015) — exatamente as 6 tabelas de lançamento permanente; `documento_linhas`/`ficha_tecnica` MANTÊM delete (desfazer diferença/tirar insumo), como documentado. Provado tabela a tabela em `testar-migracoes.mjs` grupo 8 (rodado nesta verificação) |
| FNC-11 | ✓ SATISFIED | `lib/financeiro/mes.ts::resumoDoMes` — Geral em bloco único por nome de categoria (`geralPorNome`, nunca dividido por área), veredito "sobrou/faltou", duas réguas separadas (data do documento vs. data de pagamento) |
| FNC-12 | ✓ SATISFIED | Ver truth 8; nenhum widget de escolha de área em `painel-venda.tsx`/`painel-despesa.tsx` (grep confirmado); 24 categorias semeadas por `0016_categorias-iniciais.sql`, provadas em `conferirFinanceiro` (grupo 1, grupo/área de todas as 24 conferidas, incluindo "Juros, multas e descontos") |
| FNC-13 | ✓ SATISFIED | `db/schema.ts:586-632` (`itensCatalogo`: `precoVendaCentavos` nulo = valor na hora, `atalhoVenda`/`atalhoCompra`, `controlaEstoque`+`unidade`); `ficha_tecnica` um nível (check `ficha_tecnica_item_diferente_insumo`) |
| FNC-14 | ✓ SATISFIED | `contas_fixas.ativa`, unique `documentos_conta_fixa_mes_uk`; seletor de mês (corrente + 11 seguintes, D-13/resposta do dono 20/09). Humano: itens 8, 19 |
| FNC-15 | ✓ SATISFIED | `lib/financeiro/efeito-estoque.ts` — módulo puro, cálculo em milésimos inteiros, sem gravar movimentação (contrato do BRIEFING §6). Humano: item 17 |
| FNC-16 | ✓ SATISFIED | Ver truth 9. Rótulos "n de N" preservados, Material→custo, demais→"Equipamento e obra" (grupo `fora`), saldo inicial informado por `--saldo-inicial` — todos provados em `conferirImportacaoDaVirada` |
| FNC-17 | ✓ SATISFIED (human) | Humano: item 14 "fácil e prático de usar, mesmo só com uma mão disponível" |

**Nenhum requisito órfão.** Os 17 FNC-* do ROADMAP §"Phase 04.4" aparecem todos declarados nos
planos 04.4-01 a 04.4-13 e satisfeitos por código e/ou evidência humana.

### Achado de documentação (não-bloqueante)

A segunda tabela de rastreabilidade em `.planning/REQUIREMENTS.md:403-419` marca FNC-01, FNC-04,
FNC-12 e FNC-16 como "Pending", enquanto a lista primária (com checkbox, linhas 179-195, a fonte
real de status) já os marca `[x]` e o código confirma implementação completa nesta verificação.
Mesmo padrão já achado como DOC-01 em `04.3-VERIFICATION.md` — tabela auxiliar desatualizada, não
indício de lacuna real (registrado como OPEN-04 no frontmatter).

### Prohibitions (decisões de 2026-09-19 — "não reabrir")

| Proibição | Status | Evidência |
|---|---|---|
| Sem rateio entre áreas | ✓ VERIFIED | `lib/financeiro/mes.ts` — Geral em bloco por nome, nunca dividido; comentário explícito "sem rateio" |
| Usuário nunca escolhe área ao lançar | ✓ VERIFIED | Nenhum seletor de área em `painel-venda.tsx`/`painel-despesa.tsx` (grep confirmado); área sempre derivada da categoria |
| "Competência"/"regime de caixa" nunca na tela | ✓ VERIFIED | `grep -rn` em `components/amassa/financeiro`, `components/amassa/cadastros`, `lib/financeiro`, `lib/cadastros` — zero ocorrências |
| Nenhum dado real de negócio em arquivo versionado | ✓ VERIFIED | `db/migrations/0016_categorias-iniciais.sql` só semeia categorias (nomes genéricos do protótipo aprovado); `itens_catalogo`/`contas_fixas` sobem vazios (D-14); testes de integração usam nomes/e-mails inventados (`usuaria-financeiro@exemplo.test`); `04.4-VERIFICACAO-HUMANA.md` instrui explicitamente usar "teste" e cancelar lançamentos de conferência |
| Plataforma não emite nota fiscal | ✓ VERIFIED | Nenhuma menção a nota fiscal/NFe/NFCe em nenhum módulo do Financeiro (busca dedicada, sem falsos positivos de "conferir"/"confirmar") |

### Required Artifacts

| Artefato | Esperado | Status | Detalhes |
|---|---|---|---|
| `db/migrations/0014_financeiro.sql` | 8 tabelas do Financeiro | ✓ VERIFIED | Existe; `db/schema.ts:515-890` reflete o mesmo bloco |
| `db/migrations/0015_gatilhos-financeiro.sql` | Gatilhos `atualizado_em`, restrição de soma adiada, trava de categoria, `revoke delete` | ✓ VERIFIED | Todas as 4 partes lidas e confirmadas linha a linha |
| `db/migrations/0016_categorias-iniciais.sql` | 24 categorias, incluindo "Juros, multas e descontos" | ✓ VERIFIED | Confirmado (grupo/área certos) por `conferirFinanceiro`, rodado nesta verificação |
| `lib/financeiro/{parcelas,pagamento,taxa,desconto,mes,extrato,efeito-estoque,dinheiro,calendario,documento,formato,rascunho,textos,avisos,acoes,consultas,esquemas,abas}.ts` | Módulos puros + Server Actions | ✓ VERIFIED | Todos existem; regras puras sem import de React/banco/Next (confirmado por leitura); `acoes.ts` com `exigirUsuario()` primeiro (`npm run verificar-acoes`: 0 violações) |
| `lib/cadastros/{catalogo,categorias,contas-fixas,acoes,consultas,esquemas,abas,textos}.ts` | Cadastros (Catálogo, Categorias, Contas fixas, Taxas) | ✓ VERIFIED | Todos existem, mesmo padrão de módulo |
| `lib/virada/parcelas-da-abertura.ts` + `scripts/importar-parcelas-abertura.ts` | Script único da virada | ✓ VERIFIED | Só leitura em `aberturaItens`; ensaio por padrão; idempotente por `chaveDeImportacao`; guarda contra pagamento anterior à virada — todos provados |
| `components/amassa/financeiro/` e `components/amassa/cadastros/` | UI completa (Venda, Despesa, Caixa, Mês, Catálogo, Categorias, Contas fixas, Taxas) | ✓ VERIFIED | Componentes existem, referenciados pelas rotas `/financeiro` e `/cadastros` |
| `tests/unit/{financeiro-*,cadastros-*,virada-*}.test.ts` (15 arquivos) | Prova das regras puras | ✓ VERIFIED | Todos existem; 893 testes unitários passaram nesta verificação (`npx vitest run`) |
| `tests/e2e/{financeiro-*,cadastros-*}.spec.ts` (9 arquivos) | Prova ponta a ponta | ✓ VERIFIED (não reexecutada) | Todos existem; última varredura completa da fase (04.4-11-SUMMARY.md) confirmou 580 passaram/2 falharam (as 2 falhas são o achado WINDOWS #32, flaky sob carga, não regressão) |
| `docs/operacao/11-virada-do-financeiro.md` (Roteiro 11) | Roteiro de produção da virada | ✓ VERIFIED (existência confirmada pelo script e pelos SUMMARYs; não lido linha a linha nesta verificação) | Citado por `importar-parcelas-abertura.ts` e pelos planos 04.4-04/04.4-11 |
| `04.4-VERIFICACAO-HUMANA.md` | 23 itens, portão de fechamento da fase | ✓ VERIFIED | Todos os 23 itens marcados `[X]` com Resultado escrito ou aprovação verbal registrada do dono (26/09/2026) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `painel-venda.tsx`/`painel-despesa.tsx` | `lib/financeiro/parcelas.ts::conferirParcelas` | mesma função no cliente (mostrar) | ✓ WIRED | Confirmado por import direto e comentário explícito de reuso |
| `lib/financeiro/acoes.ts::lancarVenda`/`registrarPagamento` | `lib/financeiro/parcelas.ts::conferirParcelas` | mesma função no servidor (recusar) | ✓ WIRED | `acoes.ts:102,383` — nunca confia na soma calculada no cliente |
| `documento_linhas`/`parcelas`/`documentos` | trigger `conferir_soma_do_documento()` | `create constraint trigger ... deferrable initially deferred` | ✓ WIRED | Confirmado no SQL e provado em `conferirFinanceiro` (soma divergente recusada só no commit) |
| `lib/financeiro/acoes.ts::registrarPagamento`/`desfazerPagamento` | `lib/financeiro/pagamento.ts::planejarPagamento`/`planejarDesfazer` | chamada direta, decide ação na linha | ✓ WIRED | `acoes.ts:637-701` (pagar), `acoes.ts:809-832` (desfazer) |
| `categorias.chave_do_sistema = 'diferenca'` | linha "diferença" | busca por chave, nunca por nome | ✓ WIRED | `acoes.ts:666` — comentário explícito "nunca pelo nome" |
| `scripts/importar-parcelas-abertura.ts` | `lib/virada/parcelas-da-abertura.ts::planejarImportacao` | módulo puro chamado pelo script de I/O | ✓ WIRED | Script só lê/grava; toda a decisão de categoria/parcela/rótulo vem do módulo puro |
| `lib/navegacao/itens.ts` | barra do celular / lateral | listas separadas (D-04/D-05) | ✓ WIRED | `ITENS_NAVEGACAO_CELULAR` (5 itens, sem Estoque) vs. `ITENS_NAVEGACAO_LATERAL` (6, com Estoque). Humano: item 15 confirmado |
| `scripts/testar-migracoes.mjs` | `TABELAS_ESPERADAS` | lista atualizada com as 8 tabelas do Financeiro | ✓ WIRED | Confirmado (linha 53-60), conforme a convenção do projeto (CLAUDE.md) |

### Behavioral Spot-Checks

| Behavior | Comando | Resultado | Status |
|---|---|---|---|
| Propriedade de ida-e-volta (D-03): pagar+desfazer devolve linhas/parcelas idênticas em 200 documentos | `npx vitest run tests/unit/financeiro-pagamento.test.ts` | 24 testes passaram (inclui a propriedade de 200 iterações) | ✓ PASS |
| Restrição de soma adiada recusa só no commit; revoke delete nas 6 tabelas certas; virada só lê a Abertura e é idempotente | `npm run test:migracoes` | "Todas as afirmações passaram." | ✓ PASS |
| Toda ação financeira/cadastro começa por `exigirUsuario()` | `npm run verificar-acoes` | "48 ação(ões) conferida(s), 0 violações" | ✓ PASS |
| Suíte unitária inteira (893 testes) | `npx vitest run` | 50 arquivos, 893 testes passaram | ✓ PASS |
| Lint e tipos | `npm run lint` / `npx tsc --noEmit` | ambos limpos, sem saída | ✓ PASS |
| Varredura e2e completa da fase (não reexecutada nesta verificação — custo de ~53s + suíte inteira, já rodada 3x pelo executor) | `npm run test:e2e` (histórico, 04.4-11-SUMMARY.md) | 580 passaram / 2 falharam (WINDOWS #32, flaky sob carga) | ✓ PASS (com achado aberto não-bloqueante) |

### Probe Execution

Não aplicável — a fase não declara probes de `scripts/*/tests/probe-*.sh`. A prova de
sobrevivência/idempotência/read-only da virada roda dentro de `npm run test:migracoes`
(`conferirImportacaoDaVirada`, script Node), coberta acima.

### Requirements Coverage

Ver tabela FNC-01 a FNC-17 acima. Nenhum requisito órfão — os 17 FNC-* do ROADMAP §"Phase 04.4"
aparecem todos no campo `requirements` de algum plano 04.4-01 a 04.4-13.

### Anti-Patterns Found

Nenhum bloqueador. Varredura por `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` em
`lib/financeiro/`, `lib/cadastros/`, `lib/virada/`, `components/amassa/financeiro/`,
`components/amassa/cadastros/`, `scripts/importar-parcelas-abertura.ts` e as três migrações da
fase: vazia.

### Human Verification Required

Nenhum item pendente. O checkpoint humano da fase (`04.4-VERIFICACAO-HUMANA.md`, 23 itens — Seções
A a E) está completo: itens 1-19 com Resultado escrito pelo dono, itens 20-23 (Seção E, os últimos
da lista) aprovados verbalmente na sessão de 2026-09-26 ("Itens conferidos. Pode fechar."),
registrados como tal no próprio documento. Este documento é, por desenho da fase, o portão de
fechamento — e está percorrido por inteiro.

### Gaps Summary

Nenhum gap bloqueante. Os nove critérios de sucesso do ROADMAP e os 17 requisitos FNC-01 a FNC-17
estão implementados, testados (unitário + integração contra Postgres real + e2e) e confirmados
pelo dono nos pontos que só um humano pode confirmar (o tempo da venda, o uso de pé no celular).

Quatro itens ficam registrados como abertos, não-bloqueantes (frontmatter `open_items_non_blocking`):

1. **WINDOWS #32** — `tests/e2e/financeiro-extrato.spec.ts:212` intermitente só sob a varredura
   completa sem `--grep` (mesma classe de contenção de servidor Next único já registrada 6 vezes
   antes desta fase); isolado, passa 32/32. Não é regressão determinística.
2. **WINDOWS #3** — bloqueio de login intermitente sob carga; hipótese do argon2id refutada por
   medição; permanece aberto, mas é pré-existente (Fase 2a/02b), fora do escopo desta fase.
3. **WINDOWS #34** — o contador de tentativas de login pode não ser um singleton verdadeiro entre
   a rota REST do Auth.js e a Server Action nesta build (Turbopack/standalone); achado novo,
   não confirmado, fora do escopo desta fase.
4. **REQUIREMENTS.md** — tabela auxiliar de rastreabilidade desatualizada (FNC-01/04/12/16
   marcados "Pending" apesar de implementados), mesmo padrão já achado em 04.3.

Dois pedidos do dono ficam deliberadamente fora desta fase, já anotados para o Financeiro parte 2
(não são gaps — são escopo futuro, confirmado no próprio `04.4-VERIFICACAO-HUMANA.md`):

1. Somar o desconto do mês inteiro (para saber quanto se deu de desconto no mês).
2. Saldo do Caixa considerando o mês anterior e aportes de capital de giro.

A migração de produção (Roteiro 10, 0014/0015/0016) foi aplicada e verificada de fora pelo dono em
2026-09-20; o script da virada (Roteiro 11, 0016+`importar-parcelas-abertura.ts`) está pronto,
testado e documentado, mas **corretamente não rodado** — sua execução é planejada para o fim de
novembro, perto da inauguração.

---

_Verified: 2026-09-26_
_Verifier: Claude (gsd-verifier)_
