---
phase: 04.4-financeiro-parte-1
plan: 10
type: execute
wave: 6
depends_on: ["04.4-05", "04.4-08"]
files_modified:
  - lib/cadastros/contas-fixas.ts
  - tests/unit/cadastros-contas-fixas.test.ts
  - lib/cadastros/esquemas.ts
  - lib/cadastros/acoes.ts
  - lib/cadastros/consultas.ts
  - lib/cadastros/textos.ts
  - lib/cadastros/avisos.ts
  - app/(app)/cadastros/page.tsx
  - components/amassa/cadastros/lista-contas-fixas.tsx
  - components/amassa/cadastros/dialogo-conta-fixa.tsx
  - components/amassa/cadastros/valor-conta-fixa.tsx
  - components/amassa/cadastros/botao-gerar-contas.tsx
  - tests/e2e/cadastros-contas-fixas.spec.ts
autonomous: true
requirements: [FNC-08, FNC-14, FNC-17]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 130000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Cadastros → Contas fixas lista as contas (nome · valor esperado editável na própria linha · 'todo dia N · categoria'), com a dica 'Todo mês viram a pagar no Caixa. O valor aqui é o esperado; se a conta vier diferente, você acerta na hora de pagar.' (FNC-14)."
    - "'+ Nova conta fixa' (contorno, nunca terracota) abre um diálogo com nome (1-120), categoria (Geral, Custos diretos de uma área e Fora do resultado, só ativas), valor esperado (maior que zero) e dia de vencimento (inteiro de 1 a 31) — D-13."
    - "Cada conta tem 'Desativar'/'Reativar', sem diálogo (reversível), com os avisos 'Conta fixa desativada.'/'Conta fixa reativada.'; a desativada aparece riscada e NÃO entra em 'Gerar as contas de {mês}' (D-13)."
    - "'Gerar as contas de {mês por extenso}' (o único botão terracota da sub-aba, o mês seguinte ao de hoje, como no protótipo) cria, para cada conta fixa ATIVA, uma despesa em aberto: data e vencimento no dia da conta naquele mês, uma linha com o nome, a categoria e o valor esperado, uma parcela em Pix, título '{nome} · {mês}/{ano}' e o par (conta fixa, mês) gravado."
    - "Rodar 'Gerar' duas vezes — ou dois gestores ao mesmo tempo — cria cada conta uma vez só: a restrição única (conta fixa, mês) decide, e a segunda vez responde 'As contas de {mês} já existiam.' (critério 6 do ROADMAP); a primeira responde '{N} conta(s) de {mês} criada(s) no Caixa.'."
    - "Dia 31 num mês de 30 dias, ou 29/30/31 em fevereiro, vence no último dia daquele mês."
    - "Sem nenhuma conta fixa ativa, o botão de gerar fica desabilitado; com o banco sem nenhuma conta fixa, aparece 'Nenhuma conta fixa ainda.' / 'Cadastre o aluguel, a internet e outras contas que se repetem todo mês.' com '+ Nova conta fixa' — provado na etapa `@vazio-global`."
    - "Mudar o valor esperado vale para as próximas gerações; as contas já geradas não mudam."
    - "A conta gerada tem uma linha e uma parcela: pagá-la com valor diferente no Caixa ajusta a própria linha, sem linha de diferença (D-01), e o 'Desfazer' a devolve ao valor esperado (D-03) — o exemplo 1 de despesa do protótipo (aluguel)."
    - "A geração é manual de propósito nesta fase (sem agendamento automático)."
    - statement: "Nome de conta fixa com 120 caracteres não quebra a lista a 320px."
      verification: backstop
  prohibitions:
    - statement: "Nenhuma conta fixa é apagada — só desativada; não existe ação de excluir e o papel da aplicação não tem `delete` em `contas_fixas`."
      status: resolved
      verification: test
    - statement: "Nenhum valor real de aluguel, internet ou contabilidade entra em teste ou semente — as contas fixas de produção nascem vazias (D-14)."
      status: resolved
      verification: judgment
    - statement: "Nenhuma escrita termina em atualização de roteador do Next."
      status: resolved
      verification: test
  artifacts:
    - lib/cadastros/contas-fixas.ts
    - components/amassa/cadastros/lista-contas-fixas.tsx
    - components/amassa/cadastros/botao-gerar-contas.tsx
    - tests/e2e/cadastros-contas-fixas.spec.ts
  key_links:
    - "`gerarContasDoMes` insere o documento com `on conflict (conta_fixa_id, mes_referencia) do nothing` e só cria linha e parcela para o que de fato nasceu — é o banco, não um 'já existe?' lido antes, que garante a idempotência mesmo com duas pessoas tocando ao mesmo tempo."
    - "`vencimentoNoMes` reaproveita `ultimoDiaDoMes` de `lib/financeiro/calendario.ts` (plano 06) — Cadastros e Financeiro são permanentes, a dependência é segura; nada aqui importa `lib/abertura`."
    - "O e2e que gera contas roda só no projeto desktop e num teste único em série: 'Gerar' é global (cria para toda conta ativa), e dois projetos gerando o mesmo mês em paralelo transformariam a afirmação sobre a conta desativada numa corrida."

coverage:
  - id: D1
    description: "Vencimento no mês, título e mês da geração provados sem servidor"
    requirement: "FNC-14"
    verification:
      - kind: unit
        ref: "tests/unit/cadastros-contas-fixas.test.ts"
        status: unknown
    human_judgment: false
  - id: D2
    description: "Criar, desativar, gerar duas vezes sem duplicar, e pagar o aluguel com valor diferente ajustando a linha"
    requirement: "FNC-14, FNC-08"
    verification:
      - kind: e2e
        ref: "tests/e2e/cadastros-contas-fixas.spec.ts — exemplo 1 de despesa do protótipo"
        status: unknown
    human_judgment: false
---

<objective>
As Contas fixas com cadastro completo (D-13): **criar, editar o valor esperado, desativar e
reativar, e "Gerar as contas de {mês}" que nunca duplica** — e o aluguel gerado pago no Caixa com
o valor que veio de verdade.

Purpose: aluguel, energia, internet e contabilidade se repetem todo mês; hoje isso é lembrança. Com
este plano, um toque por mês põe todas em "A pagar", e tocar de novo não faz estrago (critério 6
do ROADMAP). Em produção as contas fixas nascem vazias (D-14) — é esta tela que o dono usa para
cadastrá-las.

Output: a regra pura do vencimento e do título, as quatro ações de contas fixas, a sub-aba
completa e o e2e com o exemplo do aluguel.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-05-SUMMARY.md
@.planning/phases/04.4-financeiro-parte-1/04.4-08-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| `vencimentoNoMes`, `tituloDaContaFixa`, `mesDaGeracao` | `lib/cadastros/contas-fixas.ts` |
| `esquemaContaFixa`, `esquemaValorDaContaFixa`, `esquemaGeracao` | `lib/cadastros/esquemas.ts` |
| `criarContaFixa`, `atualizarValorDaContaFixa`, `definirContaFixaAtiva`, `gerarContasDoMes` | `lib/cadastros/acoes.ts` |
| `listarContasFixas` | `lib/cadastros/consultas.ts` |
| Avisos `conta-fixa-desativada`, `conta-fixa-reativada`, `contas-geradas` | `lib/cadastros/avisos.ts` |
| `ListaContasFixas`, `DialogoContaFixa`, `ValorContaFixa`, `BotaoGerarContas` | `components/amassa/cadastros/` |
| `data-testid`: `conta-fixa-linha`, `conta-fixa-valor`, `gerar-contas` | componentes |

## Suposição do planejador (sinalizada na verificação do plano 11)

"Gerar" gera sempre o **mês seguinte**, como no protótipo. Se o dono esquecer de gerar antes de o
mês virar, o mês corrente não tem botão — pergunta registrada para a verificação humana (um
seletor de mês seria mudança de tela, fora do protótipo).

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Contas fixas na tela — regra do vencimento, criar, valor esperado, desativar e reativar</name>
  <files>lib/cadastros/contas-fixas.ts, tests/unit/cadastros-contas-fixas.test.ts, lib/cadastros/esquemas.ts, lib/cadastros/acoes.ts, lib/cadastros/consultas.ts, lib/cadastros/textos.ts, lib/cadastros/avisos.ts, app/(app)/cadastros/page.tsx, components/amassa/cadastros/lista-contas-fixas.tsx, components/amassa/cadastros/dialogo-conta-fixa.tsx, components/amassa/cadastros/valor-conta-fixa.tsx</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-13 e D-14.
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Contas fixas — CRUD completo", §Color (por que "+ Nova conta fixa" é contorno), §Copywriting (vazio e avisos de conta fixa).
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 208 (as quatro contas de exemplo — valores inventados), 225 (`gerarFixas`), 359-362 (a tela) e 436 (edição do valor).
    - `lib/financeiro/calendario.ts` (plano 06) e `lib/financeiro/dinheiro.ts`.
    - `lib/cadastros/*`, `components/amassa/cadastros/lista-categorias.tsx` e `dialogo-categoria.tsx` (planos 02 e 05) — o molde de lista, diálogo, desativar/reativar e aviso.
    - `components/amassa/abertura/caixa-marcacao.tsx` — o salvamento otimista convergente (molde do valor editável na linha).
  </read_first>
  <behavior>
    - `vencimentoNoMes(5, '2027-01')` → '2027-01-05'; `(31, '2027-04')` → '2027-04-30'; `(31, '2027-02')` → '2027-02-28'; `(29, '2028-02')` → '2028-02-29'.
    - `tituloDaContaFixa('Aluguel', '2027-01')` → 'Aluguel · janeiro/2027'.
    - `mesDaGeracao('2026-12-18')` → '2027-01'; `('2026-11-30')` → '2026-12'.
    - Dia 0 e dia 32 são recusados pelo esquema com frase humana.
  </behavior>
  <action>
Teste primeiro. **`contas-fixas.ts`** (puro, só importa `lib/financeiro/calendario.ts`): as três funções de `<behavior>`. **Servidor**: `esquemaContaFixa` (nome 1-120, categoria, `valorTexto` > 0 pela função pura, dia inteiro 1-31); `criarContaFixa`, `atualizarValorDaContaFixa({ id, valorTexto })` e `definirContaFixaAtiva({ id, ativa })` (estado desejado, convergente), todas com `exigirUsuario()` primeiro; a categoria precisa existir, estar ativa e ser de grupo `geral`, `custo` ou `fora`. `listarContasFixas()`: todas, com nome da categoria, na ordem de criação. Avisos novos em `avisos.ts`.

**Tela.** A sub-aba "Contas fixas" renderiza `ListaContasFixas`: título "Contas fixas", a dica do protótipo, cada conta com nome, `ValorContaFixa` (campo de 44px/16px, `inputmode="decimal"`; ao sair do campo com valor mudado chama a ação sem navegar — deu certo, fica; falhou, volta ao anterior e mostra o erro), "todo dia {N} · {categoria}", e "Desativar"/"Reativar" (44px; grava e navega por completo com o aviso). Inativa riscada. Rodapé com "+ Nova conta fixa" (contorno) e o botão de gerar (Tarefa 2). `DialogoContaFixa` (estado local, diálogo único responsivo, rodapé preso por flex): "Nova conta fixa", Nome, Categoria (`optgroup` Geral / Custos diretos de uma área / Fora do resultado), "Valor esperado", "Dia do vencimento" (1-31), "Cancelar"/"Salvar"; erro no diálogo, nada perdido. Estado vazio do §Copywriting com o botão abrindo o diálogo.

Nenhuma invocação de e2e nesta tarefa — a Tarefa 2 roda o recorte cobrindo as duas.
  </action>
  <verify>
    <automated>npm test -- tests/unit/cadastros-contas-fixas.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run verificar-acoes</automated>
  </verify>
  <acceptance_criteria>
    - O teste unitário passa com cada caso de `<behavior>`.
    - `npm run verificar-acoes` sai 0 com as três ações novas começando por `exigirUsuario()`.
    - `grep -rEn "\.delete\(contasFixas\)|excluirContaFixa" lib/cadastros` não encontra nada.
    - `grep -n "Nenhuma conta fixa ainda." lib/cadastros/textos.ts` encontra a cópia.
  </acceptance_criteria>
  <done>O dono cadastra, ajusta e desativa as contas que se repetem todo mês, pela tela.</done>
</task>

<task type="auto">
  <name>Tarefa 2: "Gerar as contas de {mês}" que nunca duplica — e o aluguel pago com o valor que veio</name>
  <files>lib/cadastros/esquemas.ts, lib/cadastros/acoes.ts, lib/cadastros/textos.ts, lib/cadastros/avisos.ts, app/(app)/cadastros/page.tsx, components/amassa/cadastros/botao-gerar-contas.tsx, components/amassa/cadastros/lista-contas-fixas.tsx, tests/e2e/cadastros-contas-fixas.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 225, 362 e 430 (gerar e o aviso).
    - `db/schema.ts` — `documentos.conta_fixa_id`, `mes_referencia` e a restrição única do par (plano 01).
    - `lib/financeiro/acoes.ts` — como `lancarDespesa` monta documento, linha e parcela (o mesmo formato, sem reaproveitar a ação).
    - `tests/e2e/financeiro-caixa.spec.ts` e `tests/e2e/apoio/semear-financeiro.ts` — como pagar pelo Caixa e o Desfazer.
    - `playwright.config.ts` — por que o teste de geração roda só no desktop.
  </read_first>
  <action>
**Servidor.** `gerarContasDoMes({ mes })` com `exigirUsuario()` primeiro: o mês precisa ser `mesDaGeracao(hoje)` (a tela só oferece esse) — outro valor é recusado; numa transação, lê as contas fixas ATIVAS e, para cada uma, insere o documento (despesa, data e vencimento `vencimentoNoMes`, título `tituloDaContaFixa`, `conta_fixa_id`, `mes_referencia` = primeiro dia do mês, `criado_por`) com `on conflict` no par ignorando o que já existe e devolvendo só os criados; para cada criado, a linha (nome, categoria da conta, quantidade 1, valor esperado) e a parcela (vencimento, valor, Pix, em aberto). Devolve `{ criadas, mes }`. A tela navega por completo para `/cadastros?sub=fixas&aviso=contas-geradas&quantidade=<n>&mes=<AAAA-MM>`; `avisos.ts` valida quantidade (inteiro 0-500) e mês (AAAA-MM) e a página monta "{N} conta(s) de {mês} criada(s) no Caixa." ou, com zero, "As contas de {mês} já existiam.".

**Tela.** `BotaoGerarContas`: "Gerar as contas de {mês por extenso}" (terracota, o único da sub-aba), desabilitado sem conta ativa, desabilitado enquanto grava (duplo toque não dispara duas vezes na mesma tela — e, se disparar, o banco segura).

**E2E** (`tests/e2e/cadastros-contas-fixas.spec.ts`, "cadastros contas fixas" no título): **teste de geração, só no projeto desktop e em série** (comentário explicando a corrida que evita): cria "Aluguel" (sufixo) R$ 1.500,00 dia 5 categoria "Aluguel" e "Internet" (sufixo) R$ 120,00 dia 31 categoria "Internet e sistemas"; desativa a Internet (aviso "Conta fixa desativada.", linha riscada); "Gerar as contas de {mês seguinte}" → aviso que termina em "criada(s) no Caixa." (o número pode incluir contas de outros testes — afirme só o formato); no Caixa, "A pagar" tem exatamente UMA conta "Aluguel {sufixo} · {mês}/{ano}" e NENHUMA da Internet; gera de novo → continua exatamente uma; **exemplo 1** — "Paguei" no aluguel com R$ 1.512,00 → aviso "Pago: R$ 1.512,00" sem frase de diferença; o detalhe mostra a linha única com R$ 1.512,00; "Desfazer" → "Desfeito. A conta voltou a R$ 1.500,00 em aberto."; reativa a Internet ("Conta fixa reativada."); muda o valor esperado do aluguel para 1.600 na linha, recarrega e vê 1.600. **Teste de tela nos dois projetos:** o diálogo recusa dia 32 e valor vazio com frase; a 320px a sub-aba não rola na horizontal e os botões medem 44px. **Caso `@vazio-global`:** sem nenhuma conta fixa, o vazio aparece e o botão abre o diálogo (sem salvar).

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "cadastros contas fixas"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "cadastros contas fixas" passa: a geração repetida deixa exatamente uma conta do aluguel e nenhuma da conta desativada (critério 6 do ROADMAP).
    - `grep -n "onConflictDoNothing\|on conflict" lib/cadastros/acoes.ts` encontra a inserção idempotente em `gerarContasDoMes`.
    - `npm run verificar-acoes` sai 0 com `gerarContasDoMes` começando por `exigirUsuario()`.
    - `grep -rEn "router\.(push|refresh)\(" components/amassa/cadastros` não encontra nada.
  </acceptance_criteria>
  <done>Um toque por mês põe as contas fixas em "A pagar", e tocar de novo não duplica nada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ações de contas fixas | Nome, categoria, valor, dia e mês vêm do cliente |
| dois gestores → "Gerar" ao mesmo tempo | Corrida real que o banco precisa resolver |
| URL `?aviso=contas-geradas&quantidade=&mes=` | Números vindos da URL entram num texto |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-60 | Elevation of Privilege | as quatro ações | high | mitigate | `exigirUsuario()` primeira instrução; `verificar-acoes` |
| T-04.4-61 | Tampering | geração duplicada | high | mitigate | Restrição única (conta fixa, mês) + `on conflict do nothing`; prova no `test:migracoes` (plano 01) e no e2e |
| T-04.4-62 | Tampering | mês arbitrário na geração | medium | mitigate | O servidor só aceita o mês seguinte ao de hoje |
| T-04.4-63 | Tampering | categoria e valor | medium | mitigate | Categoria do banco, ativa, grupo permitido; valor > 0 com teto; dia 1-31 |
| T-04.4-64 | Tampering | aviso forjado pela URL | low | mitigate | Quantidade e mês validados por formato fechado; texto montado no servidor; nenhum efeito além do aviso |
| T-04.4-65 | Information Disclosure | valores de conta em teste | medium | mitigate | Valores inventados do protótipo, nomes com sufixo |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Um recorte "cadastros contas fixas" neste plano (Tarefa 2).
- O exemplo 1 de despesa do protótipo (aluguel) tem caso próprio.
</verification>

<success_criteria>
- Contas fixas com valor esperado e dia, criadas, desativadas e reativadas pela tela (FNC-14, D-13).
- "Gerar" rodado duas vezes cria uma vez só (critério 6 do ROADMAP).
- A conta gerada paga com valor diferente ajusta a própria linha e desfaz exatamente (D-01/D-03).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-10-SUMMARY.md` when done
</output>
