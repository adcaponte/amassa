---
phase: 04.4-financeiro-parte-1
plan: 07
type: execute
wave: 4
depends_on: ["04.4-06"]
files_modified:
  - lib/financeiro/abas.ts
  - components/amassa/financeiro/abas-financeiro.tsx
  - lib/financeiro/esquemas.ts
  - lib/financeiro/acoes.ts
  - lib/financeiro/consultas.ts
  - lib/financeiro/textos.ts
  - app/(app)/financeiro/page.tsx
  - components/amassa/financeiro/painel-despesa.tsx
  - components/amassa/financeiro/linha-compra.tsx
  - components/amassa/financeiro/grade-catalogo.tsx
  - components/amassa/financeiro/lista-completa.tsx
  - components/amassa/financeiro/bloco-pagamento.tsx
  - tests/e2e/financeiro-despesa.spec.ts
autonomous: true
requirements: [FNC-03, FNC-06, FNC-15, FNC-17]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 150000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A aba Despesa existe entre Venda e Caixa (`?aba=despesa`) e abre nas três pílulas do protótipo — 'Compra de material' · 'Outra despesa' · 'Pagar conta que já existe' — que são o foco visual da tela (FNC-06)."
    - "Compra de material: 'O que chegou' mostra os atalhos de compra (itens com estoque próprio e 'nos atalhos da compra'), com 'Buscar material do estoque' e 'Lista completa e atalhos' ('Todo material de estoque', com a estrela do atalho de compra); cada linha da compra pede 'quantos ({unidade})' e 'custou ao todo', sem passo de quantidade."
    - "A linha de compra grava quantidade 1, `quantidade_estoque` = quantos chegaram, valor = quanto custou ao todo, descrição e categoria da COMPRA do item (vindas do banco); quantos aceita até três casas e tem de ser maior que zero; custou ao todo tem de ser maior que zero."
    - "A compra mostra 'O que esta compra põe no estoque' já aberto — '+5 kg · Pão de queijo congelado' — com a dica 'O custo de cada unidade sai de custou ao todo ÷ quantidade. Vale quando o Estoque estiver ligado.'; nada é gravado (FNC-15)."
    - "Compra de material conta como custo no mês da compra — a data do documento é a da compra, e o Mês (plano 09) a lê por ela."
    - "Outra despesa: 'Que despesa é' com Descrição (placeholder 'ex.: jogo de estecas'), Categoria (Geral, Custos diretos de uma área com ' · Área' no nome, e Fora do resultado — só ativas) e Valor; com categoria do grupo Fora aparece 'Esta categoria sai do caixa, mas não entra como custo do mês — é investimento ou dívida, não despesa de operação.'; 'Lançar despesa' fica desabilitado sem descrição."
    - "'Pagar conta que já existe' leva ao Caixa (`?aba=caixa`), onde as contas em aberto têm 'Paguei' (plano 08)."
    - "A despesa usa o MESMO bloco de pagamento da venda, com 'Como paga', 'Entrada de 50% + saldo' e 'já paguei', incluindo '+ outra forma' no à vista (D-08) e a mesma recusa de soma que não fecha no servidor (FNC-03); despesa no cartão não tem taxa."
    - "O servidor recusa despesa com categoria de Receita, compra de item sem estoque próprio, e outra despesa com categoria desativada ('A categoria X foi desativada — escolha outra.')."
    - "Depois de lançar, o aviso é 'Despesa nº {N} lançada · {R$ total}' (com as parcelas em aberto quando houver) e o extrato mostra '− R$ X' em cor de erro."
    - "O rascunho da despesa sobrevive a trocar de aba (chave própria, separada da venda)."
    - statement: "A 320px, as três pílulas do topo da Despesa quebram linha sem rolagem horizontal e cada uma mede 44px ou mais."
      verification: backstop
  prohibitions:
    - statement: "O usuário nunca escolhe área ao lançar despesa: a área vem da categoria (da compra do item, ou da escolhida em Outra despesa, que mostra a área só como texto)."
      status: resolved
      verification: test
    - statement: "Nenhum botão 'Testar com as despesas que você descreveu' existe."
      status: resolved
      verification: test
    - statement: "Nenhuma movimentação de estoque é gravada pela compra nesta fase."
      status: resolved
      verification: test
  artifacts:
    - components/amassa/financeiro/painel-despesa.tsx
    - components/amassa/financeiro/linha-compra.tsx
    - tests/e2e/financeiro-despesa.spec.ts
  key_links:
    - "`lancarDespesa` compartilha com `lancarVenda` a conferência (`conferirParcelas`), a data e o aviso; a diferença é só a validação de categoria por grupo e a ausência de taxa. Nenhuma das duas confia em categoria vinda do cliente para linha de catálogo."
    - "`GradeCatalogo` e `ListaCompleta` recebem o modo ('venda' | 'compra'): o mesmo componente mostra preço na venda e 'em {unidade}' na compra, e a estrela grava o atalho certo por `definirAtalhoDoItem`."

coverage:
  - id: D1
    description: "Compra de material com o que põe no estoque, outra despesa com a dica do Fora, e a pílula que leva ao Caixa"
    requirement: "FNC-06, FNC-15"
    verification:
      - kind: e2e
        ref: "tests/e2e/financeiro-despesa.spec.ts — exemplos 2 e 4 de despesa do protótipo"
        status: unknown
    human_judgment: false
---

<objective>
A Despesa com os três caminhos do protótipo: **compra de material** (o que chegou, quantos e quanto
custou ao todo, e o que isso põe no estoque), **outra despesa** (descrição, categoria, valor) e
**pagar conta que já existe** (leva ao Caixa) — com o mesmo bloco de pagamento da Venda (FNC-06).

Purpose: com Venda e Despesa lançando, o Caixa passa a ter os dois sentidos do dinheiro, e o Mês
(plano 09) passa a ter custo para comparar com venda.

Output: a aba Despesa, `lancarDespesa`, os componentes de compra e o e2e com os exemplos de
despesa 2 (5 kg de pão de queijo) e 4 (ferramenta de cerâmica) do protótipo.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-06-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| Aba `despesa` | `lib/financeiro/abas.ts`, `components/amassa/financeiro/abas-financeiro.tsx` |
| `esquemaDespesa` (modos `compra` e `outra`) | `lib/financeiro/esquemas.ts` |
| `lancarDespesa`, `textoDespesaLancada` | `lib/financeiro/acoes.ts`, `lib/financeiro/textos.ts` |
| `listarCatalogoDaCompra`, categorias de despesa | `lib/financeiro/consultas.ts` |
| `PainelDespesa`, `LinhaCompra` | `components/amassa/financeiro/` |
| `data-testid`: `despesa-modo-<modo>`, `compra-linha`, `compra-quantos`, `compra-custou`, `despesa-dica-fora` | componentes |

<tasks>

<task type="auto">
  <name>Tarefa 1: Compra de material — o que chegou, quantos, quanto custou e o que põe no estoque</name>
  <files>lib/financeiro/abas.ts, components/amassa/financeiro/abas-financeiro.tsx, lib/financeiro/esquemas.ts, lib/financeiro/acoes.ts, lib/financeiro/consultas.ts, lib/financeiro/textos.ts, app/(app)/financeiro/page.tsx, components/amassa/financeiro/painel-despesa.tsx, components/amassa/financeiro/linha-compra.tsx, components/amassa/financeiro/grade-catalogo.tsx, components/amassa/financeiro/lista-completa.tsx, components/amassa/financeiro/bloco-pagamento.tsx, tests/e2e/financeiro-despesa.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 262-276 (`baseItens` e `linhasHTML` do lado da compra), 304-319 (`telaDespesa`), 394-397 (`addLinha` e `lancar` na compra) e 413 (exemplo 2 de despesa).
    - `.planning/phases/04.4-financeiro-parte-1/BRIEFING.md` §5 "Mês" (compra conta no mês da compra) e §6.
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §Foco Visual (Despesa) e §Copywriting (carrinho vazio da compra).
    - `components/amassa/financeiro/painel-venda.tsx`, `grade-catalogo.tsx`, `lista-completa.tsx`, `bloco-pagamento.tsx`, `lib/financeiro/efeito-estoque.ts`, `lib/financeiro/rascunho.ts` e `lib/financeiro/acoes.ts` como os planos 03 e 06 os deixaram.
    - `tests/e2e/apoio/semear-financeiro.ts` e `tests/e2e/financeiro-venda.spec.ts`.
  </read_first>
  <action>
**Aba.** `AbaFinanceiro` ganha `despesa`; `abaDaUrl` a reconhece; `AbasFinanceiro` mostra "Despesa" entre Venda e Caixa.

**Servidor.** `esquemaDespesa`: `modo` `compra` com `linhas` (1-100 de `{ itemId, quantidadeEstoqueTexto, valorTotalTexto }`) ou `outra` com `{ descricao (1-160), categoriaId, valorTexto }`; `data`, `pessoa` (até 160) e `parcelas` como na venda. `lancarDespesa(entradaBruta)` com `exigirUsuario()` primeiro, numa transação: compra — itens do banco numa consulta, cada um precisa ter estoque próprio (senão "Esse material não tem estoque próprio no catálogo — ajuste em Cadastros."); linha com quantidade 1, `quantidade_estoque` (via `converterQuantidade`), valor = custou ao todo (> 0), descrição e `categoria_id` da COMPRA do item, `item_id`; outra — categoria do banco, ATIVA e de grupo `custo`, `geral` ou `fora`; linha única com a descrição e o valor. Em ambos: categoria de grupo `receita` é recusada; `conferirParcelas`; nenhuma taxa; `criado_por`; aviso `textoDespesaLancada`. `listarCatalogoDaCompra()`: itens com estoque próprio, com unidade, área da categoria de compra e atalho de compra.

**Tela.** `page.tsx` carrega, para a aba Despesa, o catálogo da compra, os itens para efeito e as categorias de despesa. `PainelDespesa` (cliente): as três pílulas no topo (`aria-pressed`, 44px; a terceira é um link para `?aba=caixa`); modo compra com "O que chegou" (busca "Buscar material do estoque", `GradeCatalogo` em modo compra mostrando "em {unidade}", "Lista completa e atalhos") e "Esta compra" (Data, "Fornecedor (opcional)", as `LinhaCompra` — nome com o ponto da área, "quantos ({unidade})" e "custou ao todo" com `inputmode="decimal"`, "tirar" —, "Toque nos materiais que chegaram." quando vazio, "Total", `BlocoPagamento` com `tipo="despesa"`, o efeito "O que esta compra põe no estoque" aberto por padrão com a dica do protótipo, "Limpar" e "Lançar despesa"). Tocar num material já no carrinho não duplica a linha. `ListaCompleta` em modo compra: título "Todo material de estoque", estrela grava `atalho_compra`. Rascunho com a chave da despesa. `BlocoPagamento`: rótulos de despesa ("Como paga", "Entrada de 50% + saldo", "já paguei"), sem aviso do cartão.

**E2E** (`tests/e2e/financeiro-despesa.spec.ts`, "financeiro despesa" no título): a aba Despesa existe e abre nas três pílulas; **exemplo 2** — semeia "Pão de queijo congelado" (sufixo) com estoque em kg, categoria da compra "Insumos da cafeteria" e atalho de compra; fornecedor inventado; busca, põe na compra, quantos "5", custou ao todo "160" → Total R$ 160,00 e "+5 kg · Pão de queijo congelado…"; lança; aviso "Despesa nº … lançada · R$ 160,00"; o extrato mostra "− R$ 160,00". Tocar duas vezes no mesmo material deixa uma linha só. Uma compra de "Esmalte (pote)" (sufixo) com 100 un por R$ 2.400,00 em 3x mostra as três parcelas com "já paguei" só na primeira. A 320px a Despesa não rola na horizontal.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro despesa"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro despesa" passa nos dois projetos com o exemplo 2 de despesa do protótipo.
    - `npm run verificar-acoes` sai 0 com `lancarDespesa` começando por `exigirUsuario()`.
    - `grep -rn "Testar com as despesas" components/amassa/financeiro` não encontra nada.
    - `grep -n "conferirParcelas" lib/financeiro/acoes.ts` aparece no caminho de `lancarDespesa`.
  </acceptance_criteria>
  <done>O material que chegou é lançado com quantidade e custo, e a tela diz o que ele põe no estoque.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Outra despesa e "Pagar conta que já existe" — com a dica do Fora do resultado</name>
  <files>lib/financeiro/textos.ts, components/amassa/financeiro/painel-despesa.tsx, tests/e2e/financeiro-despesa.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 303 (`optCats`), 308-312 (outra despesa), 404 (pílula `conta` leva ao Caixa) e 414 (exemplo 4 de despesa).
    - `components/amassa/financeiro/painel-despesa.tsx` (Tarefa 1).
  </read_first>
  <action>
**Tela.** Modo "Outra despesa": bloco "Que despesa é" com "Descrição" (placeholder "ex.: jogo de estecas"), "Categoria" (`optgroup` Geral (custos da casa), Custos diretos de uma área — cada opção com " · {Área}" — e Fora do resultado; só ativas) e "Valor" (`inputmode="decimal"`, placeholder "R$"); a dica do Fora quando a categoria escolhida é do grupo `fora`; bloco "Pagamento" com Data, "Para quem (opcional)", "Total", `BlocoPagamento`, "Limpar" e "Lançar despesa" desabilitado sem descrição ou sem a soma fechar. Trocar de pílula não perde o que foi digitado no outro modo (os dois vivem no rascunho). A pílula "Pagar conta que já existe" navega para `/financeiro?aba=caixa` por link normal.

**E2E** (acrescente a `financeiro-despesa.spec.ts`): **exemplo 4** — "Jogo de estecas e desbastadores" (sufixo), "Ferramentas e utensílios", "185" → Total R$ 185,00; lança; o extrato mostra "− R$ 185,00". Escolher "Equipamento e obra" mostra a dica do Fora. Sem descrição, "Lançar despesa" desabilitado. "+ outra forma" numa outra despesa de R$ 150 (Pix 100 + Dinheiro 50) lança duas linhas no extrato, sem taxa. "Pagar conta que já existe" leva a `?aba=caixa`.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro despesa"</automated>
  </verify>
  <acceptance_criteria>
    - O caso do exemplo 4 de despesa passa nos dois projetos.
    - A dica do Fora aparece com "Equipamento e obra" e some com "Aluguel".
    - A pílula "Pagar conta que já existe" termina em `/financeiro?aba=caixa`.
  </acceptance_criteria>
  <done>Qualquer despesa do dia — material, ferramenta, aluguel avulso, equipamento — tem caminho de dois ou três toques, e a que já existe leva ao Caixa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `lancarDespesa` | Item, quantidade de estoque, custo, categoria e parcelas vêm do cliente |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-43 | Elevation of Privilege | `lancarDespesa` | high | mitigate | `exigirUsuario()` primeira instrução; `verificar-acoes` |
| T-04.4-44 | Tampering | categoria da despesa | high | mitigate | Categoria do banco, grupo `custo`/`geral`/`fora` e ativa em outra despesa; na compra, a categoria da compra do item |
| T-04.4-45 | Tampering | soma das parcelas | high | mitigate | `conferirParcelas` no servidor; restrição adiada no banco |
| T-04.4-46 | Tampering | quantidade de estoque | medium | mitigate | `converterQuantidade` (> 0, até 3 casas, teto) no Zod; `check` do banco |
| T-04.4-47 | Information Disclosure | fornecedor em teste | medium | mitigate | Fornecedores inventados com sufixo; nenhum nome real |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Dois recortes "financeiro despesa" neste plano, um por tarefa.
- Os exemplos 2 e 4 de despesa do protótipo têm caso próprio.
</verification>

<success_criteria>
- A despesa tem os três caminhos (FNC-06).
- A compra mostra o que põe no estoque, sem gravar (FNC-15).
- Mesmo bloco de pagamento, mesma recusa no servidor (FNC-03).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-07-SUMMARY.md` when done
</output>
