---
phase: 04.4-financeiro-parte-1
plan: 02
type: execute
wave: 2
depends_on: ["04.4-01"]
files_modified:
  - lib/navegacao/itens.ts
  - components/amassa/barra-inferior.tsx
  - components/amassa/barra-lateral.tsx
  - components/amassa/cabecalho-movel.tsx
  - tests/unit/navegacao.test.ts
  - tests/e2e/casca.spec.ts
  - tests/e2e/acessibilidade.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - components/amassa/financeiro/abas-financeiro.tsx
  - app/(app)/cadastros/layout.tsx
  - app/(app)/cadastros/page.tsx
  - app/(app)/cadastros/loading.tsx
  - app/(app)/cadastros/error.tsx
  - lib/cadastros/abas.ts
  - lib/cadastros/categorias.ts
  - lib/cadastros/avisos.ts
  - lib/cadastros/textos.ts
  - lib/cadastros/esquemas.ts
  - lib/cadastros/acoes.ts
  - lib/cadastros/consultas.ts
  - components/amassa/cadastros/sub-abas-cadastros.tsx
  - components/amassa/cadastros/lista-categorias.tsx
  - components/amassa/cadastros/dialogo-categoria.tsx
  - components/amassa/cadastros/formulario-taxa.tsx
  - components/amassa/cadastros/aviso-cadastros.tsx
  - tests/unit/cadastros-categorias.test.ts
  - tests/e2e/cadastros-base.spec.ts
autonomous: true
requirements: [FNC-05, FNC-12, FNC-17]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 180000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "No celular, a barra de baixo tem exatamente cinco itens, nesta ordem: Início · Encomendas · Financeiro · Agenda · Queimas — o Estoque saiu dela (D-04); 'Financeiro' abre `/financeiro` direto na Venda e usa o ícone `Wallet`."
    - "No desktop, a barra lateral tem seis itens: Início · Encomendas · Financeiro · Agenda · Queimas · Estoque — ganhou o Financeiro sem perder nada (D-05)."
    - "`lib/navegacao/itens.ts` exporta duas listas nomeadas (celular e lateral) e continua sem nenhum import; `ehItemAtivo` não mudou de regra — só a lista de entrada muda por superfície."
    - "O título do cabeçalho do celular continua certo em `/estoque` (que saiu da barra) e mostra 'Cadastros' em `/cadastros` — ele lê a lista lateral, que é a mais completa."
    - "Cadastros é rota própria, `/cadastros` (D-06), alcançada pela quinta pílula da barra do Financeiro — um link de verdade, marcado como selecionado quando o caminho começa com `/cadastros` — e `/cadastros` mostra a MESMA barra de pílulas no topo, para voltar à Venda/Caixa sem passar pelo menu do usuário. Não existe item 'Cadastros' no menu do usuário."
    - "Dentro de `/cadastros`, quatro sub-abas por `?sub=`: Catálogo · Categorias · Contas fixas · Taxas, com Catálogo como padrão quando o parâmetro falta ou é desconhecido; a 320px as duas fileiras de pílulas não rolam na horizontal (o rótulo quebra em duas linhas, nunca trunca)."
    - "A lista de categorias aparece agrupada por grupo, na ordem Receitas · Custos diretos de uma área · Geral (custos da casa) · Fora do resultado, cada categoria com a área em etiqueta e 'N lançamento(s)', e a dica 'A categoria já diz de que área é. Por isso você nunca escolhe área ao lançar.'"
    - "'+ Nova categoria' (o único botão terracota da sub-aba) cria categoria com nome, grupo e área; quando o grupo é Geral ou Fora do resultado, a área é Geral e não se escolhe — o banco recusa qualquer outra combinação."
    - "Categoria nunca é apagada: 'Desativar' a tira de toda lista de escolha e a deixa riscada em Cadastros, continuando nos relatórios e nos itens que já a usam; 'Reativar' a devolve — sem diálogo de confirmação, com o aviso 'Categoria desativada.' / 'Categoria reativada.' (UI-SPEC §Categorias)."
    - "Editar categoria troca o nome sempre; grupo e área só mudam enquanto a categoria não tem uso (lançamento, item de catálogo ou conta fixa) — com uso, os dois campos aparecem travados e a ação recusa mesmo que o cliente mande (FNC-12). A categoria 'Juros, multas e descontos' nunca muda de grupo nem de área (D-02)."
    - "Nome de categoria repetido, ignorando maiúsculas e espaços nas pontas, é recusado com 'Já existe uma categoria com esse nome.'; nome vazio ou acima de 120 caracteres é recusado com frase humana."
    - "Em Taxas, o campo 'Quanto a maquininha cobra (%)' aceita '3,5' ou '3.5', grava 350 pontos-base, e recusa acima de 30% com 'Confira: uma taxa acima de 30% não parece de maquininha.' — vale só para vendas daqui para a frente (FNC-05; a gravação congela na parcela é do plano 06)."
    - "Duas pessoas criando categorias ao mesmo tempo terminam com as duas, e duas criando o MESMO nome terminam com uma e uma frase para a outra — o índice único decide, não o cliente."
    - "Os requisitos FNC-01..17 de REQUIREMENTS.md foram reescritos para incorporar D-01..D-14 (eram rascunho) e UI-02 descreve a barra nova; a nota no critério 2 da Fase 2b do ROADMAP aponta a Fase 04.4."
    - "O estado vazio de Categorias ('Nenhuma categoria ainda.' / 'Categorias dizem de que área vêm as vendas e os custos — comece por uma.' + '+ Nova categoria') existe e é renderizado quando a lista vem vazia; como a migração 0016 semeia 24 categorias e nada se apaga, ele não é alcançável em banco nenhum desta plataforma — a cópia é conferida por busca em `lib/cadastros/textos.ts` e o ramo, por leitura do componente."
    - statement: "Nome longo de categoria (120 caracteres) não quebra o layout da lista nem do diálogo a 320px."
      verification: backstop
    - statement: "Enquanto `/cadastros` carrega, aparece um esqueleto com as duas fileiras de pílulas e uma lista, nunca a palavra 'carregando' sozinha."
      verification: backstop
  prohibitions:
    - statement: "Nenhuma ação de Cadastros apaga categoria — não existe `delete` de categoria no código, e o papel da aplicação nem tem esse privilégio (plano 01)."
      status: resolved
      verification: test
    - statement: "O usuário nunca escolhe área ao lançar — a área só aparece no cadastro da categoria e é exibida derivada dela nas telas de lançamento."
      status: resolved
      verification: judgment
    - statement: "Nenhuma escrita de Cadastros termina em atualização de roteador do Next; gravar é navegação completa e o aviso vem pela URL."
      status: resolved
      verification: test
    - statement: "Cadastros não entra no menu do usuário (D-06)."
      status: resolved
      verification: test
  artifacts:
    - lib/navegacao/itens.ts
    - app/(app)/cadastros/page.tsx
    - lib/cadastros/acoes.ts
    - lib/cadastros/categorias.ts
    - components/amassa/cadastros/lista-categorias.tsx
    - tests/e2e/cadastros-base.spec.ts
  key_links:
    - "`ITENS_NAVEGACAO_CELULAR` alimenta só `BarraInferior`; `ITENS_NAVEGACAO_LATERAL` alimenta `BarraLateral` e o título do `CabecalhoMovel`. Se o cabeçalho lesse a lista do celular, `/estoque` no celular perderia o título."
    - "`podeMudarGrupoEArea(uso)` (módulo puro) decide o travamento na tela e na ação; o gatilho `travar_grupo_e_area_da_categoria()` do plano 01 é a camada de baixo, que só enxerga lançamentos — itens e contas fixas são conferidos pela ação."
    - "`AbasFinanceiro` é o mesmo componente em `/financeiro` e em `/cadastros`; a quinta pílula é um link de rota, as outras são `?aba=` — misturar as duas coisas no mesmo `role=tablist` é o que o UI-SPEC pediu."

coverage:
  - id: D1
    description: "Barra do celular com 5 itens (Financeiro no lugar do Estoque) e lateral com 6"
    requirement: "FNC-17"
    verification:
      - kind: unit
        ref: "tests/unit/navegacao.test.ts — as duas listas, exatas e na ordem"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/casca.spec.ts — navegação visível por projeto"
        status: unknown
    human_judgment: false
  - id: D2
    description: "Categorias: criar, editar nome, travar grupo com uso, desativar/reativar, nome repetido recusado; Taxas grava"
    requirement: "FNC-12, FNC-05"
    verification:
      - kind: unit
        ref: "tests/unit/cadastros-categorias.test.ts"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/cadastros-base.spec.ts"
        status: unknown
    human_judgment: false
---

<objective>
O Financeiro entra na navegação (D-04, D-05, D-06), Cadastros ganha casa própria em `/cadastros`
com as quatro sub-abas, e as duas primeiras delas passam a funcionar de verdade: **Categorias**
(criar, editar, desativar, reativar — nunca apagar) e **Taxas** (a taxa da maquininha). Os
requisitos FNC, que eram rascunho, passam a dizer o que o CONTEXT decidiu.

Purpose: depois deste plano, o gestor chega ao Financeiro pelo polegar, na barra de baixo, e ajusta
as categorias que vieram da semente e a taxa do cartão antes de lançar a primeira venda no cartão.

Output: a navegação dividida entre celular e lateral, a rota `/cadastros` com as sub-abas,
Categorias e Taxas completas, `lib/cadastros/`, REQUIREMENTS.md atualizado e os testes antigos da
casca reescritos para a barra nova.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-01-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| `ITENS_NAVEGACAO_CELULAR`, `ITENS_NAVEGACAO_LATERAL`, chave de ícone `financeiro` | `lib/navegacao/itens.ts` |
| Pílula "Cadastros" (link para `/cadastros`) | `components/amassa/financeiro/abas-financeiro.tsx` |
| Rota `/cadastros` (layout, página, carregando, erro) | `app/(app)/cadastros/` |
| `SubCadastros`, `subDaUrl` | `lib/cadastros/abas.ts` |
| `podeMudarGrupoEArea`, `areaFixaDoGrupo`, `ordemDosGrupos` | `lib/cadastros/categorias.ts` |
| `avisoDaUrl` de Cadastros | `lib/cadastros/avisos.ts` |
| Rótulos e frases de Cadastros | `lib/cadastros/textos.ts` |
| `esquemaCategoria`, `esquemaEdicaoDeCategoria`, `esquemaAtivacao`, `esquemaTaxa` | `lib/cadastros/esquemas.ts` |
| `criarCategoria`, `editarCategoria`, `definirCategoriaAtiva`, `definirTaxaDoCartao` | `lib/cadastros/acoes.ts` |
| `listarCategoriasComUso`, `obterTaxaDoCartao` | `lib/cadastros/consultas.ts` |
| `SubAbasCadastros`, `ListaCategorias`, `DialogoCategoria`, `FormularioTaxa`, `AvisoCadastros` | `components/amassa/cadastros/` |
| `data-testid`: `cadastros-sub-<valor>`, `categoria-linha`, `categoria-uso`, `taxa-campo` | componentes |

<tasks>

<task type="auto">
  <name>Tarefa 1: O Financeiro na barra do polegar — e os requisitos dizendo o que foi decidido</name>
  <files>lib/navegacao/itens.ts, components/amassa/barra-inferior.tsx, components/amassa/barra-lateral.tsx, components/amassa/cabecalho-movel.tsx, tests/unit/navegacao.test.ts, tests/e2e/casca.spec.ts, tests/e2e/acessibilidade.spec.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `lib/navegacao/itens.ts` inteiro (o comentário de cabeçalho e `ehItemAtivo`).
    - `components/amassa/barra-inferior.tsx`, `barra-lateral.tsx` e `cabecalho-movel.tsx` inteiros — o mapa de ícones, o comentário do 5 itens e `derivarTituloDaTela`.
    - `tests/unit/navegacao.test.ts`, `tests/e2e/casca.spec.ts` (linhas 1-180: a lista de telas, `localizarNavegacaoPrincipal`, os testes de 5 itens e de `aria-current`) e `tests/e2e/acessibilidade.spec.ts` (a lista de rotas e os dois laços sobre os itens).
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-04, D-05, D-06 e todas as outras decisões (para reescrever os FNC).
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Barra do celular e barra lateral (D-04/D-05)".
    - `.planning/REQUIREMENTS.md` linhas 45-55 (UI-02) e 165-195 (FNC-01..17).
    - `.planning/ROADMAP.md` §"Phase 2b" critério 2 e o precedente da nota de correção do critério 3 da Fase 3.
  </read_first>
  <action>
**`lib/navegacao/itens.ts`.** Continua sem nenhum import. `ChaveDeIcone` ganha `financeiro`. Duas listas exportadas no lugar de `ITENS_NAVEGACAO`: `ITENS_NAVEGACAO_CELULAR` = Início (`/`), Encomendas, **Financeiro (`/financeiro`)**, Agenda, Queimas; `ITENS_NAVEGACAO_LATERAL` = Início, Encomendas, **Financeiro**, Agenda, Queimas, Estoque. Atualize o comentário de cabeçalho: celular e lateral divergem desde a Fase 04.4 (D-04/D-05); a barra definitiva (Início · Financeiro · Produção · Agenda) é da fase do `/gestao`, não daqui. `ehItemAtivo` não muda.

**Barras.** `BarraInferior` itera `ITENS_NAVEGACAO_CELULAR`; `BarraLateral` itera `ITENS_NAVEGACAO_LATERAL`; as duas mapeiam `financeiro` para o ícone `Wallet` de `lucide-react` (UI-SPEC). Corrija os comentários que dizem "os 5 itens de ITENS_NAVEGACAO".

**`CabecalhoMovel`.** `derivarTituloDaTela` procura na lista LATERAL (a mais completa — senão `/estoque` no celular perde o título) e ganha o caso `/cadastros` → "Cadastros", no mesmo molde do caso de Orçamentos. Comentário explicando as duas coisas.

**Testes.** `navegacao.test.ts`: as duas listas, exatas e na ordem; nenhuma tem `/orcamentos` nem `/cadastros`; para cada `href` das duas, exatamente um item ativo. `casca.spec.ts`: o teste de "exatamente 5 itens" passa a escolher a lista pelo projeto (`test.info().project.name` contendo "celular" → celular, 5 itens; senão lateral, 6 itens) e confere rótulo e ordem; o teste de `aria-current` percorre a lista do projeto; a lista de telas vazias continua cobrindo `/estoque` — no celular por URL direta, porque não há mais link na barra. `acessibilidade.spec.ts`: os dois laços usam a lista do projeto, e `/financeiro` e `/cadastros` entram na lista de rotas varridas pelo axe.

**`REQUIREMENTS.md`.** UI-02 passa a dizer: "No celular, a barra inferior tem 5 itens (Início, Encomendas, Financeiro, Agenda, Queimas); no desktop, a barra lateral tem esses cinco mais Estoque (Fase 04.4, D-04/D-05)", mantendo a marcação de completo e uma nota curta de que o texto antigo tinha Estoque no lugar de Financeiro. Reescreva FNC-01..17 incorporando o CONTEXT, sem mudar os identificadores nem a tabela de rastreio: FNC-02 ganha o desconto no total em R$ ou % repartido na proporção das linhas, com a sobra de centavos na maior linha, sem linha separada (D-09/D-10); FNC-03 ganha a forma por parcela (D-07) e o "+ outra forma" no à vista (D-08); FNC-05 diz que a taxa incide só na parcela do cartão; FNC-07/FNC-09 ganham o extrato por mês com filtro Todas · Dinheiro · Pix · Cartão e o saldo depois sempre global (D-11/D-12); FNC-08 ganha a linha "diferença" na categoria "Juros, multas e descontos" quando o documento tem mais de uma linha ou parcela, e o desfazer como inverso exato (D-01/D-02/D-03); FNC-12 diz que categoria nunca se apaga, só desativa, e que em produção nascem as 24 categorias (D-14); FNC-14 ganha criar, desativar e reativar conta fixa, e conta desativada fora da geração (D-13); acrescente a FNC-01 que Cadastros é rota própria alcançada pelo Financeiro (D-06). Cada frase cita o D-XX.

**`ROADMAP.md`.** No critério 2 da Fase 2b, uma nota curta no molde da nota do critério 3 da Fase 3: "(Barra do celular revista na Fase 04.4 — D-04: Financeiro entrou, Estoque saiu da barra do celular e ficou na lateral.)". Não mexa em mais nada do ROADMAP.

**Orçamento de e2e:** uma invocação, com recorte nos dois arquivos de casca e acessibilidade.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- tests/unit/navegacao.test.ts &amp;&amp; npm run test:e2e -- --grep "casca de navegação|acessibilidade"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import" lib/navegacao/itens.ts` devolve 0.
    - `grep -rn "ITENS_NAVEGACAO\b" --include=*.ts --include=*.tsx components lib tests app` não encontra o nome antigo sozinho (só as duas listas novas).
    - `grep -n "Wallet" components/amassa/barra-inferior.tsx components/amassa/barra-lateral.tsx` encontra o ícone nas duas.
    - No projeto celular a navegação principal visível tem exatamente 5 links, o terceiro é "Financeiro" e nenhum é "Estoque"; no desktop são 6, com "Financeiro" em terceiro e "Estoque" em sexto.
    - `grep -n "Financeiro" .planning/REQUIREMENTS.md` mostra UI-02 com a barra nova; `grep -c "D-0[1-9]\|D-1[0-4]" .planning/REQUIREMENTS.md` encontra as citações nos FNC.
    - `grep -n "Fase 04.4" .planning/ROADMAP.md` encontra a nota no critério 2 da Fase 2b.
  </acceptance_criteria>
  <done>O Financeiro está na barra do polegar, o Estoque continua no desktop, e os requisitos da fase descrevem o que o dono decidiu, não o rascunho.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A casa dos Cadastros, a regra da categoria e a taxa da maquininha</name>
  <files>components/amassa/financeiro/abas-financeiro.tsx, app/(app)/cadastros/layout.tsx, app/(app)/cadastros/page.tsx, app/(app)/cadastros/loading.tsx, app/(app)/cadastros/error.tsx, lib/cadastros/abas.ts, lib/cadastros/categorias.ts, lib/cadastros/avisos.ts, lib/cadastros/textos.ts, lib/cadastros/esquemas.ts, lib/cadastros/acoes.ts, lib/cadastros/consultas.ts, components/amassa/cadastros/sub-abas-cadastros.tsx, components/amassa/cadastros/formulario-taxa.tsx, components/amassa/cadastros/aviso-cadastros.tsx, tests/unit/cadastros-categorias.test.ts</files>
  <read_first>
    - `components/amassa/financeiro/abas-financeiro.tsx`, `lib/financeiro/avisos.ts`, `lib/financeiro/esquemas.ts`, `lib/financeiro/dinheiro.ts` e `components/amassa/financeiro/aviso-financeiro.tsx` como o plano 01 os deixou.
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Sub-navegação do Financeiro (D-06)", §"Dentro de Cadastros (4 sub-abas)", §Foco Visual (Taxas), §Copywriting (estados vazios de Cadastros).
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 353-358 (`telaCadastros`: pílulas, categorias, taxas) e 389 (`folhaCat`).
    - `lib/abertura/acoes.ts` — `definirDataDeInauguracao` (o `insert ... on conflict` sobre a restrição de linha única, molde de `definirTaxaDoCartao`).
    - `app/(app)/abertura/layout.tsx`, `page.tsx`, `loading.tsx`, `error.tsx`.
  </read_first>
  <behavior>
    `tests/unit/cadastros-categorias.test.ts`:
    - `areaFixaDoGrupo('geral')` e `('fora')` → `geral`; `('receita')` e `('custo')` → nulo (a área é escolhida).
    - `podeMudarGrupoEArea({ lancamentos: 0, itens: 0, contasFixas: 0, chaveDoSistema: null })` → verdadeiro; com qualquer um dos três maior que zero → falso; com `chaveDoSistema: 'diferenca'` → falso mesmo sem uso.
    - `ordemDosGrupos` é receita, custo, geral, fora.
    - `subDaUrl`: `catalogo`, `categorias`, `fixas`, `taxas` reconhecidos; ausente, vazio ou desconhecido → `catalogo`.
    - `avisoDaUrl` de Cadastros aceita só os tipos conhecidos (`categoria-desativada`, `categoria-reativada`) e devolve nulo para o resto.
  </behavior>
  <action>
Escreva o teste primeiro, veja falhar, depois implemente.

**`lib/cadastros/` (módulo novo, no molde de `lib/abertura`).** `abas.ts`: `SubCadastros` = `catalogo` | `categorias` | `fixas` | `taxas`, `subDaUrl` com padrão `catalogo`. `categorias.ts` (puro, sem import de valor): `areaFixaDoGrupo`, `podeMudarGrupoEArea(uso)`, `ordemDosGrupos`. `avisos.ts` (puro). `textos.ts` (só `import type`): rótulos das sub-abas ("Catálogo", "Categorias", "Contas fixas", "Taxas"), "Taxa do cartão", "Quanto a maquininha cobra (%)", a dica de Taxas do protótipo ("Vale para as vendas no cartão daqui para a frente. As vendas já lançadas guardam a taxa do dia em que foram feitas. Dinheiro e Pix entram inteiros."), "Salvar taxa", as frases de erro e os estados vazios de Cadastros do §Copywriting. `esquemas.ts`: reaproveita `esquemaId` de `lib/financeiro/esquemas.ts` (Financeiro e Cadastros são permanentes, o acoplamento é seguro); `esquemaTaxa` usa `converterPercentualParaPontosBase` e recusa acima de 3000 pontos-base com a frase dos 30%. `acoes.ts` (`"use server"`): `definirTaxaDoCartao(entradaBruta)` com `exigirUsuario()` primeiro e `insert ... on conflict` na restrição de linha única de `configuracao_financeira`, alterando só a taxa. `consultas.ts`: `obterTaxaDoCartao()` (0 sem linha).

**`AbasFinanceiro`.** Acrescente a quinta pílula "Cadastros" como `Link href="/cadastros"` real, selecionada quando `usePathname()` começa com `/cadastros`; as abas de `?aba=` ficam selecionadas só dentro de `/financeiro`. Mantenha o `memo` com comparador próprio sobre valores primitivos (aba e se está em Cadastros), como `abas-abertura.tsx`. A ordem final das pílulas é Venda · Despesa · Caixa · Mês · Cadastros; os planos 07 e 09 acrescentam as que faltam.

**Rota `/cadastros`.** `layout.tsx`: `exigirUsuario()` primeiro, `CabecalhoPagina` "Cadastros", e `AbasFinanceiro` no topo (a mesma barra do Financeiro). `page.tsx`: `exigirUsuario()` primeiro; `searchParams` com `sub` e `aviso`; `SubAbasCadastros` (segunda fileira de pílulas, mesmo padrão, `?sub=`); a sub-aba Taxas renderiza `FormularioTaxa`; Categorias fica com a Tarefa 3; Catálogo e Contas fixas ficam com os planos 05 e 10 (até lá a sub-aba mostra o estado vazio do §Copywriting sem o botão). `loading.tsx`: esqueleto com as duas fileiras e uma lista. `error.tsx`: "Não deu para carregar os Cadastros. Verifique a internet e tente de novo." + "Tentar de novo".

**`FormularioTaxa`** (cliente): um campo com `inputmode="decimal"`, 16px, 44px, `max-width` de 220px como no protótipo, valor atual mostrado com vírgula; "Salvar taxa" chama a ação e faz navegação completa para `/cadastros?sub=taxas`; erro em linha, campo preenchido. **`AvisoCadastros`**: no molde de `AvisoFinanceiro`, texto pronto vindo do servidor, limpa a URL depois.

Nenhuma invocação de e2e nesta tarefa — a Tarefa 3 roda o recorte de Cadastros cobrindo as duas.
  </action>
  <verify>
    <automated>npm test -- tests/unit/cadastros-categorias.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run verificar-acoes</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/cadastros-categorias.test.ts` passa com cada caso de `<behavior>` nomeado.
    - `grep -En "from \"(react|next|drizzle-orm|pg)|@/db" lib/cadastros/categorias.ts lib/cadastros/abas.ts lib/cadastros/avisos.ts` não encontra nada.
    - `npm run verificar-acoes` sai 0 — `definirTaxaDoCartao` começa por `exigirUsuario()`.
    - `grep -n "cadastros" components/amassa/menu-usuario.tsx` não encontra nada (D-06).
  </acceptance_criteria>
  <done>`/cadastros` existe com as sub-abas, a quinta pílula do Financeiro leva até lá, e a taxa da maquininha se grava e se lê.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Categorias — criar, editar, desativar e reativar, sem nunca apagar</name>
  <files>lib/cadastros/esquemas.ts, lib/cadastros/acoes.ts, lib/cadastros/consultas.ts, lib/cadastros/textos.ts, app/(app)/cadastros/page.tsx, components/amassa/cadastros/lista-categorias.tsx, components/amassa/cadastros/dialogo-categoria.tsx, tests/e2e/cadastros-base.spec.ts</files>
  <read_first>
    - `lib/cadastros/*` como a Tarefa 2 os deixou.
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Categorias — sempre desativa, nunca apaga de verdade" e §Copywriting (toasts de categoria).
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 356-357 e 389.
    - `components/amassa/cotacoes/dialogo-categoria.tsx` — o diálogo de criar/editar com um componente só (NÃO importar; o de Cadastros é outro).
    - `components/amassa/abertura/caixa-marcacao.tsx` — a ação que recebe o estado DESEJADO, nunca "inverter".
    - `db/migrations/0015_gatilhos-financeiro.sql` — a trava de grupo e área no banco.
    - `tests/e2e/abertura-tracador.spec.ts` — `fazerLogin`, `nomeUnico`.
  </read_first>
  <action>
**Servidor.** `esquemaCategoria` (nome 1-120 normalizado em NFC e contado em pontos de código, grupo do enum, área do enum) com refinamento: grupo `geral`/`fora` força área `geral`; `receita`/`custo` exige uma das quatro áreas. `esquemaEdicaoDeCategoria` = id + os mesmos campos. `esquemaAtivacao` = id + `ativa` desejada. Ações, todas com `exigirUsuario()` primeiro: `criarCategoria`; `editarCategoria` — numa transação, lê a categoria com `for update`, conta o uso (linhas lançadas, itens de catálogo como categoria de venda ou de compra, contas fixas) e, se grupo ou área mudaram e `podeMudarGrupoEArea` é falso, recusa com "Essa categoria já tem uso — o grupo e a área ficam como estão. Dá para mudar o nome."; `definirCategoriaAtiva` recebe o estado desejado (convergente: duas chamadas iguais dão o mesmo resultado). Violação do índice único vira "Já existe uma categoria com esse nome."; o erro do gatilho do banco vira a mesma frase de uso. Depois de gravar, a tela navega por completo para `/cadastros?sub=categorias` (com `&aviso=categoria-desativada|categoria-reativada` quando for o caso). `listarCategoriasComUso()`: todas as categorias (ativas e inativas) com a contagem de lançamentos (documentos distintos com linha naquela categoria, como o protótipo) e o uso total para o travamento — uma consulta agregada, nunca uma por categoria.

**`ListaCategorias`.** Cabeçalho do bloco "Categorias" com "+ Nova categoria" (terracota, o único da sub-aba) e a dica do protótipo. Agrupada pela `ordemDosGrupos`, com o rótulo do grupo em Apoio caixa alta; cada linha: nome, etiqueta da área, "N lançamento(s)", "Editar" e "Desativar"/"Reativar" (44px). Inativa aparece riscada (`.conta.canc` do protótipo). Sem diálogo de confirmação para desativar (reversível, UI-SPEC). Estado vazio do §Copywriting quando não há nenhuma.

**`DialogoCategoria`** (cliente, estado local, diálogo único responsivo, rodapé preso por flex): título "Nova categoria" ou "Editar categoria"; Nome (foco automático); "Tipo" (os quatro grupos com os rótulos do protótipo) e "Área" — a Área some e mostra "Geral" fixo quando o grupo é Geral ou Fora; com uso, Tipo e Área aparecem desabilitados com a dica "Grupo e área ficam travados depois que a categoria é usada."; "Cancelar" e "Salvar". Erro dentro do diálogo, tudo preenchido.

**E2E** (`tests/e2e/cadastros-base.spec.ts`, "cadastros base" no título): pela barra de navegação, Financeiro → pílula Cadastros → `/cadastros` com as quatro sub-abas e a barra do Financeiro no topo; sub-aba Categorias mostra "Juros, multas e descontos" no grupo Geral; cria uma categoria de receita da Loja com `nomeUnico`, vê-a no grupo Receitas com a etiqueta Loja e "0 lançamento(s)"; tenta criar outra com o mesmo nome em maiúsculas e vê a frase de nome repetido; edita o nome; troca o grupo para Custos e salva (sem uso, passa); desativa, vê riscada e o aviso "Categoria desativada."; reativa e vê "Categoria reativada."; ao escolher "Geral (custos da casa)" a área mostra "Geral" fixo; sub-aba Taxas grava "3,5" e a página recarregada mostra "3,5"; tenta "31", vê a frase dos 30% e o valor gravado continua 3,5. **A taxa é configuração global e outros testes a leem** (o cartão, no plano 06): a regra da suíte é que todo teste que escreve a taxa escreve SEMPRE o mesmo valor, 3,5% — a "taxa de teste" — e nenhum teste escreve outro valor que fique gravado; assim a ordem entre testes paralelos não muda o resultado de nenhum. Deixe esse comentário no arquivo. A 320px, `/cadastros?sub=categorias` não rola na horizontal. Nomes inventados.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "cadastros base"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "cadastros base" passa nos dois projetos.
    - `grep -rEn "delete\(categorias\)|\.delete\(.*categorias" lib/cadastros` não encontra nada.
    - `grep -rEn "router\.(push|refresh)\(" components/amassa/cadastros "app/(app)/cadastros"` não encontra nada.
    - `npm run verificar-acoes` sai 0 com `criarCategoria`, `editarCategoria` e `definirCategoriaAtiva` começando por `exigirUsuario()`.
    - `grep -n "Nenhuma categoria ainda." lib/cadastros/textos.ts` encontra a cópia do estado vazio.
  </acceptance_criteria>
  <done>O gestor ajusta as categorias que vieram prontas, cria as dele, e nenhuma delas pode sumir dos relatórios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ações de Cadastros | Nome, grupo, área e identificadores vêm do cliente |
| navegador não autenticado → `/cadastros` | Configuração financeira não pode ser lida sem login |
| cliente → travamento de grupo/área | O cliente pode mandar grupo novo mesmo com os campos desabilitados |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-12 | Elevation of Privilege | `criarCategoria`, `editarCategoria`, `definirCategoriaAtiva`, `definirTaxaDoCartao` | high | mitigate | `exigirUsuario()` primeira instrução; `verificar-acoes` |
| T-04.4-13 | Information Disclosure | rota `/cadastros` | high | mitigate | `exigirUsuario()` no layout e na página |
| T-04.4-14 | Tampering | grupo/área de categoria com uso | high | mitigate | Conferência de uso dentro da transação com `for update` na ação; gatilho do banco recusa se houver lançamento; categoria do sistema nunca muda |
| T-04.4-15 | Tampering | taxa da maquininha | medium | mitigate | Zod no servidor com teto de 30%; inteiro em pontos-base; `check` 0-10000 no banco |
| T-04.4-16 | Repudiation | desativação de categoria | low | accept | Duas pessoas de confiança; a ação é reversível pela própria tela; `atualizado_em` registra a mudança |
| T-04.4-17 | Denial of Service | nome de categoria | low | mitigate | 1-120 pontos de código no Zod e no `check` |
| T-04.4-18 | Tampering | categoria apagada | high | mitigate | Não existe caminho de apagar; `revoke delete` do plano 01 |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Dois recortes de e2e neste plano: "casca de navegação|acessibilidade" (Tarefa 1) e "cadastros base" (Tarefa 3).
- A barra do celular tem 5 itens com Financeiro e sem Estoque; a lateral tem 6.
- REQUIREMENTS.md cita D-01..D-14 nos FNC e UI-02 descreve a barra nova.
</verification>

<success_criteria>
- O Financeiro está na navegação no celular e no desktop (D-04, D-05), e Cadastros tem rota própria alcançada pelo Financeiro (D-06).
- Categorias se criam, editam, desativam e reativam, e nunca se apagam; grupo e área travam com uso (FNC-12, critério 8 do ROADMAP).
- A taxa da maquininha é cadastrada (FNC-05, parte do cadastro).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-02-SUMMARY.md` when done
</output>
