---
phase: 04.4-financeiro-parte-1
plan: 08
type: execute
wave: 5
depends_on: ["04.4-07"]
files_modified:
  - lib/financeiro/pagamento.ts
  - tests/unit/financeiro-pagamento.test.ts
  - lib/financeiro/esquemas.ts
  - lib/financeiro/acoes.ts
  - lib/financeiro/consultas.ts
  - lib/financeiro/textos.ts
  - lib/financeiro/avisos.ts
  - app/(app)/financeiro/page.tsx
  - components/amassa/financeiro/listas-caixa.tsx
  - components/amassa/financeiro/cartao-conta.tsx
  - components/amassa/financeiro/dialogo-documento.tsx
  - components/amassa/financeiro/dialogo-baixa.tsx
  - components/amassa/financeiro/confirmar-cancelar-documento.tsx
  - components/amassa/financeiro/aviso-financeiro.tsx
  - components/amassa/financeiro/extrato-caixa.tsx
  - tests/e2e/apoio/semear-conta-a-pagar.ts
  - tests/e2e/financeiro-caixa.spec.ts
autonomous: true
requirements: [FNC-05, FNC-07, FNC-08, FNC-10, FNC-17]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 200000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "O Caixa mostra, abaixo dos quatro tiles, 'A pagar' e 'A receber' (lado a lado a partir de 980px): cada conta com o título (numa linha), o valor, a etiqueta de rótulo ('parcela 26 de 60', '7 de 10') ou 'k de N' quando o documento tem mais de uma parcela, a pessoa, 'vence DD/MM/AA', a etiqueta 'vencida' quando o vencimento é ANTES de hoje (vence hoje não é vencida), e os botões 'Ver' e 'Paguei'/'Recebi' — ordenadas por vencimento (FNC-07)."
    - "Com um documento de uma parcela só, não aparece '1 de 1'; com a lista vazia aparece 'Nenhuma conta em aberto.' (a pagar) ou 'Ninguém deve nada.' (a receber), cada lista com o próprio vazio."
    - "'Paguei'/'Recebi' abre o diálogo com Valor (o previsto), Quando (hoje; nunca depois de hoje nem antes do saldo inicial) e Forma (a da parcela), e 'Confirmar' tira a conta da lista e move o saldo (critério 4 do ROADMAP, FNC-08)."
    - "Quando o valor pago difere do previsto e o documento tem MAIS DE UMA linha ou MAIS DE UMA parcela, o sistema acrescenta sozinho uma linha 'diferença' com o valor que sobrou ou faltou (positivo ou negativo), na categoria 'Juros, multas e descontos' (D-01/D-02), e a soma das parcelas continua fechando com o total das linhas — o boleto de R$ 800 pago com R$ 812 vira parcela de R$ 812 e linha de + R$ 12,00."
    - "Com UMA linha e UMA parcela (conta fixa, tipicamente), o valor diferente ajusta a própria linha, sem linha de diferença (D-01)."
    - "O aviso depois do pagamento é '{Recebido/Pago}: {R$ valor}' e, quando criou diferença, '. {R$ diferença} {a mais/a menos} viraram uma linha de diferença.' — com o botão 'Desfazer' por 7 segundos."
    - "'Desfazer' é o INVERSO EXATO do 'Paguei/Recebi' (D-03): apaga a linha de diferença que o pagamento criou (ou devolve a linha única ao valor anterior), devolve a parcela ao valor e à forma de antes, e a deixa em aberto — o aviso é 'Desfeito. A conta voltou a {R$ valor previsto} em aberto.'. Pagar e desfazer, em qualquer documento, devolve o documento idêntico ao que era (propriedade provada em teste unitário)."
    - "Desfazer só vale para pagamento registrado por 'Paguei/Recebi' (a parcela guarda o previsto); a parcela paga no próprio lançamento, ou de documento cancelado, é recusada com frase que diz o que fazer."
    - "Receber no cartão uma parcela de venda congela a taxa daquele instante na parcela; o que entra no caixa é o valor menos a taxa (FNC-05)."
    - "Duas pessoas pagando a mesma conta: a segunda recebe 'Essa conta já foi paga — recarregue a página para ver como ela está.' e nada se duplica — a parcela é travada (`for update`) dentro da transação; pagar parcela de documento cancelado por outra pessoa recebe 'Esse lançamento foi cancelado — ele não recebe mais pagamento.'"
    - "'Ver' abre o detalhe do documento ('Venda/Despesa nº N · DD/MM/AA · pessoa'), com as linhas (e a etiqueta da categoria de cada uma, inclusive a linha de diferença), as parcelas ('k/N · vence DD/MM/AA', com 'recebida/paga em DD/MM/AA' nas pagas), e 'Cancelar esta venda/despesa'."
    - "Cancelar pede confirmação com 'Cancelar {esta venda/esta despesa} nº {N} «{título}»? Ela fica riscada no extrato, sai do saldo e do Mês. Quem cancelou e quando ficam registrados. Isso não pode ser desfeito — se foi engano, lance de novo depois.' e, confirmado, NADA é apagado: o documento ganha `cancelado_em`/`cancelado_por`, fica riscado no extrato com 'cancelada' e sem saldo, as parcelas em aberto dele somem das listas, e o detalhe mostra 'Cancelado por {nome} em DD/MM/AA' (FNC-10, critério 5 do ROADMAP)."
    - "Cancelar duas vezes é recusado com 'Esse lançamento já foi cancelado.'; o aviso depois de cancelar é 'Lançamento nº {N} cancelado. Continua visível, riscado.'"
    - "O saldo do Caixa bate com o saldo inicial mais a soma do extrato também depois de pagar, desfazer e cancelar — o tile é igual ao 'saldo depois' do movimento não cancelado mais recente (critério 7 do ROADMAP)."
    - "Os exemplos de despesa 3 (Financiamento, parcela 26 de 60) e 5 (parcela da compra de esmaltes) do protótipo são pagos pelo Caixa no e2e."
    - "Com o banco vazio, 'Nenhuma conta em aberto.' e 'Ninguém deve nada.' aparecem — provado na etapa `@vazio-global`."
    - statement: "O título longo de documento é cortado numa linha só no cartão de conta, e o detalhe mostra o título inteiro."
      verification: backstop
    - statement: "O botão 'Desfazer' do aviso funciona pelo teclado e pelo toque, no mesmo padrão do Desfazer de queima; não há teste visual próprio além do e2e funcional."
      verification: backstop
    - statement: "A 320px, os quatro tiles com valores altos ('R$ 123.456,78') caem para uma ou duas colunas sem quebrar o número e sem rolagem horizontal da página."
      verification: backstop
  prohibitions:
    - statement: "Nenhum documento, parcela ou linha lançada pelo usuário é apagado por nenhuma ação; a única exclusão física do módulo é a linha de diferença, e só pelo 'Desfazer' do pagamento que a criou."
      status: resolved
      verification: test
    - statement: "Nenhuma escrita do Caixa termina em atualização de roteador do Next: pagar, desfazer e cancelar são navegação completa, e o aviso (com o Desfazer) é montado pela página a partir da URL."
      status: resolved
      verification: test
    - statement: "A linha 'diferença' nunca vai para a área do documento: a categoria dela é sempre a de `chave_do_sistema = 'diferenca'` (Geral)."
      status: resolved
      verification: test
  artifacts:
    - lib/financeiro/pagamento.ts
    - components/amassa/financeiro/listas-caixa.tsx
    - components/amassa/financeiro/dialogo-baixa.tsx
    - components/amassa/financeiro/dialogo-documento.tsx
    - components/amassa/financeiro/confirmar-cancelar-documento.tsx
    - tests/e2e/financeiro-caixa.spec.ts
  key_links:
    - "`planejarPagamento` e `planejarDesfazer` (puros) decidem o que muda; `registrarPagamento` e `desfazerPagamento` só executam o plano numa transação que trava documento e parcela. A restrição adiada do banco (plano 01) confere, no fim de cada uma, que as parcelas fecham com as linhas — é o que torna o 'inverso exato' verificável, não só pretendido."
    - "`valor_previsto_centavos`, `forma_prevista` e `parcela_diferenca_id` (plano 01) são o que o desfazer lê; sem eles, desfazer seria adivinhar."
    - "O aviso `pago` leva o id da parcela na URL; a página confere que ela ainda está paga antes de mostrar o 'Desfazer' — recarregar depois de desfazer não oferece desfazer de novo."
    - "O detalhe do documento acha o documento na lista JÁ carregada pela página (contas em aberto + movimentos do mês), nunca por uma segunda consulta ao abrir."

coverage:
  - id: D1
    description: "Pagar e desfazer, com linha de diferença ou linha ajustada, são inversos exatos"
    requirement: "FNC-08"
    verification:
      - kind: unit
        ref: "tests/unit/financeiro-pagamento.test.ts — casos e propriedade de ida e volta"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/financeiro-caixa.spec.ts — exemplo 5 (esmaltes R$ 800 pagos com R$ 812 e desfeito)"
        status: unknown
    human_judgment: false
  - id: D2
    description: "Listas a pagar e a receber com vencidas, detalhe, cancelamento sem apagar"
    requirement: "FNC-07, FNC-10"
    verification:
      - kind: e2e
        ref: "tests/e2e/financeiro-caixa.spec.ts"
        status: unknown
    human_judgment: false
---

<objective>
O Caixa que age: **as listas a pagar e a receber com as vencidas marcadas, o "Paguei"/"Recebi" com
a linha de diferença (D-01/D-02), o "Desfazer" como inverso exato (D-03), o detalhe do documento e
o cancelamento que risca sem apagar** (FNC-07, FNC-08, FNC-10).

Purpose: é aqui que as contas em aberto que a Venda, a Despesa, as contas fixas e a virada criam
viram dinheiro de verdade no saldo — e onde um toque errado precisa ter volta exata. D-03 é
`costly` no CONTEXT: o schema do plano 01 já guardou o previsto; este plano é quem o usa.

Output: `lib/financeiro/pagamento.ts` testado (incluindo a propriedade de ida e volta), as três
ações do Caixa, as listas, os três diálogos, o aviso com "Desfazer" e o e2e com os exemplos 3 e 5
de despesa do protótipo.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-01-SUMMARY.md
@.planning/phases/04.4-financeiro-parte-1/04.4-07-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| `planejarPagamento`, `planejarDesfazer`, tipos `PlanoDePagamentoDaParcela`, `PlanoDeDesfazer` | `lib/financeiro/pagamento.ts` |
| `esquemaPagamento`, `esquemaDesfazer`, `esquemaCancelamento` | `lib/financeiro/esquemas.ts` |
| `registrarPagamento`, `desfazerPagamento`, `cancelarDocumento` | `lib/financeiro/acoes.ts` |
| `listarContasEmAberto`, `listarDocumentosParaDetalhe`, `obterParcelaParaAviso` | `lib/financeiro/consultas.ts` |
| `textoDoPagamento`, `textoDoDesfazer`, `textoDoCancelamento`, frases de recusa | `lib/financeiro/textos.ts` |
| Avisos `pago`, `desfeito`, `cancelado` | `lib/financeiro/avisos.ts`, `components/amassa/financeiro/aviso-financeiro.tsx` |
| `ListasCaixa`, `CartaoConta`, `DialogoDocumento`, `DialogoBaixa`, `ConfirmarCancelarDocumento` | `components/amassa/financeiro/` |
| `semearContaAPagar` | `tests/e2e/apoio/semear-conta-a-pagar.ts` |
| `data-testid`: `caixa-a-pagar`, `caixa-a-receber`, `conta-cartao`, `conta-vencida`, `conta-rotulo`, `documento-detalhe`, `documento-linha`, `documento-parcela`, `baixa-valor`, `baixa-data`, `baixa-forma` | componentes |

## Divergências deliberadas do protótipo (vão para a tabela do plano 11)

- **"Paguei"/"Recebi" com contorno, não terracota:** o UI-SPEC fecha a lista de botões em acento
  (um por tela) e o Caixa não está nela; no protótipo eles eram `.pri`.
- **A dica do detalhe perde "e o estoque é devolvido":** nesta fase nada é gravado no estoque (a
  Fase 6 devolve essa parte da frase junto com o estorno).
- **O detalhe mostra quem cancelou e quando** — FNC-10 manda guardar; mostrar é o que torna isso
  conferível.

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O pagamento e o desfazer como par exato — regra pura, testada primeiro</name>
  <files>lib/financeiro/pagamento.ts, tests/unit/financeiro-pagamento.test.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-01, D-02 e D-03, palavra por palavra.
    - `.planning/phases/04.4-financeiro-parte-1/BRIEFING.md` §5 "Pagar / receber".
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linha 420 (`confirmar-baixa`).
    - `db/schema.ts` — `parcelas` (`valor_previsto_centavos`, `forma_prevista`, `taxa_pontos_base`) e `documento_linhas` (`parcela_diferenca_id`).
    - `lib/financeiro/taxa.ts` e `tests/unit/financeiro-extrato.test.ts`.
  </read_first>
  <behavior>
    - Pago igual ao previsto → nenhuma mudança de linha; a parcela guarda previsto e forma anterior mesmo assim (para o desfazer ser uniforme).
    - Uma linha e uma parcela, 148000 previsto pago com 150000 → ajustar a linha para 150000; sem diferença.
    - Uma linha e três parcelas (esmaltes), parcela de 80000 paga com 81200 → linha de diferença de +1200; paga com 79000 → −1000.
    - Duas linhas e uma parcela → linha de diferença (não ajusta nenhuma das duas).
    - Venda paga no cartão com 350 pontos-base → taxa 350 na parcela; despesa no cartão → nula; venda no Pix → nula.
    - Desfazer com linha de diferença → remover a linha, parcela volta ao previsto e à forma anterior, em aberto.
    - Desfazer do caso de linha única ajustada → linha volta ao previsto.
    - Desfazer sem previsto guardado (paga no lançamento) → recusa com a frase de cancelar e lançar de novo.
    - Propriedade de ida e volta: para 200 documentos gerados de forma determinística (semente fixa: 1 a 5 linhas, 1 a 12 parcelas, valores e pagos aleatórios, formas variadas), aplicar `planejarPagamento` e depois `planejarDesfazer` sobre um modelo em memória devolve linhas e parcelas idênticas às de antes, e a soma das parcelas é igual à soma das linhas depois de CADA passo.
    - Pagar a segunda parcela depois da primeira (com duas diferenças) e desfazer só a primeira mantém a diferença da segunda.
    - A função não muta a entrada.
  </behavior>
  <action>
Teste primeiro, falhando pelo motivo certo. Depois `lib/financeiro/pagamento.ts`, puro: `planejarPagamento({ tipoDocumento, quantidadeDeLinhas, quantidadeDeParcelas, previstoCentavos, pagoCentavos, formaAnterior, formaNova, taxaPontosBaseAtual })` → `{ acaoNaLinha: 'nenhuma' | 'ajustar' | 'diferenca', diferencaCentavos, taxaPontosBase }`; `planejarDesfazer({ temLinhaDeDiferenca, quantidadeDeLinhas, quantidadeDeParcelas, previstoCentavos, pagoCentavos, formaPrevista })` → o que restaurar, ou recusa. Comentário no topo citando D-01/D-02/D-03 e dizendo que a regra "uma linha e uma parcela ajusta a linha" é do briefing, e a da diferença é do CONTEXT. Sem import de React, Next ou do cliente do banco; sem `Date`. Nenhuma invocação de e2e.
  </action>
  <verify>
    <automated>npm test -- tests/unit/financeiro-pagamento.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - O teste passa, com o caso da propriedade de ida e volta sobre 200 documentos (D-03).
    - Os casos "uma linha e uma parcela ajusta a linha" e "mais de uma linha ou parcela cria diferença" existem com nome próprio (D-01).
    - `grep -En "from \"(react|next|drizzle-orm|pg)|@/db|@/lib/abertura" lib/financeiro/pagamento.ts` não encontra nada.
  </acceptance_criteria>
  <done>O "Desfazer" é o inverso exato do "Paguei" por construção, e isso é um teste que roda em milissegundos.</done>
</task>

<task type="auto">
  <name>Tarefa 2: As listas a pagar e a receber, o detalhe do documento e o cancelamento que risca sem apagar</name>
  <files>lib/financeiro/esquemas.ts, lib/financeiro/acoes.ts, lib/financeiro/consultas.ts, lib/financeiro/textos.ts, lib/financeiro/avisos.ts, app/(app)/financeiro/page.tsx, components/amassa/financeiro/listas-caixa.tsx, components/amassa/financeiro/cartao-conta.tsx, components/amassa/financeiro/dialogo-documento.tsx, components/amassa/financeiro/confirmar-cancelar-documento.tsx, components/amassa/financeiro/aviso-financeiro.tsx, components/amassa/financeiro/extrato-caixa.tsx, tests/e2e/apoio/semear-conta-a-pagar.ts, tests/e2e/financeiro-caixa.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 321-332 (`contaHTML`, `telaCaixa`), 368-373 (`folhaDoc`) e 421 (cancelar).
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §Color (destrutivo e "vencida"), §Copywriting (vazios do Caixa, confirmação de cancelamento, aviso de cancelado), §"Cancelamento (FNC-10)", §UI Considerations (partial, zero-one-many).
    - `components/amassa/cotacoes/confirmar-remover-categoria.tsx` — AlertDialog que acha a linha na lista já carregada e fecha pelos dois caminhos.
    - `components/amassa/encomendas/` — o `AlertDialogAction` com `event.preventDefault()` e `open` controlado, que não fecha até a resposta do servidor (decisão 03-05 no STATE).
    - `lib/financeiro/consultas.ts`, `avisos.ts`, `aviso-financeiro.tsx`, `extrato-caixa.tsx`, `tiles-caixa.tsx` como os planos anteriores os deixaram.
    - `tests/e2e/apoio/semear-financeiro.ts` e `tests/e2e/financeiro-tracador.spec.ts`.
  </read_first>
  <action>
**Servidor.** `cancelarDocumento({ documentoId })` com `exigirUsuario()` primeiro: numa transação, trava o documento (`for update`), recusa se já cancelado ("Esse lançamento já foi cancelado."), grava `cancelado_em` = agora e `cancelado_por` = usuário; devolve `{ documentoId, numero }`. Nenhum delete. `listarContasEmAberto()`: parcelas em aberto de documentos não cancelados, com número, tipo, título calculado, pessoa, data, quantidade de parcelas do documento e rótulo; ordenadas por vencimento e número. `listarDocumentosParaDetalhe(ids)`: documentos com linhas (descrição, quantidade, quantidade de estoque e unidade do item, nome da categoria, valor, se é diferença), parcelas (número, vencimento, valor, forma, pago em, rótulo) e, quando cancelado, o nome de quem cancelou e a data — uma consulta por tabela. `avisos.ts` ganha `cancelado` (documento); a página monta "Lançamento nº {N} cancelado. Continua visível, riscado."

**Tela.** A aba Caixa passa a ter, abaixo dos tiles, `ListasCaixa` ("A pagar" e "A receber", duas colunas a partir de 980px), e o extrato ganha o link "ver" por linha. `CartaoConta`: o título com `line-clamp-1`, o valor com `tabular-nums`, a etiqueta de rótulo ou "k de N" (só com N > 1), pessoa, "vence DD/MM/AA", etiqueta "vencida" (fundo `--color-erro-fundo`, texto `--color-erro`) só com vencimento antes de hoje; "Ver" e "Paguei"/"Recebi" com contorno e 44px (o diálogo de pagamento é a Tarefa 3; nesta tarefa o botão já existe e abre o diálogo quando ele chegar). `DialogoDocumento` (estado local; acha o documento na lista já carregada): título, "Venda/Despesa nº N · DD/MM/AA · pessoa", "cancelada" quando for o caso com "Cancelado por {nome} em DD/MM/AA", as linhas com a etiqueta da categoria, "Parcelas" com as etiquetas de pagas, "Cancelar esta venda/despesa" (destrutivo, contorno vermelho) e "Fechar", e a dica "Cancelar não apaga: o lançamento fica riscado no histórico e o dinheiro sai do saldo." `ConfirmarCancelarDocumento`: `AlertDialog` com a cópia exata do UI-SPEC, "Voltar" e "Cancelar venda"/"Cancelar despesa"; o botão de confirmar não fecha antes da resposta; sucesso → `window.location.assign('/financeiro?aba=caixa&aviso=cancelado&documento=<id>')`; falha → frase dentro do diálogo.

**`tests/e2e/apoio/semear-conta-a-pagar.ts`**: `semearContaAPagar({ titulo, pessoa, categoria, valorCentavos, vencimento, rotulo?, linhas?, tipo? })` insere documento, linha(s) e parcela em aberto numa transação (o `criado_por` é o gestor de `E2E_EMAIL_TESTE`), devolvendo os ids; nomes com sufixo único. Toda data relativa a hoje usa `hojeNoAtelie()`/`somarDiasAoHoje()` de `semear-financeiro.ts` (dia civil de Brasília), nunca a data em UTC do processo de teste.

**E2E** (`tests/e2e/financeiro-caixa.spec.ts`, bloco "financeiro caixa contas"): caso `@vazio-global` com os dois vazios; uma conta semeada com vencimento cinco dias atrás aparece em "A pagar" com "vencida", e uma com vencimento hoje aparece sem; uma venda 3x lançada pela tela aparece em "A receber" com "2 de 3" e "3 de 3", e nenhum "1 de 1" aparece para documento de uma parcela; "Ver" mostra as parcelas com "recebida em" só na primeira; cancelar a venda de valor livre lançada no teste: a confirmação tem a cópia exata, o aviso de cancelado aparece, a linha do extrato fica riscada com "cancelada" e sem saldo, o tile de saldo bate com o saldo depois do movimento não cancelado mais recente, o detalhe mostra "Cancelado por" e não tem mais o botão; cancelar a venda 3x tira as duas parcelas dela de "A receber"; uma segunda página aberta antes do cancelamento tenta cancelar de novo e recebe "Esse lançamento já foi cancelado.". A 320px o Caixa não rola na horizontal.

**Orçamento de e2e:** uma invocação, com recorte "financeiro caixa contas".
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro caixa contas"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro caixa contas" passa nos dois projetos e o caso `@vazio-global` passa na etapa de vazio.
    - `grep -rEn "\.delete\((documentos|parcelas)\)" lib/financeiro` não encontra nada.
    - `grep -n "cancelado_por\|canceladoPor" lib/financeiro/acoes.ts` mostra a gravação de quem cancelou.
    - `npm run verificar-acoes` sai 0 com `cancelarDocumento` começando por `exigirUsuario()`.
    - `grep -rEn "router\.(push|refresh)\(" components/amassa/financeiro` não encontra nada.
  </acceptance_criteria>
  <done>O Caixa mostra o que falta pagar e receber, com o atrasado marcado, e cancelar deixa rastro em vez de buraco.</done>
</task>

<task type="auto">
  <name>Tarefa 3: "Paguei"/"Recebi" com a linha de diferença, e o "Desfazer" de 7 segundos</name>
  <files>lib/financeiro/esquemas.ts, lib/financeiro/acoes.ts, lib/financeiro/consultas.ts, lib/financeiro/textos.ts, lib/financeiro/avisos.ts, app/(app)/financeiro/page.tsx, components/amassa/financeiro/dialogo-baixa.tsx, components/amassa/financeiro/cartao-conta.tsx, components/amassa/financeiro/aviso-financeiro.tsx, tests/e2e/financeiro-caixa.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"A linha 'diferença' (D-01/D-02/D-03) — UI nova" e as linhas de aviso de pagamento e desfazer do §Copywriting.
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 374-378 (`folhaBaixa`) e 415 (exemplos 3 e 5 de despesa).
    - `components/amassa/queimas/registrar-queima.tsx` linhas 45-100 — `toast.success` com `duration: 7000` e `action` (o molde do Desfazer; a atualização de roteador de lá é o antipadrão que não se copia).
    - `lib/financeiro/pagamento.ts` (Tarefa 1) e os arquivos da Tarefa 2.
    - `tests/e2e/apoio/semear-conta-a-pagar.ts`, `semear-financeiro.ts` e `tests/e2e/financeiro-despesa.spec.ts` (a compra de esmaltes em 3x).
  </read_first>
  <action>
**Servidor.** `registrarPagamento({ parcelaId, valorTexto, pagoEm, forma })` com `exigirUsuario()` primeiro: numa transação, lê a parcela, trava o documento e depois a parcela (`for update`, sempre nessa ordem); recusa documento cancelado e parcela já paga com as frases de `must_haves`; `pagoEm` até hoje e não antes de `data_saldo_inicial`; valor maior que zero pela função pura; conta linhas e parcelas do documento; aplica `planejarPagamento` — ajustar a linha única, ou inserir a linha de diferença (descrição "Diferença no pagamento da parcela {k} de {N}", ou "Diferença no pagamento" quando N = 1; categoria achada por `chave_do_sistema = 'diferenca'`; quantidade 1; `parcela_diferenca_id`; `ordem` = maior + 1); grava na parcela o valor pago, `valor_previsto_centavos`, `forma_prevista`, a forma nova, `pago_em`, `pago_por` e a taxa do plano. `desfazerPagamento({ parcelaId })` com `exigirUsuario()` primeiro: mesma trava; recusa cancelado, em aberto e sem previsto; aplica `planejarDesfazer` — apaga a linha de `parcela_diferenca_id` desta parcela (a ÚNICA exclusão física do módulo) ou devolve a linha única ao previsto; devolve valor e forma à parcela e limpa pago, taxa, previsto e forma prevista. A restrição adiada do banco confere a soma no `commit` das duas. `obterParcelaParaAviso(id)`: tipo do documento, valor, se está paga, previsto e o valor da linha de diferença dela.

**Avisos.** `avisos.ts` ganha `pago` e `desfeito` (parcela). A página só oferece o aviso `pago` se a parcela ainda está paga com previsto guardado; o texto é `textoDoPagamento` ("{Recebido/Pago}: {R$ valor}" e, com diferença, ". {R$ |diferença|} {a mais/a menos} viraram uma linha de diferença."). `AvisoFinanceiro`, para `pago`, mostra o aviso com `duration: 7000` e a ação "Desfazer", que chama `desfazerPagamento` e, dando certo, faz `window.location.assign('/financeiro?aba=caixa&aviso=desfeito&parcela=<id>')`; falhando, "Não deu para desfazer. Verifique a internet e tente de novo." `desfeito` → "Desfeito. A conta voltou a {R$ valor} em aberto."

**Tela.** `DialogoBaixa` (estado local; aberto por "Paguei"/"Recebi" do `CartaoConta`): título "Recebi: {título}"/"Paguei: {título}", Valor (`inputmode="decimal"`, o previsto preenchido), Quando (`type="date"`, hoje, `max` = hoje), Forma (a da parcela), a dica "Se o valor veio diferente do previsto, corrija aqui — o lançamento é ajustado junto.", "Voltar" e "Confirmar"; sucesso → `window.location.assign('/financeiro?aba=caixa&aviso=pago&parcela=<id>')`; falha → frase no diálogo, nada perdido.

**E2E** (bloco "financeiro caixa pagamento" em `financeiro-caixa.spec.ts`): **exemplo 3** — conta semeada "Financiamento" (sufixo), pessoa inventada, "Financiamento (parcela)", R$ 1.480,00, rótulo "parcela 26 de 60" → o cartão mostra o rótulo; "Paguei" com R$ 1.480,00 → aviso "Pago: R$ 1.480,00", a conta sai de "A pagar", o extrato mostra "− R$ 1.480,00". **Linha única** — outra conta semeada de R$ 1.480,00 paga com R$ 1.500,00 → aviso sem a frase da diferença; o detalhe mostra a linha com R$ 1.500,00 e nenhuma linha de diferença; "Desfazer" → "Desfeito. A conta voltou a R$ 1.480,00 em aberto." e a conta volta à lista com R$ 1.480,00. **Exemplo 5** — compra de "Esmalte (pote)" (sufixo) com 100 un por R$ 2.400,00 em 3x lançada pela tela; "A pagar" mostra "2 de 3" e "3 de 3"; "Paguei" na 2 com R$ 812,00 → "Pago: R$ 812,00. R$ 12,00 a mais viraram uma linha de diferença."; o detalhe mostra "Diferença no pagamento da parcela 2 de 3" com a etiqueta "Juros, multas e descontos" e R$ 12,00; "Desfazer" dentro dos 7 segundos → "Desfeito. A conta voltou a R$ 800,00 em aberto.", a parcela volta a R$ 800,00 e a linha de diferença some do detalhe; paga de novo com R$ 790,00 → "R$ 10,00 a menos…" e a linha de diferença de − R$ 10,00. **Recebi no cartão** — `garantirTaxaDeTeste()`; venda 3x lançada pela tela; "Recebi" na parcela 2 no Cartão → o extrato mostra a taxa na linha. **Duas pessoas** — duas páginas abrem "Paguei" na mesma conta semeada; a primeira confirma; a segunda confirma e vê "Essa conta já foi paga — recarregue a página para ver como ela está."; e numa conta cancelada por uma página, a outra tenta pagar e vê a frase do cancelado. **Data futura** — o campo Quando não aceita amanhã, e o servidor recusa se forçado.

**Orçamento de e2e:** uma invocação, com recorte "financeiro caixa pagamento".
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro caixa pagamento"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro caixa pagamento" passa nos dois projetos, com os exemplos 3 e 5 de despesa do protótipo.
    - `grep -n "duration: 7000" components/amassa/financeiro/aviso-financeiro.tsx` encontra o Desfazer de 7 segundos.
    - `grep -n "chave_do_sistema\|chaveDoSistema" lib/financeiro/acoes.ts` mostra a categoria da diferença achada pela chave, nunca pelo nome.
    - `grep -rEn "\.delete\(" lib/financeiro/acoes.ts` só encontra a exclusão de `documentoLinhas` filtrada por `parcela_diferenca_id` dentro de `desfazerPagamento`.
    - `grep -n "for update\|\.for(\"update\")" lib/financeiro/acoes.ts` encontra a trava em `registrarPagamento`, `desfazerPagamento` e `cancelarDocumento`.
    - `npm run verificar-acoes` sai 0 com as três ações novas começando por `exigirUsuario()`.
  </acceptance_criteria>
  <done>O boleto que veio com juros é pago com o valor real, o Mês continua fechando, e o toque errado tem volta exata em 7 segundos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `registrarPagamento`, `desfazerPagamento`, `cancelarDocumento` | Identificadores, valor, data e forma vêm do cliente; a tela pode estar velha |
| dois gestores → mesma parcela | Concorrência real entre Theo e Andressa |
| URL `?aviso=` → Desfazer | O id da parcela vem da URL |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-48 | Elevation of Privilege | as três ações | high | mitigate | `exigirUsuario()` primeira instrução; `verificar-acoes` |
| T-04.4-49 | Tampering | pagamento duplicado / concorrente | high | mitigate | Trava documento e parcela com `for update` na mesma ordem; recusa parcela já paga; e2e com duas páginas |
| T-04.4-50 | Tampering | soma depois de pagar/desfazer | high | mitigate | `planejarPagamento`/`planejarDesfazer` provados por propriedade de ida e volta; restrição adiada do banco no `commit` |
| T-04.4-51 | Tampering | desfazer de pagamento alheio ao Paguei | medium | mitigate | Desfazer exige previsto guardado; recusa parcela paga no lançamento e documento cancelado |
| T-04.4-52 | Repudiation | quem pagou / quem cancelou | medium | mitigate | `pago_por`, `cancelado_por` e `cancelado_em` gravados do servidor; detalhe mostra quem cancelou |
| T-04.4-53 | Tampering | exclusão de lançamento | high | mitigate | Cancelar só marca; a única exclusão física é a linha de diferença pelo desfazer; `revoke delete` em documentos e parcelas |
| T-04.4-54 | Tampering | `?aviso=pago&parcela=` forjado | low | mitigate | uuid validado; o Desfazer ainda passa por `desfazerPagamento`, que confere tudo no servidor; o texto do aviso vem do banco |
| T-04.4-55 | Tampering | data de pagamento | medium | mitigate | Até hoje e não antes do saldo inicial, conferido no servidor |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Dois recortes neste plano: "financeiro caixa contas" (Tarefa 2) e "financeiro caixa pagamento" (Tarefa 3).
- Os exemplos 3 e 5 de despesa do protótipo têm caso próprio.
</verification>

<success_criteria>
- O Caixa mostra saldo, a receber, a pagar e "se tudo se cumprir", e as listas com as vencidas (FNC-07).
- "Paguei"/"Recebi" move o saldo, cria a linha de diferença quando precisa (D-01/D-02) e desfaz exatamente (D-03) — critério 4 do ROADMAP.
- Cancelar risca, tira do saldo e das listas, e registra quem e quando (FNC-10, critério 5 do ROADMAP).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-08-SUMMARY.md` when done
</output>
