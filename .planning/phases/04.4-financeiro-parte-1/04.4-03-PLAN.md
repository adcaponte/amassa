---
phase: 04.4-financeiro-parte-1
plan: 03
type: execute
wave: 2
depends_on: ["04.4-01"]
files_modified:
  - lib/financeiro/desconto.ts
  - lib/financeiro/efeito-estoque.ts
  - lib/financeiro/rascunho.ts
  - lib/financeiro/documento.ts
  - lib/financeiro/esquemas.ts
  - lib/financeiro/acoes.ts
  - lib/financeiro/consultas.ts
  - lib/financeiro/textos.ts
  - app/(app)/financeiro/page.tsx
  - components/amassa/financeiro/painel-venda.tsx
  - components/amassa/financeiro/grade-catalogo.tsx
  - components/amassa/financeiro/lista-completa.tsx
  - components/amassa/financeiro/linha-carrinho.tsx
  - components/amassa/financeiro/campo-desconto.tsx
  - components/amassa/financeiro/efeito-estoque.tsx
  - components/amassa/financeiro/dialogo-valor-livre.tsx
  - tests/unit/financeiro-desconto.test.ts
  - tests/unit/financeiro-efeito-estoque.test.ts
  - tests/unit/financeiro-rascunho.test.ts
  - tests/unit/financeiro-documento.test.ts
  - tests/e2e/apoio/semear-financeiro.ts
  - tests/e2e/financeiro-venda.spec.ts
autonomous: true
requirements: [FNC-01, FNC-02, FNC-04, FNC-15, FNC-17]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 200000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A Venda mostra os atalhos do catálogo como cartões com a borda esquerda na cor da área, filtráveis pelas pílulas Todos os atalhos · Cafeteria · Espaço · Peças · Loja (`aria-pressed`, 44px), uma busca que troca a grade por uma lista agrupada por área, 'Lista completa e atalhos' e '+ Valor livre' — FNC-01."
    - "Tocar num item põe uma linha no carrinho; tocar de novo no MESMO item soma um na quantidade em vez de criar outra linha; '−' até zero tira a linha; 'tirar' tira a linha inteira."
    - "O preço vem do catálogo no campo 'cada' e pode ser editado; quando o valor da linha difere do de tabela, aparece a etiqueta 'tabela R$ X' — FNC-02. Item com preço 'valor na hora' entra com o campo vazio, placeholder 'R$', não soma no total e deixa 'Lançar venda' desabilitado até o valor ser digitado."
    - "Mudar o preço de um item no catálogo depois de lançar não muda a venda já lançada: a linha guarda o próprio valor, a descrição e a categoria do instante do lançamento."
    - "Com linhas de duas ou mais áreas, aparece 'Um recebimento só, dividido sozinho entre A, B e C.'; com uma área só, não aparece nada — a área é sempre derivada da categoria, ninguém a escolhe."
    - "A data do documento aceita dia anterior a hoje (FNC-04) e mostra 'Lançando com data de DD/MM/AA — serve para fechar um dia que já passou.'; o fechamento do dia da cafeteria é uma venda só com várias linhas e quantidades (22 cafés, 40 pães de queijo), e a parcela à vista fica paga naquela data."
    - "Data inválida (30/02), anterior a 2020 ou mais de um ano à frente é recusada no servidor com frase humana."
    - "No '+ Valor livre', a categoria vem das Receitas e também do grupo Fora do resultado — é por aí que um aporte dos sócios entra no caixa sem virar venda de nenhuma área (suposição sinalizada abaixo)."
    - "O campo 'Desconto', abaixo do total, aceita R$ ou % (pílulas `aria-pressed`, campo de 44px e 16px); o sistema reparte o desconto entre as linhas na proporção do valor de cada uma, cada linha afetada mostra 'tabela R$ X', e o desconto NÃO vira linha separada nem vai para o Geral (D-09)."
    - "A sobra de centavos da repartição vai para a maior linha (empate: a primeira), e o total final bate EXATAMENTE com o total menos o desconto digitado — R$ 153,00 com desconto de R$ 3,00 dá R$ 150,00 (D-10)."
    - "Desconto maior que o total é recusado; desconto em % aceita até duas casas e no máximo 100; um desconto que deixaria alguma linha negativa pela regra da maior linha é recusado com 'Esse desconto não dá para repartir entre as linhas — diminua o desconto ou tire uma linha.' — tudo decidido pela MESMA função pura no cliente e no servidor."
    - "A venda mostra 'O que esta venda tira do estoque': item com ficha técnica baixa cada insumo (quantidade da ficha × quantidade da linha); sem ficha, item que controla estoque baixa ele mesmo; item sem nenhum dos dois não aparece; nada é gravado (FNC-15) — 'Café 200 ml' com 15 g de grão, vendido 2 vezes, mostra '−30 g · Grão de café'."
    - "A conta do efeito no estoque é feita em milésimos inteiros (0,04 kg × 4 = 0,16 kg exatos), nunca em ponto flutuante acumulado, e soma o mesmo insumo vindo de linhas diferentes numa entrada só."
    - "O que foi montado no carrinho sobrevive a trocar de aba e voltar, e a recarregar a página, na mesma aba do navegador — guardado no `sessionStorage` e apagado ao lançar com sucesso ou ao tocar em 'Limpar' (suposição sinalizada abaixo)."
    - "O servidor não confia no cliente para item de catálogo: descrição, categoria e existência do item vêm do banco; o cliente manda só o identificador, a quantidade (1 a 9999) e o texto do 'cada'."
    - "Uma venda tem de 1 a 100 linhas; zero linha é recusada com 'A venda precisa de pelo menos uma linha.'"
    - "Com 3 a 8 linhas de áreas diferentes (o exemplo 5 tem seis), a Venda segue o layout do protótipo: duas colunas (`1.15fr 1fr`, catálogo à esquerda) a partir de 980px e uma coluna abaixo, sem rolagem horizontal a 320px."
    - statement: "Título de documento muito longo (muitas linhas) é cortado em 'primeiras duas + N' e numa linha só nos cartões."
      verification: backstop
  prohibitions:
    - statement: "O usuário nunca escolhe área ao lançar — não existe seletor de área em nenhum componente de Venda."
      status: resolved
      verification: test
    - statement: "O desconto nunca vira linha de documento nem entra no Geral."
      status: resolved
      verification: test
    - statement: "Nenhuma movimentação de estoque é gravada nesta fase — não existe tabela nem escrita de movimentação."
      status: resolved
      verification: test
    - statement: "Nenhum botão 'Testar com as vendas que você descreveu' existe — os exemplos do protótipo viraram casos de e2e."
      status: resolved
      verification: test
    - statement: "Nenhuma escrita termina em atualização de roteador do Next; a estrela de atalho é otimista e convergente, e lançar é navegação completa."
      status: resolved
      verification: test
  artifacts:
    - lib/financeiro/desconto.ts
    - lib/financeiro/efeito-estoque.ts
    - lib/financeiro/rascunho.ts
    - components/amassa/financeiro/painel-venda.tsx
    - components/amassa/financeiro/grade-catalogo.tsx
    - tests/e2e/apoio/semear-financeiro.ts
    - tests/e2e/financeiro-venda.spec.ts
  key_links:
    - "`repartirDesconto` roda duas vezes com a mesma entrada: no cliente para mostrar e no servidor para gravar. O servidor nunca aceita valores de linha já descontados vindos do cliente — recebe o texto do 'cada' e o texto do desconto e refaz a conta."
    - "`efeitoNoEstoque` é o contrato que a Fase 6 vai trocar de 'mostrar' para 'gravar': mesma entrada (linhas + catálogo com fichas), mesma saída (item, unidade, variação em milésimos, custo unitário na compra). Nenhum componente calcula efeito por conta própria."
    - "`semear-financeiro.ts` nasce completo aqui e os planos seguintes só o importam — se precisarem de outra semeadura, criam arquivo próprio (evita dois planos da mesma onda mexendo no mesmo auxiliar)."

coverage:
  - id: D1
    description: "Os exemplos 1, 2, 3, 5 e 7 do protótipo montam o carrinho certo, com o total, a dica de áreas e o efeito no estoque esperados"
    requirement: "FNC-01, FNC-15"
    verification:
      - kind: e2e
        ref: "tests/e2e/financeiro-venda.spec.ts — um caso por exemplo"
        status: unknown
    human_judgment: false
  - id: D2
    description: "Desconto em R$ e em %: repartição proporcional, sobra na maior linha, total exato, recusas"
    requirement: "FNC-02"
    verification:
      - kind: unit
        ref: "tests/unit/financeiro-desconto.test.ts"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/financeiro-venda.spec.ts — exemplo 6 com desconto de R$ 2,00"
        status: unknown
    human_judgment: false
  - id: D3
    description: "Venda com data retroativa e preço de catálogo que muda depois não reescreve o passado"
    requirement: "FNC-02, FNC-04"
    verification:
      - kind: e2e
        ref: "tests/e2e/financeiro-venda.spec.ts"
        status: unknown
    human_judgment: false
---

<objective>
A Venda completa, menos o pagamento parcelado: **o carrinho montado pelo catálogo** — atalhos por
área, busca, lista completa com a estrela de atalho, valor livre, quantidade, preço editável com a
etiqueta "tabela", desconto no total repartido entre as linhas (D-09/D-10), data retroativa para
fechar o dia, e o que a venda tira do estoque — lançado à vista.

Purpose: é a tela que decide se o Financeiro é usado de pé, no balcão. Os exemplos do protótipo
que o dono descreveu (café com pão de queijo, a aula com materiais, as quatro áreas numa venda só)
viram casos de teste que montam o carrinho de verdade.

Output: três módulos puros testados (`desconto`, `efeito-estoque`, `rascunho`), a Venda com o
catálogo, o lançamento de linhas de catálogo e de valor livre no servidor, e o e2e com os
exemplos.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md
@.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md
@.planning/phases/04.4-financeiro-parte-1/04.4-01-SUMMARY.md
</context>

## Artifacts this phase produces — neste plano

| Artefato | Onde |
|---|---|
| `repartirDesconto`, tipo `Desconto` | `lib/financeiro/desconto.ts` |
| `efeitoNoEstoque`, `formatarEfeito`, tipos `ItemParaEfeito`, `LinhaParaEfeito`, `EfeitoNoEstoque` | `lib/financeiro/efeito-estoque.ts` |
| `serializarRascunho`, `lerRascunho`, `CHAVE_RASCUNHO_VENDA`, `CHAVE_RASCUNHO_DESPESA` | `lib/financeiro/rascunho.ts` |
| `areasDaVenda`, `listaEmPortugues` | `lib/financeiro/documento.ts` |
| Linha `item` em `esquemaVenda`, campo `desconto` | `lib/financeiro/esquemas.ts` |
| `lancarVenda` com linhas de catálogo e desconto; `definirAtalhoDoItem` | `lib/financeiro/acoes.ts` |
| `listarCatalogoDaVenda`, `listarItensParaEfeito` | `lib/financeiro/consultas.ts` |
| `GradeCatalogo`, `ListaCompleta`, `LinhaCarrinho`, `CampoDesconto`, `EfeitoEstoque` | `components/amassa/financeiro/` |
| `semearItem`, `buscarCategoriaPorNome`, `definirPrecoDoItem`, `garantirTaxaDeTeste`, `TAXA_DE_TESTE`, `hojeNoAtelie`, `somarDiasAoHoje` | `tests/e2e/apoio/semear-financeiro.ts` |
| `data-testid`: `venda-atalho`, `venda-filtro-<area>`, `venda-busca`, `venda-linha-quantidade`, `venda-linha-tabela`, `venda-dica-areas`, `venda-efeito`, `venda-desconto` | componentes |

## Suposições do planejador (sinalizadas ao dono na verificação do plano 11)

1. **Aporte dos sócios entra pelo "+ Valor livre".** O protótipo só oferece Receitas no valor livre,
   e a semente de D-14 tem "Aporte dos sócios" no grupo Fora — sem este caminho, um aporte só
   poderia ser lançado como despesa, que é dinheiro saindo. O plano 09 mostra o Fora do resultado
   com sinal (+ entrou, − saiu) por causa disso.
2. **O carrinho sobrevive a trocar de aba** (rascunho em `sessionStorage`, só naquela aba do
   navegador) — regra do design system: formulário nunca perde o que foi digitado.
3. **A dica de áreas usa "A, B e C"** (lista em português) em vez das vírgulas do protótipo — é a
   cópia do UI-SPEC ("{área, área e área}").

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O desconto que fecha no centavo, o que sai do estoque e o rascunho que não se perde</name>
  <files>lib/financeiro/desconto.ts, lib/financeiro/efeito-estoque.ts, lib/financeiro/rascunho.ts, lib/financeiro/documento.ts, tests/unit/financeiro-desconto.test.ts, tests/unit/financeiro-efeito-estoque.test.ts, tests/unit/financeiro-rascunho.test.ts, tests/unit/financeiro-documento.test.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-09 e D-10, palavra por palavra.
    - `.planning/phases/04.4-financeiro-parte-1/BRIEFING.md` §6 (o contrato do efeito no estoque).
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 181-207 (o catálogo com `ficha`, `estoque`, `un`), 249-250 (`efeitoEstoque`) e 286 (`areas`).
    - `lib/financeiro/documento.ts`, `lib/financeiro/formato.ts` (`formatarQuantidade`) e `tests/unit/financeiro-documento.test.ts` como o plano 01 os deixou.
    - `tests/unit/abertura-parcelas.test.ts` — estilo.
  </read_first>
  <behavior>
    `financeiro-desconto.test.ts` — `repartirDesconto(subtotais, desconto)`:
    - [15300] com R$ 3,00 → [15000] (o "arredondar R$ 153 para R$ 150" do dono).
    - [18000, 15200] (exemplo 6) com R$ 2,00 → soma 33000, cada linha proporcional, sobra na maior.
    - [1000, 1000, 1000] com R$ 1,00 → dois recebem 33 e a maior (empate: a primeira) recebe 34; total 2900.
    - Percentual: [10000, 5000] com 10% → [9000, 4500]; percentual que dá meio centavo arredonda para cima no desconto total e o total final é exato.
    - Desconto zero → subtotais iguais; sem linhas e desconto zero → lista vazia.
    - Desconto igual ao total → todas as linhas zero; maior que o total → recusa "O desconto é maior que o total."
    - [3, 3, 3] com 8 centavos → as partes são 2, 2, 2, a sobra de 2 vai para a primeira (parte 4 > 3), que ficaria negativa → recusa com a frase da repartição impossível.
    - Percentual acima de 100 ou negativo → recusa.
    - A soma dos valores devolvidos é SEMPRE total − desconto (caso com 50 entradas aleatórias determinísticas, semente fixa).
    - Não muta a lista recebida.
    `financeiro-efeito-estoque.test.ts` — `efeitoNoEstoque(linhas, itens, sentido)`:
    - Café (ficha 15 g de grão) × 1 → grão −15 g; × 2 → −30 g.
    - Pão de queijo (ficha 0,04 kg de pão congelado) × 40 → −1,6 kg exato; × 4 → −0,16 kg exato.
    - Refil (30 g) + Café (15 g) na mesma venda → uma entrada só de grão, −45 g.
    - Copo (controla estoque, sem ficha) × 1 → −1 un do próprio copo.
    - Item com ficha E controla estoque → vale a ficha (o próprio não baixa).
    - Hora de uso do espaço (nem ficha nem estoque) → nenhuma entrada.
    - Ficha de um nível só: insumo que tem ficha própria não é expandido.
    - Compra: Esmalte (pote), 1 linha com quantidade de estoque "100", valor 240000 → +100 un, custo unitário 2400 centavos; pão congelado "5" kg por 16000 → +5 kg, custo 3200.
    - Linha sem item (valor livre) → nenhuma entrada.
    - Ordem das entradas = ordem de primeira aparição.
    - `formatarEfeito` → "−15 g · Grão de café", "+100 un · Esmalte (pote)", "−1,6 kg · Pão de queijo congelado", "−0,5 L · Leite".
    `financeiro-rascunho.test.ts`:
    - `serializarRascunho` e `lerRascunho` fazem ida e volta do mesmo objeto.
    - Texto que não é JSON, JSON de outra versão ou de formato errado → rascunho vazio, nunca exceção.
    - Linha cujo item não existe mais no catálogo recebido → descartada na leitura.
    `financeiro-documento.test.ts` (acréscimo):
    - `areasDaVenda` devolve as áreas distintas na ordem de primeira aparição.
    - `listaEmPortugues(['Cafeteria'])` → "Cafeteria"; duas → "Cafeteria e Peças"; três → "Espaço, Peças e Cafeteria".
  </behavior>
  <action>
Testes primeiro, falhando pelo motivo certo; depois os módulos.

**`desconto.ts`.** `repartirDesconto(subtotaisCentavos, desconto)` com `desconto` = `{ modo: 'reais', centavos }` ou `{ modo: 'percentual', pontosBase }`. Desconto total: em reais, o próprio; em percentual, total × pontos-base / 10000 arredondado meio-para-cima. Recusas: percentual fora de 0 a 10000; desconto maior que o total. Repartição: parte de cada linha = piso(subtotal × desconto / total); a sobra (desconto menos a soma das partes) vai inteira para a maior linha, a primeira em caso de empate (D-10, ao pé da letra). Se alguma linha ficar negativa, recusa com a frase da repartição impossível. Devolve os valores finais e o desconto total. Comentário no topo citando D-09/D-10 e explicando por que a recusa existe (a regra da maior linha é do dono; a recusa só pega o caso degenerado de desconto quase igual ao total com linhas de poucos centavos).

**`efeito-estoque.ts`.** Quantidades como texto decimal (o que vem de `numeric`) convertidas para milésimos inteiros; toda multiplicação e soma em milésimos; a saída devolve milésimos e o `formatarEfeito` monta o texto com `formatarQuantidade` (unidade `l` aparece "L"). Venda: sinal negativo; compra: positivo, com `custoUnitarioCentavos` = arredondamento de valor ÷ quantidade. Um nível: nunca olha a ficha do insumo.

**`rascunho.ts`.** Versão numérica no objeto; `lerRascunho(texto, idsDeItensExistentes)` valida a forma campo a campo sem Zod (módulo puro) e devolve o rascunho vazio em qualquer dúvida. Duas chaves exportadas, uma por tela.

**`documento.ts`.** `areasDaVenda(linhas)` e `listaEmPortugues(nomes)`.

Nenhum dos quatro importa React, Next, o cliente do banco, `lib/abertura` ou `lib/cotacoes`. Nenhuma invocação de e2e.
  </action>
  <verify>
    <automated>npm test -- tests/unit/financeiro-desconto.test.ts tests/unit/financeiro-efeito-estoque.test.ts tests/unit/financeiro-rascunho.test.ts tests/unit/financeiro-documento.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os quatro arquivos de teste passam, com cada caso de `<behavior>` nomeado.
    - O caso "R$ 153 com desconto de R$ 3 dá R$ 150" e o caso "sobra na maior linha, primeira no empate" existem (D-09/D-10).
    - `grep -En "from \"(react|next|drizzle-orm|pg)|@/db|@/lib/abertura|@/lib/cotacoes" lib/financeiro/desconto.ts lib/financeiro/efeito-estoque.ts lib/financeiro/rascunho.ts` não encontra nada.
    - `grep -n "new Date\|Date.now\|sessionStorage" lib/financeiro/desconto.ts lib/financeiro/efeito-estoque.ts lib/financeiro/rascunho.ts` não encontra nada (o acesso ao armazenamento fica no componente).
  </acceptance_criteria>
  <done>As três regras novas da Venda estão provadas sem servidor, e o contrato do efeito no estoque que a Fase 6 vai herdar está escrito e testado.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O carrinho pelo catálogo — atalhos, busca, lista completa, quantidade, preço e data retroativa</name>
  <files>lib/financeiro/esquemas.ts, lib/financeiro/acoes.ts, lib/financeiro/consultas.ts, lib/financeiro/textos.ts, app/(app)/financeiro/page.tsx, components/amassa/financeiro/painel-venda.tsx, components/amassa/financeiro/grade-catalogo.tsx, components/amassa/financeiro/lista-completa.tsx, components/amassa/financeiro/linha-carrinho.tsx, components/amassa/financeiro/efeito-estoque.tsx, components/amassa/financeiro/dialogo-valor-livre.tsx, tests/e2e/apoio/semear-financeiro.ts, tests/e2e/financeiro-venda.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/prototipo.html` linhas 260-301 (`EX_V`, `baseItens`, `listaHTML`, `gradeItens`, `folhaLista`, `linhasHTML`, `telaVenda`), 390 (`folhaLivre`), 394 (`addLinha`) e 404-418 (eventos `filtro`, `add`, `fav`, `qtd`, `tirar`, `limpar`, `livre-add`).
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §Foco Visual (Venda), §Spacing (44px), §Typography, §Copywriting, §UI Considerations (linhas "partial" e "zero-one-many").
    - `lib/financeiro/*` e `components/amassa/financeiro/*` como o plano 01 e a Tarefa 1 os deixaram.
    - `components/amassa/abertura/caixa-marcacao.tsx` — o salvamento otimista com estado desejado (molde da estrela).
    - `tests/e2e/apoio/semear-abertura.ts` — o auxiliar de semeadura pelo cliente `pg` (molde de `semear-financeiro.ts`).
    - `.planning/PROXIMA-SESSAO.md` §1 (atualização de tela depois de gravar).
  </read_first>
  <action>
**Servidor.** `esquemaVenda` ganha a linha `{ tipo: 'item', itemId, quantidade (inteiro 1-9999), valorUnitarioTexto }` na união discriminada, e a linha `livre` passa a aceitar categoria de grupo `receita` OU `fora` (suposição 1). `lancarVenda`: carrega do banco os itens citados numa consulta só; item inexistente ou sem categoria de venda → "Um dos itens saiu do catálogo — tire a linha e tente de novo."; descrição, `item_id` e categoria da linha vêm do ITEM (mesmo com a categoria desativada — UI-SPEC partial); o valor unitário é convertido pela função pura e precisa ser maior que zero ("Informe o valor de cada linha."); subtotal = quantidade × unitário; aplica `repartirDesconto` quando houver desconto (Tarefa 3); a linha guarda o valor final e a `ordem`. `definirAtalhoDoItem({ itemId, tipo: 'venda' | 'compra', marcado })` com `exigirUsuario()` primeiro, grava o estado DESEJADO (convergente), recusa atalho de venda em item que não aparece na venda e de compra em item que não controla estoque. `listarCatalogoDaVenda()`: itens que aparecem na venda, com área (da categoria de venda), preço, atalho; `listarItensParaEfeito()`: todos os itens com unidade, se controla estoque e as fichas — duas consultas, nunca uma por item.

**Tela.** `page.tsx` carrega, para a aba Venda, catálogo, itens para efeito e categorias de receita e fora. `PainelVenda` (cliente, dono do estado do carrinho): bloco "O que foi vendido" — busca "Buscar no catálogo", pílulas de área, `GradeCatalogo`, "Lista completa e atalhos" e "+ Valor livre" (botões secundários de 44px); bloco "Esta venda" — Data e "Pessoa (opcional)" (placeholder "quem comprou"), a dica de data retroativa só quando a data é anterior a hoje, as linhas (`LinhaCarrinho`), "Total", a dica de áreas (`areasDaVenda` + `listaEmPortugues`, só com 2+ áreas), a forma, o bloco `EfeitoEstoque` ("O que esta venda tira do estoque", recolhível, com a dica do protótipo "Aparece aqui para validar a regra. Passa a valer quando o módulo Estoque estiver ligado."), "Limpar" e "Lançar venda". `GradeCatalogo`: cartões dos atalhos filtrados pela área; sem atalho: "Nenhum atalho aqui. Abra a lista completa e marque ★ nos itens que quer ver nesta tela."; com busca: lista agrupada por área, e "Nada encontrado." quando vazia. `ListaCompleta`: diálogo "Tudo o que se vende", busca, dica do protótipo, agrupado por área; tocar no nome adiciona e mostra o aviso "+ {nome}"; a estrela (44×44, `aria-pressed`, nome acessível "Atalho: {nome}") muda na hora e chama `definirAtalhoDoItem` sem navegar — falhou, volta ao estado anterior e mostra o erro; "Pronto" fecha. `LinhaCarrinho`: ponto de cor da área, nome, subtotal, passo −/+ (44px, `aria-label` "menos um"/"mais um"), "cada" (`inputmode="decimal"`, 16px, placeholder "R$"), etiqueta "tabela R$ X" quando o valor por unidade difere do de tabela, "tirar". Tocar no mesmo item soma quantidade. O rascunho é lido do `sessionStorage` ao montar (com os ids do catálogo) e gravado a cada mudança; apagado ao lançar com sucesso (antes da navegação) e em "Limpar". `DialogoValorLivre` passa a agrupar Receitas e Fora do resultado em `optgroup`.

**`tests/e2e/apoio/semear-financeiro.ts`** (nasce completo — os planos seguintes só importam): `buscarCategoriaPorNome(nome)`; `semearItem({ nome, categoriaVenda?, precoCentavos?, apareceNaVenda, atalhoVenda, controlaEstoque, unidade?, categoriaCompra?, atalhoCompra, ficha? })` devolvendo o id e falhando alto se não inserir; `definirPrecoDoItem(id, centavos)`; `TAXA_DE_TESTE` = 350 e `garantirTaxaDeTeste()` (upsert da configuração com 350 — a regra da suíte do plano 02: todo teste que escreve a taxa escreve 350); `hojeNoAtelie()` e `somarDiasAoHoje(n)`, que calculam o dia civil em America/Sao_Paulo com `Intl.DateTimeFormat` (o CI roda em UTC e o servidor em Brasília — perto da meia-noite, um "hoje" em UTC erra o dia e reprova o teste sem defeito nenhum). Nomes sempre com sufixo único.

**E2E** (`tests/e2e/financeiro-venda.spec.ts`, "financeiro venda" no título), cada caso semeando os próprios itens com sufixo único e usando a busca para achá-los (a grade é global): **exemplo 1** (Café 200 ml R$ 8 + 2 Pães de queijo R$ 4 → R$ 16,00, efeito "−15 g · Grão de café…" e "−0,08 kg · Pão de queijo congelado…", nenhuma dica de áreas); **exemplo 2** (Refil + Copo para pintar → R$ 59,00, dica "Cafeteria e Peças", efeito −30 g e −1 un); **exemplo 3** (2 Queimas M + Argila 1 kg → R$ 44,00, dica Peças e Loja); **exemplo 5** (3 h de espaço, 3 queimas M, refil, fatia de bolo, 4 pães, boleira → R$ 382,00, três áreas na dica); **exemplo 7** (aula + dois pincéis + kit de tintas → R$ 253,00); mesmo item tocado duas vezes vira quantidade 2; "−" até zero tira a linha; item "valor na hora" deixa "Lançar venda" desabilitado até digitar; editar o "cada" mostra "tabela R$ X"; filtro Loja mostra o atalho da Loja e esconde o da Cafeteria; a estrela na lista completa faz o item aparecer na grade depois de fechar e continua marcada depois de recarregar; o carrinho sobrevive a ir ao Caixa e voltar; venda lançada com data do primeiro dia do mês corrente mostra a dica de retroativo e aparece no extrato naquela data (pule, com comentário, quando hoje é dia 1); depois de lançar, `definirPrecoDoItem` muda o preço e a linha do extrato continua com o valor lançado; valor livre com "Aporte dos sócios" é aceito. A 320px a Venda não rola na horizontal. Nenhuma afirmação global do banco.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verificar-acoes &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro venda"</automated>
  </verify>
  <acceptance_criteria>
    - O e2e "financeiro venda" passa nos dois projetos, com um caso para cada exemplo 1, 2, 3, 5 e 7 do protótipo.
    - `grep -rn "Testar com\|pil ex\|data-a=\"exv\"" components/amassa/financeiro` não encontra nada.
    - `grep -rEn "router\.(push|refresh)\(" components/amassa/financeiro` não encontra nada.
    - `grep -rn "sessionStorage" components/amassa/financeiro` só aparece em `painel-venda.tsx` (e no painel de despesa quando existir).
    - `grep -rin "escolha a área\|selecione a área\|name=\"area\"" components/amassa/financeiro` não encontra nada.
    - `npm run verificar-acoes` sai 0 com `definirAtalhoDoItem` começando por `exigirUsuario()`.
  </acceptance_criteria>
  <done>O gestor monta no celular a venda que o protótipo descrevia, com o que ela tira do estoque, e o preço de tabela de amanhã não reescreve a venda de hoje.</done>
</task>

<task type="auto">
  <name>Tarefa 3: O desconto no total — R$ ou %, repartido entre as linhas, sem linha separada</name>
  <files>lib/financeiro/esquemas.ts, lib/financeiro/acoes.ts, lib/financeiro/textos.ts, components/amassa/financeiro/painel-venda.tsx, components/amassa/financeiro/campo-desconto.tsx, components/amassa/financeiro/linha-carrinho.tsx, tests/e2e/financeiro-venda.spec.ts</files>
  <read_first>
    - `.planning/phases/04.4-financeiro-parte-1/04.4-CONTEXT.md` D-09 e D-10.
    - `.planning/phases/04.4-financeiro-parte-1/04.4-UI-SPEC.md` §"Desconto (D-09/D-10) — UI nova" e o placeholder de desconto no §Copywriting.
    - `lib/financeiro/desconto.ts` (Tarefa 1) e `painel-venda.tsx`, `linha-carrinho.tsx` (Tarefa 2).
  </read_first>
  <action>
**Servidor.** `esquemaVenda` ganha `desconto` opcional: `{ modo: 'reais' | 'percentual', texto }`, convertido pela função pura (reais → centavos; percentual → pontos-base). `lancarVenda` aplica `repartirDesconto` aos subtotais, devolve a frase da função em qualquer recusa, e grava cada linha com o valor final. O total e a conferência das parcelas usam o total JÁ descontado. Não existe coluna nem linha de desconto (D-09).

**Tela.** `CampoDesconto` logo abaixo do "Total" da Venda (a Despesa não tem): rótulo "Desconto", par de pílulas "R$" / "%" (`aria-pressed`, 44px, mesmo visual das pílulas de filtro), campo `inputmode="decimal"` de 44px e 16px com placeholder "0". O painel recalcula com `repartirDesconto` a cada digitação: o "Total" mostra o valor descontado, o subtotal de cada linha mostra o valor final, e toda linha com parte do desconto mostra a etiqueta "tabela R$ X" (o preço de tabela por unidade do item; em linha de valor livre, o valor digitado) — a MESMA etiqueta do preço editado, nunca um segundo tipo. Recusa da função aparece como a mensagem de falta/sobra (fundo de erro), e "Lançar venda" fica desabilitado. O desconto entra no rascunho.

**E2E** (acrescente a `financeiro-venda.spec.ts`): **exemplo 6** — Kit 4 xícaras (R$ 180) + 4 Pratos 15 cm (R$ 38) = R$ 332,00; desconto R$ 2,00 → Total R$ 330,00, as duas linhas com "tabela"; lança e o extrato mostra "+ R$ 330,00". Mesma venda com 10% → R$ 298,80. Uma linha de R$ 153,00 com desconto de R$ 3,00 → R$ 150,00 (o exemplo do dono). Desconto maior que o total → mensagem e botão desabilitado. Nenhuma linha "desconto" aparece no carrinho.

**Orçamento de e2e:** uma invocação, com recorte.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run test:e2e -- --grep "financeiro venda"</automated>
  </verify>
  <acceptance_criteria>
    - O caso do exemplo 6 com desconto de R$ 2,00 fecha em R$ 330,00 na tela e no extrato.
    - O caso "R$ 153 − R$ 3 = R$ 150" passa.
    - `grep -rn "desconto" db/schema.ts` não encontra coluna nenhuma (o desconto mora nas linhas).
    - `grep -n "repartirDesconto" lib/financeiro/acoes.ts components/amassa/financeiro/painel-venda.tsx` encontra a mesma função nos dois lados.
  </acceptance_criteria>
  <done>O arredondamento de balcão (R$ 153 por R$ 150) é um campo, e o Mês continua sabendo quanto cada área vendeu de verdade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `lancarVenda` | Identificador de item, quantidade, texto de preço e de desconto vêm do cliente |
| navegador → `definirAtalhoDoItem` | Identificador e estado desejado vêm do cliente |
| `sessionStorage` → painel | O rascunho pode estar corrompido ou ser de outra versão |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-19 | Elevation of Privilege | `lancarVenda`, `definirAtalhoDoItem` | high | mitigate | `exigirUsuario()` primeira instrução; `verificar-acoes` |
| T-04.4-20 | Tampering | categoria/descrição de linha de catálogo | high | mitigate | O servidor lê item, descrição e categoria do banco; o cliente manda só id, quantidade e preço digitado |
| T-04.4-21 | Tampering | desconto e total | high | mitigate | Servidor refaz a repartição com `repartirDesconto` e recalcula o total; valores já descontados vindos do cliente não existem na entrada |
| T-04.4-22 | Denial of Service | tamanho da venda | medium | mitigate | 1-100 linhas, quantidade 1-9999, texto limitado; teto de total 10^9 |
| T-04.4-23 | Tampering | rascunho corrompido | low | mitigate | `lerRascunho` valida campo a campo e cai no rascunho vazio; nada do rascunho vai ao servidor sem passar pelo Zod |
| T-04.4-24 | Information Disclosure | rascunho no navegador | low | accept | `sessionStorage` vive só na aba do gestor autenticado e some ao fechar a aba; não guarda nada além do que está na tela |
| T-04.4-25 | Information Disclosure | dados de teste | high | mitigate | Itens semeados com os nomes inventados do protótipo e sufixo único; nenhum cliente real |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- Dois recortes "financeiro venda" neste plano (Tarefas 2 e 3), um por tarefa.
- Os exemplos 1, 2, 3, 5, 6 e 7 do protótipo têm caso próprio no e2e.
</verification>

<success_criteria>
- A Venda monta o carrinho pelo catálogo, por busca, pela lista completa ou por valor livre (FNC-01).
- Preço editável com "tabela R$ X" e passado intocado (FNC-02); desconto repartido (D-09/D-10).
- Data retroativa fecha o dia (FNC-04).
- O efeito no estoque é mostrado e nunca gravado (FNC-15).
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.4-financeiro-parte-1/04.4-03-SUMMARY.md` when done
</output>
