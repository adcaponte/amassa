---
phase: 04.2-abertura-do-espaco
plan: 04
type: execute
wave: 4
depends_on: ["04.2-03"]
files_modified:
  - lib/abertura/parcelas.ts
  - lib/abertura/prazos.ts
  - lib/abertura/textos.ts
  - lib/abertura/esquemas.ts
  - lib/abertura/acoes.ts
  - lib/abertura/consultas.ts
  - app/(app)/abertura/page.tsx
  - app/(app)/abertura/loading.tsx
  - components/amassa/abertura/abas-abertura.tsx
  - components/amassa/abertura/painel-resumo.tsx
  - components/amassa/abertura/lista-meses.tsx
  - components/amassa/abertura/data-inauguracao.tsx
  - tests/unit/abertura-parcelas.test.ts
  - tests/unit/abertura-prazos.test.ts
  - tests/e2e/abertura-painel.spec.ts
autonomous: true
requirements: [ABE-12, ABE-13, ABE-14]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 140000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A visão \"Por mês\" soma, em cada mês, todas as parcelas de todos os itens que caem nele, lista de quais itens e quais parcelas o mês é feito, e identifica o mês mais pesado — ABE-13, critério 2 do ROADMAP."
    - "Uma parcela que já venceu continua somando no mês dela: o mês passado aparece na lista, esmaecido, com o valor que saiu — a visão é do fluxo inteiro, não só do que falta (D-16)."
    - "Um item de 6 parcelas com a primeira em novembro aparece em seis meses distintos, atravessando a virada de ano, e cada mês mostra a fração dele (\"Forno · 3/6\")."
    - "A barra de cada mês compara os meses **entre si** e a escala é nomeada: o mês mais pesado se identifica como tal e os outros dizem sua porcentagem em relação a ele — barra sem escala visível é enfeite com aparência de informação (D-16)."
    - "A barra de comparação tem `role=\"img\"` e `aria-label` com a proporção em texto — quem usa leitor de tela não recebe silêncio no lugar do número."
    - "O painel mostra três blocos, nesta ordem: comprometido (separado em à vista e a prazo), sai neste mês (com o próximo mês ao lado) e precisa de atenção — ABE-12/D-15."
    - "O bloco \"precisa de atenção\" soma tarefas atrasadas e entregas vencidas, mostra os dois números separados abaixo, e fica em vermelho só quando a soma é maior que zero."
    - "\"Sai neste mês\" e \"próximo mês\" usam a chave de mês do dia civil de Brasília, e em dezembro o próximo mês é janeiro do ano seguinte — nunca o mês 13."
    - "A data de inauguração é editável no cabeçalho e a contagem regressiva acompanha na hora — ABE-14, critério 8 do ROADMAP."
    - "Depois de a data passar, a contagem mostra quantos dias se passaram em vez de zero: espaço que abriu tarde é informação, não erro — D-17."
    - "Enquanto nenhuma data de inauguração foi definida, o cabeçalho pede a data em vez de inventar uma — a migração não semeia data nenhuma, e a tabela de configuração pode estar vazia."
    - "`abertura_configuracao` nunca tem duas linhas: a gravação é um `insert ... on conflict` sobre a restrição de linha única, não um `insert` seguido de `update`."
    - "As linhas de composição do mês truncam com reticências e nenhuma tela do módulo exige rolagem horizontal no celular — a visão \"Por mês\" é a de maior risco disso (UI-SPEC §Comportamento responsivo)."
    - statement: "Dois gestores definindo a data de inauguração ao mesmo tempo deixam a tabela com uma linha só, com a última data recebida — a restrição de linha única é a barreira, independentemente da ordem de confirmação."
      verification: backstop
  prohibitions:
    - statement: "Nenhum total do painel nem da visão por mês é calculado dentro de componente React — os três blocos e o fluxo mensal vêm de funções puras de `lib/abertura/`."
      status: resolved
      verification: test
    - statement: "Nenhuma barra de comparação existe sem escala nomeada em texto ao lado dela."
      status: resolved
      verification: test
    - statement: "Nenhuma data de inauguração é inventada por padrão de banco, de código ou de formulário quando a configuração está vazia."
      status: resolved
      verification: test
    - statement: "Custo recorrente (aluguel, energia, água, internet, contador) não entra em lugar nenhum deste módulo — D-03 mandou para o módulo financeiro."
      status: resolved
      verification: judgment
  artifacts:
    - lib/abertura/parcelas.ts
    - lib/abertura/prazos.ts
    - lib/abertura/acoes.ts
    - components/amassa/abertura/painel-resumo.tsx
    - components/amassa/abertura/lista-meses.tsx
    - components/amassa/abertura/data-inauguracao.tsx
    - tests/unit/abertura-parcelas.test.ts
  key_links:
    - "`fluxoMensal` é a fonte única do que sai em cada mês — o bloco \"Sai neste mês\" do painel lê dela, nunca de uma segunda soma própria, ou os dois números divergem na primeira mudança."
    - "`proximoMes` (plano 01) é o que faz dezembro virar janeiro do ano seguinte no bloco do painel — somar 1 ao número do mês produziria o mês 13."
    - "A escala nomeada da barra é o que a transforma de enfeite em informação; foi corrigida no protótipo depois de o dono perguntar o que ela media."
    - "A restrição de linha única de `abertura_configuracao` (criada no plano 01) é o que permite `on conflict` — sem ela, dois gestores produziriam duas linhas e a leitura passaria a depender de ordem."

coverage:
  - id: D1
    description: "A visão por mês soma o que sai em cada mês, diz de quais itens e parcelas, e identifica o mês mais pesado"
    requirement: "ABE-13"
    verification:
      - kind: unit
        ref: "tests/unit/abertura-parcelas.test.ts — fluxoMensal com dois itens, virada de ano, mês passado e pico"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/abertura-painel.spec.ts — a aba Por mês com itens semeados, composição e escala conferidas na tela"
        status: unknown
    human_judgment: false
  - id: D2
    description: "A barra de comparação tem escala nomeada em texto e rótulo acessível com a proporção"
    requirement: "ABE-13"
    verification:
      - kind: e2e
        ref: "tests/e2e/abertura-painel.spec.ts — role img e o aria-label da barra, mais o texto da escala"
        status: unknown
    human_judgment: false
  - id: D3
    description: "O painel mostra comprometido separado em à vista e a prazo, quanto sai neste mês e no próximo, e a soma de tarefas atrasadas com entregas vencidas"
    requirement: "ABE-12"
    verification:
      - kind: unit
        ref: "tests/unit/abertura-parcelas.test.ts — resumoDoPainel, inclusive a virada de dezembro para janeiro"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/abertura-painel.spec.ts — os três blocos lidos na tela"
        status: unknown
    human_judgment: false
  - id: D4
    description: "A data de inauguração é editável e a contagem regressiva acompanha, inclusive quando a data já passou"
    requirement: "ABE-14"
    verification:
      - kind: unit
        ref: "tests/unit/abertura-prazos.test.ts — contagemRegressiva antes, no dia e depois"
        status: unknown
      - kind: e2e
        ref: "tests/e2e/abertura-painel.spec.ts — edita a data no cabeçalho e lê a contagem"
        status: unknown
    human_judgment: false
  - id: D5
    description: "Com a configuração vazia, o cabeçalho pede a data em vez de inventar uma"
    requirement: "ABE-14"
    verification:
      - kind: e2e
        ref: "tests/e2e/abertura-painel.spec.ts (@vazio-global) — o cabeçalho antes de qualquer data existir"
        status: unknown
    human_judgment: false
  - id: D6
    description: "A aba Por mês tem estado vazio próprio e nenhuma tela do módulo exige rolagem horizontal no celular"
    requirement: "ABE-13"
    verification:
      - kind: e2e
        ref: "tests/e2e/abertura-painel.spec.ts — estado vazio da aba e largura no viewport de celular com nome de item longo"
        status: unknown
    human_judgment: true
    rationale: "Que a leitura mês a mês seja compreensível de relance, com a barra e a composição juntas, é julgamento visual — o e2e prova que os elementos existem e que a largura não estoura, não que a leitura funciona."
---

<objective>
Entregar as duas leituras que o módulo existe para produzir e que não se leem em lugar nenhum
hoje: **quanto sai em cada mês, de que ele é feito**, e **os três números do topo** — mais a data
de inauguração editável com a contagem regressiva que a acompanha.

Purpose: D-15 registra a troca deliberada do segundo bloco de "falta comprar" para "sai neste mês",
com a razão: o quanto falta comprar já se lê na lista; o quanto sai este mês não se lia em lugar
nenhum, e é o número que aperta. E D-16 chama a visão por mês de a mais valiosa e a mais fácil de
fazer errado — ela precisa somar **todas** as parcelas de **todos** os itens no mês certo,
incluindo as que já passaram.

Output: `fluxoMensal` e `resumoDoPainel` em módulo puro, a terceira aba, o painel de três blocos,
e o cabeçalho com a data editável.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/phases/04.2-abertura-do-espaco/04.2-CONTEXT.md
@.planning/phases/04.2-abertura-do-espaco/04.2-UI-SPEC.md
@.planning/phases/04.2-abertura-do-espaco/04.2-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Artefato | Onde |
|---|---|
| `fluxoMensal`, `resumoDoPainel`, `nomeDoMes` | `lib/abertura/parcelas.ts` |
| `contagemRegressiva` | `lib/abertura/prazos.ts` |
| `esquemaInauguracao` | `lib/abertura/esquemas.ts` |
| `definirDataDeInauguracao` | `lib/abertura/acoes.ts` |
| `obterConfiguracaoDaAbertura` | `lib/abertura/consultas.ts` |
| `PainelResumo`, `ListaMeses`, `DataInauguracao` | `components/amassa/abertura/` |
| Terceira aba "Por mês" | `components/amassa/abertura/abas-abertura.tsx` |
| `data-testid` novos: `abertura-bloco-mes`, `abertura-bloco-atencao`, `abertura-mes`, `abertura-mes-barra`, `abertura-mes-escala`, `abertura-mes-composicao`, `abertura-regressiva`, `abertura-editar-inauguracao` | componentes |

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O fluxo mensal — a soma que não pode errar</name>
  <files>lib/abertura/parcelas.ts, lib/abertura/textos.ts, lib/abertura/consultas.ts, app/(app)/abertura/page.tsx, components/amassa/abertura/abas-abertura.tsx, components/amassa/abertura/lista-meses.tsx, tests/unit/abertura-parcelas.test.ts, tests/e2e/abertura-painel.spec.ts</files>
  <read_first>
    - `.planning/phases/04.2-abertura-do-espaco/prototipo.html` — a função `porMes()`, a função `nomeMes`, e o bloco `// POR MÊS` de `desenhar()` inteiro. O bloco define a estrutura visual do cartão de mês: nome + valor no topo, barra, escala em texto, e a composição abaixo de uma linha divisória. O `role="img"` e o `aria-label` da barra já estão lá.
    - `.planning/phases/04.2-abertura-do-espaco/04.2-CONTEXT.md` — D-16 inteiro, e a seção `<specifics>`, que nomeia esta visão como a mais valiosa e a mais fácil de errar.
    - `.planning/phases/04.2-abertura-do-espaco/04.2-UI-SPEC.md` §"Comportamento responsivo" — a visão por mês é a de maior risco de rolagem horizontal.
    - `lib/abertura/parcelas.ts` como ficou nos planos 01 e 03 — `calcularParcelas`, `chaveDoMes` e `proximoMes` já existem e são a base desta função.
    - `tests/unit/abertura-parcelas.test.ts` — a suíte de bordas do plano 01, que esta tarefa estende.
  </read_first>
  <behavior>
    Casos novos em `tests/unit/abertura-parcelas.test.ts`, todos com `hoje` literal:
    - Dois itens à vista, um em 2026-09-05 e outro em 2026-09-20 → um mês só, com o total somado e **duas** linhas de composição.
    - Um item de 980000 centavos em 6 parcelas com a primeira em 2026-11-10 → seis meses, de 2026-11 a 2027-04, cada um com uma linha de composição rotulada com a fração (`"Forno · 1/6"` … `"Forno · 6/6"`) e o mesmo valor. A virada de ano acontece dentro do vetor.
    - Um item à vista tem a linha de composição **sem** fração no rótulo — `de === 1` não vira "1/1".
    - Mês no passado continua na lista: item com a primeira parcela em 2026-01-10 e `hoje: "2026-09-10"` → 2026-01 aparece, marcado como passado.
    - `ehMesAtual` é verdadeiro exatamente para a chave de mês de `hoje`.
    - Pico: com três meses de 1000, 3000 e 2000, o do meio tem `ehPico` verdadeiro e `percentualDoPico` 100; os outros têm 33 e 67 (arredondamento para inteiro). Com **um** mês só, `ehPico` é falso — um mês sozinho não se compara com nada, e chamá-lo de mais pesado seria ruído.
    - Empate no topo: dois meses com o mesmo valor máximo — defina e teste o comportamento (ambos marcados, ou o primeiro). Escolha, documente no comentário da função e afirme no teste; o que não pode é o resultado depender da ordem de iteração de um `Map` sem ninguém ter decidido.
    - Ordem: as chaves saem em ordem cronológica crescente, sempre.
    - Lista vazia → vetor vazio, sem lançar e sem divisão por zero no cálculo do pico.
    - A soma dos totais de todos os meses é igual à soma dos valores de todos os itens — a invariante que prova que nenhuma parcela foi perdida nem contada duas vezes. Inclua um item de 10000 centavos em 3 parcelas para exercer a divisão não exata.
  </behavior>
  <action>
**`lib/abertura/parcelas.ts` — `fluxoMensal(itens, hoje)`.** Devolve um vetor ordenado por chave de
mês crescente, cada entrada com: `chave` (`"YYYY-MM"`), `totalEmCentavos`, `composicao`
(`{ rotulo, valorEmCentavos }[]`), `ehMesAtual`, `ehPassado` (chave menor que a de `hoje`),
`ehPico` e `percentualDoPico` (inteiro de 0 a 100).

Percorre os itens, chama `calcularParcelas` de cada um — **a mesma função que a linha do item usa**,
nunca uma segunda implementação — e joga cada parcela no balde do mês dela. O rótulo da composição é
o nome do item, com `· k/n` acrescentado só quando `n > 1`. O total do mês é a soma exata dos
valores das parcelas, em centavos, sem arredondar: o arredondamento continua acontecendo só em
`formatarReais`. Se ele acontecesse aqui, a soma dos meses deixaria de bater com o total
comprometido, e os dois números do topo passariam a se contradizer na tela.

`percentualDoPico` é `Math.round(total / pico * 100)`. Com a lista vazia não há pico — devolva o
vetor vazio antes de dividir.

Acrescente também `nomeDoMes(chave)`, que devolve "novembro de 2026" por `Intl` a partir de um
`Date.UTC` do dia 1 daquele mês, com `timeZone: "UTC"` — nunca com o fuso do runtime. Como esta
função usa `Intl`, ela mora em `lib/abertura/formato.ts`, não no módulo puro de cálculo; mantenha a
separação que o plano 01 estabeleceu.

**`components/amassa/abertura/abas-abertura.tsx`.** Agora as três abas do protótipo existem: Itens,
Tarefas e Por mês (`?aba=meses`). É este plano que a introduz, porque é ele que dá conteúdo a ela.

**`components/amassa/abertura/lista-meses.tsx`.** Server Component. Cabeçalho da lista: "Quanto sai
por mês" à esquerda e "N meses · R$ X" à direita. Um cartão por mês, na ordem cronológica:
- topo: o nome do mês (com "· este mês" quando for o atual) e o valor;
- barra de comparação com largura proporcional ao pico, `role="img"` e `aria-label` com a proporção em texto ("48% do mês mais pesado") — sem isso, quem usa leitor de tela recebe silêncio no lugar do número;
- **escala nomeada em texto**, logo abaixo da barra: "mês mais pesado" no pico, e "N% do mês mais pesado" nos demais. Este texto não é redundante com a barra: é o que transforma a barra de enfeite em informação, e foi acrescentado ao protótipo depois de o dono perguntar o que ela media;
- composição: uma linha por parcela do mês, com o rótulo à esquerda e o valor à direita. **O rótulo trunca com reticências**, nunca empurra a largura — a visão por mês é a tela de maior risco de rolagem horizontal no celular, e um nome de item longo é o que a provoca. Ponha o nome completo em `title`.
- mês no passado: esmaecido, como no protótipo. Ele continua na lista — a visão é do fluxo inteiro, não só do que falta (D-16).

**Estado vazio da aba.** "Sem nada a pagar ainda." + "Cadastre um item com data e o fluxo aparece
aqui." — verbatim do UI-SPEC. Marque o teste correspondente com `@vazio-global`, pelo mesmo motivo
dos planos 01 e 02: ele afirma que nenhum item existe no banco.

**No e2e** (`tests/e2e/abertura-painel.spec.ts`, título contendo `abertura painel`): semeie um item
à vista com data no mês corrente e um item de 6 parcelas começando no mês corrente; confira que a
aba Por mês lista os meses em ordem, que o mês do pico traz o texto de escala do pico, que os outros
trazem a porcentagem, que a barra tem rótulo acessível, e que a composição mostra a fração da
parcela. No viewport de celular, com um item de nome longo, confira que não há rolagem horizontal.
  </action>
  <verify>
    <automated>npm test -- tests/unit/abertura-parcelas.test.ts && npm run lint && npx tsc --noEmit && npm run test:e2e -- --grep "abertura painel"</automated>
  </verify>
  <acceptance_criteria>
    - `fluxoMensal` soma todas as parcelas de todos os itens no mês certo, incluindo as vencidas, e a soma dos meses é igual à soma dos itens — afirmado com valor literal no teste.
    - Um item de 6 parcelas aparece em seis meses distintos, atravessando a virada de ano, cada um com a fração no rótulo.
    - O mês mais pesado é identificado como tal em texto, e os demais dizem a porcentagem em relação a ele; com um mês só, nenhum é chamado de pico.
    - O comportamento de empate no topo está decidido, comentado na função e afirmado no teste.
    - A barra tem `role="img"` e `aria-label` com a proporção.
    - No celular, um item de nome longo não produz rolagem horizontal na aba Por mês.
    - Com nenhum item no banco, a aba mostra o estado vazio do UI-SPEC, e o teste está marcado `@vazio-global`.
  </acceptance_criteria>
  <done>A visão "Por mês" soma certo, mostra de que cada mês é feito, e a barra diz o que mede.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Os três blocos do topo</name>
  <files>lib/abertura/parcelas.ts, lib/abertura/textos.ts, app/(app)/abertura/page.tsx, app/(app)/abertura/loading.tsx, components/amassa/abertura/painel-resumo.tsx, tests/unit/abertura-parcelas.test.ts, tests/e2e/abertura-painel.spec.ts</files>
  <read_first>
    - `.planning/phases/04.2-abertura-do-espaco/prototipo.html` — a `<section class="resumo">` do cabeçalho e o trecho de `desenhar()` que preenche `r-total`, `r-vista`, `r-prazo`, `r-mes`, `r-mes2`, `r-urgentes`, `r-atras` e `r-pendentes`, incluindo a classe `alerta` condicional no terceiro bloco.
    - `.planning/phases/04.2-abertura-do-espaco/04.2-CONTEXT.md` — D-15, com a explicação da troca deliberada do segundo bloco.
    - `.planning/phases/04.2-abertura-do-espaco/04.2-UI-SPEC.md` §"Estados de carregamento" — os três blocos com esqueleto na altura final, para a página não pular.
    - `lib/abertura/prazos.ts` — `contarEntregasVencidas` (plano 03) e `urgenciaDaTarefa` (plano 02), que alimentam o terceiro bloco.
    - `components/amassa/cartao-painel.tsx` — o cartão de painel que o design system já oferece.
  </read_first>
  <behavior>
    Casos novos em `tests/unit/abertura-parcelas.test.ts` para `resumoDoPainel(itens, tarefas, hoje)`:
    - Dois itens à vista e um a prazo → `comprometidoEmCentavos` é a soma dos três, e `aVistaEmCentavos + aPrazoEmCentavos` é igual a ele.
    - `saiNesteMesEmCentavos` traz só as parcelas cuja chave de mês é a de `hoje`, e inclui a parcela que **já venceu** dentro do mês corrente.
    - **Virada de ano no bloco:** `hoje: "2026-12-20"` com parcelas em 2026-12 e 2027-01 → `saiNesteMesEmCentavos` é o de dezembro e `saiNoProximoMesEmCentavos` é o de janeiro de 2027. É o caso que pega uma implementação que soma 1 ao número do mês.
    - Mês sem nenhuma parcela → o bloco correspondente vale 0, e não é omitido nem indefinido.
    - `tarefasAtrasadas` conta só as não concluídas com prazo anterior a `hoje`; `entregasVencidas` usa a mesma regra dos três fatos do plano 03; `precisamDeAtencao` é a soma dos dois.
    - Listas vazias → todos os campos em 0, sem lançar.
  </behavior>
  <action>
**`lib/abertura/parcelas.ts` — `resumoDoPainel(itens, tarefas, hoje)`.** Devolve
`{ comprometidoEmCentavos, aVistaEmCentavos, aPrazoEmCentavos, saiNesteMesEmCentavos,
saiNoProximoMesEmCentavos, tarefasAtrasadas, entregasVencidas, precisamDeAtencao }`.

Os dois valores mensais saem de `fluxoMensal` — **a mesma função da Tarefa 1**, procurando a chave
de `hoje` e a de `proximoMes(chave)`. Não escreva uma segunda soma por mês aqui: duas somas com a
mesma intenção divergem na primeira mudança, e o número do topo passaria a contradizer a lista de
baixo na mesma tela. `proximoMes` é o que faz dezembro virar janeiro do ano seguinte; somar 1 ao
número do mês produziria o mês 13 e um bloco em branco todo mês de dezembro.

`entregasVencidas` reusa `contarEntregasVencidas` e `tarefasAtrasadas` reusa a classificação de
`urgenciaDaTarefa` — nenhuma das duas regras é reescrita aqui.

**`components/amassa/abertura/painel-resumo.tsx`.** Server Component. Três blocos, na ordem de D-15,
uma coluna no celular e três a partir de 660px:
1. **Comprometido** — o total grande, e abaixo "R$ X à vista · R$ Y a prazo".
2. **Sai neste mês** — o valor grande, e abaixo "próximo mês R$ Z".
3. **Precisa de atenção** — a soma grande, e abaixo "N tarefas atrasadas · M entregas vencidas". O valor grande fica em `--color-erro` **só quando a soma é maior que zero**; com zero, ele é neutro. Um zero em vermelho gritaria sem ter o que gritar.

Nenhum dos números é calculado aqui: o componente recebe o resultado de `resumoDoPainel` já pronto.
O primeiro bloco substitui o bloco isolado que o plano 01 desenhou.

**`loading.tsx`.** Os três blocos com esqueleto na **altura final**, para a página não pular quando
os números chegarem — é o motivo pelo qual o UI-SPEC pede isso explicitamente para este painel.

**No e2e** (mesmo arquivo, mesmo `--grep`): com itens e tarefas semeados de forma controlada, leia
os três blocos e confira os oito números contra o esperado. Inclua uma tarefa atrasada e uma entrega
vencida para o terceiro bloco ficar diferente de zero e em vermelho.
  </action>
  <verify>
    <automated>npm test -- tests/unit/abertura-parcelas.test.ts && npm run test:e2e -- --grep "abertura painel"</automated>
  </verify>
  <acceptance_criteria>
    - O painel mostra os três blocos na ordem de D-15, com os oito números do protótipo.
    - "Sai neste mês" e "próximo mês" saem de `fluxoMensal`, não de uma segunda soma — conferido no diff.
    - Em dezembro, "próximo mês" mostra janeiro do ano seguinte — afirmado com valor literal no teste unitário.
    - "Precisa de atenção" soma tarefas atrasadas e entregas vencidas e só fica vermelho quando a soma passa de zero.
    - O esqueleto dos três blocos tem a altura final e a página não salta quando os números chegam.
    - `npm run verificar` sai 0.
  </acceptance_criteria>
  <done>Os três números do topo existem e nenhum deles é calculado dentro de um componente.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: A data de inauguração e a contagem que não para em zero</name>
  <files>lib/abertura/prazos.ts, lib/abertura/textos.ts, lib/abertura/esquemas.ts, lib/abertura/acoes.ts, lib/abertura/consultas.ts, app/(app)/abertura/page.tsx, components/amassa/abertura/data-inauguracao.tsx, tests/unit/abertura-prazos.test.ts, tests/e2e/abertura-painel.spec.ts</files>
  <read_first>
    - `.planning/phases/04.2-abertura-do-espaco/prototipo.html` — o `<header class="cab">` com o botão `quando`, o painel `quando-edita`, e o trecho de `desenhar()` que preenche `dias` e `dias-rot`. Os quatro rótulos possíveis estão lá: "dias", "é hoje", "dia atrás" e "dias atrás".
    - `.planning/phases/04.2-abertura-do-espaco/04.2-CONTEXT.md` — D-17, e a frase que explica por que a contagem continua depois de a data passar.
    - `db/schema.ts` → `aberturaConfiguracao` como ficou no plano 01, em especial a restrição de linha única.
    - `lib/abertura/prazos.ts` — `diferencaEmDias` já está disponível por importação de `parcelas.ts`.
  </read_first>
  <behavior>
    Casos novos em `tests/unit/abertura-prazos.test.ts` para `contagemRegressiva(inauguracaoEm, hoje)`:
    - `("2026-10-24", "2026-09-10")` → `{ tipo: "faltam", dias: 44 }`.
    - `("2026-10-24", "2026-10-23")` → `{ tipo: "faltam", dias: 1 }`.
    - `("2026-10-24", "2026-10-24")` → `{ tipo: "hoje", dias: 0 }`.
    - `("2026-10-24", "2026-10-25")` → `{ tipo: "passou", dias: 1 }` — e **nunca** zero nem número negativo: espaço que abriu tarde é informação (D-17).
    - `("2026-10-24", "2026-10-27")` → `{ tipo: "passou", dias: 3 }`.
    - `("2026-12-31", "2027-01-03")` → `{ tipo: "passou", dias: 3 }` — atravessa o ano.
    - `contagemRegressiva(null, hoje)` → `null`, para o cabeçalho saber que ainda não há data.
  </behavior>
  <action>
**`lib/abertura/prazos.ts` — `contagemRegressiva`.** União discriminada
`{ tipo: "faltam" | "hoje" | "passou"; dias: number }`, com `dias` sempre não negativo. Aceita
`null` e devolve `null`. Usa `diferencaEmDias` sobre datas civis — não um cálculo em milissegundos
sobre instantes, que é o que o protótipo faz e o que faria a contagem trocar de valor sozinha ao
cruzar a meia-noite de um fuso que não é o do ateliê.

**`lib/abertura/esquemas.ts` — `esquemaInauguracao`.** Uma data `YYYY-MM-DD` validada por regex e
por reconstrução. Sem limite de intervalo: uma inauguração adiada para o ano que vem é legítima, e
uma data no passado também (D-17 conta com isso).

**`lib/abertura/consultas.ts` — `obterConfiguracaoDaAbertura()`.** Devolve `{ inauguracaoEm }` ou
`null`. **Não escreve nada**: uma leitura que cria a linha quando não a encontra é uma escrita
disfarçada, e faria a primeira visita de qualquer gestor gravar uma data que ninguém deu.

**`lib/abertura/acoes.ts` — `definirDataDeInauguracao(entradaBruta)`.** `exigirUsuario()` primeiro;
`safeParse`; e então **um** `insert ... on conflict` sobre a restrição de linha única, atualizando
`inauguracao_em`. Um `select` seguido de `insert` ou `update` seria uma corrida entre dois gestores
salvando ao mesmo tempo; o `on conflict` resolve isso numa instrução só, e é para isso que a
restrição de linha única foi criada no plano 01. `revalidatePath("/abertura")` no fim.

**`components/amassa/abertura/data-inauguracao.tsx`.** Client Component no cabeçalho, no molde do
protótipo: em repouso, o texto "Inauguração em 24 de outubro de 2026" como botão discreto com
`aria-label` dizendo que altera a data; ao acionar, ele dá lugar a um campo de data com os botões
Salvar e Cancelar; `Enter` salva e `Escape` cancela. À direita, a contagem regressiva: o número
grande e, abaixo, o rótulo que muda com o tipo — "dias" quando faltam, "é hoje" no dia, "dia atrás"
para exatamente um e "dias atrás" para mais de um.

**Quando não há data definida** (a tabela está vazia, que é o estado logo depois da migração): o
cabeçalho mostra "Defina a data de inauguração" como botão, e o espaço da contagem fica com um
travessão em vez de um número. Não invente uma data, nem no padrão do banco, nem no valor inicial do
campo, nem em constante no código — uma data de inauguração inventada é uma informação falsa exibida
com a mesma autoridade das verdadeiras.

**No e2e** (mesmo arquivo, mesmo `--grep`): com a configuração vazia, confira o cabeçalho pedindo a
data (marque este caso com `@vazio-global`); defina uma data futura e confira a contagem e o rótulo;
troque para uma data no passado e confira que a contagem mostra os dias decorridos com o rótulo de
passado, e não zero.
  </action>
  <verify>
    <automated>npm test -- tests/unit/abertura-prazos.test.ts && npm run verificar-acoes && npm run test:e2e -- --grep "abertura painel"</automated>
  </verify>
  <acceptance_criteria>
    - A data de inauguração é editável no cabeçalho e a contagem acompanha imediatamente.
    - Com a data no passado, a contagem mostra quantos dias se passaram e o rótulo de passado — nunca zero e nunca número negativo.
    - Com a configuração vazia, o cabeçalho pede a data e nenhuma data é inventada em lugar nenhum.
    - Salvar duas vezes não cria uma segunda linha em `abertura_configuracao`.
    - `Enter` salva e `Escape` cancela a edição da data; o botão de repouso tem `aria-label`.
    - `npm run verificar` sai 0.
  </acceptance_criteria>
  <done>A data de inauguração é do dono, a contagem acompanha, e o dia em que ela passa não vira um erro na tela.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `definirDataDeInauguracao` | Entrada não confiável que grava numa tabela de configuração de linha única |
| dados do banco → cálculo do painel | Nenhum dado externo entra aqui, mas um valor extremo já armazenado atravessa para o cálculo do fluxo mensal |

## STRIDE Threat Register (ASVS L1 · bloqueia em `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-18 | Elevation of Privilege | `definirDataDeInauguracao` | high | mitigate | `exigirUsuario()` como primeira instrução do corpo, verificado por `npm run verificar-acoes` |
| T-04.2-19 | Tampering | `abertura_configuracao` | medium | mitigate | `insert ... on conflict` sobre a restrição de linha única — a tabela não pode chegar a duas linhas nem sob duas escritas simultâneas |
| T-04.2-20 | Tampering | `esquemaInauguracao` | medium | mitigate | Data validada por formato **e** por reconstrução: 2026-02-30 é recusada antes de tocar o banco |
| T-04.2-21 | Denial of Service | `fluxoMensal` | medium | mitigate | O número de meses é limitado pelo teto de 36 parcelas por item (`check` do plano 01) — a visão não pode ser levada a desenhar milhares de cartões a partir de uma linha |
| T-04.2-22 | Information Disclosure | `lista-meses.tsx` | low | mitigate | Nomes de item interpolados normalmente pelo React; o `title` do truncamento recebe o mesmo valor escapado |
</threat_model>

<verification>
- `npm run verificar` sai 0.
- `npm run test:e2e -- --grep "abertura painel"` sai 0 — as três invocações deste plano, uma por tarefa.
- A soma dos meses da visão "Por mês" é igual ao total do bloco "Comprometido" na mesma tela, com dados semeados — conferido no e2e e registrado no SUMMARY.
- Os testes `@vazio-global` novos (aba Por mês e cabeçalho sem data) rodam na cadeia `vazio-*`.
</verification>

<success_criteria>
- A visão "Por mês" soma o que sai em cada mês, diz de quais itens e parcelas, e identifica o mês mais pesado (critério 2 do ROADMAP).
- A data de inauguração é editável e a contagem regressiva acompanha, inclusive depois de a data passar (critério 8 do ROADMAP).
- O painel mostra os três blocos de D-15 com os oito números do protótipo.
- Nenhum total é calculado dentro de componente React.
- `npm run verificar` sai 0.
</success_criteria>

<output>
Create `.planning/phases/04.2-abertura-do-espaco/04.2-04-SUMMARY.md` when done
</output>
