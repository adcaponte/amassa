---
phase: 04.3
slug: comparador-de-compras
status: draft
shadcn_initialized: true
preset: radix-nova (baseColor neutral, CLI 3.8.5 pinned — 02b-01/02b-02 decisions), lucide-react icons
created: 2026-09-17
---

# Phase 04.3 — Comparador de Compras — UI Design Contract

> Aba nova dentro de `/abertura` (ao lado de Itens, Tarefas, Por mês). A fonte de verdade visual
> continua sendo `prototipo.html` (D-01) — este documento cobre **o que o protótipo com
> `localStorage` não consegue expressar** (dado vindo do Postgres, dois usuários simultâneos,
> Server Actions, D-23) e registra as poucas correções deliberadas exigidas por D-05–D-08, D-13, e
> pelo contrato já fixado em `04.2-UI-SPEC.md`, que esta aba **herda e não redefine**.

---

## Herdado de 04.2-UI-SPEC.md — não redefinir aqui

Ler `04.2-UI-SPEC.md` antes de implementar. Esta fase reaproveita, sem alteração:

- Tokens de cor e tipografia (`04-DESIGN-SYSTEM.md` §2/§4) — nenhuma cor ou tamanho novo.
- Esqueleto no formato do conteúdo para carregamento; nunca "carregando..." solto.
- Erro em linguagem humana; formulário nunca perde o que foi digitado.
- Salvamento otimista nos toques mais frequentes; criar/editar/remover podem esperar o servidor.
- Alvos de toque ≥44px; campo de formulário nunca <16px; `aria-label` em botão só com ícone;
  `aria-pressed`/estado correto em controles de alternância; foco visível; contraste AA;
  `prefers-reduced-motion` respeitado.
- Toda remoção nomeia o que se perde; nenhuma exclusão silenciosa.
- Voz afetiva e direta, nunca corporativa (`04-DESIGN-SYSTEM.md` §9).
- Rodapé de diálogo preso por flex, nunca `position: sticky` (D-24).
- Padrão de navegação D-23: abrir/fechar diálogo por `history.pushState`
  (`url-sem-navegar.ts`); gravar termina em navegação completa
  (`window.location.assign`); nunca `router.refresh()`.
- Dialog único com conteúdo responsivo por CSS (nunca `Dialog` + `Sheet` simultâneos — decisão de
  projeto 03-06, motivo: dois `Root` modais abertos ao mesmo tempo levam o Radix a marcar ambos
  `aria-hidden`). **Toda janela desta fase usa `components/ui/dialog.tsx`**, nunca `sheet.tsx`.
- Breakpoints do módulo Abertura: celular <660px, intermediário 660–980px, desktop ≥980px
  (não os 720px do protótipo — ver Desvios Deliberados).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (já inicializado no projeto) |
| Preset | `radix-nova`, `baseColor: neutral`, CLI fixada em 3.8.5 (não `@latest` — decisões 02b-01/02b-02) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Archivo Narrow (títulos) / Inter (corpo) — opção A do `04-DESIGN-SYSTEM.md` §4, pendente de confirmação final do Theo |

---

## Spacing Scale

Herdada de `04-DESIGN-SYSTEM.md`/`04.2-UI-SPEC.md`, sem token novo:

| Token | Value | Usage nesta fase |
|-------|-------|-------------------|
| xs | 4px | Gap entre ícone de alerta e texto; gap interno de selo |
| sm | 8px | Espaço entre pílulas de categoria; padding de selo |
| md | 16px | Padding de célula de tabela e de cartão mobile; gap de campo de formulário |
| lg | 24px | Padding de painel/diálogo; margem lateral da barra de abas (`mx-6`, igual às 3 abas atuais) |
| xl | 32px | Espaço entre a barra de sub-abas e o conteúdo |
| 2xl | 48px | Padding vertical do estado vazio |

Exceções:
- Caixa de marcação ("marcar para comparar"): glifo visual de 20×20px dentro de uma zona de toque
  de 44×44px (padding absorve a diferença) — mesma técnica do checklist de `04.2` para os botões
  de 30×30px dentro de linha de 44px+.
- Botões "···" (editar categoria) e ícone de alerta: 44×44px de zona de toque com glifo menor
  dentro, nunca o glifo isolado como alvo.

---

## Typography

**Herdada por inteiro de `04-DESIGN-SYSTEM.md` §4 — nenhum tamanho novo introduzido.** O projeto
já declara 4 papéis com 3 pesos (400/500/600); esta fase reusa os 4, não reduz a 2 pesos porque
isso contrariaria o token já em produção.

| Role | Size | Weight | Line Height | Uso nesta fase |
|------|------|--------|-------------|-----------------|
| Corpo | 16px | 400 | 24px (1.5) | Texto de célula/cartão, campos de formulário, valor dos campos longos no detalhe |
| Apoio | 14px | 400 | 20px (1.43) | Metadados: contagem "N cotações", rótulo de pílula de categoria, dica do campo |
| Micro | 12px | 500, letter-spacing 0.06em, caixa alta | 16px | Cabeçalho de coluna da tabela; rótulo dos 6 campos longos no detalhe/comparação |
| Título | 20px | 600 | 28px (1.4) | Título de diálogo ("Nova cotação", "Editar categoria", detalhe da cotação) |

Preço em destaque (cabeçalho do detalhe e da coluna de comparação) usa **20px/700**, número
tabular (`font-variant-numeric: tabular-nums`), igual ao protótipo — é a única ocorrência de peso
700 nesta aba, reservada a esse único elemento (preço em destaque), nunca a texto corrido.

---

## Color

Os tokens do protótipo já são, byte a byte, os tokens reais da plataforma
(`--fundo`↔`--color-fundo`, `--acento`↔`--color-acento`, `--sucesso`↔`--color-sucesso`,
`--erro`↔`--color-erro` etc. — mesmos valores hex). Nenhuma cor nova.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-fundo` `#F6F3F0` | Fundo da página/aba |
| Secondary (30%) | `--color-superficie` `#FFFFFF` / `--color-superficie-2` `#EFEAE5` | Painel da tabela, cartões mobile, colunas de comparação, fundo de pílula não-selecionada |
| Accent (10%) | `--color-acento` `#894025` | Ver lista fechada abaixo |
| Destructive | `--color-erro` `#B91C1C` | Ver lista fechada abaixo |

**Accent reservado para (lista fechada — nunca "todo elemento interativo"):**
- Botão primário "+ Nova cotação" (o único botão terracota desta tela — `04-DESIGN-SYSTEM.md` §3:
  "um botão terracota por tela, no máximo").
- Selo de situação "cotando" (fundo `--color-acento-fundo`, texto `--color-acento`) — herdado
  literalmente do protótipo, é rótulo de estado, não ação.
- Anel de foco (`outline`/`ring`) de campos de formulário e da pílula de aba ativa.

**Descartado deliberado:** a pílula de categoria ativa e a pílula da aba ativa "Cotações" **não**
usam preenchimento terracota (diferente do `.aba.ativa` do protótipo) — usam o mesmo tratamento
neutro que `abas-abertura.tsx` já usa (`bg-background text-foreground font-semibold shadow-sm`
sobre fundo `bg-muted`). Ver Desvios Deliberados.

**Destructive reservado para:**
- Botão "Excluir" dentro do formulário de edição e do `AlertDialog` de confirmação (categoria e
  cotação).
- Selo de situação "descartado" **não** usa vermelho — usa neutro (`--color-superficie-2` /
  `--color-tinta-fraca`), igual ao protótipo (`selo-descartado`). Vermelho aqui é reservado a
  perigo/alerta, não a "descartado".
- Campo `alertas` preenchido: rótulo em `--color-erro`, valor com fundo `--color-erro-fundo`
  (herdado do protótipo) **mais** o ícone `TriangleAlert` (ver Estados e Comportamento) — nunca só
  cor (WCAG 1.4.1).

Semânticas herdadas sem alteração: `favorito` = `--color-sucesso`/`--color-sucesso-fundo`.

---

## Copywriting Contract

Vocabulário: esta aba **nunca** usa a palavra solta "item" para uma cotação — o módulo já usa
"item" para a lista de compras (`abertura_itens`, aba "Itens"). Usar sempre **"cotação"**.

| Element | Copy |
|---------|------|
| Primary CTA | **"+ Nova cotação"** (botão terracota; título do diálogo: "Nova cotação" / "Editar cotação") |
| CTA secundário (nova categoria) | "+ Nova categoria" |
| CTA secundário (comparar) | "Comparar selecionados" ↔ "Voltar à lista" (alterna, `btn-sec`, nunca terracota) |
| Empty — nenhuma categoria (comparador vazio) | **"Nenhuma categoria ainda."** / "Crie uma categoria para começar a comparar — fornos, torno, moedor de café…" + botão "+ Nova categoria" |
| Empty — categoria sem cotações | **"Nenhuma cotação aqui ainda."** / "Adicione a primeira cotação de {nome da categoria, minúsculo}." + botão "+ Nova cotação" |
| Empty — comparação com menos de 2 marcadas | **"Marque pelo menos duas cotações."** / "Selecione na lista e volte aqui para comparar lado a lado." |
| Error state (salvar) | "Não deu para salvar. Verifique a internet e tente de novo." (o formulário permanece preenchido) |
| Destructive confirmation — categoria, 0 cotações | "Excluir a categoria «{nome}»? Ela não tem nenhuma cotação." |
| Destructive confirmation — categoria, 1 cotação | "Excluir a categoria «{nome}»? A cotação dela será perdida." |
| Destructive confirmation — categoria, N cotações | "Excluir a categoria «{nome}»? As {N} cotações dela serão perdidas." |
| Destructive confirmation — cotação | "Excluir a cotação de «{empresa}»? Todas as informações dela serão perdidas." |
| Aviso (toast) — cotação criada | "Cotação adicionada." |
| Aviso (toast) — cotação salva | "Alterações salvas." |
| Aviso (toast) — cotação excluída | "Cotação excluída." |
| Placeholder — campo preço | "R$ 00.000 — deixe em branco para “sob consulta”" |
| Preço nulo exibido | "—" (visualmente); leitor de tela recebe "sob consulta" via `aria-label` |

---

## UI Considerations

Applicable state considerations resolved: 11 covered, 3 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | nenhuma categoria (nav + list-collection) | ✅ covered | Estado vazio nomeado acima mostra só "+ Nova categoria" — comparador sobe vazio, sem importação |
| empty | categoria sem cotações (list-collection) | ✅ covered | Estado vazio nomeado acima mostra "+ Nova cotação" |
| empty | marcação de comparação <2 (list-collection) | ✅ covered | Mensagem única para 0 ou 1 marcada, nomeada acima |
| loading | aba Cotações ao entrar (list-collection, nav) | 🧪 backstop | Esqueleto: 3 pílulas de categoria (retângulos arredondados) + cabeçalho de tabela + 4 linhas na altura final (56px), sem "carregando" solto |
| error | salvar cotação/categoria (form) | ✅ covered | Mensagem de erro herdada de 04.2; formulário permanece com os valores digitados, diálogo não fecha |
| populated | lista de cotações, volume típico (list-collection) | ✅ covered | 3–8 cotações por categoria, como o exemplo do protótipo; tabela ≥660px, cartões <660px |
| overflow | comparação lado a lado (list-collection) | ✅ covered | Rolagem horizontal **dentro do `<div>` da comparação**, nunca na página (D-13); página nunca ganha `overflow-x` |
| overflow | pílulas de categoria, muitas categorias (nav) | ✅ covered | `flex-wrap`, nunca rolagem/clip — mesmo padrão do protótipo, cresce em altura |
| zero-one-many | contagem de cotações por categoria (list-collection) | ✅ covered | Pílula de categoria mostra a contagem ("Fornos 3"); 0 usa singular implícito (sem número exibido = "Fornos" só, ou "0" — ver Assunções) |
| long-text | especificação do produto em cartão mobile (static-content) | 🧪 backstop | `line-clamp-2` no cartão; tabela (≥660px) não trunca (célula cresce em altura) |
| long-text | campos longos (diferenciais, observações etc.) no detalhe/comparação (static-content) | ✅ covered | `white-space: pre-wrap`, sem truncamento — é o conteúdo que a comparação existe para mostrar por inteiro |
| long-text | nome da categoria na pílula (nav) | 🧪 backstop | Sem truncamento definido no protótipo; limite de 60 caracteres no campo (ver Campos) mantém a pílula dentro de 2 linhas no pior caso |
| interactive-control | caixa de marcação "comparar" | ✅ covered | `<input type="checkbox">` nativo, zona de toque 44×44px, `aria-label="Marcar «{empresa}» para comparar"`, estado lido nativamente pelo leitor de tela |
| interactive-control | ícone de alerta na linha/cartão | ✅ covered | `aria-hidden` no ícone + `sr-only "— tem alerta"` anexado ao nome acessível da linha, nunca só a cor |

---

## Layout & Navigation Contract

### A quarta aba

- **Rótulo: "Cotações". Parâmetro de URL: `aba=cotacoes`** (sem acento, minúsculo, plural —
  mesmo padrão de `itens`/`tarefas`/`meses`).
- **Por que não "Comparador" nem "Compras":** "Comparador" é mais longo que qualquer rótulo atual
  (risco de quebra de linha maior); "Compras" colide semanticamente com a aba "Itens", que já É a
  lista de compras. "Cotações" é o substantivo que `04.3-CONTEXT.md` usa em toda parte (D-06, D-15)
  e não colide com nenhum rótulo existente.
- **Cabimento em 320px, calculado:** container `mx-6` (24px cada lado) → 272px de largura para a
  barra; `p-1` (4px cada lado) → 264px internos; 4 pílulas + 3 `gap-1` (4px) → 252px ÷ 4 = **63px
  por pílula**, contra ~90px quando eram 3. Em `text-corpo` (16px), "Cotações" (8 caracteres) pode
  não caber numa linha só nesse ponto extremo. **Decisão:** permitir que o rótulo quebre em duas
  linhas dentro da pílula — a régua de 44px é um **piso**, não um teto; a pílula cresce em altura
  quando o texto quebra, nunca trunca com reticências, nunca estoura a largura. Nenhuma mudança de
  fonte/padding é necessária. Verificar em execução real a 320px (iPhone SE) antes de fechar o
  plano — se a quebra ficar visualmente ruim na prática, a correção é aumentar `mx-6`→`mx-4` nesse
  breakpoint específico, não reescrever o componente.
- Estrutura do componente: estender `ABAS` em `abas-abertura.tsx` com
  `{ valor: "cotacoes", rotulo: "Cotações" }` e `AbaAbertura` com `"cotacoes"`; mesma navegação
  por `<Link>` (Next.js), mesmo `role="tablist"`/`role="tab"`, mesmo `memo` com comparador próprio.

### Sub-abas de categoria (dentro da aba Cotações)

- Diferente da barra de 4 abas (contagem fixa, `flex-1` de largura igual), as categorias têm
  **contagem variável** (criadas pelo usuário) — usam o layout do protótipo: `flex-wrap`, largura
  por conteúdo, nunca `flex-1`, nunca rolagem horizontal (quebra de linha em vez disso).
- Navegação por query string, no mesmo padrão da aba: `?aba=cotacoes&categoria={id}` via
  `<Link>` normal (RSC, **não** `history.pushState` — trocar de categoria é navegação de conteúdo
  igual a trocar de aba, não abrir/fechar diálogo). Sem `categoria` na URL, o servidor usa a
  primeira categoria por ordem de criação.
- Pílula ativa: mesmo tratamento neutro da aba principal (`bg-background` sobre `bg-muted`), **não**
  o preenchimento terracota do protótipo — ver Desvios Deliberados.
- Cada pílula mostra o nome e a contagem de cotações: `"Fornos 3"` (herdado do protótipo, com
  `apoio`/opacidade reduzida no número).
- **Criar categoria (D-14, <10s):** pílula tracejada "+ Nova categoria" abre um `Dialog` pequeno
  por `history.pushState` (abrir/fechar de diálogo, D-23) com **um único campo** (nome, foco
  automático) e um botão "Criar". Enter no campo submete. Ao salvar, navegação completa para
  `/abertura?aba=cotacoes&categoria={id-novo}` (D-23, gravar = navegação completa). Do clique ao
  ver a nova aba: um diálogo, um campo, um clique — cabe em 10 segundos.
- **Editar/excluir categoria:** botão "···" (ícone-só, `aria-label="Editar categoria «{nome}»"`)
  abre um `Dialog` com campo de nome pré-preenchido + botão "Salvar" e, separado, um botão
  `btn-perigo` "Excluir categoria" que abre um `AlertDialog` de confirmação (copy acima, com a
  contagem de cotações certa — controlado por `open`/`onOpenChange` + `event.preventDefault()` até
  a resposta do servidor, padrão de 03-05).

### Responsivo

| Faixa | Lista de cotações | Comparação | Formulário |
|---|---|---|---|
| Celular (<660px) | **Cartões empilhados** (não tabela) — caixa de marcação, empresa, selo, preço, especificação truncada em 2 linhas | Rolagem horizontal **dentro do próprio contêiner** da comparação — igual ao desktop, é a única exceção de rolagem horizontal do módulo (D-13) | Campos empilhados, `Dialog` quase em tela cheia com rolagem interna |
| Intermediário (660–980px) | Tabela completa (todas as colunas, incluindo Situação) | Colunas de 260px, rolagem horizontal no contêiner | Campos em 2 colunas |
| Desktop (≥980px) | Tabela completa | Colunas de 300px, rolagem horizontal só se não couberem todas | Campos em 2–3 colunas |

**Diferente do protótipo:** a quebra celular/desktop usa 660px/980px (padrão já fixado por
`04.2-UI-SPEC.md`), não os 720px do protótipo — ver Desvios Deliberados. Não existe coluna
"Situação" escondida por CSS: abaixo de 660px não há tabela, e a partir de 660px há espaço para
todas as colunas.

---

## Estados e Comportamento — o que o protótipo não expressa

### Carregamento

Esqueleto no formato do conteúdo (`04-DESIGN-SYSTEM.md` §7): 3 retângulos arredondados (altura de
pílula) para as sub-abas + cabeçalho de tabela + 4 linhas na altura final (56px). Nunca
"carregando..." solto; nunca tela em branco.

### Erro

- Salvar cotação/categoria: formulário permanece aberto e preenchido; mensagem herdada de 04.2
  aparece perto do botão de salvar.

### Vazio (três níveis, obrigatórios)

1. **Comparador inteiro vazio** (zero categorias) — o comparador sobe sem nenhum dado (o dono
   confirmou que o protótipo não tem nada cadastrado; não há importação nesta fase). O estado
   convida a criar a primeira categoria.
2. **Categoria sem cotações.**
3. **Menos de duas cotações marcadas** ao abrir a comparação.

### Situação (cotando/favorito/descartado)

Troca **só dentro do formulário completo de edição**, exatamente como no protótipo — não existe
seletor rápido fora dele. (Um seletor rápido foi cogitado e descartado: ver Deferred Ideas em
`04.3-CONTEXT.md`, escopo novo fora desta fase.) A única interação otimista do cliente nesta aba é
marcar itens para comparar, abaixo.

### Marcar para comparar

Estado **só de cliente**, nunca gravado no servidor nem na URL (mesmo modelo do protótipo:
`marcados`/`modo` vivem em memória do componente). Por não tocar o servidor, já é instantâneo por
natureza — não precisa do mecanismo de reversão otimista, só precisa persistir enquanto o usuário
não troca de categoria (trocar de categoria zera a marcação, como no protótipo). Isso também
significa que a visão de comparação **não é compartilhável por link** nesta fase — decisão
consciente, não lacuna.

### Descartado (D-10)

- Na tabela/cartão: opacidade reduzida (`opacity-60`) em empresa/especificação/preço; o **selo
  continua 100% opaco e com o texto "descartado"** (o texto já é a pista não-visual — não depende
  só da opacidade).
- Descartados **podem ser marcados** para comparação (resolução da discricionariedade). Na coluna
  de comparação, a mesma opacidade reduzida se aplica ao conteúdo da coluna inteira, com o selo
  "descartado" visível no topo — evita que a coluna pareça um erro de carregamento.

### Alerta (D-12) — cor mais uma pista não-visual

O campo `alertas`, quando preenchido:
- No **detalhe**: rótulo em vermelho + fundo `--color-erro-fundo` (herdado do protótipo) **mais**
  um ícone `TriangleAlert` (lucide) de 16px antes do rótulo, `aria-hidden="true"`.
- Na **tabela/cartão**: mesmo ícone `TriangleAlert` em `--color-erro`, 16px, imediatamente antes do
  nome da empresa — visível ao passar o olho pela lista sem abrir cada linha. Acompanhado de um
  `<span class="sr-only">— tem alerta</span>` junto ao nome acessível da linha, para quem usa
  leitor de tela.
- Na **comparação**: mesmo tratamento do detalhe, por coluna.

### Preço

- `null` exibido como **"—"**, envolvido em `<span aria-label="Preço sob consulta">—</span>`
  (nunca deixar o leitor de tela anunciar só "traço"/"hífen").
- Formatação de valor presente reaproveita `formatarReais` de `lib/abertura/formato.ts` — nenhuma
  segunda função de formatação de moeda nesta fase.
- Campo do formulário: `<input type="text">` comum (não `type="number"` — precisa aceitar
  "R$ 24.900" como o usuário digita), 16px, `placeholder` acima. A conversão para centavos (D-08)
  é sempre no servidor; nenhuma máscara client-side é obrigatória nesta fase (**sugestão opcional,
  fora do escopo mínimo:** um texto pequeno abaixo do campo espelhando ao vivo como o valor será
  interpretado, no espírito do rodapé ao vivo do formulário de Encomendas — não bloqueia o plano se
  não for feito).

---

## Component Inventory

Reaproveitados sem mudança (já em `components/ui/`): `dialog`, `alert-dialog`, `button`, `input`,
`label`, `field`, `skeleton`, `sonner` (toast), `select` (se necessário no formulário — avaliar no
plano).

**Novos, via `npx shadcn add` (CLI 3.8.5, mesma fixação de 02b-01/02b-02):**
- `checkbox` — caixa de marcação "marcar para comparar" (nenhum componente equivalente existe hoje;
  `caixa-marcacao.tsx` do módulo Abertura é um botão com `aria-pressed`, semântica diferente de uma
  seleção múltipla independente — não reaproveitar para este caso).
- `textarea` — os 6 campos longos da cotação (não existe `textarea.tsx` no projeto hoje).

**Não usar `Table`/`Badge` do shadcn** (não instalados, e o projeto não usa essas abstrações em
nenhum outro módulo): tabela como HTML semântico (`<table>`) com classes Tailwind, mesmo padrão de
qualquer outra tela; selos de situação como `<span>` com classes Tailwind mapeando os tokens de cor
acima — mesmo padrão dos selos já existentes no projeto (ex. Fornos).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | `checkbox`, `textarea` | not required — componentes oficiais do registro padrão, mesma cadeia já aprovada em 02b-01 (radix-nova, CLI 3.8.5, lucide-react) |

Nenhum registro de terceiros declarado. Nenhum bloco de terceiros usado.

---

## Assunções e Desvios Deliberados

> Registrado explicitamente porque nem CONTEXT.md, nem o protótipo, nem `04.2-UI-SPEC.md`
> respondiam sozinhos — decisão conservadora, alinhada ao módulo hospedeiro.

1. **Pílula de aba/categoria ativa é neutra, não terracota**, diferente de `.aba.ativa` do
   protótipo. Justificativa: `04-DESIGN-SYSTEM.md` §3 limita a um botão terracota por tela, e
   `abas-abertura.tsx` (já em produção) já usa tratamento neutro para a aba selecionada — manter a
   mesma tela com dois sistemas de destaque (pílula de aba neutra, pílula de categoria terracota)
   seria inconsistente. O terracota fica só no CTA "+ Nova cotação".
2. **Breakpoints 660px/980px, não os 720px do protótipo** — para herdar exatamente a régua já
   fixada e testada em `04.2-UI-SPEC.md` para o mesmo módulo, em vez de introduzir uma terceira
   régua de responsividade dentro da mesma página.
3. **Vocabulário "cotação", nunca "item" solto** — para não colidir com a aba "Itens" (lista de
   compras) do mesmo módulo, que já usa "item" para outra entidade.
4. **Contagem "0" na pílula de categoria vazia:** exibir o número (`"Torno de esteca 0"`), não
   omitir — consistente com o protótipo, que sempre mostra `c.itens.length` mesmo quando zero.
5. **Limites de campo (discricionário, valores sensatos):** `empresa` 1–160 caracteres
   (obrigatório); `produto/especificação` 0–200; nome de categoria 1–60; os 6 campos longos
   (`diferenciais`, `assistencia`, `pagamento`, `contato`, `observacoes`, `alertas`) 0–2000
   caracteres cada, validados com Zod no servidor.
6. **Categorias não são reordenáveis** (confirmado — "não é critério" em CONTEXT.md); exibidas em
   ordem de criação, igual ao protótipo.
7. **Ordem padrão da tabela antes de qualquer clique em "Ordenar por preço":** ordem de cadastro
   (`criado_em` ascendente), igual ao protótipo (que sempre `push` no fim do array).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
