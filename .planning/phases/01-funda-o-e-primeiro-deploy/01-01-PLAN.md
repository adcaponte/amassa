---
phase: 01-funda-o-e-primeiro-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/api/health/route.ts
  - db/index.ts
  - db/schema.ts
  - db/migrate.ts
  - db/migrations/
  - drizzle.config.ts
  - docker/compose.yml
  - docker/compose.dev.yml
  - .env.example
  - eslint.config.mjs
  - .prettierrc
  - vitest.config.ts
  - lib/saude.ts
  - tests/unit/saude.test.ts
autonomous: true
requirements: [INFRA-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/api/health` responde `ok` e confirma uma consulta real ao banco"
    - "A página inicial mostra o nome AMASSA e uma frase dizendo que a plataforma está no ar"
    - "O Postgres roda em contêiner com volume nomeado e sem porta publicada no arquivo de produção"
    - "`npm run lint` e `npm test` existem, rodam e falham quando devem falhar"
  artifacts:
    - path: "app/page.tsx"
      provides: "Página mínima com a marca AMASSA e a frase alterável (D-12, D-13)"
      contains: "FRASE_NO_AR"
    - path: "app/api/health/route.ts"
      provides: "Endpoint de saúde que consulta o banco de verdade"
      exports: ["GET"]
    - path: "db/index.ts"
      provides: "Pool do node-postgres e instância Drizzle exportada como `db`"
      exports: ["db", "pool"]
    - path: "db/schema.ts"
      provides: "Tabela mínima `verificacao_infraestrutura` lida por /api/health"
      contains: "verificacaoInfraestrutura"
    - path: "docker/compose.yml"
      provides: "Serviço postgres com volume nomeado, sem porta publicada e sem fuso injetado"
      contains: "dados_postgres"
    - path: ".env.example"
      provides: "Todas as variáveis do projeto declaradas sem nenhum valor"
      contains: "DATABASE_URL="
  key_links:
    - from: "app/api/health/route.ts"
      to: "db/index.ts"
      via: "importa `db` e executa uma consulta sobre `verificacaoInfraestrutura`"
      pattern: "from ['\"]@/db['\"]"
    - from: "db/index.ts"
      to: "docker/compose.yml"
      via: "DATABASE_URL aponta para o serviço postgres do compose"
      pattern: "process\\.env\\.DATABASE_URL"
    - from: "db/migrations/"
      to: "db/schema.ts"
      via: "SQL gerado por drizzle-kit a partir do schema TypeScript"
      pattern: "verificacao_infraestrutura"
---

<objective>
Traçar o caminho completo — do arquivo-fonte ao navegador, passando por uma consulta real ao
Postgres — na máquina do desenvolvedor. É a fatia mais fina que atravessa todas as camadas que
esta fase vai modificar: página Next.js, rota de API, Drizzle, migração versionada e contêiner
de banco. Tudo o que vem depois (imagem de produção, pipeline, servidor) apenas aprofunda a
fidelidade deste mesmo caminho; nada acrescenta camada nova.

Purpose: provar cedo, com os melhores tokens de contexto, que a arquitetura escolhida em
`01-ARQUITETURA.md` §2 e §3 fecha ponta a ponta. Se algo aqui não fechar, descobrimos depois de
um commit, não depois de dez.

Output: projeto Next.js 15 com TypeScript estrito, página mínima da marca (D-12), `/api/health`
consultando o banco de verdade, Drizzle conectado e migrando, `docker/compose.yml` com o serviço
`postgres`, e a base de qualidade (ESLint, Prettier, Vitest) que o pipeline da Fase 1 vai usar
como portão.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-e-primeiro-deploy/01-CONTEXT.md
@amassa-plataforma/01-ARQUITETURA.md
@amassa-plataforma/02-MODELO-DE-DADOS.md
@amassa-plataforma/00-BRIEFING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Caminho completo — página, /api/health e Postgres em contêiner</name>
  <precondition>Docker está em execução na máquina do desenvolvedor (`docker info` responde sem erro)</precondition>
  <files>package.json, tsconfig.json, next.config.ts, app/layout.tsx, app/page.tsx, app/globals.css, app/api/health/route.ts, db/index.ts, db/schema.ts, db/migrate.ts, db/migrations/, drizzle.config.ts, docker/compose.yml, docker/compose.dev.yml, .env.example</files>
  <read_first>
    - `amassa-plataforma/01-ARQUITETURA.md` §2 (tabela de stack e o que não usar), §3 (estrutura
      de pastas), §5 (segurança do banco — sem porta publicada, volume nomeado)
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` §0 (armadilha de fuso: nunca injetar fuso no
      contêiner do Postgres) e §6 (fluxo de trabalho com Drizzle)
    - `.planning/phases/01-funda-o-e-primeiro-deploy/01-CONTEXT.md` — decisões D-04, D-05, D-12,
      D-13, D-14 e a lista "Decisões já fechadas nos documentos fonte"
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §2 (somente para os dois valores hexadecimais) e §9
      (voz da interface)
    - `.gitignore` na raiz do repositório — conferir cobertura, não recriar
  </read_first>
  <action>
Criar o projeto Next.js 15 na raiz do repositório com App Router, TypeScript e Tailwind CSS v4,
usando o gerador oficial `create-next-app` (sem `src/`, sem Turbopack obrigatório), preservando
`.planning/`, `.claude/`, `amassa-plataforma/` e o `.gitignore` já versionado. Fixar as versões
exatas de `next` e `react` no `package.json`.

Em `tsconfig.json`, manter `strict: true` e o alias `@/*` para a raiz — `any` só com comentário
justificando (critério 2 de `00-BRIEFING.md` §11). Em `next.config.ts`, definir `output` como
`standalone`, porque a imagem de produção da Fase 1 depende disso.

Limpar `app/globals.css` deixando apenas a importação do Tailwind v4. Remover as variáveis de cor
padrão e o bloco de mapeamento de tema que o gerador cria — os tokens de design do AMASSA e o
mapeamento para o shadcn pertencem à Fase 2 e precisam vir antes de qualquer componente shadcn
ser instalado (D-14). Nenhum componente shadcn é instalado nesta fase.

Escrever `app/layout.tsx` com `lang="pt-BR"`, título `AMASSA` e a fonte padrão do sistema —
sem Archivo Narrow, sem Inter, sem token de tipografia (Fase 2).

Escrever `app/page.tsx` como a página mínima da marca (D-12): fundo `#F6F3F0`, tinta `#1D2221`,
aplicados por classes utilitárias de valor arbitrário do Tailwind, sem token e sem CSS global
novo. A página mostra o nome `AMASSA` e uma única frase. Declarar a frase como a constante
exportada `FRASE_NO_AR`, no topo do arquivo, com o valor `A plataforma do ateliê está no ar.` —
ela precisa ser trivialmente encontrável no código porque o critério INFRA-02 é literalmente
"alterar um texto, dar push e ver a mudança aparecer" (D-13). Registro afetivo e direto, nunca
corporativo (`04-DESIGN-SYSTEM.md` §9).

Instalar `drizzle-orm` e `pg` como dependências, e `drizzle-kit`, `tsx` e `@types/pg` como
dependências de desenvolvimento. Todas constam da tabela de stack de `01-ARQUITETURA.md` §2 ou
são o driver oficial dela; nenhuma dependência fora dessa tabela entra nesta fase.

Escrever `db/schema.ts` declarando uma única tabela, `verificacao_infraestrutura`, exportada como
`verificacaoInfraestrutura`, com `id` uuid chave primária com `gen_random_uuid()`, `criado_em`
timestamptz com `now()` e `nota` text. É deliberadamente a menor tabela possível: existe para que
`drizzle-kit generate` produza uma migração real e para que `/api/health` faça uma consulta real.
Nenhuma tabela de produto é modelada aqui — elas são da Fase 2 em diante.

Escrever `db/index.ts` criando um `Pool` do `pg` a partir de `process.env.DATABASE_URL` e
exportando `pool` e a instância Drizzle como `db`. Escrever `db/migrate.ts` usando o migrator do
`drizzle-orm/node-postgres`, apontando para `db/migrations`. Escrever `drizzle.config.ts` com
`schema` em `db/schema.ts`, `out` em `db/migrations` e dialeto `postgresql`.

Acrescentar ao `package.json` os scripts `db:generate` (drizzle-kit generate) e `db:migrate`
(`tsx db/migrate.ts`). Rodar `db:generate` e versionar o SQL produzido em `db/migrations/`.

Escrever `app/api/health/route.ts` exportando `GET`, com `runtime` node e renderização dinâmica
forçada. A rota executa uma contagem sobre `verificacaoInfraestrutura` através do `db`; em
sucesso responde HTTP 200 com o JSON `{"status":"ok","banco":"ok"}`; em falha responde HTTP 503
com `{"status":"erro","banco":"erro"}` e registra o erro no log do servidor. Não responder 200
sem tocar no banco — o critério INFRA-03 exige consulta real.

Escrever `docker/compose.yml` contendo, nesta ordem, o serviço `postgres` e a seção de volumes.
O serviço usa a imagem oficial `postgres` na versão estável mais recente (mínimo 15), declara
`POSTGRES_USER`, `POSTGRES_PASSWORD` e `POSTGRES_DB` por interpolação de variável (`${...}`) em
um bloco `environment` próprio do serviço, monta o volume nomeado `dados_postgres` em
`/var/lib/postgresql/data`, define `restart: unless-stopped` e um `healthcheck` com `pg_isready`.
O serviço não publica nenhuma porta no host e não recebe nenhuma variável de fuso horário — as
duas coisas são armadilhas nomeadas em `01-ARQUITETURA.md` §5 e `02-MODELO-DE-DADOS.md` §0.
Nenhum bloco de arquivo de ambiente compartilhado entre serviços: cada serviço declara o que
precisa, um a um.

Escrever `docker/compose.dev.yml` como sobreposição usada **apenas** na máquina do desenvolvedor,
publicando o Postgres em `127.0.0.1:5433` para que `npm run dev` alcance o banco a partir do
host. Este arquivo nunca vai para o servidor.

Escrever `.env.example` versionado, com todas as variáveis do projeto e **nenhum valor** depois
do sinal de igual: `NEXT_PUBLIC_SITE_URL`, `IMAGEM_APP`, `POSTGRES_USER`, `POSTGRES_PASSWORD`,
`POSTGRES_DB`, `DATABASE_URL`, `AUTH_SECRET`, `AUTH_TRUST_HOST`, `TZ` e `RCLONE_REMOTE`. As
quatro últimas só passam a ser usadas na Fase 2, mas entram já aqui para que o arquivo seja a
lista completa. Criar o `.env.local` da máquina com valores reais de desenvolvimento — ele já é
coberto pelo `.gitignore` e nunca é versionado.
  </action>
  <verify>
    <automated>docker compose -f docker/compose.yml -f docker/compose.dev.yml up -d postgres && npm run db:migrate && npm run build && (npm run start &) && sleep 10 && curl -sf http://localhost:3000/api/health | grep -q '"banco":"ok"' && curl -sf http://localhost:3000/ | grep -q 'A plataforma do ateliê está no ar.'</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s http://localhost:3000/api/health` devolve HTTP 200 e JSON contendo `"banco":"ok"`
    - Parando o serviço postgres, `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/health` devolve `503`
    - `grep -c 'ports:' docker/compose.yml` devolve `0`
    - `grep -c '5432:5432' docker/compose.yml` devolve `0`
    - `grep -c 'dados_postgres' docker/compose.yml` é maior que `0`
    - `grep -c 'env_file' docker/compose.yml` devolve `0`
    - `grep -c 'America/Sao_Paulo' docker/compose.yml` devolve `0`
    - `grep -c '@theme' app/globals.css` devolve `0`
    - `ls components/ui 2>/dev/null | wc -l` devolve `0` — nenhum componente shadcn instalado
    - `grep -c 'FRASE_NO_AR' app/page.tsx` é maior que `0` e o valor é `A plataforma do ateliê está no ar.`
    - `grep -c '#F6F3F0' app/page.tsx` e `grep -c '#1D2221' app/page.tsx` são maiores que `0`
    - `grep -Eq "output.*standalone" next.config.ts` sai com código 0
    - `grep -Eq '"strict"\s*:\s*true' tsconfig.json` sai com código 0
    - `ls db/migrations/*.sql | wc -l` é maior que `0` e o SQL contém `verificacao_infraestrutura`
    - `grep -E '^(DATABASE_URL|AUTH_SECRET|POSTGRES_PASSWORD|NEXT_PUBLIC_SITE_URL)=$' .env.example` casa as quatro linhas — nenhum valor depois do igual
    - `git status --porcelain | grep -c '^?? .env.local'` devolve `0` — o arquivo real está ignorado
  </acceptance_criteria>
  <done>Um navegador em `http://localhost:3000` mostra AMASSA e a frase; `/api/health` confirma uma consulta real ao Postgres do contêiner; a migração que criou a tabela lida está versionada em `db/migrations/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Portões de qualidade — ESLint, Prettier e Vitest com teste que falha de verdade</name>
  <files>eslint.config.mjs, .prettierrc, vitest.config.ts, lib/saude.ts, tests/unit/saude.test.ts, app/api/health/route.ts, package.json</files>
  <read_first>
    - `amassa-plataforma/01-ARQUITETURA.md` §2 — linha "Lint/format" da tabela de stack
    - `amassa-plataforma/00-BRIEFING.md` §11 — critérios 1, 2 e 5
    - `package.json` e `app/api/health/route.ts` criados na Task 1
  </read_first>
  <behavior>
    - `lib/saude.ts` expõe `interpretarSaudeDoBanco(consultaFuncionou: boolean)` e devolve
      `{ status: 'ok', banco: 'ok', http: 200 }` quando a consulta funcionou
    - devolve `{ status: 'erro', banco: 'erro', http: 503 }` quando a consulta falhou
    - `app/api/health/route.ts` passa a montar sua resposta a partir dessa função, em vez de
      montar o JSON inline
  </behavior>
  <action>
Configurar o ESLint em formato flat (`eslint.config.mjs`) sobre a configuração recomendada do
Next.js e do TypeScript, e o Prettier em `.prettierrc`. Acrescentar ao `package.json` os scripts
`lint` (executando o ESLint com `--max-warnings=0`, exigência de `01-ARQUITETURA.md` §2),
`format` e `test`.

Instalar `vitest` como dependência de desenvolvimento e escrever `vitest.config.ts` cobrindo
`tests/unit/**`. O script `test` roda o Vitest em modo de execução única, não em modo watch —
em modo watch o processo nunca termina e o pipeline trava.

Extrair a decisão de status de `app/api/health/route.ts` para o módulo puro `lib/saude.ts`, com
a função `interpretarSaudeDoBanco` descrita em `<behavior>`. O módulo não importa React nem o
cliente do banco — recebe dados, devolve dados (regra da pasta `lib/` em `01-ARQUITETURA.md` §3).
Escrever `tests/unit/saude.test.ts` cobrindo os dois casos antes de trocar a rota, e só então
fazer a rota consumir a função.

Este é o par de comandos que o pipeline da Fase 1 vai usar como portão antes de qualquer
publicação; se `npm test` não devolver código de saída diferente de zero quando um teste quebra,
o critério INFRA-07 não tem como ser satisfeito depois.
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` sai com código `0`
    - `grep -c -- '--max-warnings=0' package.json` é maior que `0`
    - `npm test` sai com código `0` e reporta pelo menos 2 testes passando
    - Invertendo temporariamente uma expectativa em `tests/unit/saude.test.ts`, `npm test` sai com código diferente de `0`; a inversão é desfeita antes do commit
    - `grep -c '^import' lib/saude.ts` devolve `0` — o módulo puro não importa nada
    - `grep -c "from ['\"]@/lib/saude['\"]" app/api/health/route.ts` é maior que `0`
    - `curl -sf http://localhost:3000/api/health | grep -q '"banco":"ok"'` continua funcionando depois da refatoração
  </acceptance_criteria>
  <done>`npm run lint` e `npm test` existem, passam com o código atual e falham de verdade quando o código está errado.</done>
</task>

</tasks>

## Artifacts this phase produces

Criados por este plano:

| Tipo | Nome / caminho |
|------|----------------|
| Rota | `/` — `app/page.tsx` |
| Rota | `/api/health` — `app/api/health/route.ts`, export `GET`, JSON `{"status","banco"}` |
| Símbolo | `FRASE_NO_AR` em `app/page.tsx` |
| Símbolo | `db`, `pool` em `db/index.ts` |
| Símbolo | `verificacaoInfraestrutura` em `db/schema.ts` (tabela `verificacao_infraestrutura`) |
| Símbolo | `interpretarSaudeDoBanco` em `lib/saude.ts` |
| Arquivo | `db/migrate.ts`, `drizzle.config.ts`, `db/migrations/` |
| Serviço compose | `postgres` em `docker/compose.yml` |
| Volume Docker | `dados_postgres` |
| Arquivo | `docker/compose.dev.yml` (sobreposição só de desenvolvimento, porta `127.0.0.1:5433`) |
| Scripts npm | `dev`, `build`, `start`, `lint`, `format`, `test`, `db:generate`, `db:migrate` |
| Variáveis de ambiente | `NEXT_PUBLIC_SITE_URL`, `IMAGEM_APP`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `DATABASE_URL`, `AUTH_SECRET`, `AUTH_TRUST_HOST`, `TZ`, `RCLONE_REMOTE` (declaradas sem valor em `.env.example`) |
| Arquivo | `eslint.config.mjs`, `.prettierrc`, `vitest.config.ts`, `tests/unit/saude.test.ts` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → rota Next.js | requisição não confiável atravessa aqui |
| aplicação → Postgres | credencial do banco atravessa aqui; a rede é interna do Docker |
| máquina do desenvolvedor → repositório público | um segredo que cruzar esta fronteira é um segredo queimado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | `docker/compose.yml` — serviço `postgres` | high | mitigate | Sem porta publicada e sem bind mount de host; volume nomeado `dados_postgres`. Verificado por `grep -c 'ports:' docker/compose.yml` devolvendo 0 |
| T-01-02 | Information disclosure | `app/api/health/route.ts` | medium | mitigate | O corpo da resposta de erro não inclui mensagem do driver nem string de conexão; o detalhe fica no log do servidor |
| T-01-03 | Information disclosure | `.env.local` na máquina do desenvolvedor | high | mitigate | `.gitignore` já cobre `.env*` exceto `.env.example`; conferido antes de qualquer commit desta fase |
| T-01-04 | Tampering | `db/migrations/` | medium | accept | Migração versionada em repositório público sem segredo; o SQL não protege nada e é revisável em diff |
| T-01-SC | Tampering | instalações npm | high | mitigate | Toda dependência instalada consta da tabela de stack de `01-ARQUITETURA.md` §2 ou é o driver oficial dela — a tabela é a fonte de legitimidade desta fase, já que a pesquisa está desativada por configuração do projeto. Nenhum pacote fora dessa lista entra sem verificação em `npmjs.com/package` |
</threat_model>

<verification>
- `docker compose -f docker/compose.yml -f docker/compose.dev.yml up -d postgres` sobe o banco
- `npm run db:migrate` aplica a migração e cria `verificacao_infraestrutura`
- `npm run build && npm run start` e depois `curl -sf localhost:3000/api/health | grep '"banco":"ok"'`
- `curl -sf localhost:3000/` contém `AMASSA` e a frase de `FRASE_NO_AR`
- `npm run lint && npm test` saem com código 0
- `git log --all --full-history -- .env` não devolve nada
</verification>

<success_criteria>
O caminho do arquivo-fonte até o navegador está provado ponta a ponta na máquina local, com uma
consulta real ao Postgres em contêiner, migração versionada e portões de qualidade que falham
quando devem falhar. Nada de Fase 2 (autenticação, backup, tokens de design, shadcn) entrou.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-e-primeiro-deploy/01-01-SUMMARY.md` when done
</output>
