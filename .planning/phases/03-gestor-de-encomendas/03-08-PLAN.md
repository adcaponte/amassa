---
phase: 03-gestor-de-encomendas
plan: 08
type: execute
wave: 6
depends_on: ["03-05", "03-07"]
files_modified:
  - app/(app)/encomendas/imprimir/page.tsx
  - app/(app)/encomendas/imprimir/impressao.module.css
  - app/globals.css
  - components/amassa/encomendas/botao-imprimir.tsx
  - app/(app)/encomendas/page.tsx
  - lib/encomendas/consultas.ts
  - lib/encomendas/textos.ts
  - tests/e2e/encomendas-impressao.spec.ts
  - tests/e2e/encomendas.spec.ts
  - tests/e2e/acessibilidade.spec.ts
  - docs/operacao/04-migracao-encomendas.md
  - docs/convencoes-de-interface.md
autonomous: false
requirements: [ENC-12, ENC-14]
user_setup: []

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "ENC-14 — `/encomendas/imprimir` produz uma folha A4 com quatro colunas exatas: Nome · Cliente · Etapa atual · Conclusão prevista, com `@page { size: A4; margin: 15mm; }` e sem nenhuma biblioteca de PDF (D-18)"
    - "ENC-14 — a folha usa ESCOPO PRÓPRIO e fixo, independente do filtro/busca/ordenação vigentes na tela: sempre todas as `rascunho` + `em_producao`, ordenadas por data de início (decisão resolvida no UI-SPEC, e é por isso que a rota não lê nenhum parâmetro de filtro)"
    - "ENC-14/boundary — com 15 encomendas ativas a tabela cabe em uma página A4 nas medidas especificadas (cabeçalho de coluna 8pt, corpo 10pt, altura de linha ~24px, margem 15mm); acima disso a tabela flui para uma segunda folha com `break-inside: avoid` por linha e o cabeçalho de coluna repetindo — nunca encolhendo a fonte, nunca cortando encomenda"
    - "ENC-14/adjacency — duas encomendas com a MESMA data de início ocupam linhas separadas, na mesma ordem determinística do índice; nada é agrupado nem fundido"
    - "ENC-14/empty — sem nenhuma encomenda ativa, o botão de imprimir fica DESABILITADO com a nota 'Nada ativo para imprimir agora.' logo abaixo (não some: um controle que aparece e desaparece é mais difícil de aprender do que um que fica sempre no mesmo lugar, às vezes apagado). Acessar `/encomendas/imprimir` direto nesse estado mostra a mesma frase e não manda nada para a impressora"
    - "ENC-14/encoding — nome com acento e com caractere composto sai impresso idêntico ao que está no banco: uma encomenda de teste chamada «Coleção Açúcar Ñ» aparece com todos os caracteres, sem retângulo e sem substituição"
    - "ENC-14/ordering — a folha usa o MESMO comparador do índice (data de início, desempate por nome com `localeCompare('pt-BR')`, depois id); a folha nunca mostra uma ordem que a tela não mostraria"
    - "ENC-14/precision — a coluna 'Conclusão prevista' imprime a data que `calcularCronograma` devolve, no mesmo formato da tela; a página de impressão não refaz nenhuma conta de data por conta própria"
    - "ENC-14 — na folha, 'Etapa atual' reaproveita o texto da tabela de casos de borda SEM depender de cor (impressão pode ser P&B): o caso atrasada vira o sufixo textual '{etapa} (atrasada)', e nome de encomenda `rascunho` leva o sufixo ' (rascunho)' depois do nome"
    - "ENC-12 — uma encomenda criada num contexto de navegador aparece no outro AO RECARREGAR a página, provado por um teste com dois contextos independentes do Playwright; não há WebSocket, polling nem revalidação automática, e essa ausência é deliberada"
    - "E10 impressão — vazio: botão desabilitado com a nota dizendo por quê; ninguém gasta papel para descobrir que não havia nada, e a própria tela já diz 'A roda ainda não gira.'"
    - "E10 impressão — carregando: sem estado de carregamento perceptível; a folha não busca nada por conta própria além da consulta da própria rota"
    - "E10 impressão — erro: falha de consulta cai no `EstadoErro` da rota, com a mesma copy do índice"
    - "E10 impressão — populado: uma linha por encomenda ativa, com as quatro colunas"
    - "E10 impressão — parcial: encomenda sem etapa atual definida (ainda não começou, ou já passou da data) imprime o rótulo do caso de borda do ENC-09 — nunca célula vazia, que em papel não tem como ser investigada"
    - "E10 impressão — transbordo: segunda página via `break-inside: avoid` e `@page`, sem truncamento e sem encolher o texto abaixo do legível"
    - "E10 impressão — zero-um-muitos: uma encomenda e muitas usam a mesma tabela, e o cabeçalho de coluna repete na segunda página"
    - "E10 impressão — texto longo: nome longo QUEBRA na célula, nunca trunca — em papel não existe `title` nem tooltip para recuperar o que foi cortado"
    - "A migração de produção é aplicada À MÃO, depois de um backup, por alguém que está olhando, seguindo `docs/operacao/04-migracao-encomendas.md` — nunca pelo pipeline automático (CLAUDE.md §Migrações)"
    - "`docs/operacao/04-migracao-encomendas.md` segue o formato dos roteiros 1 a 3: cada bloco de comando acompanhado de 'o que faz' e 'o que você deve ver', com a instrução de parar quando a tela divergir"
    - "`axe-core` não encontra nenhuma violação de nível A/AA em `/encomendas`, `/encomendas/{id}`, `/encomendas?nova` e `/encomendas/imprimir`, nos dois projetos"
  artifacts:
    - app/(app)/encomendas/imprimir/page.tsx
    - app/(app)/encomendas/imprimir/impressao.module.css
    - components/amassa/encomendas/botao-imprimir.tsx
    - tests/e2e/encomendas-impressao.spec.ts
    - docs/operacao/04-migracao-encomendas.md
  key_links:
    - "`listarEncomendasAtivas()` (escopo próprio da folha) ≠ `listarEncomendasDoIndice(hoje)` (janela de 12 meses): duas consultas de propósitos diferentes, e confundi-las faria a folha imprimir histórico ou o índice perder o histórico"
    - "`situacaoEm` → `textoDaSituacao` → coluna 'Etapa atual' da folha, na variante SEM cor: é o mesmo cálculo da tela, com uma apresentação diferente, e não uma segunda regra"
    - "`db/migrations/0005_encomendas.sql` + `0006_gatilhos-encomendas.sql` → `docs/operacao/04-migracao-encomendas.md` → o banco de produção: o elo que só existe se um humano executá-lo, e por isso ele é um checkpoint bloqueante e não uma tarefa automática"
  prohibitions:
    - statement: "A folha impressa NUNCA omite silenciosamente uma encomenda ativa nem encolhe o texto abaixo do legível para forçar tudo numa página. Se não couber, vai para a segunda folha. Omitir dado operacional em papel é pior do que omitir na tela: no papel não há como investigar o que faltou, e alguém no ateliê vai tomar decisão com a lista incompleta achando que está completa (D-18)"
---

<objective>
A folha A4 de "o que tem e em que pé está" — para levar para a bancada, sem biblioteca de PDF e sem
custo recorrente. Mais o fechamento da fase: o teste ponta a ponta de criar → editar → excluir nos
dois tamanhos de tela, a prova de que uma encomenda criada num dispositivo aparece no outro ao
recarregar, a varredura de acessibilidade nas telas novas, e o roteiro que o dono vai executar à
mão, depois do backup, para aplicar a migração em produção.

Purpose: entregar ENC-14 e provar ENC-12 de verdade; e fechar a fase com a migração aplicada do
jeito que este projeto decidiu aplicar migração — por alguém que está olhando, nunca por um `git
push`.
Output: `/encomendas/imprimir`, o botão no cabeçalho, a suíte ponta a ponta completa e
`docs/operacao/04-migracao-encomendas.md`.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md
@.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md
@.planning/phases/03-gestor-de-encomendas/03-PATTERNS.md
@.planning/phases/03-gestor-de-encomendas/03-05-SUMMARY.md
@.planning/phases/03-gestor-de-encomendas/03-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: A folha A4 — rota de impressão, `@media print` e o botão que sabe quando não há nada</name>
  <precondition>O Docker está rodando (`docker info` responde) — o e2e da impressão cria encomendas reais pelas Server Actions.</precondition>
  <files>app/(app)/encomendas/imprimir/page.tsx, app/(app)/encomendas/imprimir/impressao.module.css, app/globals.css, components/amassa/encomendas/botao-imprimir.tsx, app/(app)/encomendas/page.tsx, lib/encomendas/consultas.ts, lib/encomendas/textos.ts, tests/e2e/encomendas-impressao.spec.ts</files>
  <read_first>
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — a seção inteira "Impressão A4 (D-18,
      ENC-14)": o escopo próprio e fixo, o leiaute, a escala tipográfica de impressão, a regra contra
      truncamento, o `@page`, e a subseção "Sem nenhuma encomenda ativa — decisão do dono"; mais a
      tabela E10 de "UI Considerations"
    - `.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md` D-18 — as duas coisas que o
      planejador precisava resolver e onde elas foram resolvidas
    - `.planning/ROADMAP.md` §"Phase 3", critério de sucesso 12 — as quatro colunas exatas, nem mais
      nem menos
    - `lib/encomendas/consultas.ts` — `listarEncomendasDoIndice(hoje)` e a janela de 12 meses, para
      NÃO confundir com a consulta da folha
    - `lib/encomendas/cronograma.ts` — `calcularCronograma`, `situacaoEm`
    - `lib/encomendas/textos.ts` e o `textoDaSituacao` do plano 02, com a opção `semCor` já prevista
      na assinatura — a variante sem cor precisa sair do mesmo lugar, nunca de uma segunda tabela de
      frases
    - `lib/encomendas/gantt.ts` — `ordenarParaGantt`, o comparador que a folha reusa
    - `app/globals.css` — onde o bloco `@media print` global pode viver sem tocar em nenhum token
    - `app/(app)/encomendas/page.tsx` — o cabeçalho onde o botão de imprimir entra
    - `components/amassa/estado-vazio.tsx` — `notaBotao`, exatamente o mecanismo que o botão
      desabilitado com explicação precisa
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §3 — a regra de um botão terracota por tela (a folha de
      impressão não tem nenhum)
  </read_first>
  <behavior>
    - `/encomendas/imprimir` mostra o cabeçalho "AMASSA — Encomendas ativas" e "Impresso em {data e
      hora}" à direita
    - A tabela tem exatamente quatro colunas: Nome, Cliente, Etapa atual, Conclusão prevista
    - Só encomendas `rascunho` e `em_producao` aparecem; concluídas e canceladas nunca
    - A ordem é por data de início, com o mesmo desempate do índice
    - Um filtro aplicado na tela do índice NÃO muda o que a folha mostra
    - Nome de encomenda `rascunho` leva o sufixo " (rascunho)"
    - Encomenda atrasada mostra "{etapa} (atrasada)" na coluna de etapa atual, sem depender de cor
    - Encomenda que ainda não começou mostra o rótulo do caso, nunca célula vazia
    - Um nome de 120 caracteres quebra dentro da célula e não é truncado
    - Com 20 encomendas ativas, nenhuma some da folha
    - Sem nenhuma encomenda ativa, o botão de imprimir no índice está `disabled` e a nota "Nada ativo
      para imprimir agora." está no documento
    - `/encomendas/imprimir` sem sessão redireciona para `/login`
    - `axe-core` não encontra violação A/AA na rota de impressão
  </behavior>
  <action>
Escreva `tests/e2e/encomendas-impressao.spec.ts` primeiro e rode para ver falhar. Para as asserções
de impressão, use `page.emulateMedia({ media: "print" })` — é o que permite medir o estilo aplicado
pelo `@media print` no navegador em vez de confiar na leitura do CSS.

**`lib/encomendas/consultas.ts`** — acrescente `listarEncomendasAtivas()`, sem janela de data e sem
filtro de parâmetro: `status in ('rascunho','em_producao')`, ordenada por `data_inicio`. É uma
consulta DIFERENTE de `listarEncomendasDoIndice(hoje)`, de propósito — a folha tem escopo próprio e
fixo, e confundir as duas faria a folha imprimir histórico.

**`app/(app)/encomendas/imprimir/page.tsx`** — Server Component com `exigirUsuario()` na primeira
instrução. Calcula `hoje` com `hojeEmBrasilia`, ordena com `ordenarParaGantt` e renderiza a tabela
de quatro colunas. A coluna "Etapa atual" usa uma variante SEM cor de `textoDaSituacao` — a mesma
função, com um parâmetro que troca o realce de cor por sufixo textual, nunca uma segunda tabela de
frases. Sem nenhuma ativa, renderize `EstadoVazio` com a mesma nota do botão.

Nenhum botão terracota nesta tela (`03-UI-SPEC.md` §Color: a folha é só leitura/impressão). Um
botão "Imprimir" discreto no topo, com `@media print { display: none }`, é permitido para disparar
`window.print()`.

**`app/(app)/encomendas/imprimir/impressao.module.css`** — as regras de tela e de impressão da
tabela: `break-inside: avoid` por linha, `thead { display: table-header-group }` para o cabeçalho de
coluna repetir na segunda página, cabeçalho de coluna 8pt caixa alta com `letter-spacing`, corpo de
linha 10pt regular, rodapé de data 8pt, altura de linha ~24px, e `overflow-wrap: anywhere` nas
células de nome e cliente. **Nunca** encolher abaixo dessas medidas — é a restrição explícita de
D-18, e a proibição autorada em `must_haves` deste plano.

**`app/globals.css`** — acrescente ao FIM do arquivo um bloco `@media print` com
`@page { size: A4; margin: 15mm; }` e as regras que escondem a casca da aplicação na impressão
(barra lateral, barra inferior, cabeçalho móvel). `@page` é regra global e não sobrevive dentro de
um módulo CSS; este é o único acréscimo permitido ao arquivo neste plano — nenhum token existente é
alterado, e um critério de aceite confere isso por `git diff`.

**`components/amassa/encomendas/botao-imprimir.tsx`** — botão no cabeçalho do índice levando a
`/encomendas/imprimir`. Quando a contagem de ativas for 0, fica `disabled` com
`aria-disabled="true"` e a nota "Nada ativo para imprimir agora." abaixo dele, no documento (nunca
só em `title`/tooltip, porque precisa ser lida sem interação, inclusive por leitor de tela — é a
mesma regra que `estado-vazio.tsx` já documenta para `notaBotao`). Se for um botão só com ícone,
`aria-label="Imprimir folha de encomendas ativas"`.

Acrescente a `lib/encomendas/textos.ts`: `TITULO_FOLHA_IMPRESSAO` ("AMASSA — Encomendas ativas"),
`NOTA_NADA_PARA_IMPRIMIR` ("Nada ativo para imprimir agora.") e `ROTULO_IMPRIMIR`.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "impressão de encomendas"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- --grep "impressão de encomendas"` sai com 0 nos dois projetos
    - `app/(app)/encomendas/imprimir/page.tsx` contém `exigirUsuario` como primeira instrução e `npm run verificar-acoes` sai com 0
    - `app/(app)/encomendas/imprimir/page.tsx` importa `listarEncomendasAtivas`, não `listarEncomendasDoIndice`
    - `app/globals.css` contém `@page` e `size: A4` e `margin: 15mm`
    - `git diff app/globals.css | grep -c '^-'` retorna 0 — nada foi removido do arquivo, só acrescentado
    - `app/(app)/encomendas/imprimir/impressao.module.css` contém `break-inside: avoid` e `table-header-group`
    - `grep -ci 'truncate\|text-overflow' 'app/(app)/encomendas/imprimir/impressao.module.css'` retorna 0
    - `grep -ci 'jspdf\|pdfkit\|puppeteer\|html2canvas' package.json` retorna 0 — nenhuma biblioteca de PDF entrou
    - `tests/e2e/encomendas-impressao.spec.ts` cria 20 encomendas ativas e afirma que as 20 linhas estão no documento
    - `tests/e2e/encomendas-impressao.spec.ts` cria uma encomenda com nome contendo acento e caractere composto e afirma o texto exato na folha
    - `npm run lint`, `npx tsc --noEmit` e `npm run build` saem com 0
  </acceptance_criteria>
  <done>Um toque no botão produz uma folha A4 legível com o que está ativo e em que pé está — sem
  biblioteca nova, sem custo recorrente, e sem nunca cortar uma encomenda em silêncio para caber.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A prova ponta a ponta da fase — criar, editar, excluir, e ver no outro dispositivo</name>
  <precondition>O Docker está rodando (`docker info` responde) — a suíte ponta a ponta usa o Postgres de teste efêmero com as migrações 0005 e 0006 aplicadas.</precondition>
  <files>tests/e2e/encomendas.spec.ts, tests/e2e/acessibilidade.spec.ts</files>
  <read_first>
    - `tests/e2e/encomendas.spec.ts` — o caso do traçado do plano 01, a estender
    - `tests/e2e/acessibilidade.spec.ts` — a varredura `axe-core` da 2b e a forma de acrescentar
      rotas novas
    - `tests/e2e/apoio/preparar-usuario.ts` — o `globalSetup` que cria a conta de teste e publica
      `E2E_EMAIL_TESTE`/`E2E_SENHA_TESTE`
    - `tests/e2e/casca.spec.ts` (linhas 1–40) — o helper `fazerLogin` e a regra de nunca ramificar por
      nome de projeto do Playwright
    - `tests/e2e/backup.spec.ts` — o precedente de teste que precisa serializar acesso a estado
      compartilhado (o `pg_advisory_lock`), útil se a suíte nova disputar recursos
    - `amassa-plataforma/03-ROADMAP.md` §M2, fase 9 — "Teste ponta a ponta: criar → editar → excluir,
      em desktop e em viewport de celular", que é o que esta tarefa entrega
    - `.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md` `<domain>` — a ausência deliberada de
      sincronização em tempo real
    - `.planning/phases/02b-design-system-e-casca-da-aplica-o/02b-05-SUMMARY.md` — como a 2b montou a
      varredura `axe-core` e a medição de alvo de toque
  </read_first>
  <behavior>
    - Fluxo completo no projeto `desktop`: criar uma encomenda com dois itens e as seis etapas →
      abrir o detalhe → editar pelo formulário → conferir a mudança no Gantt → excluir com
      confirmação → conferir que sumiu do índice
    - O mesmo fluxo completo no projeto `celular`, usando a lista de cartões e a folha do formulário
    - ENC-12: em dois contextos independentes do navegador, criar num e recarregar no outro faz a
      encomenda aparecer; sem recarregar, ela NÃO aparece (a ausência de tempo real é asserção, não
      omissão)
    - `axe-core` não encontra violação A/AA em `/encomendas`, `/encomendas/{id}`, `/encomendas?nova`
      e `/encomendas/imprimir`, nos dois projetos
    - Todo botão só com ícone das telas novas tem nome acessível
  </behavior>
  <action>
Estenda `tests/e2e/encomendas.spec.ts` com o fluxo completo, um `test.describe` por projeto de
viewport (mas sem ramificar por `testInfo.project.name` dentro dos casos: localize pela metade
visível, do jeito que `casca.spec.ts` estabeleceu).

Para ENC-12, use dois `browser.newContext()` independentes, cada um com seu login. Afirme as DUAS
coisas: que a encomenda criada no contexto A **não** aparece no contexto B antes do `reload()`, e
que **aparece** depois. A primeira asserção é o que transforma "não implementamos tempo real" em uma
decisão testada em vez de uma omissão — se um dia alguém acrescentar revalidação automática sem
conversar, o teste fica vermelho e a conversa acontece.

Se a suíte ficar instável por disputa de recursos, siga o precedente do projeto: rode em série
(`test.describe.configure({ mode: "serial" })`), como `autenticacao.spec.ts` e `casca.spec.ts` já
fazem. Registre no SUMMARY se precisou.

Estenda `tests/e2e/acessibilidade.spec.ts` com as quatro rotas novas. Mantenha a mesma configuração
de regras da 2b — não afrouxe nenhuma regra para fazer passar; se uma violação aparecer, corrija o
componente.

**Observação registrada, não contornada:** `tests/e2e/autenticacao.spec.ts:72` (sexta tentativa de
bloqueio) trava de forma pré-existente e independente desta fase (STATE.md, `deferred-items.md` da
02b, WINDOWS.md id 3). Se a suíte inteira estourar por causa dele, rode com `--grep-invert` para
isolar e registre — mas **não** o "conserte" dentro desta fase: está fora de `files_modified` e a
causa provável é o pool do `pg` em `db/index.ts`.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "encomenda"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- --grep "encomenda"` sai com 0 nos dois projetos
    - `tests/e2e/encomendas.spec.ts` contém `browser.newContext` e uma asserção de contagem 0 ANTES do `reload()` e 1 depois
    - `tests/e2e/acessibilidade.spec.ts` inclui as rotas `/encomendas/`, `?nova` e `/encomendas/imprimir`
    - `git diff tests/e2e/acessibilidade.spec.ts | grep -ci 'disableRules\|withRules\|exclude'` retorna 0 — nenhuma regra foi afrouxada
    - `npm run test:e2e` (suíte inteira) sai com 0
    - `npm test`, `npm run lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e `npm run build` saem com 0
  </acceptance_criteria>
  <done>O fluxo que a M2 pediu — criar, editar, excluir, no computador e no celular — é uma suíte que
  roda em minutos, e a ausência de sincronização em tempo real virou uma asserção explícita em vez
  de um silêncio.</done>
</task>

<task type="auto">
  <name>Tarefa 3: O roteiro de migração de produção e a convenção de interface atualizada</name>
  <files>docs/operacao/04-migracao-encomendas.md, docs/convencoes-de-interface.md</files>
  <read_first>
    - `docs/operacao/01-preparar-servidor.md` (linhas 1–40) — o formato do roteiro: "como ler cada
      passo", os marcadores entre `<` e `>`, o par "o que faz" / "o que você deve ver", e a
      instrução de parar quando a tela divergir
    - `docs/operacao/02-publicar-e-dominio.md` — o formato dos comandos "na sua máquina" versus "no
      servidor", e a lista de critérios de aceite no fim
    - `docs/operacao/03-backup-e-restauracao.md` — como o backup é disparado e conferido; o roteiro
      novo começa por ele
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` §6 (linhas 663–704) — o fluxo de trabalho com
      Drizzle, e o aviso literal "Nunca aplique migração pelo pipeline automático"
    - `db/migrations/0005_encomendas.sql` e `db/migrations/0006_gatilhos-encomendas.sql` — o que o
      roteiro vai aplicar
    - `db/migrate.ts` e o script `npm run db:migrate` do `package.json` — o comando exato
    - `docs/convencoes-de-interface.md` §1 e §4 — a convenção de confirmação destrutiva (UI-08) que
      diz "chega na Fase 3", e a seção de componentes shadcn a atualizar
    - `.planning/phases/03-gestor-de-encomendas/03-01-SUMMARY.md` — quais componentes foram
      efetivamente instalados
  </read_first>
  <action>
**`docs/operacao/04-migracao-encomendas.md`** — roteiro para o dono executar sozinho, comando a
comando, no mesmo formato dos três anteriores. Estrutura:

0. O que este roteiro faz e o que ele NÃO faz. Uma linha dizendo que a migração é aplicada à mão,
   depois de um backup, por alguém que está olhando — e por quê (uma migração ruim aplicada por um
   `git push` acidental não tem desfazer, e agora o banco é dele).
1. **Backup primeiro, sempre.** `./scripts/backup.sh --agora`, com "o que faz" e "o que você deve
   ver", incluindo como conferir em `/api/health/backup` que o dump é recente.
2. **Conferir o que vai ser aplicado.** `docker compose exec app ls db/migrations` e a leitura dos
   dois arquivos novos, para a pessoa ver o SQL antes de rodá-lo.
3. **Aplicar.** `docker compose exec app npm run db:migrate`, com a saída esperada nomeando as duas
   migrações.
4. **Conferir de fora, não pelo relato do comando.** Uma consulta que lista as três tabelas novas,
   outra que confirma os três gatilhos `tocar_atualizado_em_*`, outra que confirma a restrição
   `marcos_zero_ou_um`, e um `insert` de teste com `dias = 2` numa etapa de marco que **precisa
   falhar** — um portão que nunca foi visto falhando é indistinguível de um portão quebrado. Termine
   com o `rollback`/`delete` da linha de teste.
5. **Conferir os grants.** Uma consulta confirmando que `amassa_app` tem
   `select/insert/update/delete` nas três tabelas novas — o `alter default privileges` da migração
   0003 deve ter cuidado disso sozinho, e este passo é o que prova.
6. **Critérios de aceite da fase**, em lista, para conferir um a um: criar encomenda, ver o Gantt,
   ler no celular, imprimir a folha.

Marcadores entre `<` e `>` com a mesma advertência dos roteiros anteriores sobre substituir o
marcador inteiro. Nenhuma senha real, nenhum IP real, nenhum dado de cliente do ateliê no
documento.

**`docs/convencoes-de-interface.md`** — atualize:
- §1: a confirmação destrutiva deixou de ser "chega na Fase 3" e passou a ser implementada;
  aponte para `components/amassa/encomendas/confirmar-excluir.tsx` como a referência do formato, e
  registre a decisão PD-01 (nome longo quebra, nunca trunca, dentro do título do diálogo).
- §4 (ou a seção de componentes shadcn): acrescente os sete componentes instalados na Fase 3 à lista
  do que já existe, para a Fase 4 não reinstalar.
- Uma seção nova e curta sobre o padrão de escrita não-otimista com reversão e aviso de falha
  (`ajuste-rapido-etapa.tsx`), já que as Fases 4 a 6 vão precisar dele — e sobre a distinção que o
  §7 do design system faz: presença e estoque são otimistas, encomendas não.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operacao/04-migracao-encomendas.md` existe e contém as cadeias `o que faz`, `o que você deve ver`, `backup.sh --agora`, `db:migrate`, `marcos_zero_ou_um` e `tocar_atualizado_em`
    - `docs/operacao/04-migracao-encomendas.md` contém um passo que exige que um `insert` inválido FALHE
    - `grep -ciE '[0-9]{1,3}(\.[0-9]{1,3}){3}' docs/operacao/04-migracao-encomendas.md` retorna 0 — nenhum IP real no documento
    - `docs/convencoes-de-interface.md` não contém mais a cadeia `Chega na Fase 3` na seção de confirmação destrutiva
    - `docs/convencoes-de-interface.md` cita `alert-dialog`, `sonner`, `select`, `dialog`, `form`, `label` e `switch` como instalados
    - `npm run lint` e `npx tsc --noEmit` saem com 0
  </acceptance_criteria>
  <done>Existe um documento que o dono consegue seguir sozinho para aplicar a migração em produção,
  com backup antes, conferência de fora depois, e um passo que só passa se uma inserção inválida
  falhar de verdade.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Tarefa 4: Aplicar a migração das encomendas no servidor de produção</name>
  <what-built>As migrações `0005_encomendas.sql` e `0006_gatilhos-encomendas.sql` estão commitadas e
  provadas contra o banco de teste efêmero por toda a suíte ponta a ponta. O que NÃO aconteceu, e
  não pode acontecer automaticamente, é a aplicação no banco de produção: `CLAUDE.md` é explícito —
  *"Migrações: aplicadas à mão, depois de um backup, por alguém que está olhando. Nunca pelo
  pipeline automático."* Nenhum passo do CI roda `db:migrate` contra produção, e este checkpoint é o
  que fecha o elo. Ele nunca é aprovado automaticamente, mesmo com `auto_advance` ligado.</what-built>
  <how-to-verify>
Abra `docs/operacao/04-migracao-encomendas.md` e execute o roteiro, passo a passo, na ordem:

1. Rode o backup e confira em `/api/health/backup` que o dump é recente.
2. Leia o SQL das duas migrações antes de aplicá-las.
3. Aplique com `docker compose exec app npm run db:migrate`.
4. Rode as conferências de fora: as três tabelas existem, os três gatilhos existem, a restrição
   `marcos_zero_ou_um` existe, e o `insert` de teste com `dias = 2` num marco **falha**.
5. Confira os grants de `amassa_app` nas três tabelas.
6. Abra `https://<seu-domínio>/encomendas` e crie uma encomenda de verdade, com dois itens. Confira
   no celular que ela aparece na lista vertical e que não há rolagem para o lado. Toque no botão de
   imprimir e veja a folha.

**Se qualquer passo devolver algo muito diferente do descrito, pare naquele passo** e traga a saída
aqui — é assim que o problema fica localizável onde aconteceu, em vez de aparecer três passos
depois sem explicação.

Além do roteiro, duas conferências que só uma pessoa consegue fazer:

- **A voz das frases novas.** "A roda ainda não gira.", "Nada por aqui com esse filtro.", os dois
  textos de confirmação e os avisos: eles soam como o AMASSA (afetivo, sensorial, direto) ou soam
  corporativos? Qualquer um que soar errado, diga qual e o que ele deveria dizer.
- **A leitura sob luz forte, no celular, em pé.** As cores das seis etapas se distinguem? A trilha
  do cartão é legível? Os botões -/+ da trilha são confortáveis com o polegar?
  </how-to-verify>
  <resume-signal>Digite "aplicado" quando a migração tiver rodado e as conferências passarem, ou descreva o passo que divergiu</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `/encomendas/imprimir` | rota nova que renderiza a lista operacional inteira do ateliê |
| repositório → banco de produção | as duas migrações atravessam essa fronteira uma única vez, por mão humana |
| tela → papel | dado impresso sai do controle do sistema: não há como corrigir, revogar nem investigar o que faltou |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-45 | Elevation of Privilege | `app/(app)/encomendas/imprimir/page.tsx` | high | mitigate | `exigirUsuario()` como primeira instrução; `lib/auth/rotas-publicas.ts` não é tocado, e um critério de aceite confere que a rota sem sessão redireciona para `/login` |
| T-03-46 | Tampering | migração aplicada sem backup ou por pipeline | high | mitigate | Checkpoint humano bloqueante (Tarefa 4) com roteiro que começa pelo backup; nenhum passo do CI roda `db:migrate` contra produção. Restrição de `CLAUDE.md` |
| T-03-47 | Repudiation | não há prova de que a migração de produção rodou | medium | mitigate | O roteiro exige conferência de fora (tabelas, gatilhos, restrição, inserção inválida que falha) e o registro da saída no SUMMARY — o mesmo padrão que a Fase 1 usou com a tabela `verificacao_infraestrutura` |
| T-03-48 | Information Disclosure | folha impressa com nomes de clientes esquecida na bancada | low | accept | É informação interna do ateliê, para uso do ateliê, num espaço físico controlado por cinco pessoas. Aceito conscientemente: o valor operacional de ter a lista na bancada é o próprio requisito |
| T-03-49 | Information Disclosure | dado de cliente real entrando em documentação versionada | high | mitigate | O roteiro usa marcadores entre `<` e `>` e dados inventados; um critério de aceite grepa por IP real, e a proibição do plano 01 cobre nomes de cliente |
| T-03-50 | Tampering | folha omitindo encomendas para caber numa página | medium | mitigate | Proibição autorada em `must_haves.prohibitions`; critério de aceite com 20 encomendas afirmando 20 linhas; `break-inside: avoid` e segunda folha em vez de recorte |
</threat_model>

<verification>
- `npm test`, `npm run test:e2e`, `npm run lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e
  `npm run build` saem todos com 0.
- Toda a suíte e2e das fases 1, 2a e 2b continua passando sem edição (com a ressalva pré-existente
  de `autenticacao.spec.ts:72`, registrada e fora do escopo).
- `git diff --exit-code lib/auth/ middleware.ts` retorna 0.
- A migração de produção foi aplicada pelo roteiro, com a saída de cada conferência registrada no
  SUMMARY.
</verification>

<success_criteria>
Um botão produz uma folha A4 legível com as encomendas ativas — nome, cliente, etapa atual e
conclusão prevista — cabendo em uma página no volume atual do ateliê e indo para a segunda folha em
vez de cortar quando não couber; o fluxo criar → editar → excluir passa nos dois tamanhos de tela; e
o banco de produção está migrado por mão humana, depois de um backup, com cada conferência feita de
fora.
</success_criteria>

<output>
Create `.planning/phases/03-gestor-de-encomendas/03-08-SUMMARY.md` when done
</output>

---

## Artefatos que este plano produz

**Arquivos novos:** `app/(app)/encomendas/imprimir/page.tsx`,
`app/(app)/encomendas/imprimir/impressao.module.css`,
`components/amassa/encomendas/botao-imprimir.tsx`, `tests/e2e/encomendas-impressao.spec.ts`,
`docs/operacao/04-migracao-encomendas.md`.

**Arquivos modificados:** `app/globals.css` (bloco `@media print` acrescentado ao fim, nenhum token
alterado), `app/(app)/encomendas/page.tsx` (botão de imprimir no cabeçalho), `lib/encomendas/consultas.ts`
(`listarEncomendasAtivas`), `lib/encomendas/textos.ts`, `tests/e2e/encomendas.spec.ts`,
`tests/e2e/acessibilidade.spec.ts`, `docs/convencoes-de-interface.md`.

**Símbolos exportados novos:** `BotaoImprimir`, `listarEncomendasAtivas`,
`TITULO_FOLHA_IMPRESSAO`, `NOTA_NADA_PARA_IMPRIMIR`, `ROTULO_IMPRIMIR`.

**Rotas novas:** `/encomendas/imprimir`.
**Regras CSS globais novas:** `@media print` com `@page { size: A4; margin: 15mm; }` e as regras que
escondem a casca na impressão.
**Pacotes novos:** nenhum — e nenhuma biblioteca de PDF, por decisão de custo (D-18).
