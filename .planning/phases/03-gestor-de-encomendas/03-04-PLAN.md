---
phase: 03-gestor-de-encomendas
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - app/(app)/encomendas/page.tsx
  - app/(app)/encomendas/loading.tsx
  - app/(app)/encomendas/error.tsx
  - components/amassa/estado-vazio.tsx
  - components/amassa/encomendas/lista-encomendas.tsx
  - components/amassa/encomendas/gantt.tsx
  - components/amassa/encomendas/cartao-encomenda.tsx
  - components/amassa/encomendas/trilha-segmentos.tsx
  - tests/e2e/encomendas-indice.spec.ts
autonomous: true
requirements: [ENC-03, ENC-06, ENC-07, ENC-08, ENC-09, ENC-13]
user_setup: []

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ENC-06 — no desktop, uma barra de etapa de N dias mede exatamente N×18 píxeis, lido por `boundingBox()` do Playwright, e a linha de 'Hoje' fica na posição que `deslocamentoEmPixels` prevê para a data de hoje; a prova é medida, não visual"
    - "ENC-06 — o cabeçalho da timeline traz células de quinzena (1–15 e 16–fim do mês), a coluna de nome+cliente fica fixa à esquerda e não rola horizontalmente com as barras"
    - "ENC-06/empty — sem nenhuma encomenda no banco, o `EstadoVazio` substitui o Gantt inteiro; nunca um Gantt com eixo e cabeçalho de quinzena desenhados e nenhuma barra dentro"
    - "ENC-07 — ao abrir `/encomendas` no desktop, o `scrollLeft` do contêiner rolável é igual ao valor que `rolagemInicial` calcula para a largura visível, deixando 'Hoje' aproximadamente centralizado — e nunca negativo"
    - "ENC-07/idempotency — recarregar a página reposiciona a rolagem no mesmo lugar; a rolagem inicial é aplicada uma vez por montagem e não volta a mexer no `scrollLeft` depois disso"
    - "ENC-07/concurrency — se a pessoa rolar a timeline antes de a rolagem inicial ser aplicada, a rolagem inicial não sobrescreve o gesto: ela roda uma única vez, no layout da montagem, e nunca em resposta a dado que chegue depois"
    - "ENC-08 — no viewport de celular (projeto `celular` do Playwright), `document.documentElement.scrollWidth` não passa de `clientWidth` em `/encomendas` com várias encomendas carregadas: nenhuma rolagem horizontal, que é a regra dura de `04-DESIGN-SYSTEM.md` §6"
    - "ENC-08/adjacency — na trilha de 6 segmentos do cartão, dois segmentos vizinhos se tocam sem se fundir: cada segmento desenhado tem largura mínima de 4px e as bordas ficam distinguíveis mesmo quando uma etapa de 1 dia fica ao lado de uma de 20"
    - "ENC-08/empty — encomenda com as SEIS etapas em `dias: 0` mostra no cartão o texto 'Nenhuma etapa ligada' no lugar da trilha, nunca uma barra de largura zero nem um espaço em branco sem explicação"
    - "ENC-08/ordering — a lista do celular e o Gantt do desktop usam a MESMA função `ordenarParaGantt`, então as duas metades do índice nunca mostram ordens diferentes para o mesmo conjunto de encomendas"
    - "ENC-13 — com o banco vazio, a tela mostra literalmente 'A roda ainda não gira.' com o botão 'Nova encomenda' ATIVO, levando a `/encomendas?nova`"
    - "ENC-13/empty — o `EstadoVazio` aparece exatamente UMA vez no documento, não uma por metade: o Gantt e a lista estão os dois no HTML (D-02), e um estado vazio duplicado seria lido duas vezes por leitor de tela"
    - "ENC-13/adjacency — 'A roda ainda não gira.' significa que não existe encomenda nenhuma; 'Nada por aqui com esse filtro.' significa que existem e nenhuma bate. Os dois nunca aparecem juntos, e o que decide é a contagem TOTAL carregada, não a filtrada"
    - "ENC-13/encoding — o teste compara a frase importando a constante de `lib/encomendas/textos.ts`, nunca redigitando o texto com acento no arquivo de teste (mesma convenção que a 02b adotou com `ITENS_NAVEGACAO`)"
    - "ENC-13/ordering — o estado vazio SUBSTITUI o conteúdo, nunca aparece ao lado dele: com uma encomenda no banco, a frase 'A roda ainda não gira.' não está no documento"
    - "ENC-03 — no Gantt, `queima1`, `queima2` e `entrega` são desenhados como losango (quadrado de 12px rotacionado 45°), nunca como retângulo e nunca com o ícone `Diamond` do `lucide-react`; `producao`, `secagem` e `esmaltacao` são retângulos"
    - "Etapa com `dias: 0` não é desenhada no Gantt — nem losango, nem retângulo, nem espaço reservado (`04-DESIGN-SYSTEM.md` §8)"
    - "O Gantt desenha apenas encomendas `rascunho` e `em_producao`; `concluida` e `cancelada` nunca aparecem nele (D-06)"
    - "Encomenda `rascunho` no Gantt usa hachura diagonal (`repeating-linear-gradient` a 45°) com a cor cheia da etapa preservada, mais borda tracejada e o selo 'RASCUNHO' uma única vez na coluna fixa — nunca opacidade reduzida, que é a linguagem de 'desabilitado' do sistema (`03-UI-SPEC.md`)"
    - "Rótulo de etapa dentro da barra aparece só quando a barra passa de 46px; texto sobre `--color-secagem` usa `#3A331F`, nunca branco"
    - "E1 Gantt — carregando: `app/(app)/encomendas/loading.tsx` mostra esqueleto no FORMATO do Gantt (coluna fixa com 3–4 barras cinza + retângulos em posições variadas), nunca um 'carregando…' solto"
    - "E1 Gantt — erro: `app/(app)/encomendas/error.tsx` reusa `EstadoErro` com 'Algo não funcionou.' / 'Não deu para carregar as encomendas. Verifique a internet e tente de novo.' / botão 'Tentar de novo'"
    - "E1 Gantt — parcial: encomenda com etapas de duração 0 desenha só as etapas que existem, resultando numa barra mais curta, nunca um vão vazio no meio da linha"
    - "E1 Gantt — zero-um-muitos: com UMA encomenda só, a timeline ainda desenha a quinzena de folga em cada ponta"
    - statement: "E1 Gantt — transbordo: o nome na coluna fixa trunca com reticências em uma linha, com o nome completo no atributo `title`. Verificar à mão com um nome de 60+ caracteres antes de fechar a fase"
      verification: backstop
    - "E2 lista mobile — vazio: o mesmo `EstadoVazio` do desktop, com a mesma frase e o mesmo botão nas duas larguras"
    - "E2 lista mobile — carregando: esqueleto no formato dos cartões (barra de título + barra de subtítulo + trilha de 6 segmentos cinza)"
    - "E2 lista mobile — erro: o mesmo `EstadoErro` do desktop"
    - "E2 lista mobile — populado: cartões empilhados com rolagem vertical, sem paginação"
    - "E2 lista mobile — parcial: cartão de encomenda com etapas desligadas mostra menos segmentos, e os segmentos existentes somam a largura inteira sem lacuna"
    - "E2 lista mobile — transbordo: nome e cliente QUEBRAM em linha, sem cortar — o cartão tem altura livre, ao contrário da coluna fixa do Gantt"
    - "E2 lista mobile — zero-um-muitos: um cartão e muitos cartões usam o mesmo leiaute"
    - "ENC-09 no cartão: a etapa atual ganha borda de 2px na cor da própria etapa e o texto de dias restantes aparece abaixo da trilha, com o texto exato do caso devolvido por `situacaoEm` — inclusive os casos 'Atrasada' (com badge e `--color-atencao`) e 'Ainda não começou'"
  artifacts:
    - app/(app)/encomendas/loading.tsx
    - app/(app)/encomendas/error.tsx
    - components/amassa/encomendas/lista-encomendas.tsx
    - components/amassa/encomendas/gantt.tsx
    - components/amassa/encomendas/cartao-encomenda.tsx
    - components/amassa/encomendas/trilha-segmentos.tsx
    - tests/e2e/encomendas-indice.spec.ts
  key_links:
    - "`lib/encomendas/gantt.ts` (números) → `gantt.tsx` (SVG/CSS) → `boundingBox()` do Playwright: se o componente multiplicar por 18 por conta própria em vez de chamar `retanguloDaEtapa`, o teste de unidade continua verde e o desenho fica errado"
    - "`page.tsx` (Server Component, busca) → `lista-encomendas.tsx` (Client Component, casca) → `gantt.tsx` + `cartao-encomenda.tsx`: a casca cliente nasce aqui porque o plano 07 vai pôr filtro/busca/ordenação dentro dela (D-11); os planos 06 e 07 modificam ESTE arquivo"
    - "`components/amassa/estado-vazio.tsx` ganha a possibilidade de botão ativo — é a primeira vez que o botão do estado vazio faz alguma coisa; as telas de Agenda, Queimas, Estoque e Orçamentos continuam usando o botão inerte e não podem regredir"
    - "`app/globals.css` (tokens de etapa, desde a 2b) → classes utilitárias no Gantt e na trilha: os tokens são CONSUMIDOS aqui, nunca redefinidos"
---

<objective>
O índice de verdade: Gantt no desktop, lista vertical de cartões no celular, os dois no mesmo HTML
alternando só por CSS (D-02), e os três estados obrigatórios no formato do conteúdo que substituem.

O 18px/dia, a posição da linha de "Hoje" e a rolagem de abertura são provados por medição no
navegador — `boundingBox()` e `scrollLeft`, não "o Gantt aparece". É a mesma resposta que a 2b deu
ao `@theme inline`, aplicada à classe de defeito que esta fase mais teme.

Purpose: transformar os números do plano 02 em píxeis conferíveis, e entregar as duas leituras que
o ateliê usa de verdade — a do computador e a do celular em pé, com a mão suja.
Output: `/encomendas` desenhando o cronograma real nos dois tamanhos, com vazio, carregando e erro
no formato certo.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md
@.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md
@.planning/phases/03-gestor-de-encomendas/03-PATTERNS.md
@.planning/phases/03-gestor-de-encomendas/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Gantt no desktop — 18px/dia medido no navegador, quinzenas, coluna fixa e a linha de "Hoje"</name>
  <precondition>O Docker está rodando (`docker info` responde) — `scripts/testar-e2e.mjs` sobe o Postgres de teste e roda as migrações 0005 e 0006 antes do Playwright.</precondition>
  <files>components/amassa/encomendas/gantt.tsx, components/amassa/encomendas/lista-encomendas.tsx, app/(app)/encomendas/page.tsx, tests/e2e/encomendas-indice.spec.ts</files>
  <read_first>
    - `lib/encomendas/gantt.ts` — `PIXELS_POR_DIA`, `calcularIntervalo`, `celulasDeQuinzena`,
      `deslocamentoEmPixels`, `retanguloDaEtapa`, `rolagemInicial`, `ordenarParaGantt` e as
      assinaturas exatas
    - `lib/encomendas/cronograma.ts` — `calcularCronograma`, `situacaoEm`, `ETAPAS_MARCO`
    - `lib/encomendas/formato.ts` — `formatarDiaCurto` e `hojeEmBrasilia`
    - `lib/encomendas/textos.ts` (criado no plano 02) — `FRASE_VAZIO_TITULO`, `FRASE_VAZIO_CORPO`,
      `FRASE_ERRO_TITULO`, `FRASE_ERRO_CORPO`, `ROTULO_NOVA_ENCOMENDA`, `ROTULO_ETAPA`,
      `SELO_RASCUNHO`, `SELO_ATRASADA` e `textoDaSituacao`: as frases e o rótulo de etapa saem
      DAQUI, nunca são redigitados num componente nem num teste
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — seções "Gantt Desktop — Contrato
      Vinculante" (a tabela inteira), "Rascunho no Gantt — Tratamento Atenuado" (a especificação
      literal da hachura, da borda tracejada e do selo) e "Design Tokens — cores de etapa"
      (o cuidado com texto sobre `--color-secagem`)
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §8, bloco **Encomendas** — os detalhes a preservar
      literalmente; §3 — um botão terracota por tela e "cor é informação, não decoração"
    - `.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md` D-02, D-06, D-10 — a alternância só
      por CSS (e a proibição explícita de usar `hooks/use-mobile.ts`), o escopo do Gantt e o
      rascunho atenuado
    - `app/(app)/encomendas/page.tsx` — a versão do plano 01, a estender
    - `app/globals.css` (linhas 28–60) — os tokens de etapa já existentes, a consumir
    - `tests/e2e/design-system.spec.ts` — o padrão de asserção por valor computado lido do navegador
    - `tests/e2e/casca.spec.ts` (linhas 1–40) — o helper `fazerLogin` e a regra de localizar pela
      metade visível, nunca ramificando por nome do projeto do Playwright
  </read_first>
  <behavior>
    - Com uma encomenda de `producao: 3`, a barra de produção mede 54px de largura (3×18) por
      `boundingBox()`, com tolerância de ±1px para arredondamento de layout
    - A linha de "Hoje" tem `left` igual a `deslocamentoEmPixels(intervalo, hoje)` dentro da área
      rolável
    - O `scrollLeft` do contêiner rolável, logo depois de carregar, é igual ao valor de
      `rolagemInicial` para a largura visível daquele viewport
    - Rolar horizontalmente a área de barras não move a coluna de nome+cliente
    - O cabeçalho mostra células rotuladas por quinzena, e a soma das larguras das células é igual à
      largura total da área rolável
    - Uma etapa com `dias: 0` não produz nenhum elemento no DOM daquela linha
    - `queima1`, `queima2` e `entrega` desenham um losango; `producao`, `secagem` e `esmaltacao`
      desenham um retângulo
    - Uma encomenda `rascunho` tem `background-image` contendo `repeating-linear-gradient` e o selo
      'RASCUNHO' aparece exatamente uma vez naquela linha
    - Encomendas `concluida` e `cancelada` não têm linha no Gantt
    - Uma barra de 2 dias (36px) não mostra rótulo de etapa; uma de 4 dias (72px) mostra
  </behavior>
  <action>
Escreva `tests/e2e/encomendas-indice.spec.ts` primeiro, no projeto `desktop`, criando os dados pela
Server Action real (nunca por `INSERT` direto no banco — o teste precisa exercitar o caminho que a
pessoa usa). Rode e veja falhar.

Todas as cadeias de interface vêm de `lib/encomendas/textos.ts`, criado no plano 02 — os testes as
importam em vez de redigitar acento, que é a convenção da 02b com `ITENS_NAVEGACAO`. Se faltar
alguma constante, acrescente-a lá e não escreva a frase solta no componente.

**`components/amassa/encomendas/gantt.tsx`** — Client Component (`"use client"`), porque precisa de
`useLayoutEffect` para a rolagem inicial. Recebe as encomendas JÁ ordenadas e os cronogramas JÁ
calculados; não chama o banco e não recalcula data. Estrutura: uma grade de duas colunas — a coluna
fixa (nome, cliente, selo "RASCUNHO" quando for o caso) com `position: sticky; left: 0` e a área
rolável (`overflow-x: auto`) com o cabeçalho de quinzenas em cima e uma linha por encomenda.

Toda posição e toda largura saem de `retanguloDaEtapa` e `deslocamentoEmPixels` — o componente
NUNCA multiplica por 18 por conta própria. Se ele multiplicar, o teste de unidade do plano 02
continua verde e o desenho fica errado; é exatamente o elo que precisa ficar visível.

Marco: um `<div>` de 12×12px com `transform: rotate(45deg)`, cor da etapa, centrado na posição do
dia. **Não** use o ícone `Diamond` do `lucide-react` (proporção de naipe de carta, visualmente
diferente do losango do protótipo) — `03-UI-SPEC.md` é explícito.

Rascunho: as barras e losangos daquela linha recebem
`repeating-linear-gradient(45deg, {cor} 0 4px, color-mix(in srgb, {cor} 100%, black 20%) 4px 8px)`,
mais borda de 1px tracejada na cor da etapa a 70%. O selo "RASCUNHO" (papel `micro`, caixa alta)
vai na coluna fixa, ao lado do nome — uma vez por encomenda, nunca por barra.

Rótulo dentro da barra: só quando `retanguloDaEtapa(...).mostrarRotulo` for `true`. Sobre
`--color-secagem`, o texto é `#3A331F`; sobre as outras cinco, branco.

Linha de "Hoje": `<div>` de 1px, `--color-erro`, altura total da área de barras, `left` igual a
`deslocamentoEmPixels(intervalo, hoje)`, com `aria-hidden="true"` e um rótulo textual "Hoje" no
cabeçalho para quem usa leitor de tela.

Rolagem de abertura: `useLayoutEffect` com array de dependências VAZIO, aplicando
`rolagemInicial(intervalo, hoje, containerRef.current.clientWidth)` uma única vez. Dependência
vazia é o que satisfaz as bordas ENC-07/idempotency e ENC-07/concurrency — um efeito que reroda a
cada mudança de dado arrancaria a timeline do lugar quando a pessoa já estivesse rolando.

**`components/amassa/encomendas/lista-encomendas.tsx`** — Client Component que é a casca do índice.
Recebe a lista completa por props do Server Component e renderiza
`<div className="hidden md:block"><Gantt …/></div>` e
`<div className="md:hidden">{cartões}</div>` (D-02: os dois no HTML, um escondido por CSS; **não**
use `hooks/use-mobile.ts`, que existe no projeto e seria o erro fácil). Nesta tarefa a metade do
celular pode ficar como um marcador simples — a Tarefa 2 a preenche. O filtro entra aqui no plano
07; deixe o arquivo preparado para receber estado local, sem inventar o filtro agora.

**`app/(app)/encomendas/page.tsx`** — mantenha `exigirUsuario()` na primeira instrução. Calcule
`hoje` com `hojeEmBrasilia(new Date())` no servidor e passe para baixo como string (o cliente nunca
decide qual é o dia de hoje — o fuso do aparelho de quem está no ateliê não é confiável). Calcule
os cronogramas no servidor com `calcularCronograma` e passe o resultado pronto; ordene com
`ordenarParaGantt`. Renderize `<ListaEncomendas>` quando houver encomendas, e o `EstadoVazio`
(Tarefa 3) quando não houver.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "índice de encomendas" --project=desktop</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- --grep "índice de encomendas" --project=desktop` sai com 0
    - `tests/e2e/encomendas-indice.spec.ts` contém uma asserção com o valor literal `54` (3 dias × 18px) e outra sobre `scrollLeft`
    - `grep -c 'use-mobile' 'components/amassa/encomendas/lista-encomendas.tsx'` retorna 0
    - `grep -c 'use-mobile' 'components/amassa/encomendas/gantt.tsx'` retorna 0
    - `grep -c 'Diamond' 'components/amassa/encomendas/gantt.tsx'` retorna 0
    - `grep -c 'repeating-linear-gradient' 'components/amassa/encomendas/gantt.tsx'` retorna 1 ou mais
    - `grep -cE '\* *18|18 *\*' 'components/amassa/encomendas/gantt.tsx'` retorna 0 — o componente não multiplica por 18 por conta própria
    - `components/amassa/encomendas/gantt.tsx` importa `retanguloDaEtapa` e `deslocamentoEmPixels` de `@/lib/encomendas/gantt`
    - `components/amassa/encomendas/lista-encomendas.tsx` contém `hidden md:block` e `md:hidden`
    - `app/(app)/encomendas/page.tsx` contém `hojeEmBrasilia`
    - `npm run lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e `npm run build` saem com 0
  </acceptance_criteria>
  <done>O Gantt desenha o cronograma real na escala do protótipo, e a escala, a posição da linha de
  "Hoje" e a rolagem de abertura são números lidos do navegador por asserção — não uma olhada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Lista vertical de cartões no celular, sem rolagem horizontal</name>
  <precondition>O Docker está rodando (`docker info` responde) — o projeto `celular` do Playwright precisa do mesmo banco de teste efêmero.</precondition>
  <files>components/amassa/encomendas/cartao-encomenda.tsx, components/amassa/encomendas/trilha-segmentos.tsx, components/amassa/encomendas/lista-encomendas.tsx, tests/e2e/encomendas-indice.spec.ts</files>
  <read_first>
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §6 — a linha "Encomendas" da tabela de adaptações
      (nome, cliente, trilha das 6 etapas como segmentos, etapa atual destacada, dias restantes) e a
      **regra dura** de que nenhuma tela pode exigir rolagem horizontal no celular
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — seções "Lista Vertical Mobile", o
      destaque da etapa atual (borda de 2px), e a tabela inteira "Etapa Atual e Dias Restantes
      (ENC-09) — Casos de Borda" com o texto exato de cada caso
    - `lib/encomendas/cronograma.ts` — `situacaoEm` e os oito ramos de `Situacao`
    - `lib/encomendas/formato.ts` — `formatarDiaCurto` e `formatarDiaCompleto`
    - `lib/encomendas/textos.ts` — `ROTULO_ETAPA`, `SELO_RASCUNHO`, `SELO_ATRASADA` e
      `textoDaSituacao` (criados no plano 02, a IMPORTAR — nunca a reescrever)
    - `components/amassa/cartao-painel.tsx` — a convenção de `Card`/`CardHeader`/`CardContent` e as
      classes tipográficas (`text-titulo`, `text-corpo`, `text-apoio`, `text-muted-foreground`)
    - `tests/e2e/casca.spec.ts` — o teste de ausência de rolagem horizontal a 320px já existente na
      2b, a repetir aqui com dado real
    - `app/globals.css` (linhas 28–60) — os tokens de etapa e `--color-atencao`
  </read_first>
  <behavior>
    - No projeto `celular`, `/encomendas` com 5 encomendas carregadas tem
      `document.documentElement.scrollWidth <= clientWidth`
    - Cada cartão mostra nome, cliente, a trilha de 6 segmentos e o texto de situação
    - O segmento da etapa atual tem borda de 2px na cor da própria etapa
    - Uma encomenda com todas as etapas em 0 mostra "Nenhuma etapa ligada" no lugar da trilha
    - Cada segmento desenhado tem largura de no mínimo 4px, mesmo quando uma etapa de 1 dia divide o
      cartão com uma de 20
    - A soma das larguras dos segmentos desenhados preenche a largura da trilha, sem lacuna
    - Um nome de 60+ caracteres quebra em linha dentro do cartão e não corta, e não empurra nada
      para fora da largura da tela
    - Uma encomenda atrasada mostra o badge "ATRASADA" e o texto do caso `atrasada`, com
      `--color-atencao`, nunca `--color-erro`
    - Uma encomenda que ainda não começou mostra "Começa em {N} dias" e nenhum segmento destacado
    - Uma encomenda `rascunho` mostra o selo "RASCUNHO" no cabeçalho do cartão e a mesma hachura nos
      segmentos
    - A ordem dos cartões é igual à ordem das linhas do Gantt para o mesmo conjunto
  </behavior>
  <action>
Acrescente os casos do `<behavior>` a `tests/e2e/encomendas-indice.spec.ts`, no projeto `celular`,
antes de escrever os componentes.

**`components/amassa/encomendas/trilha-segmentos.tsx`** — Server Component puro de apresentação.
Recebe as faixas do cronograma e a situação, e desenha os 6 segmentos horizontais **proporcionais à
duração**, não na escala de 18px/dia (que não cabe no celular — `04-DESIGN-SYSTEM.md` §6 é
explícito). Etapas com `dias: 0` não geram segmento. Largura de cada segmento:
`percentual = dias / duracaoTotal`, com `min-width: 4px` para que uma etapa de 1 dia ao lado de uma
de 20 continue visível — é a resposta à borda de adjacência. Quando `duracaoTotal` é 0, renderize o
texto "Nenhuma etapa ligada" (papel `apoio`, `--color-tinta-fraca`) no lugar da trilha.

O segmento da etapa atual (a que `situacaoEm` devolver) ganha `border: 2px solid` na cor da própria
etapa, mais saturada que o preenchimento. Cada segmento tem `title` e `aria-label` com o rótulo da
etapa e a duração, porque uma faixa de cor sozinha não é legível por leitor de tela.

**`components/amassa/encomendas/cartao-encomenda.tsx`** — usa `Card` do shadcn, com a convenção
tipográfica de `cartao-painel.tsx`. Conteúdo: nome (papel `título`, quebrando em linha com
`break-words`, NUNCA truncado — o cartão tem altura livre), cliente (papel `apoio`, "—" quando
vazio), selo "RASCUNHO" ou "ATRASADA" quando aplicável (papel `micro`), a `TrilhaSegmentos`, e o
texto de situação abaixo dela.

O texto de situação vem de `textoDaSituacao` (plano 02, `lib/encomendas/textos.ts`), nunca de um
encadeamento de ternários dentro do JSX: os oito casos precisam ser legíveis lado a lado, e o
compilador precisa reclamar quando um ramo novo aparecer. O cartão inteiro é um link para
`/encomendas/{id}` (a página nasce no plano 05), com alvo de toque de no mínimo 56px de altura.

**`lista-encomendas.tsx`** — preencha a metade `md:hidden` com os cartões, na mesma ordem que o
Gantt recebe (`ordenarParaGantt`, calculado no servidor e recebido por props — não reordene aqui).
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "índice de encomendas" --project=celular</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- --grep "índice de encomendas" --project=celular` sai com 0
    - `tests/e2e/encomendas-indice.spec.ts` contém uma asserção sobre `scrollWidth` e `clientWidth`
    - `components/amassa/encomendas/trilha-segmentos.tsx` contém `min-w-[4px]` ou `minWidth`
    - `grep -c 'truncate\|text-ellipsis' 'components/amassa/encomendas/cartao-encomenda.tsx'` retorna 0 — no cartão o nome quebra, nunca corta
    - `components/amassa/encomendas/cartao-encomenda.tsx` contém `break-words`
    - `grep -c 'color-erro\|text-destructive' 'components/amassa/encomendas/cartao-encomenda.tsx'` retorna 0 — atraso usa `--color-atencao`, nunca vermelho de erro
    - `components/amassa/encomendas/cartao-encomenda.tsx` importa `textoDaSituacao` de `@/lib/encomendas/textos` e não redefine nenhuma frase de situação
    - `npm run lint`, `npx tsc --noEmit` e `npm run build` saem com 0
  </acceptance_criteria>
  <done>Dá para ler o andamento de todas as encomendas em pé, num celular, sem rolar para o lado — e
  o teste mede isso em vez de confiar no relato de quem olhou.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Os três estados no formato do conteúdo, e o botão do estado vazio que finalmente faz algo</name>
  <files>app/(app)/encomendas/loading.tsx, app/(app)/encomendas/error.tsx, components/amassa/estado-vazio.tsx, app/(app)/encomendas/page.tsx, tests/e2e/encomendas-indice.spec.ts</files>
  <read_first>
    - `components/amassa/estado-vazio.tsx` — o componente inteiro, em especial a linha 30, onde o
      botão está `disabled` fixo; e o comentário que explica por que `rotuloBotao` ausente remove o
      botão inteiro
    - `components/amassa/estado-erro.tsx` — a assinatura com `acao?: ReactNode` e o `role="alert"`
    - `app/(app)/loading.tsx` — o padrão de esqueleto no formato do conteúdo, a copiar em forma
    - `app/(app)/error.tsx` — o boundary de erro do grupo protegido, incluindo o `"use client"`
      obrigatório e a regra de não renderizar nenhuma propriedade do objeto `error` na tela
    - `docs/convencoes-de-interface.md` §2 — os três componentes compartilhados e a regra de que
      nenhuma tela nova reimplementa a moldura
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — a tabela "Estados de Carregamento e
      Erro" (as linhas do índice desktop e do índice mobile) e o Copywriting Contract
    - `lib/encomendas/textos.ts` — as constantes de frase criadas no plano 02
    - `app/(app)/agenda/page.tsx` — uma das quatro telas que continuam usando o botão INERTE e não
      podem regredir com a mudança de assinatura
  </read_first>
  <action>
**`components/amassa/estado-vazio.tsx`** — acrescente uma propriedade opcional `hrefBotao?: string`.
Quando presente, o botão vira um `Link` com aparência de botão, habilitado; quando ausente, o
comportamento atual não muda em nada — botão `disabled` com `aria-disabled="true"`. É a mudança
mínima que atende o índice sem tocar nas quatro telas que ainda usam o botão inerte (Agenda,
Queimas, Estoque, Orçamentos). NÃO remova nem renomeie `notaBotao`: `/orcamentos` depende dele.

**`app/(app)/encomendas/loading.tsx`** — esqueleto no formato do índice, não o genérico da casca:
uma faixa de cabeçalho, e abaixo dela a estrutura de duas colunas do Gantt (coluna fixa com 3–4
`Skeleton` do tamanho de um nome, área de barras com retângulos cinza em posições e larguras
variadas). Na largura de celular, `md:hidden` mostra 3–4 `Skeleton` no formato do cartão (título +
subtítulo + faixa de 6 segmentos). Só componentes `Skeleton` e leiaute — nenhuma cadeia de texto
solta entre as tags, mesma regra de `app/(app)/loading.tsx`.

**`app/(app)/encomendas/error.tsx`** — `"use client"` (exigência do App Router), reusando
`EstadoErro` com `FRASE_ERRO_TITULO`, `FRASE_ERRO_CORPO` e um `Button` chamando `reset()`. Nenhuma
propriedade de `error` renderizada na tela; só `console.error`, como em `app/(app)/error.tsx`.

**`app/(app)/encomendas/page.tsx`** — o ramo vazio passa a usar `hrefBotao="/encomendas?nova"` e
some com `notaBotao`. Garanta que o `EstadoVazio` é renderizado UMA vez, fora das duas metades
`hidden md:block` / `md:hidden` — senão ele aparece duas vezes no documento e é lido duas vezes por
leitor de tela, que é a borda ENC-13/empty.

Acrescente ao e2e: com o banco vazio, a frase aparece uma vez só (`toHaveCount(1)`), o botão está
habilitado e leva a `/encomendas?nova`; com uma encomenda no banco, a frase NÃO está no documento.
As asserções importam as constantes de `lib/encomendas/textos.ts` — nada de redigitar acento.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "índice de encomendas"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- --grep "índice de encomendas"` sai com 0 nos dois projetos
    - `components/amassa/estado-vazio.tsx` exporta `EstadoVazioProps` com `hrefBotao?: string` e mantém `rotuloBotao` e `notaBotao`
    - `npm run test:e2e -- --grep "casca"` continua saindo com 0 — as quatro telas com botão inerte não regrediram
    - `app/(app)/encomendas/loading.tsx` importa `Skeleton` e não contém a cadeia `carregando`
    - `app/(app)/encomendas/error.tsx` começa com `"use client"` e não renderiza `error.message` nem `error.digest`
    - `tests/e2e/encomendas-indice.spec.ts` importa de `@/lib/encomendas/textos` e afirma `toHaveCount(1)` para a frase de estado vazio
    - `npm run lint`, `npx tsc --noEmit` e `npm run build` saem com 0
  </acceptance_criteria>
  <done>As três telas obrigatórias do índice existem no formato do conteúdo que substituem, e o
  botão "Nova encomenda" do estado vazio deixa de ser um enfeite — sem que nenhuma das outras
  quatro telas do sistema mude de comportamento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `/encomendas` | rota protegida que agora renderiza dado real de produto |
| dado do banco → JSX | nome, cliente e descrição são texto livre digitado por gestor e chegam à tela |
| servidor → cliente (props) | a lista inteira carregada viaja para o navegador, inclusive a metade do Gantt que o celular não mostra (consequência aceita de D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Elevation of Privilege | `app/(app)/encomendas/page.tsx` | high | mitigate | `exigirUsuario()` como primeira instrução; `middleware.ts` e `lib/auth/rotas-publicas.ts` não são tocados por este plano, e um critério de aceite confere que continuam intactos |
| T-03-24 | Tampering | nome/cliente/descrição renderizados no JSX | medium | mitigate | React escapa por padrão; nenhum `dangerouslySetInnerHTML` em nenhum componente da fase. O único ponto com HTML montado à mão é o `background-image` da hachura, que usa apenas tokens de cor fixos, nunca texto de usuário |
| T-03-25 | Information Disclosure | o Gantt inteiro viaja para o celular sem ser exibido (D-02) | low | accept | Todo gestor tem acesso a todas as encomendas; não existe campo restrito por papel. Consequência já registrada e aceita em D-02 |
| T-03-26 | Information Disclosure | `app/(app)/encomendas/error.tsx` | medium | mitigate | Copy fixa; nenhuma propriedade do objeto `error` (mensagem, pilha, `digest`) é renderizada — só `console.error`, mesmo padrão de `app/(app)/error.tsx` |
| T-03-27 | Denial of Service | lista muito grande renderizando no navegador | low | accept | O volume do ateliê é de dezenas; a janela de 12 meses do plano 07 põe um teto. Aceito com ponto de revisão explícito registrado em D-11 |
</threat_model>

<verification>
- `npm run test:e2e` inteiro sai com 0, nos dois projetos, incluindo as suítes das fases anteriores.
- `npm test`, `npm run lint`, `npx tsc --noEmit`, `npm run verificar-acoes` e `npm run build` saem
  com 0.
- `git diff --exit-code middleware.ts lib/auth/` retorna 0 — nenhuma rota foi aberta.
- As telas `/agenda`, `/queimas`, `/estoque` e `/orcamentos` continuam com o botão inerte e a nota,
  provado pela suíte `casca`.
</verification>

<success_criteria>
`/encomendas` mostra o cronograma real: Gantt de 18px/dia com quinzenas, coluna fixa e a linha de
"Hoje" no lugar certo no desktop; lista vertical de cartões legível sem rolagem lateral no celular;
e vazio, carregando e erro cada um no formato do conteúdo que substitui — tudo medido por teste, não
conferido a olho.
</success_criteria>

<output>
Create `.planning/phases/03-gestor-de-encomendas/03-04-SUMMARY.md` when done
</output>

---

## Artefatos que este plano produz

**Arquivos novos:** `app/(app)/encomendas/loading.tsx`, `app/(app)/encomendas/error.tsx`,
`components/amassa/encomendas/gantt.tsx`, `components/amassa/encomendas/lista-encomendas.tsx`,
`components/amassa/encomendas/cartao-encomenda.tsx`,
`components/amassa/encomendas/trilha-segmentos.tsx`, `tests/e2e/encomendas-indice.spec.ts`.

**Arquivos modificados:** `app/(app)/encomendas/page.tsx`, `components/amassa/estado-vazio.tsx`.

**Símbolos exportados novos:** `Gantt`, `ListaEncomendas`, `CartaoEncomenda`, `TrilhaSegmentos`.
As frases e `textoDaSituacao` vêm de `lib/encomendas/textos.ts`, criado no plano 02 — este plano é
consumidor, não autor.

**Assinatura alterada:** `EstadoVazioProps` ganha `hrefBotao?: string` (aditivo — `rotuloBotao` e
`notaBotao` continuam com o mesmo comportamento).

**Rotas novas:** nenhuma; `/encomendas` ganha `loading.tsx` e `error.tsx` próprios.
**Pacotes novos:** nenhum.
