---
phase: 03-gestor-de-encomendas
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/encomendas/cronograma.ts
  - lib/encomendas/gantt.ts
  - lib/encomendas/formato.ts
  - lib/encomendas/textos.ts
  - tests/unit/cronograma.test.ts
  - tests/unit/gantt.test.ts
  - tests/unit/formato-encomenda.test.ts
  - tests/unit/textos-encomenda.test.ts
autonomous: true
requirements: [ENC-01, ENC-02, ENC-03, ENC-04, ENC-06, ENC-07, ENC-09, ENC-11]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ENC-01/boundary — todas as 6 etapas em `dias: 0` produzem `duracaoTotalEmDias: 0`, `dataDeConclusao: null` e nenhuma faixa com `desenhada: true`; uma única etapa em `dias: 1` produz `duracaoTotalEmDias: 1` e `dataDeConclusao` igual ao próprio `dataInicio`"
    - "ENC-01/precision — não existe divisão nem arredondamento em `cronograma.ts`: `dias` é inteiro e a aritmética é soma de dias inteiros sobre `Date.UTC`, então não há meio-dia nem erro de ponto flutuante acumulando ao longo das 6 etapas"
    - "ENC-02/boundary — reduzir `secagem` de 6 para 0 desliga a etapa (`desenhada: false`, `ultimoDia: null`) e faz `queima1` começar no mesmo dia em que `producao` termina; aumentar de 0 para 1 devolve a etapa e empurra tudo um dia"
    - "ENC-02/precision — mudar `secagem` de 6 para 7 desloca cada uma das etapas seguintes em EXATAMENTE 1 dia (nem 0, nem 2), e `duracaoTotalEmDias` sobe exatamente 1"
    - "ENC-03/boundary — marco com `dias: 0` e marco com `dias: 1` são aceitos; `dias: 2` e `dias: -1` em `queima1`, `queima2` ou `entrega` fazem `calcularCronograma` lançar `RangeError` com mensagem em português nomeando a etapa (ENC-03)"
    - "ENC-03/precision — um marco nunca é coagido em silêncio para 0 ou 1: o valor fora de {0,1} é rejeitado, não corrigido, exatamente como `frescor.ts` trata cada caso com um ramo próprio em vez de um `default` que engole"
    - "ENC-04/boundary — desligar `entrega` (1 → 0) faz `dataDeConclusao` recuar exatamente um dia e a faixa de `entrega` sair do desenho (`desenhada: false`); religar (0 → 1) devolve a data anterior"
    - "ENC-04/precision — com `entrega` desligada, `dataDeConclusao` passa a ser o `ultimoDia` da última etapa com `dias > 0` (a `queima2`), nunca `null` — `null` só quando as SEIS estão em 0"
    - "ENC-06/boundary — `retanguloDaEtapa` devolve largura `18` para 1 dia e `null` para 0 dias; `mostrarRotulo` é `false` para uma barra de 46px (2 dias + 10) e `true` só ACIMA de 46px, porque `04-DESIGN-SYSTEM.md` §8 diz 'mais de 46px', não 'a partir de'"
    - "ENC-06/adjacency — duas etapas adjacentes nunca se sobrepõem nem deixam vão: a `esquerda` da etapa seguinte é exatamente `esquerda + largura` da anterior, consequência direta do fim exclusivo"
    - "ENC-06/ordering — duas encomendas com a MESMA `data_inicio` têm ordem especificada e estável: desempate por `nome` com `localeCompare('pt-BR')` e, se o nome também empatar, por `id`. Nunca a ordem que o banco devolveu por acaso"
    - "ENC-06/precision — toda posição e toda largura devolvidas por `lib/encomendas/gantt.ts` são múltiplos inteiros de 18; não existe fração de pixel em nenhum retorno do módulo"
    - "ENC-07/boundary — `rolagemInicial` devolve 0 quando 'hoje' cai antes do início do intervalo desenhado, e `larguraTotal - larguraVisivel` quando cai depois do fim; nunca um valor negativo nem maior que o máximo rolável"
    - "ENC-07/precision — `rolagemInicial` devolve inteiro (arredondado), nunca fração de pixel, para o `scrollLeft` não produzir meia-coluna de deslocamento"
    - "ENC-09/boundary — com `hoje` exatamente igual a `dataInicio`, a situação é a primeira etapa com `dias > 0`; um dia antes, é `nao-comecou` com `diasAteInicio: 1`; com `hoje` no dia seguinte ao `ultimoDia` da última etapa e status `em_producao`, é `atrasada` com `diasDeAtraso: 1`"
    - "ENC-09/boundary — quando `hoje` cai exatamente na fronteira entre duas etapas (fim exclusivo), a situação aponta a etapa que COMEÇA naquele dia, nunca a que termina (`03-UI-SPEC.md` §'Etapa de hoje — destaque')"
    - "ENC-09/precision — 'dias restantes' e 'dias de atraso' são contagem inteira de dias civis, obtida por diferença de `Date.UTC` dividida por 86400000, nunca por diferença de instantes com hora — o que garante o mesmo resultado às 23h de Brasília e às 2h UTC do dia seguinte"
    - "ENC-09/idempotency — `situacaoEm` chamada duas vezes com os mesmos argumentos devolve resultado estruturalmente igual: nenhuma leitura de relógio, nenhum estado, nenhuma mutação dos argumentos"
    - "ENC-11/boundary — com as 6 etapas em 0, o rodapé recebe `duracaoTotalEmDias: 0` e `dataDeConclusao: null`; com uma etapa em 1, recebe 1 e a própria `dataInicio`"
    - "ENC-11/precision — `duracaoTotalEmDias` é a soma exata dos seis inteiros `dias`, sem arredondamento e sem recalcular a partir das datas (recalcular a partir das datas introduziria a chance de as duas contas divergirem)"
    - "`lib/encomendas/cronograma.ts`, `lib/encomendas/gantt.ts` e `lib/encomendas/formato.ts` não têm NENHUMA declaração `import` e nenhum deles lê o relógio por dentro"
    - "Os seis valores de `ORDEM_DAS_ETAPAS` e os quatro de `STATUS_DE_ENCOMENDA` batem exatamente, na mesma ordem, com `etapaEncomenda.enumValues` e `statusEncomenda.enumValues` de `db/schema.ts` — provado por teste que importa os dois lados, já que o módulo puro não pode importar o schema"
    - "Virada de mês e ano bissexto: uma encomenda começando em 2028-02-27 com `producao: 3` termina em 2028-02-29 (2028 é bissexto) e a etapa seguinte começa em 2028-03-01"
    - "`textoDaSituacao` trata os OITO ramos de `Situacao` com `switch` exaustivo e devolve a frase exata da tabela do `03-UI-SPEC.md`; o compilador reclama se um ramo novo aparecer sem tratamento"
    - "Toda frase fixa da interface de Encomendas vive em `lib/encomendas/textos.ts` e é IMPORTADA pelos testes, nunca redigitada com acento no arquivo de teste — a convenção que a 02b estabeleceu com `ITENS_NAVEGACAO`"
  artifacts:
    - lib/encomendas/cronograma.ts
    - lib/encomendas/gantt.ts
    - lib/encomendas/formato.ts
    - lib/encomendas/textos.ts
    - tests/unit/cronograma.test.ts
    - tests/unit/gantt.test.ts
    - tests/unit/formato-encomenda.test.ts
    - tests/unit/textos-encomenda.test.ts
  key_links:
    - "`calcularCronograma` → `situacaoEm` → a tabela 'Etapa Atual e Dias Restantes' de `03-UI-SPEC.md`: os planos 04, 05 e 08 renderizam o texto A PARTIR do tipo devolvido; se um caso da tabela não tiver ramo no tipo, a interface improvisa e o erro não aparece em lugar nenhum"
    - "`calcularCronograma` → `calcularIntervalo` → `retanguloDaEtapa`/`deslocamentoEmPixels`/`rolagemInicial`: a escala de 18px/dia entra uma vez e sai em todos os lugares; nenhum componente multiplica por 18 por conta própria"
    - "`etapaEncomenda.enumValues` (db/schema.ts) ↔ `ORDEM_DAS_ETAPAS` (cronograma.ts): duas listas que precisam continuar iguais e que nada, além do teste desta plano, obriga a continuar iguais"
    - "`lib/encomendas/textos.ts` nasce na onda 2 porque DOIS planos da onda 3 o consomem em paralelo (o índice do plano 04 e o detalhe do plano 05) — se ele nascesse em um dos dois, o outro dependeria de um irmão da mesma onda"
  prohibitions:
    - statement: "A cascata de datas, a escala de 18px/dia e o cálculo de etapa atual NUNCA são reimplementados fora de `lib/encomendas/cronograma.ts` e `lib/encomendas/gantt.ts` — nem 'só para a pré-visualização do formulário', nem 'só para o cartão do celular'. Duas versões da mesma verdade divergem em silêncio, e quando divergem ninguém sabe qual está certa (D-15)"
---

<objective>
A regra de negócio mais delicada do sistema, escrita com os testes antes do código, e a geometria
do Gantt tirada do componente e posta num módulo que pode ser medido em píxel por teste de unidade.

O modo de falha desta fase é um cálculo de data que erra em silêncio: não quebra o build, não
loga nada, a barra fica um dia fora do lugar e ninguém repara até uma queima ser marcada no dia
errado. A resposta é a mesma que a 2b deu ao `@theme inline`: teste que mede, não conferência
visual.

Purpose: fechar as 19 bordas de valor desta fase num lugar só, onde cada uma cabe num teste de
milissegundos, antes que quatro superfícies diferentes passem a depender delas.
Output: `cronograma.ts` completo (cascata, fim exclusivo, marcos, virada de mês, bissexto, os sete
casos de situação do ENC-09), `gantt.ts` (18px/dia, quinzenas, "Hoje", rolagem de abertura) e
`formato.ts` (datas em português de Brasília), cada um com sua bateria de testes nomeados.
</objective>

<execution_context>
@C:/Users/Andre/amassa/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Andre/amassa/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md
@.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md
@.planning/phases/03-gestor-de-encomendas/03-PATTERNS.md
@.planning/phases/03-gestor-de-encomendas/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: `cronograma.ts` completo — a bateria inteira de fronteiras, testes antes do código</name>
  <files>lib/encomendas/cronograma.ts, tests/unit/cronograma.test.ts</files>
  <read_first>
    - `lib/encomendas/cronograma.ts` — o núcleo que o plano 01 deixou (cascata, fim exclusivo,
      duração total, data de conclusão) e a assinatura já publicada
    - `tests/unit/cronograma.test.ts` — os casos que já existem, a estender, nunca a substituir
    - `amassa-plataforma/00-BRIEFING.md` §5 (linhas 110–176) — as 6 etapas, marcos valendo 0 ou 1,
      etapas de 0 dias não desenhadas, e a frase que fixa o fim exclusivo
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` §1 "Regras" (linhas 197–210) — os padrões das 6
      etapas e a regra de que as datas não são armazenadas
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — a tabela inteira
      "Etapa Atual e Dias Restantes (ENC-09) — Casos de Borda": os 8 casos que o tipo de retorno
      precisa cobrir, um a um
    - `lib/backup/frescor.ts` — a forma a repetir: união discriminada, um `if` por caso com retorno
      próprio, `motivo` legível, instante atual sempre como argumento
    - `tests/unit/frescor.test.ts` — a forma do teste: constante fixa de "agora", um `it` por
      fronteira, o valor da fronteira no nome do teste
    - `db/schema.ts` — os `enumValues` de `etapaEncomenda` e `statusEncomenda`, que o teste vai
      comparar com as constantes do módulo puro
  </read_first>
  <behavior>
    - Cascata: com `DIAS_PADRAO` e `dataInicio: "2026-08-12"`, `producao` vai de 2026-08-12 a
      2026-08-15 (exclusivo) e `secagem` começa em 2026-08-15
    - Fim exclusivo: `faixa.fimExclusivo` de uma etapa é igual ao `faixa.inicio` da seguinte, para
      todos os cinco pares
    - `ultimoDia` é o dia anterior a `fimExclusivo`, e é `null` quando `dias === 0`
    - Etapa de 0 dias: `desenhada: false`, `inicio === fimExclusivo`, e a etapa seguinte começa no
      mesmo dia — a etapa desligada não consome tempo nenhum
    - Marco com `dias: 2` lança `RangeError` nomeando a etapa; com `dias: -1`, idem; com 0 e com 1,
      não lança
    - Duração total é a soma dos seis `dias`; com os seis em 0, é 0 e `dataDeConclusao` é `null`
    - `dataDeConclusao` é o `ultimoDia` da última etapa com `dias > 0` — com `entrega` desligada, é
      o `ultimoDia` de `queima2`
    - Virada de mês: `2026-08-30` + `producao: 3` termina em 2026-09-01 (exclusivo), `ultimoDia`
      2026-08-31
    - Ano bissexto: `2028-02-27` + `producao: 3` tem `ultimoDia` 2028-02-29 e `fimExclusivo`
      2028-03-01; o mesmo cálculo em 2027 (não bissexto) tem `ultimoDia` 2027-03-01
    - Virada de ano: `2026-12-30` + `producao: 3` tem `fimExclusivo` 2027-01-02
    - `situacaoEm` com `hoje` antes de `dataInicio` devolve `{ tipo: "nao-comecou", diasAteInicio }`
    - `situacaoEm` com `hoje` dentro de uma etapa de intervalo devolve
      `{ tipo: "em-etapa-intervalo", etapa, proximaEtapa, diasAteProxima }` — `proximaEtapa` pula as
      etapas desligadas
    - `situacaoEm` com `hoje` no dia de um marco devolve `{ tipo: "em-etapa-marco", etapa }`
    - `situacaoEm` com `hoje` na última etapa com `dias > 0` devolve
      `{ tipo: "ultima-etapa", etapa, diasAteEntrega }`
    - `situacaoEm` na fronteira exata entre duas etapas aponta a que COMEÇA naquele dia
    - `situacaoEm` com `hoje` depois de `dataDeConclusao` e status `em_producao` devolve
      `{ tipo: "atrasada", dataPrevista, diasDeAtraso }` — um dia depois é `diasDeAtraso: 1`
    - `situacaoEm` com status `concluida` devolve `{ tipo: "concluida", dataDeConclusao }`, mesmo
      quando `hoje` já passou da data prevista
    - `situacaoEm` com status `cancelada` devolve `{ tipo: "cancelada" }`, sem etapa atual
    - `situacaoEm` com status `rascunho` segue a mesma lógica de data de `em_producao` — rascunho é
      só um aviso a mais na interface, não muda o cálculo
    - `situacaoEm` chamada duas vezes com os mesmos argumentos devolve resultado estruturalmente
      igual, e não muda os argumentos recebidos
    - `ORDEM_DAS_ETAPAS` casa exatamente com `etapaEncomenda.enumValues`, e `STATUS_DE_ENCOMENDA`
      com `statusEncomenda.enumValues`, na mesma ordem
  </behavior>
  <action>
Escreva cada caso do `<behavior>` como um `it` em `tests/unit/cronograma.test.ts` ANTES de tocar em
`lib/encomendas/cronograma.ts`, e rode `npm test` uma vez para vê-los falhar de verdade. Nome do
teste carrega o valor da fronteira, no estilo de `frescor.ts` ("2028-02-27 + 3 dias termina em
2028-02-29, porque 2028 é bissexto"), não uma descrição genérica.

Depois estenda o módulo. Acrescente ao que o plano 01 deixou:

`STATUS_DE_ENCOMENDA` (readonly, os quatro literais na ordem do enum do banco) e
`type StatusDeEncomenda`. O módulo é sem imports, então esta lista é uma segunda cópia da verdade
que vive em `db/schema.ts` — o teste desta tarefa é o único elo que obriga as duas a continuarem
iguais, e é por isso que ele existe.

`type Situacao`, união discriminada por `tipo` com exatamente estes oito ramos, um por linha da
tabela de `03-UI-SPEC.md`: `"nao-comecou"` (`diasAteInicio`, `dataInicio`),
`"em-etapa-intervalo"` (`etapa`, `proximaEtapa`, `diasAteProxima`), `"em-etapa-marco"` (`etapa`),
`"ultima-etapa"` (`etapa`, `diasAteEntrega`), `"atrasada"` (`dataPrevista`, `diasDeAtraso`),
`"concluida"` (`dataDeConclusao`), `"cancelada"`, e `"sem-etapas"` (todas as seis em 0 — não há
etapa atual possível, e é o único caso que a tabela do UI-SPEC não nomeia; devolvê-lo
explicitamente é melhor do que devolver `nao-comecou` para uma encomenda que nunca vai começar).

`function situacaoEm(cronograma: Cronograma, status: StatusDeEncomenda, hoje: string): Situacao`.
Ordem dos ramos, e ela importa: `cancelada` primeiro, `concluida` segundo, `sem-etapas` terceiro,
`nao-comecou` quarto, `atrasada` quinto, e só então a busca da etapa que contém `hoje`. Um `if` por
caso com retorno próprio; nenhum `default` que engula um caso novo em silêncio.

Validação de marco: em `calcularCronograma`, antes de qualquer cálculo, lance
`new RangeError(...)` com mensagem em português nomeando a etapa quando uma etapa de
`ETAPAS_MARCO` vier com `dias` fora de `{0, 1}`, ou quando qualquer etapa vier com `dias` negativo
ou não inteiro. A restrição `marcos_zero_ou_um` do banco e o `refine` do Zod já barram o caminho
normal; esta terceira barreira existe porque um módulo puro que coage em silêncio esconde
exatamente o defeito que esta fase mais teme.

Aritmética: continue sobre strings `YYYY-MM-DD` e `Date.UTC`. Diferença de dias é
`(a - b) / 86400000`, sempre com os dois lados construídos por `Date.UTC(ano, mes-1, dia)` — nunca
`new Date(string)`, nunca `getDate()` de um `Date` local.
  </action>
  <verify>
    <automated>npm test -- cronograma</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` sai com 0 e o relatório de `tests/unit/cronograma.test.ts` lista pelo menos 20 casos
    - `grep -c '^import' lib/encomendas/cronograma.ts` retorna 0
    - `grep -c 'new Date(' lib/encomendas/cronograma.ts` retorna 0
    - `lib/encomendas/cronograma.ts` exporta `STATUS_DE_ENCOMENDA`, `StatusDeEncomenda`, `Situacao` e `situacaoEm`
    - `grep -c 'RangeError' lib/encomendas/cronograma.ts` retorna 1 ou mais
    - `tests/unit/cronograma.test.ts` contém as cadeias `2028-02-29`, `2027-01-02` e `2026-09-01`
    - `tests/unit/cronograma.test.ts` importa `etapaEncomenda` e `statusEncomenda` de `@/db/schema` e compara com `ORDEM_DAS_ETAPAS` e `STATUS_DE_ENCOMENDA`
    - O relatório do Vitest contém um caso para cada um dos oito valores de `Situacao["tipo"]`
    - `npm run lint` e `npx tsc --noEmit` saem com 0
  </acceptance_criteria>
  <done>Todas as fronteiras de data desta fase têm um teste que as nomeia e um ramo de código que as
  trata; um marco com valor impossível para de passar despercebido e passa a lançar.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: `gantt.ts` — a geometria fora do componente, medida em píxel por teste de unidade</name>
  <files>lib/encomendas/gantt.ts, tests/unit/gantt.test.ts</files>
  <read_first>
    - `lib/encomendas/cronograma.ts` — os tipos `Cronograma` e `FaixaDeEtapa` que este módulo consome
    - `amassa-plataforma/00-BRIEFING.md` §5 (linhas 133–141) — 18px/dia, cabeçalho em quinzenas
      (1–15 e 16–fim do mês), linha vermelha de "Hoje", a timeline abrindo rolada até o "Hoje", a
      extensão automática com uma quinzena de folga em cada ponta, etapas de 0 dias não desenhadas
    - `amassa-plataforma/04-DESIGN-SYSTEM.md` §8, bloco **Encomendas** — os cinco detalhes a
      preservar literalmente, incluindo "rótulo da etapa só aparece se a barra tiver mais de 46px"
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — seções "Gantt Desktop — Contrato
      Vinculante" e "Spacing Scale" (os valores 18px, 46px e por que não são arredondados para a
      escala de 4pt)
    - `.planning/phases/03-gestor-de-encomendas/03-CONTEXT.md` `<specifics>` — a instrução de testar
      a posição em píxeis da linha de "Hoje" e o 18px/dia, não só que "o Gantt aparece"
    - `tests/unit/frescor.test.ts` — a forma do teste de fronteira
  </read_first>
  <behavior>
    - `PIXELS_POR_DIA` é exatamente 18 e `LARGURA_MINIMA_PARA_ROTULO` é exatamente 46
    - `calcularIntervalo` com uma encomenda começando em 2026-08-12 e terminando em 2026-08-25
      devolve `primeiroDia` no começo da quinzena ANTERIOR à que contém 2026-08-12 (2026-07-16) e
      `ultimoDiaExclusivo` no dia seguinte ao fim da quinzena SEGUINTE à que contém 2026-08-25
      (2026-09-16)
    - `calcularIntervalo` com lista vazia devolve um intervalo centrado em `hoje`, com uma quinzena
      de folga de cada lado — nunca largura 0
    - `calcularIntervalo` com uma encomenda só ainda desenha a quinzena de folga nas duas pontas
    - `larguraEmPixels` do intervalo é `totalDeDias * 18`, inteiro
    - `celulasDeQuinzena` devolve células que não se sobrepõem, cobrem o intervalo inteiro sem vão, e
      cuja soma de larguras é igual a `larguraEmPixels`
    - A primeira e a última célula podem ser quinzenas parciais quando o intervalo não começa em dia
      1 ou 16 — o rótulo diz o mês, e a largura reflete só os dias que a célula ocupa
    - `deslocamentoEmPixels(intervalo, dia)` devolve `(dias desde primeiroDia) * 18`; para
      `primeiroDia` devolve 0
    - `retanguloDaEtapa` devolve `null` quando `faixa.dias === 0` — nem losango, nem retângulo, nem
      espaço reservado
    - `retanguloDaEtapa` de uma etapa de 1 dia devolve `largura: 18` e `mostrarRotulo: false`
    - `retanguloDaEtapa` de uma etapa que resulta em largura exatamente 46 devolve
      `mostrarRotulo: false`; a partir de 47 devolve `true`
    - Duas etapas adjacentes: `esquerda` da seguinte é exatamente `esquerda + largura` da anterior
    - `rolagemInicial(intervalo, hoje, larguraVisivel)` centraliza "hoje" quando cabe; devolve 0
      quando "hoje" está antes do intervalo ou perto demais do começo; devolve
      `larguraEmPixels - larguraVisivel` quando está perto demais do fim
    - `rolagemInicial` devolve sempre um inteiro `>= 0`
    - `ordenarParaGantt` ordena por `data_inicio` ascendente e desempata por `nome` com
      `localeCompare('pt-BR')` e, em último caso, por `id` — duas encomendas com a mesma data têm
      ordem determinística
  </behavior>
  <action>
Escreva `tests/unit/gantt.test.ts` inteiro antes de criar `lib/encomendas/gantt.ts`, e rode uma vez
para ver falhar.

`lib/encomendas/gantt.ts` é o segundo módulo puro da fase: nenhum import (nem de `cronograma.ts` —
os tipos que ele precisa entram como parâmetro estrutural, ou são redeclarados localmente como
tipos estruturais compatíveis), nenhuma leitura de relógio, `hoje` sempre argumento.

Exporte: `PIXELS_POR_DIA` (18), `LARGURA_MINIMA_PARA_ROTULO` (46),
`type IntervaloDaTimeline = { primeiroDia: string; ultimoDiaExclusivo: string; totalDeDias: number; larguraEmPixels: number }`,
`type CelulaDeQuinzena = { chave: string; rotulo: string; inicio: string; dias: number; esquerda: number; largura: number }`,
`type RetanguloDeEtapa = { esquerda: number; largura: number; mostrarRotulo: boolean }`,
`calcularIntervalo`, `celulasDeQuinzena`, `deslocamentoEmPixels`, `retanguloDaEtapa`,
`rolagemInicial` e `ordenarParaGantt`.

Definição precisa de "uma quinzena de folga em cada ponta", que é o ponto onde uma implementação
plausível erra em silêncio: a quinzena que contém um dia é `[1..15]` ou `[16..fim do mês]` daquele
mês. `primeiroDia` é o primeiro dia da quinzena IMEDIATAMENTE ANTERIOR à que contém a menor
`inicio` das encomendas desenhadas. `ultimoDiaExclusivo` é o dia seguinte ao último dia da quinzena
IMEDIATAMENTE POSTERIOR à que contém o maior `fimExclusivo`. Com a lista vazia, use `hoje` no lugar
das duas pontas.

Rótulo da célula de quinzena: o mês abreviado em português com o intervalo de dias, no formato
`"1–15 ago"` / `"16–31 ago"`. A formatação do mês vem de `lib/encomendas/formato.ts` (Tarefa 3) —
mas `gantt.ts` não pode importar; então devolva `inicio` e `dias` na célula e deixe o `rotulo` ser
montado por quem renderiza, OU aceite um parâmetro `formatarMes: (dia: string) => string` na
assinatura de `celulasDeQuinzena`. Escolha a segunda (injeção por parâmetro): mantém o módulo puro
e sem import, e deixa a célula pronta para desenhar. Registre a escolha no SUMMARY.

`mostrarRotulo` usa `largura > LARGURA_MINIMA_PARA_ROTULO` — estritamente maior, porque a fonte diz
"mais de 46px". Um `>=` aqui passa despercebido para sempre.

`rolagemInicial`: `Math.round(deslocamentoEmPixels(intervalo, hoje) - larguraVisivel / 2)`, limitado
a `[0, max(0, larguraEmPixels - larguraVisivel)]`.
  </action>
  <verify>
    <automated>npm test -- gantt</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` sai com 0 e o relatório de `tests/unit/gantt.test.ts` lista pelo menos 14 casos
    - `grep -c '^import' lib/encomendas/gantt.ts` retorna 0
    - `lib/encomendas/gantt.ts` contém `PIXELS_POR_DIA = 18` e `LARGURA_MINIMA_PARA_ROTULO = 46`
    - `grep -c 'largura >= LARGURA_MINIMA_PARA_ROTULO' lib/encomendas/gantt.ts` retorna 0
    - `tests/unit/gantt.test.ts` contém uma asserção com o valor literal `46` e outra com `47`
    - `tests/unit/gantt.test.ts` contém as cadeias `2026-07-16` e `2026-09-16`
    - `npm run lint` e `npx tsc --noEmit` saem com 0
  </acceptance_criteria>
  <done>O 18px/dia, a quinzena de folga, o limiar de 46px do rótulo e a posição de "Hoje" são
  números que um teste de unidade lê, não uma impressão visual de quem olhou a tela.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: `formato.ts` e `textos.ts` — datas de Brasília e as frases fixas da interface</name>
  <files>lib/encomendas/formato.ts, lib/encomendas/textos.ts, tests/unit/formato-encomenda.test.ts, tests/unit/textos-encomenda.test.ts</files>
  <read_first>
    - `.planning/phases/03-gestor-de-encomendas/03-UI-SPEC.md` — os formatos literais que a interface
      usa: `"12 a 18 ago"` (intervalo na trilha), `"19 ago"` (marco), `"12 ago – 3 set"` (período no
      histórico), e o papel `mono` com `tabular-nums` para todas as datas; mais o Copywriting
      Contract inteiro (as frases de estado vazio, erro, "Nada encontrado" e os toasts) e a tabela
      "Etapa Atual e Dias Restantes (ENC-09) — Casos de Borda" com o texto exato de cada caso
    - `lib/encomendas/cronograma.ts` — os oito ramos de `Situacao`, que `textoDaSituacao` precisa
      tratar exaustivamente
    - `lib/navegacao/itens.ts` e `tests/e2e/casca.spec.ts` — a convenção da 02b de exportar as
      cadeias de interface de um módulo e importá-las nos testes, em vez de redigitar acento
    - `amassa-plataforma/02-MODELO-DE-DADOS.md` §0 (linhas 17–38) — por que o fuso é
      `America/Sao_Paulo` e por que `current_date` do Postgres não serve
    - `scripts/testar-migracoes.mjs` — o precedente do projeto de calcular a data de Brasília com
      `Intl.DateTimeFormat` nativo do Node em vez de instalar uma dependência de fuso
    - `lib/encomendas/cronograma.ts` — o formato `YYYY-MM-DD` que entra em todas as funções daqui
  </read_first>
  <behavior>
    - `hojeEmBrasilia(new Date("2026-08-09T02:30:00.000Z"))` devolve `"2026-08-08"` — 23h30 do dia 8
      em Brasília, e é exatamente o caso que `current_date` do Postgres erraria
    - `hojeEmBrasilia(new Date("2026-08-09T03:30:00.000Z"))` devolve `"2026-08-09"`
    - `formatarDiaCurto("2026-08-12")` devolve `"12 ago"`
    - `formatarDiaCurto("2026-09-03")` devolve `"3 set"` — sem zero à esquerda no dia
    - `formatarIntervalo("2026-08-12", "2026-08-18")` devolve `"12 a 18 ago"`
    - `formatarIntervalo("2026-08-28", "2026-09-03")` devolve `"28 ago a 3 set"` — quando o mês muda,
      os dois meses aparecem
    - `formatarDiaCompleto("2026-08-12")` devolve `"12 de agosto de 2026"`
    - `formatarPeriodo("2026-08-12", "2026-09-03")` devolve `"12 ago – 3 set"` (travessão, conforme o
      UI-SPEC), e `"12 ago – —"` nunca acontece: com fim `null` a função devolve só `"12 ago"`
    - Nenhuma função lê o relógio: `hojeEmBrasilia` recebe o instante como argumento
    - `textoDaSituacao` devolve uma frase não vazia para cada um dos OITO ramos de `Situacao`
    - `textoDaSituacao({ tipo: "atrasada", … })` contém a palavra "Atrasada" e a data prevista
    - `textoDaSituacao({ tipo: "nao-comecou", … })` contém "Começa em" e o número de dias
    - `textoDaSituacao(situacao, { semCor: true })` devolve a variante textual usada na folha
      impressa: o caso `atrasada` vira o sufixo "(atrasada)" em vez de depender de `--color-atencao`
    - `FRASE_VAZIO_TITULO` é exatamente `"A roda ainda não gira."` e `FRASE_FILTRO_VAZIO_TITULO` é
      exatamente `"Nada por aqui com esse filtro."` — duas cadeias distintas, nunca a mesma
    - `ROTULO_ETAPA` tem uma entrada para cada valor de `Etapa`, e nenhuma entrada a mais
  </behavior>
  <action>
Escreva `tests/unit/formato-encomenda.test.ts` primeiro, rode, veja falhar.

`lib/encomendas/formato.ts` é o terceiro módulo puro: sem `import`, usando só `Intl.DateTimeFormat`
global. Exporte `hojeEmBrasilia(agora: Date): string`, `formatarDiaCurto(dia: string): string`,
`formatarIntervalo(inicio: string, ultimoDia: string): string`,
`formatarDiaCompleto(dia: string): string` e
`formatarPeriodo(inicio: string, fim: string | null): string`.

`hojeEmBrasilia` usa `Intl.DateTimeFormat("en-CA", { timeZone: "America/Sao_Paulo", year: "numeric",
month: "2-digit", day: "2-digit" })` — a localidade `en-CA` produz `YYYY-MM-DD` direto, que é o
formato interno do projeto; é o mesmo caminho que `scripts/testar-migracoes.mjs` já usa.

As demais formatam a partir da string `YYYY-MM-DD` construindo um `Date.UTC` do dia civil e
formatando com `timeZone: "UTC"` — nunca com o fuso do runtime, senão o dia salta quando o servidor
está em UTC e o formatador em Brasília. O mês abreviado do `pt-BR` sai com ponto em alguns runtimes
(`"ago."`): remova o ponto final explicitamente, porque `03-UI-SPEC.md` escreve `"12 a 18 ago"` sem
ponto e a diferença apareceria como ruído em toda data da tela.

Este módulo é também o que `celulasDeQuinzena` (Tarefa 2) recebe por parâmetro para montar o rótulo
de quinzena — a fronteira que mantém `gantt.ts` sem import.

**`lib/encomendas/textos.ts`** — as frases fixas da interface de Encomendas e a função que traduz
`Situacao` em frase. Ele nasce AQUI, na onda 2, e não no plano do índice, porque dois planos da onda
3 o consomem em paralelo (o índice e o detalhe); se nascesse num deles, o outro dependeria de um
irmão da mesma onda.

Exporte as constantes que os testes vão importar em vez de redigitar acento — a convenção que a 02b
estabeleceu com `ITENS_NAVEGACAO`: `FRASE_VAZIO_TITULO` ("A roda ainda não gira."),
`FRASE_VAZIO_CORPO`, `FRASE_FILTRO_VAZIO_TITULO` ("Nada por aqui com esse filtro."),
`FRASE_FILTRO_VAZIO_CORPO`, `FRASE_ERRO_TITULO` ("Algo não funcionou."), `FRASE_ERRO_CORPO`,
`FRASE_FALHA_AO_SALVAR`, `ROTULO_NOVA_ENCOMENDA` ("Nova encomenda"), `SELO_RASCUNHO` ("RASCUNHO"),
`SELO_ATRASADA` ("ATRASADA") e `ROTULO_ETAPA` (mapa `Etapa` → rótulo: "Produção", "Secagem",
"Queima (biscoito)", "Esmaltação", "Queima (esmalte)", "Entrega"). Os planos 07 e 08 acrescentam
constantes próprias a este mesmo arquivo, em ondas posteriores.

E exporte `textoDaSituacao(situacao: Situacao, opcoes?: { semCor?: boolean }): string`, com `switch`
exaustivo sobre os oito ramos e as frases literais da tabela "Etapa Atual e Dias Restantes" do
`03-UI-SPEC.md`. A opção `semCor` existe para a folha impressa do plano 08, onde o caso `atrasada`
vira o sufixo textual "(atrasada)" em vez de depender de `--color-atencao` — mesma função, duas
apresentações, nunca duas tabelas de frases. Este arquivo pode importar o TIPO `Situacao` de
`cronograma.ts` (import de tipo não quebra a pureza de `cronograma.ts`, que continua sem import
nenhum), mas não importa React nem o cliente do banco.
  </action>
  <verify>
    <automated>npm test -- formato-encomenda textos-encomenda</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` sai com 0 e o relatório de `tests/unit/formato-encomenda.test.ts` lista pelo menos 9 casos
    - `grep -c '^import' lib/encomendas/formato.ts` retorna 0
    - `lib/encomendas/formato.ts` contém `America/Sao_Paulo`
    - `tests/unit/formato-encomenda.test.ts` contém a cadeia `2026-08-09T02:30:00.000Z` e afirma o resultado `2026-08-08`
    - `grep -c 'date-fns' package.json` retorna 0
    - `lib/encomendas/textos.ts` exporta `textoDaSituacao`, `FRASE_VAZIO_TITULO`, `FRASE_FILTRO_VAZIO_TITULO` e `ROTULO_ETAPA`
    - `tests/unit/textos-encomenda.test.ts` tem um caso por ramo de `Situacao` (oito) e afirma que nenhuma frase volta vazia
    - `grep '^import' lib/encomendas/textos.ts | grep -vc '^import type'` retorna 0 — só import de tipo é permitido aqui
    - `npm run lint` e `npx tsc --noEmit` saem com 0
  </acceptance_criteria>
  <done>Toda data que a interface vai mostrar sai de uma função testada, no fuso de Brasília, sem
  pacote novo; e toda frase fixa da fase vive num módulo que os testes importam, com os oito casos de
  situação cobertos por um `switch` que o compilador vigia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do banco → módulos puros | valores já persistidos (inclusive gravados antes de uma restrição existir) entram nas funções de cálculo |
| módulos puros → toda superfície da fase | Gantt, trilha, cartão, rodapé e folha impressa leem o mesmo resultado; um erro aqui aparece em cinco lugares e em nenhum log |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | `calcularCronograma` recebendo `dias` fora do contrato | high | mitigate | `RangeError` explícito em vez de coação silenciosa; teste dedicado para `dias: 2` e `dias: -1` em cada uma das três etapas de marco |
| T-03-11 | Tampering | divergência entre `ORDEM_DAS_ETAPAS` e `etapaEncomenda.enumValues` | medium | mitigate | Teste que importa os dois lados e compara elemento a elemento; sem ele, acrescentar uma etapa no banco deixaria o módulo puro silenciosamente desatualizado |
| T-03-12 | Denial of Service | entrada com número de etapas inesperado | low | accept | `esquemaEtapas` (plano 01) já limita a exatamente 6; o módulo puro é chamado com dados já validados. Aceito: uma segunda validação de tamanho aqui seria redundante e não protege de nada novo |
| T-03-13 | Information Disclosure | mensagens de erro dos módulos puros | low | mitigate | As mensagens nomeiam a etapa e o valor recusado, nunca dados de cliente — a mensagem pode aparecer em log de servidor |
</threat_model>

<verification>
- `npm test` sai com 0 com a suíte inteira, incluindo os testes anteriores das Fases 1, 2a e 2b.
- `npm run lint` e `npx tsc --noEmit` saem com 0.
- `git diff --exit-code db/ app/ components/` retorna 0 — este plano não toca em nenhuma camada além
  de `lib/encomendas/` e `tests/unit/`.
</verification>

<success_criteria>
As 19 fronteiras de valor desta fase têm cada uma um teste que a nomeia, e o 18px/dia, o limiar de
46px e a posição de "Hoje" são números lidos por teste de unidade em milissegundos — não uma
conferência visual feita uma vez e nunca repetida.
</success_criteria>

<output>
Create `.planning/phases/03-gestor-de-encomendas/03-02-SUMMARY.md` when done
</output>

---

## Artefatos que este plano produz

**Arquivos novos:** `lib/encomendas/gantt.ts`, `lib/encomendas/formato.ts`,
`lib/encomendas/textos.ts`, `tests/unit/gantt.test.ts`, `tests/unit/formato-encomenda.test.ts`,
`tests/unit/textos-encomenda.test.ts`.

**Arquivos modificados:** `lib/encomendas/cronograma.ts` (estendido),
`tests/unit/cronograma.test.ts` (estendido).

**Símbolos exportados novos:**
- `lib/encomendas/cronograma.ts`: `STATUS_DE_ENCOMENDA`, `StatusDeEncomenda`, `Situacao` (união de
  oito ramos: `nao-comecou`, `em-etapa-intervalo`, `em-etapa-marco`, `ultima-etapa`, `atrasada`,
  `concluida`, `cancelada`, `sem-etapas`), `situacaoEm`
- `lib/encomendas/gantt.ts`: `PIXELS_POR_DIA`, `LARGURA_MINIMA_PARA_ROTULO`, `IntervaloDaTimeline`,
  `CelulaDeQuinzena`, `RetanguloDeEtapa`, `calcularIntervalo`, `celulasDeQuinzena`,
  `deslocamentoEmPixels`, `retanguloDaEtapa`, `rolagemInicial`, `ordenarParaGantt`
- `lib/encomendas/formato.ts`: `hojeEmBrasilia`, `formatarDiaCurto`, `formatarIntervalo`,
  `formatarDiaCompleto`, `formatarPeriodo`
- `lib/encomendas/textos.ts`: `textoDaSituacao`, `FRASE_VAZIO_TITULO`, `FRASE_VAZIO_CORPO`,
  `FRASE_FILTRO_VAZIO_TITULO`, `FRASE_FILTRO_VAZIO_CORPO`, `FRASE_ERRO_TITULO`, `FRASE_ERRO_CORPO`,
  `FRASE_FALHA_AO_SALVAR`, `ROTULO_NOVA_ENCOMENDA`, `SELO_RASCUNHO`, `SELO_ATRASADA`, `ROTULO_ETAPA`

**Rotas novas:** nenhuma. **Tabelas novas:** nenhuma. **Scripts npm novos:** nenhum.
**Pacotes novos:** nenhum — `date-fns` foi conscientemente dispensado (PD-04, plano 01).
